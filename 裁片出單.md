# 裁片出單功能規格

最後整理：2026-09-06

## 1. 功能定位

- 導覽模組：`Xuất phiếu chuẩn bị vật liệu / 備料出單`。
- 子功能：`Xuất phiếu cắt chi tiết / 裁片出單`。
- 功能程式：`js/piece-cutting.js`；資料層：`js/piece-cutting-store.js`；樣式：`styles/features/piece-cutting.css`。
- 與裁帶共用既有 `preparationMain（備料主功能）`及 `cutting（備料出單子權限）`，不新增第二個權限勾選。
- 裁帶與裁片只有入口與權限共同管理；主檔、快取、轉檔服務、輸出排版及歷史動作完全分開。
- 左側「備料出單」分類下直接顯示裁帶出單與裁片出單；裁片頁不再增加第二層裁帶／裁片切換列。
- 裁片訂單頁沿用裁帶頁的操作列、四項摘要與核對結果框架；四項摘要依序為總款號、通過、缺少模板與錯誤。使用說明顯示裁片工具專用資料夾與啟動檔；清除目前資料只清除本頁尚未匯出的訂單，不刪除裁片主檔與歷史。

## 2. 主檔格式與辨識

只接受一個 `.xlsx（Excel 表格檔）`，並讀取檔案內全部非空白工作表。每個非空白工作表都必須在前 50 列找到固定 A–G 表頭；完全空白的工作表自動忽略，任一有資料但格式不合的工作表會阻擋整份主檔：

| 欄 | 越文表頭 | 用途 |
|---|---|---|
| A | `MÃ HÀNG` | 款號 |
| B | `SIZE` | 尺寸 |
| C | `TÊN VẬT LIỆU` | 布料名稱 |
| D | `BỘ PHẬN CẮT` | 裁片名稱 |
| E | `SỐ KIỆN` | 每件用量，正整數 1–999 |
| F | `GHI CHÚ` | 備註，可空白 |
| G | `HÌNH ẢNH` | 圖片合併儲存格與圖片 |

- G 欄同一合併儲存格範圍先建立一個圖片來源群組；群組身分包含工作表編號與列範圍，不同工作表的相同列號不會互相混用。本機工具會以圖片的 SHA-256（內容驗證碼）辨識實際圖片內容，因此分處不同工作表或不同 G 欄合併範圍、但圖片內容完全相同的群組仍可正確合併。
- 圖片群組內只有 A、B 同時有值的列才建立款號與尺寸；空白列不沿用上一筆款號。
- 圖片群組內全部有效款號共同適用該群組全部裁片列。
- C 欄只依實際合併儲存格取得布料名稱；未合併的空白格不任意向下沿用。
- 正規化後的款號在整份主檔中只能出現一次，包含不同工作表；重複時顯示兩個工作表與原始列並阻擋匯入，避免一張訂單被套用兩組尺寸或裁片。
- 同一圖片群組內，正規化後的「布料名稱＋裁片名稱」只能有一列；重複時顯示工作表與原始列，要求合併成一列並把正確每件用量寫入 `SỐ KIỆN`，避免同一裁片重複加總。
- 不建立人工裁片編號；布料、裁片名稱、尺寸為輸出加總條件。
- 檔案大小上限為 100 MiB（約 104.9 MB）。

## 3. 訂單與計算

- 訂單格式、表頭、數量、重複款號、總數量與訂單編號辨識沿用裁帶已公開的正式訂單規則。
- 訂單欄可一次選擇或拖入多個檔案；每個檔案獨立解析、驗證與顯示狀態。有效檔案保留在本次工作區，錯誤檔案可單獨移除或重新選擇，不讓錯誤內容混入計算。
- 每個檔案都必須辨識到正式訂單編號；找不到時阻擋該檔案，不使用檔名冒充訂單編號。
- 多個檔案辨識到相同正式訂單編號時，全部重複檔案都標示錯誤並停止加總；即使重新命名檔案也不能把同一張訂單重複加入。移除重複檔案後會立即重新驗證剩餘檔案。
- 錯誤明細顯示來源檔案、訂單、工作表、原始列、款號、原因與處理方式；目前共用訂單解析器的正式定位粒度為原始列，不額外猜測儲存格。
- 訂單只提供款號與數量；系統依款號從裁片主檔取得尺寸、布料、裁片、每件用量及圖片群組。
- `SỐ KIỆN`代表每件產品所需的裁片數量；裁片數量＝訂單數量 × `SỐ KIỆN`。
- 同一款號分別出現在不同訂單時合併加總；同一訂單內重複款號仍依正式訂單規則列為錯誤。
- 實際圖片內容與同一布料完整名稱都相同時，款號不同也可合併；其中正規化後的裁片名稱及尺寸相同，就加總到同一格。文字只整理前後空格、重複空格及大小寫，不做模糊比對。
- 訂單沒有的款號、尺寸不輸出；主檔找不到的訂單款號列為缺少主檔，不猜測替代。匯入後會彈窗列出缺少款號，畫面保持可修正狀態，並停用及再次阻擋 PDF 匯出，不能產生少算的工作單。

## 4. PDF（可攜式文件）輸出

- 輸出最高分組是「實際圖片內容＋布料完整名稱」。實際圖片內容以快取建立時已計算的 SHA-256（內容驗證碼）判斷：分處不同 G 欄合併範圍但圖片內容相同且布料完整名稱相同時會合併；同布料但圖片內容不同時必須分開，同圖片但布料、厚度、顏色或完整名稱不同時也必須分開。
- 每個圖片布料組使用固定 A4 橫式版面，頁面內列出該組全部相關裁片，不是每個裁片各印一頁；內容超過安全高度時才建立同組延續頁。
- 左上以 `MÃ HÀNG`標示相關款號，中上以`ĐƠN HÀNG`標示本次全部正式訂單號碼；表格列為裁片、欄為尺寸。
- 表格欄寬固定，長布料與裁片文字在自己的欄位內換行，列高依內容增加；單一裁片列不得拆到兩頁。
- 每頁最多 8 個尺寸；超過時建立延續頁並重複抬頭及裁片列。
- 每頁最多安排 8 個一般高度裁片；長文字或備註需要較多高度時自動減少本頁裁片數，保證整列完整且不縮小到無法閱讀。
- 頁面下方固定為左側備註表、右側產品照片。備註表的表頭與空白框永遠保留；沒有內容時不建立備註明細列。
- 備註明細固定顯示`MÃ HÀNG`、`SIZE`、`BỘ PHẬN CẮT`、`GHI CHÚ`。相同尺寸、裁片與備註可合併列出多個款號；任何一項不同就分列。備註放不下時先減少同頁裁片，極端情況建立不重複數量的備註延續頁。
- 每頁右下只顯示該圖片群組的一張固定框代表產品照片；同一群組內多個款號不重複圖片，延續頁重複同一張代表照片。照片只等比例縮放並留白，不裁切、不拉伸。
- 代表照片依 Excel 主檔保存的原始方向、明確旋轉角度及裁切範圍輸出；沒有旋轉設定時保持原圖，不依照片寬高猜測或強制轉成橫向。
- 單頁組不顯示頁碼；發生延續頁時，依該圖片布料組顯示 `Trang 1/2`、`Trang 2/2`，下一組重新計算。
- 全頁字級統一按 A4 閱讀距離配置：主標題約 22 pt；款號／訂單標籤與頁碼約 9 pt；款號／訂單值約 13 pt 粗體；表頭約 10.5 pt 粗體；布料、裁片與備註約 10–10.5 pt；尺寸與數量 14 pt 粗體。只有數量過長時依序縮至約 13／12 pt 並保持單行。
- PDF 內容只使用越文，不顯示中文副標；網站操作介面仍遵守中央越文／中文顯示規則。
- 匯出前先確認獨立本機服務可用，再開啟系統儲存位置選擇視窗；使用者取消就停止，不產生空白檔案。

## 5. 資料、版本與快取

- `pieceCuttingTemplates/main（裁片主檔版本）`是唯一 freshness source（資料新舊判斷來源）。
- `pieceCuttingTemplateChunks/main_00000...（裁片主檔分塊）`保存原始 `.xlsx`；每塊最多 650,000 個 Base64（文字編碼）字元。
- 主檔版本文件只保存檔名、大小、SHA-256（內容驗證碼）、分塊數、摘要、操作者及時間，不重複保存另一套版本。
- 使用者真正開啟裁片頁時只讀主檔版本 1 筆；只有需要配對訂單且本機沒有相同內容驗證碼時，才讀全部分塊。
- 瀏覽器 `IndexedDB（本機資料庫）`及 `piece-cutting-cache（裁片本機工具快取）`都以內容驗證碼確認，不使用過期檔案；本機圖片快取第三版保存工作表身分、Excel 旋轉、裁切及處理後圖片內容驗證碼。既有第一／二版只會在該電腦第一次匯出時重建裁片圖片快取，不影響瀏覽器主檔快取、裁帶或正式業務資料。
- 裁片主檔與裁帶模板相同，是經核准的同一台可信任電腦跨帳號共用快取例外；不得延伸到訂單、產能、帳號或其他業務資料。
- 同一工作階段重複版本檢查共用載入工作；兩次載入重疊時，只允許較新的請求發布結果。

## 6. 權限、安全與歷史

- 管理員，或同時具有 `preparationMain`與`cutting`的有效角色，才能讀寫裁片主檔及查看裁片操作紀錄。
- 未登入、未核准、停用、無效角色或缺少任一權限者全部拒絕。
- 主檔匯入／覆蓋、主檔刪除及 PDF 匯出寫入 `operationLogs（獨立操作紀錄）`；動作為 `pieceCuttingTemplateImport`、`pieceCuttingTemplateDelete`、`pieceCuttingPdfExport`。
- 主檔分塊、版本與對應操作紀錄使用同一批次寫入；一般網站功能不得修改或刪除已建立的操作紀錄。
- 歷史只在使用者開啟歷史分頁後讀取，每次最多 50 筆並以「載入更多」續讀。

## 7. 獨立本機工具

- 裁片使用 `local-piece-cutting-server.ps1`及`local-piece-cutting-launcher.ps1`，服務位置固定為 `127.0.0.1:8766`。
- 裁帶仍使用原本 `127.0.0.1:8765`，兩者不共用程式、連接埠、快取或暫存資料夾。
- 裁片工具使用 `piece-cutting-cache`與 `piece-cutting-pdf-`暫存前綴。
- 網站只接受健康檢查回傳 `piece-cutting-pdf-local`的裁片工具；不會把裁帶服務誤認為裁片服務。

## 8. 雲端用量預算

假設分塊大小 650,000 字元，分塊數 `C = ceil(Base64 字元數 ÷ 650,000)`；目前方案即時剩餘額度未知。安全規則身分與角色相依讀取可能另計。

| 情境 | Reads（讀取） | Writes（寫入） | Deletes（刪除） | 傳輸／儲存 |
|---|---:|---:|---:|---|
| 開啟裁片頁 | 1 | 0 | 0 | 僅版本文件 |
| 快取命中後配對／匯出 | 0 個分塊 | PDF 成功後 1 筆歷史 | 0 | 主檔不重傳雲端 |
| 多訂單加入、驗證、移除與重新計算 | 0 | 0 | 0 | 全部在瀏覽器本機處理；成功匯出仍只寫 1 筆歷史 |
| 多工作表解析、重複款號／裁片／訂單檢查、缺少主檔檢查 | 0 | 0 | 0 | 全部使用已選檔案與已載入主檔在瀏覽器本機處理 |
| 冷載入主檔 | `1 + C` | 0 | 0 | 約原始檔 1.33 倍 Base64 傳輸 |
| 首次匯入主檔 | 1 次舊版本檢查 | `C + 1` 主檔資料 + 1 歷史 | 0 | 新檔完整上傳與保存 |
| 覆蓋主檔 | 1 | `C新 + 1` 主檔資料 + 1 歷史 | `max(C舊-C新,0)` | 新檔完整上傳；舊多餘分塊刪除 |
| 刪除主檔 | 1 | 1 筆歷史 | `C + 1` | 釋放主檔儲存量 |
| 歷史第一頁／載入更多 | 最多 50／次 | 0 | 0 | 少量紀錄文字 |

檔案冷載入估算：5 MiB 約 12 Reads、10 MiB 約 23 Reads、20 MiB 約 45 Reads、100 MiB 最壞約 217 Reads。工作表數量不會另外增加雲端讀寫，仍只依整個 `.xlsx`檔案大小決定分塊數。版本或快取失效尖峰以「每次冷載入 × 裝置／瀏覽器設定檔 × 重載次數」計算：20 MiB、20 台裝置、每台 2 次約 1,800 Reads；100 MiB 同條件約 8,680 Reads，傳輸約 5.2 GiB。主檔不因網站版本更新自動失效，只依內容驗證碼失效；第三版本機圖片快取只在各電腦本機重建，不產生額外 Firebase（雲端資料庫）用量。本機開發與離線驗收的正式環境 Reads、Writes、Deletes 均為 0。

## 9. 驗收重點

1. 裁帶舊程式、8765 服務、模板、輸出及歷史行為完全不變。
2. 全部非空白工作表都能解析；空白工作表忽略，有資料但缺少正式表頭的工作表阻擋整份主檔；各工作表的圖片依工作表身分與 G 欄列範圍正確對應。
3. 圖片合併群組內的空白 A／B 不產生錯誤款號，也不沿用前一筆。
4. 同一款號在同一或不同工作表重複時阻擋；同一圖片群組內重複的布料＋裁片阻擋並指出原始位置。
5. 實際圖片內容相同時，相同布料、裁片及尺寸跨款號正確依「訂單數量 × SỐ KIỆN」加總；不同尺寸保持分欄。
6. 相同布料但實際圖片內容不同時分開；不同 G 欄合併範圍使用完全相同圖片時可合併；每頁只有一張代表照片與一種布料。
7. 超過 8 個尺寸與過多裁片列能正確分頁、顯示群組內頁碼並保持文字可讀。
8. 0°圖片保持原方向，只有 Excel 明確設定角度時才旋轉，裁切範圍與最終顯示內容正確。
9. 有備註時依款號、尺寸、裁片與內容正確列出；無備註時保留空白框；備註與長文字不得讓裁片列跨頁。
10. 多訂單的有效檔案可合併加總；相同訂單編號的重新命名副本全部阻擋，移除副本後其餘檔案恢復；錯誤明細能定位來源檔案、工作表與原始列，缺少正式訂單編號不得匯出。
11. 任一訂單款號缺少主檔時顯示款號與原因並阻擋匯出，補齊或移除錯誤訂單前不得產生少算的 PDF。
12. 網站的雙語、只顯示越文、只顯示中文三種模式均完整且對齊；輸出 PDF 僅有越文。
13. 使用者取消儲存、不支援選擇儲存位置、工具未啟動、主檔缺少及網路錯誤時不留下未完成 PDF。
14. 安全規則完成管理員、所有有效非管理員角色、未核准、停用、無效角色與未登入允許／拒絕測試。
15. 左側兩個備料入口不受內容搜尋或內部分頁影響；裁片頁的使用說明、四項摘要、清除目前資料與核對結果在三種語言模式下均可操作且對齊。

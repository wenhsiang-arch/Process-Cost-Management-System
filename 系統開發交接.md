# 系統開發交接

最後更新：2026 年 8 月 6 日

## 一、文件用途

- 本文件只記錄目前仍有效的決定、尚未完成的工作及接手注意事項；已完成且不再影響後續開發的過程說明不保留。
- 開始工作前必須先閱讀 `AGENTS.md（開發規則文件）`、`系統架構規劃.md（全系統架構規格）`及本文件。
- 安全細節參考 `安全規則盤點.md（安全盤點文件）`；大型資料讀取方式參考 `各功能資料讀取方式與預估用量.md（大型功能讀取說明）`。
- 裁帶本機工具參考 `CUTTING_LOCAL_SERVER.md（本機裁帶服務說明）`及 `工具轉換系統.MD（工具轉換系統說明）`。

## 二、目前系統定位與架構

- 本系統是工廠輔助資料管理與效率工具，不取代正式採購、庫存、財務、人事或企業流程；主要透過 Excel（表格檔）匯入、系統登記、查詢、計算及匯出使用。
- 目前只開發及驗收桌機介面；手機與平板等桌機功能完成後，再依實際需要另外設計。
- 網站由 `index.html（主畫面檔案）`及 `js（功能程式資料夾）`組成，透過 GitHub Pages（GitHub 靜態網站服務）發布。
- 登入使用 Firebase Authentication（Firebase 身分驗證）與 Google（Google 帳號）；正式資料保存於 Cloud Firestore（雲端文件資料庫）。
- `js/features.js（中央功能程式）`統一管理導覽、功能分頁、權限、程式依賴、資料載入及開頁動作；功能程式與 Excel（表格檔）工具均採按需載入。
- 登入只讀取身分、帳號狀態、角色權限及輕量首頁設定，不預先下載款號、訂單、裁帶或成本資料。
- 業務資料依功能保存到 IndexedDB（瀏覽器本機資料庫），並以 `system/dataVersions（資料版本）`判斷是否更新；不同帳號、功能及資料版本不得混用快取。
- 款號已使用增量更新；訂單工序依單張訂單按需讀取。大型資料仍需持續避免登入預讀、整包監聽及無限制查詢。

## 三、帳號與權限現況

- 保留角色為管理員、課長、文員、開發、生管及業務；班長與員工登入、手機報工及舊審批流程已淘汰。
- 帳號使用 `userAccess（使用者存取權限）`管理；非管理員角色使用 `rolePermissions（角色功能權限）`管理功能。
- 權限層級固定為「主功能→功能分頁→敏感資料」。關閉主功能會隱藏全部下層；關閉分頁會隱藏並拒絕該分頁。
- 開啟功能分頁即包含查看、新增、修改、刪除、匯入、匯出及歷史，不再建立一般操作子開關。只有同一分頁混合一般資料與敏感資料時，才新增敏感資料子開關。
- `costView（顯示產品工價）`是款號總表目前保留的敏感資料子開關；成本設定、成本變動記錄與產品工價匯出使用各自分頁權限。
- 帳號管理與權限管理維持只限管理員。`orderImport（舊訂單匯入權限）`只作舊文件相容欄位，實際值跟隨 `progress（訂單資料分頁）`。
- `productionDevelopment（開發角色）`介面只顯示「開發」；`sales（業務角色）`已加入帳號與權限設定。
- 新功能必須同時更新中央功能清單、權限頁、角色欄位、Firestore Rules（雲端安全規則）及允許／拒絕測試；非管理員的新權限預設關閉。

## 四、目前主要底層成果

- `safe-dom.js（安全文字程式）`統一處理 Excel（表格檔）、雲端文字、搜尋高亮、行內參數及錯誤訊息。
- `operationLogs（獨立操作紀錄）`用於正式資料的匯入、匯出、刪除、撤銷、批次修改及重要設定異動；每次操作一份文件，建立後禁止網站修改或刪除。
- 裁帶歷史分頁只在使用者點開後查詢最近 50 筆模板匯入、匯出及刪除紀錄，不在登入或裁帶主頁預讀。
- 款號使用 `product-cache.js（款號快取程式）`及 `productChanges（款號增量變更）`；訂單使用 `order-process-cache.js（訂單工序快取程式）`。
- 款號匯入及刪除使用 Transaction（雲端交易），使款號、增量紀錄及版本一起成功或失敗；單次匯入上限為 498 款。
- 目前測試基底包含 18 項本機功能與架構測試，以及 26 項 Firebase Emulator Suite（Firebase 本機模擬器）安全規則測試；本次全部通過。
- 仍保留的舊式相依：訂單明細會載入 `data.js（資料與報表程式）`取得工序分類文字；成本設定仍會重畫其他隱藏頁面。兩項均列入後續整理。

## 五、後續主要功能方向

- 舊員工報工、幹部審批、員工管理、獨立考勤及舊效率報表不再恢復。
- 新功能暫定為「產量與工時登記」，由文員依紙本資料統一登記；員工姓名只作選擇名冊，不是登入帳號。
- 同時支援系統逐筆輸入與 Excel（表格檔）整批匯入，兩種方式必須寫入相同資料結構並執行相同檢查。
- 標準產量至少包含日期、員工、訂單、款號、工序及數量；正式工序只能從款號工序表選擇。
- 返工、臨時工作及其他非標準作業可手寫說明與工時，納入有效工作時間，避免效率被錯誤拉低。
- 後續規劃包含 BOM（物料清單）、預料、庫存、缺料計算、採購單價、訂單用量金額及績效獎金；細節尚未定案，但必須沿用目前權限、快取、安全與操作紀錄架構。

## 六、雲端資料、安全規則與發布狀態

- Firestore Rules（雲端安全規則）採預設拒絕及最小權限；舊員工、報工及考勤集合的明確拒絕規則與拒絕測試必須保留。
- `attendance（舊出勤）`、`employees（舊員工）`及 `reports（舊報工）`共 166 筆測試文件，以及三筆相關舊複合索引已完成實體清除。
- `system/accounts（舊帳號）`、`system/permissions（舊權限）`及部分舊 `system（系統設定）`文件仍需逐份確認；不得直接刪除整個 `system（系統設定集合）`。
- `cuttingTemplateChunks（裁帶模板分段）`、`cuttingTemplates（裁帶模板）`、`orderLocks（訂單鎖定）`、`orderProcesses（訂單工序）`、`orders（訂單）`、`products（款號）`、`rolePermissions（角色功能權限）`及 `userAccess（使用者存取權限）`仍是現行資料，不得清除。
- `operationLogs（操作紀錄）`的 `permissionKey（功能代碼）`升冪加 `createdAt（建立時間）`降冪複合索引已在正式資料庫啟用。
- 已確認非管理員操作紀錄失敗的根因：舊規則每次業務請求同時判斷 UID（使用者識別碼）與 Email（電子信箱）權限文件，複雜功能會超過 Firebase Security Rules（雲端安全規則）單次 1,000 次運算限制；不是操作紀錄欄位或複合索引錯誤。
- 底層已改為「電子信箱只供預先核准，首次登入以同一筆 Transaction（雲端交易）自動建立 `userAccess/{UID}（使用者識別碼權限文件）`並刪除電子信箱邀請」。後續全部業務權限只查 UID；不需管理員手動填 UID。
- 本機已驗證管理員、課長、文員、開發、生管及業務，以及未核准、停用、冒用、權限提升與重複文件拒絕案例。程式與規則尚需依本次版本一起推送及發布；發布完成前正式環境仍使用舊底層。
- 尚待安全項目：完全未登入的外部拒絕驗收、App Check（應用程式驗證）評估、CSP（內容安全政策）及異常登入／用量警示。

## 七、裁帶本機 PDF（可攜式文件）工具定案

- 裁帶特殊 PDF（可攜式文件）保留獨立本機轉檔方式，不改成網頁內建。
- 主要調用位置為 OneDrive（雲端硬碟）的 `1MAY9/Cong cu chuyen doi PDF（PDF 轉換工具）`；工具、啟動器及 `cutting-cache（裁帶快取）`維持 OneDrive（雲端硬碟）同步。
- `cutting-cache（裁帶快取）`按模板或模板版本保存，不應每次轉檔新增一份；專案與 OneDrive（雲端硬碟）的 `local-cutting-server.ps1（本機轉檔服務）`必須同步更新。
- 工具故障只停止裁帶匯出，不得阻止其他系統頁面。只有匯出格式改變、安全問題、瀏覽器停止支援或使用者主動要求時才重新評估架構。

## 八、全域代碼盤點：盤點完成，尚未處理

盤點日期：2026 年 8 月 6 日。現有主功能仍可使用；17/17 本機功能測試、網頁與轉檔程式語法檢查均通過，206 個頁面識別碼沒有重複，已檢查的畫面按鈕均有對應函式。本節只記錄待處理內容，尚未修改下列程式。

### 8.1 訂單權限重新判定

- 原盤點認為「可以查看訂單」不應等於「可以修改或封存訂單」；但目前正式權限架構已明確決定，開啟訂單分頁就包含匯入、修改、封存及刪除等完整操作。
- 因此 `orders.js（訂單程式）`與 `firestore.rules（雲端安全規則）`目前不是權限衝突，不列為程式錯誤。
- 若未來確實需要「只看訂單、不能修改」的職務，必須先重新確認權限架構，再新增敏感動作或唯讀分頁；不得直接修改安全規則造成前端與雲端不一致。

### 8.2 可清除的失效或無效程式

- 舊獨立工序明細頁已不存在，可整理 `summary.js（款號摘要程式）`內的 `rDet（舊明細顯示函式）`、`auth.js（登入程式）`的 `dPage（舊明細頁碼）`、`utils.js（工具程式）`的 `goDP（舊明細換頁函式）`及其呼叫。款號表目前仍使用的「列內展開工序明細」不得刪除。
- `data.js（資料與報表程式）`內 4 個未使用的匯出表格小計樣式函式可刪除。
- `data.js（資料與報表程式）`的備份畫面仍計算已不存在的 `bk-stats（備份統計區）`，可移除無效計算，但不得影響備份匯出本身。
- `firebase.js（雲端資料庫程式）`的舊 `fbSave（舊設定儲存函式）`、`fbSaveWithStatus（舊狀態儲存函式）`及未使用對外介面，需再次確認沒有動態呼叫後分批刪除。
- `utils.js（工具程式）`的 `isOrderMutationLocked（訂單鎖定判斷）`目前沒有呼叫，可於訂單整理時刪除。

### 8.3 功能互相牽連

- `settings.js（成本設定程式）`輸入成本時仍同時重畫款號、舊明細、產品工價匯出及備份畫面，造成成本頁必須載入額外程式，款號增加後可能產生不必要延遲。
- 改善方向是只更新目前開啟的頁面，其他頁面等實際開啟時再計算；此項不改雲端資料，風險中等。
- 訂單明細依賴 `data.js（資料與報表程式）`的工序分類文字，待修改訂單或資料程式時再拆成小型共用工具，不進行一次全面重構。

### 8.4 安全與版本補強

- 訂單及訂單工序規則已限制必要資料類型，但尚未完整使用允許欄位清單；需補上額外欄位拒絕測試，避免授權帳號寫入系統未定義欄位。
- 成本設定仍以整包 JSON（資料交換格式）文字保存，安全規則只能限制文字大小，不能逐欄檢查薪資、匯率及範圍。未來調整資料結構時需規劃轉換與回復，不能直接改格式。
- `index.html（主畫面檔案）`的 Tabler Icons（圖示工具）仍使用 `@latest（永遠最新版）`，應改成測試過的固定版本，避免外部更新造成介面變動。

### 8.5 不得當作失效程式刪除

- 舊員工、報工、考勤集合的明確拒絕規則。
- 已淘汰班長與員工角色的拒絕測試。
- 舊雲端文件尚未確認清除前所需的安全相容處理。
- 裁帶本機轉檔工具、安裝式網頁設定與目前仍在使用的款號列內工序明細。

### 8.6 建議處理順序

1. 推送本次 UID（使用者識別碼）自動轉換程式後，立即發布同版本 Firebase Security Rules（雲端安全規則），再以一個非管理員帳號重新登入驗收。
2. 清理已確認無使用的舊明細頁、表格樣式、備份統計及共用函式。
3. 解除成本設定與其他隱藏頁面的重畫牽連。
4. 補強訂單欄位白名單、安全測試及圖示固定版本。
5. 成本設定資料格式轉換另立高風險任務，不與小型清理一起修改。

## 九、目前工作區與接手順序

- 工作區保留使用者原有的 `local-cutting-launcher.ps1（本機裁帶啟動程式）`、`裁切測試（裁切測試資料夾）`及未追蹤筆記；不得自行刪除、還原或加入版本。
- 每次修改前先確認檔案、目的、影響範圍及風險並取得同意；提交前只暫存當次相關檔案。
- 先處理本文件第八節的正式故障與確認失效程式，再開始 BOM（物料清單）、庫存或產量與工時登記。
- 新功能直接沿用中央功能登記、按需載入、資料版本、獨立快取、安全文字、分頁權限及操作紀錄；不得另外建立互相衝突的架構。
- 修改完成後先執行本機功能與安全規則測試，再依使用者指示決定是否推送及發布。

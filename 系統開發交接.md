# 系統開發交接

最後更新：2026 年 8 月 6 日

## 一、文件用途

- 本文件記錄整個 Process Cost Management System（製程成本管理系統）的目前開發狀態、重要決定、待辦事項及接手注意事項。
- 開始任何新工作前，必須先閱讀 `AGENTS.md（開發規則文件）`、`系統架構規劃.md（全系統架構規格）`及本文件。
- 裁帶本機工具的詳細資料，另外參考 `CUTTING_LOCAL_SERVER.md（本機裁帶服務說明）`及 `工具轉換系統.MD（工具轉換系統說明）`。
- 本文件只負責交接，不取代安全規則、程式碼或正式資料庫設定。

## 二、目前系統架構

- 全系統長期共用的功能分工、資料讀取、快取、按需載入、安全、權限、Excel（表格檔）匯入匯出、使用體驗、版本轉換及驗收規則，已統一記錄於 `系統架構規劃.md（全系統架構規格）`。
- 後續新增的 `BOM（物料清單）`、庫存、預料、採購單價、文員工序登記及績效獎金，必須從第一天遵守架構文件；現有功能在日後修改時逐步套用，不立即全面重構。
- 網站前端由 `index.html（主畫面檔案）`與 `js（功能程式資料夾）`組成，公開網站透過 GitHub Pages（GitHub 靜態網站服務）提供。
- 登入使用 Firebase Authentication（Firebase 身分驗證）與 Google（Google 帳號）登入。
- 正式資料使用 Cloud Firestore（雲端文件資料庫）。
- Cloud Firestore Security Rules（雲端文件資料庫安全規則）採用預設拒絕及最小權限原則；沒有明確列出的資料路徑與角色必須拒絕。
- 帳號使用 `userAccess（使用者存取權限）`管理；角色功能權限使用 `rolePermissions（角色功能權限）`管理。
- 各功能資料版本使用 `system/dataVersions（各功能資料版本）`管理，供本機快取判斷資料是否需要重新讀取。

## 三、登入與權限現況

- Google（Google 帳號）登入已啟用，管理員已實際測試成功。
- 系統啟動後會先確認是否已有登入狀態；確認沒有既有登入後，才開放 Google（Google 帳號）登入按鈕。
- 管理員可以預先核准 Google 電子信箱；使用者第一次登入後，系統會將帳號綁定 UID（使用者識別碼）。
- 管理員、課長、文員、開發、生管及業務都必須具有有效且啟用的 `userAccess（使用者存取權限）`文件。
- 管理員可以管理全部核准功能；課長、文員、開發、生管及業務依 `rolePermissions（角色功能權限）`使用功能。
- 權限管理頁只顯示目前保留的桌面功能；管理員固定權限會顯示但無法關閉。
- 權限層級統一為「主功能→功能分頁→敏感資料」；關閉主功能會隱藏並禁止全部下層分頁，關閉分頁會隱藏並禁止該分頁。
- 職務只要開啟某功能分頁，就可使用該頁的查看、新增、修改、刪除、匯入、匯出及查看歷史；不再為匯入、匯出或歷史另設隱藏權限。操作紀錄建立後仍禁止修改與刪除，避免審計資料被改寫。
- `costView（顯示產品工價）`是目前款號總表內唯一的敏感資料子開關；只控制款號頁內的工價顯示，不再綁定成本歷史或產品工價匯出分頁。
- 成本設定、成本變動記錄與產品工價匯出各自使用分頁開關；帳號管理與權限管理屬系統安全例外，維持只限管理員。
- `orderImport（舊訂單匯入權限）`不再顯示於權限頁，只保留為舊 `rolePermissions（角色功能權限）`文件的相容欄位，實際值永遠跟隨 `progress（訂單資料分頁）`。
- `productionDevelopment（開發角色）`保留原資料識別，介面名稱固定顯示 `Phát triển / 開發`；新增 `sales（業務角色）`並顯示 `Kinh doanh / 業務`。兩者都依 `rolePermissions（角色功能權限）`開放功能。
- 班長及員工登入角色、手機報工入口與舊審批流程已確定淘汰，不列入重新開放項目。
- 登入與權限相關檔案屬高風險範圍，修改前必須先取得使用者明確同意並完成登入、拒絕及登出測試。

### 新功能權限同步規則

- 未來新增任何需要角色控制的新功能時，必須在同一次開發中同步更新權限管理頁、功能權限清單、角色預設值、Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）及允許／拒絕測試。
- 課長、文員、開發、生管及業務的新功能權限一律預設關閉，只有管理員明確儲存開啟後才能使用。
- 舊的 `rolePermissions（角色功能權限）`文件缺少新功能欄位時，必須安全視為關閉，不得自動開放，也不得造成整份權限載入失敗。
- 權限頁可以由中央功能清單自動產生顯示項目，但雲端資料權限仍必須在 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）中逐項明確設定，不得自動放行。
- 新功能未完成管理員、課長、文員、開發、生管、業務及未授權帳號的畫面權限與直接資料存取驗收前，不得標記為完成或安全上線。

### 中央功能登記與按需載入第一階段

- 2026 年 8 月 6 日已新增 `js/features.js（中央功能登記與載入程式）`；導覽、權限頁、頁面程式依賴、資料載入需求及開頁動作改用同一份清單。
- 登入只預先載入共用工具、資料快取、中央功能清單、登入權限及 Firebase（雲端資料庫）核心程式；登入成功先顯示不讀取業務資料的輕量功能首頁，款號、訂單、裁帶、成本、帳號及權限畫面程式在使用者實際開頁時載入。
- Excel（表格檔）工具只在實際匯入或匯出時載入；未使用的 JSZip（壓縮工具）已從首頁移除。
- 本機功能程式的登入預載量由 13 個、約 `290,332 bytes（位元組）`降為 6 個、約 `89,499 bytes（位元組）`，約減少 `69.2%`。這是四階段完成後的最終原始檔大小；程式下載量改善與 Firebase Firestore（雲端文件資料庫）讀取量改善分開計算。
- 9 個頁面已完成獨立載入、載入順序、開頁函式及權限欄位檢查；發布後仍需以管理員及各角色帳號做一次實際開頁確認。
- 目前保留的已知相依：訂單明細暫時連同 `data.js（資料與報表程式）`載入，以取得工序分類文字；待日後修改訂單或資料程式時再拆出共用工具。

### 架構四階段完成狀態

- 安全基底：新增 `safe-dom.js（安全文字顯示程式）`，Excel（表格檔）、雲端文字、搜尋高亮、行內參數及錯誤訊息共用同一套安全處理。
- 操作紀錄：新增 `operationLogs（獨立操作紀錄）`，款號、訂單、成本及裁帶的匯入／匯出與設定異動改成每次操作一份文件，禁止網站修改及刪除。
- 裁帶歷史：裁帶統計新增 `Lịch sử thao tác / 歷史操作紀錄`分頁，使用者實際點開後才查詢最新 50 筆模板匯入及匯出紀錄；權限直接跟隨裁帶統計，不新增隱藏權限或登入預讀。
- 操作紀錄載入：款號匯入歷史是款號總表的附屬資料；查詢暫時失敗時只顯示雙語提醒，不阻斷款號主資料。成本變動記錄已是獨立分頁，只在使用者開啟該分頁時載入，成本設定頁不再預讀歷史。
- 款號試行：新增 `product-cache.js（款號快取程式）`與 `productChanges（款號增量變更）`；日常版本更新只補讀真正改變的款號，缺漏時安全退回完整重讀。
- 款號同時操作：匯入及刪除改用 Transaction（雲端交易），款號、增量紀錄與版本一起成功或一起失敗；單次匯入上限 498 款。
- 訂單優化：新增 `order-process-cache.js（訂單工序快取程式）`；訂單頁開啟只載入摘要，展開、搜尋或同步時才讀單張訂單工序。
- 測試基底：已新增 `firebase.json（Firebase 專案設定檔）`、`firestore.indexes.json（資料庫索引設定檔）`及 `tests（本機測試資料夾）`。本次權限架構已完成 8/8 架構測試、全部 17/17 本機功能測試與 23/23 安全規則測試。
- Java（Java 執行環境）與 Firebase Emulator Suite（Firebase 本機模擬器）安裝於 `D:\JAVA(開發系統用)`，只供開發測試，不是使用者工作功能；防火牆公開網路規則已停用。
- 此批程式已以版本 `c3d63c0（完成架構基底、按需載入與安全驗收）`推送到 `main（主要分支）`；新版 `firestore.rules（雲端資料庫安全規則檔）`已由管理員於 Firebase（雲端資料庫）網站手動發布，並以 `sales（業務角色）`規則內容確認為新版。`firestore.indexes.json（資料庫索引設定檔）`所需的操作紀錄索引已於 2026 年 8 月 6 日在正式 Firebase（雲端資料庫）啟用；管理員及各角色的實際逐頁驗收仍需完成。

## 四、舊報工流程移除與新登記方向

- 2026 年 8 月 5 日決定：舊員工手機報工、班長審批、桌面報工審批、報工紀錄、補登報工、員工管理、獨立考勤及舊效率報表全部淘汰，不保留回復舊流程的安排。
- 新功能暫定名稱為「產量與工時登記」，由文員依生產現場交回的紙本資料統一登記；員工與班長不登入系統、不參與報工或審批。
- 未來仍需要「員工姓名名冊」供文員選擇，但它只是作業資料，不等於員工登入帳號；名冊的管理畫面與資料格式需配合新流程重新設計。
- 登記方式同時支援兩種：直接在系統逐筆輸入，以及 Excel（表格檔）整批匯入。兩種方式必須寫入相同資料結構、執行相同檢查，避免結果不一致。
- 系統逐筆輸入應以比 Excel（表格檔）更快速、直覺及容易修改為設計目標；應支援連續輸入、保留共用欄位、鍵盤操作、即時搜尋及送出前集中檢查。
- 標準產量登記至少包含工作日期、員工姓名、訂單、款號、工序及完成數量；工序必須從系統款號工序表選擇，不允許自由手寫正式工序名稱。
- 返工、臨時工作及其他非標準作業必須支援手動說明與實際工時；這些工時不計入標準產量，但必須計入當日有效工作時間，避免員工因返工四小時而被錯誤計算成低效率。
- 考勤、有效工時、產量效率及績效獎金應在同一套登記資料上銜接，不再要求文員到另一個獨立考勤功能重複匯入；實際獎金公式及異常處理仍待後續討論。
- Excel（表格檔）匯入必須具有範本、預覽、錯誤列提示、重複資料檢查及操作歷史；未來所有匯入、匯出、撤銷或批次修改都要留下操作者、時間、檔名、影響筆數與結果。
- 本段只固定工作方向與底層原則，不代表畫面、欄位、獎金公式或完整業務流程已定案；後續開發可以調整細節，但不得重新引入員工登入、班長審批或舊報工資料結構。
- 使用者工作順暢優先；在不犧牲連續輸入、修改及查詢體驗的前提下，再使用分頁、條件查詢、版本及 IndexedDB（瀏覽器本機資料庫）快取降低雲端讀取量。

## 五、舊雲端資料與索引清理提醒

- `system/accounts（舊系統帳號文件）`及 `system/permissions（舊字串權限資料）`已由安全規則禁止網站讀取與寫入。
- `system/accounts（舊系統帳號文件）`本身仍可能存在於 Cloud Firestore（雲端文件資料庫），尚未確認完成實體清除。
- 清除前必須確認現行管理員、課長、文員、開發、生管及業務都已轉換到 Google（Google 帳號）登入與 `userAccess（使用者存取權限）`。
- 清除時不得誤刪 `userAccess（使用者存取權限）`、`rolePermissions（角色功能權限）`或 Firebase Authentication（Firebase 身分驗證）內的現行使用者。
- 在舊文件完成備份、刪除及驗收前，每次安全檢查都必須提醒使用者。
- 2026 年 8 月 6 日已取得高風險刪除同意，並從正式 Firebase（雲端資料庫）實體清除 `attendance（舊出勤集合）`11 筆、`employees（舊員工集合）`19 筆及 `reports（舊報工集合）`136 筆，共 166 筆舊流程測試文件；重新載入資料頁後三個集合均已消失。
- `system/employeeUserHistory（舊員工帳號歷史）`、`system/impHist（舊匯入紀錄）`、`system/cLog（舊成本紀錄）`及 `system/settings（舊合併設定）`也要逐份確認是否仍需轉換或追溯；不得直接刪除整個 `system（系統設定集合）`。
- `cuttingTemplateChunks（裁帶模板分段）`、`cuttingTemplates（裁帶模板）`、`orderLocks（訂單鎖定）`、`orderProcesses（訂單工序）`、`orders（訂單）`、`products（款號）`、`rolePermissions（角色功能權限）`及 `userAccess（使用者存取權限）`仍是現行功能資料，不得當成舊集合刪除。
- Firebase（雲端資料庫）的「資料」頁與「索引」頁已分開驗收。`operationLogs（獨立操作紀錄）`的 `permissionKey（功能權限代碼）`升冪加 `createdAt（建立時間）`降冪複合索引已啟用，索引識別碼為 `CICAgJim14AK（操作紀錄索引識別碼）`。
- 已刪除三筆確認無現行程式使用的舊複合索引：`attendance（舊出勤集合）`的員工加日期索引，以及 `reports（舊報工集合）`的狀態加建立時間、員工加建立時間索引。正式索引頁重新載入後只保留上述操作紀錄索引。
- 後續集合與索引清理仍屬高風險工作；必須再次逐項比對現行程式、`firestore.indexes.json（資料庫索引設定檔）`與正式查詢，不得因本次已完成清理而省略確認。

## 六、全系統讀取與快取決定

- 登入階段只能讀取身分、帳號狀態、角色權限及首頁必要的小型設定，不得登入後自動下載全部業務資料。
- 使用者實際開啟某項功能時，才讀取該功能所需資料；大型集合需使用條件、數量限制、日期範圍或分頁。
- 資料讀取後可以保存到 IndexedDB（瀏覽器本機資料庫）；再次開啟功能時，先比對 `system/dataVersions（各功能資料版本）`，版本相同才使用快取。
- 各功能快取必須分開管理；某項功能更新時，只讓該功能的快取失效。
- 2026 年 8 月 5 日決定：本機業務快取暫時維持 `1 GB（十億位元組）`動態上限，不取消容量限制，也不改成無上限快取。
- 接近容量上限時，應優先清除最久未使用的非必要業務快取，不得清除登入、權限或目前功能正在使用的必要狀態。
- 若瀏覽器容量不足或發生 `QuotaExceededError（容量超限錯誤）`，系統應停止新增快取並改由雲端重新讀取，不得讓主要功能故障。
- 清除本機快取只會讓資料下次重新下載，不會刪除 Firebase（雲端資料庫）的正式資料。
- 未經重新評估目前資料量、瀏覽器容量及清除策略，不得移除 `1 GB（十億位元組）`上限。

### 大型資料讀取量關注與款號表試行

- 款號表已完成第一輪試行：依 UID（使用者識別碼）隔離的 IndexedDB（瀏覽器本機資料庫）快取、15 秒記憶體版本、增量變更補讀、完整重讀保護及實際讀取量記錄。
- 以約 1,150 款、1 份變更紀錄及 10 款變更為例，舊方式約 1,151 次直接讀取，新方式約 12 次，約減少 98.96%。第一次使用仍約 `N + 2`，因為完成後多做一次版本一致性檢查。
- 款號畫面仍是完整本機搜尋、排序與每頁 20 款；目前不是雲端真正分頁。接近數萬款前需再決定依款號／客人分區或雲端搜尋，避免第一次完整下載接近免費額度。
- 訂單工序已改成單張訂單按需載入。開頁由舊的「全部訂單＋全部工序」改為只讀訂單摘要；展開時才讀該單工序，版本相同時使用單張訂單快取。
- 詳細現況、估算公式及仍需注意的成長門檻已記錄於 `各功能資料讀取方式與預估用量.md（大型功能讀取說明）`。

## 七、安全現況與後續項目

已完成：

- Firebase Authentication（Firebase 身分驗證）與 Google（Google 帳號）登入。
- 未核准帳號拒絕。
- `userAccess（使用者存取權限）`及 `rolePermissions（角色功能權限）`。
- `system/accounts（舊系統帳號資料）`及 `system/permissions（舊字串權限資料）`禁止網站存取。
- 登入後不再自動下載全部業務資料，並開始採用按需讀取及 IndexedDB（瀏覽器本機資料庫）快取。
- 完整權限頁已統一為主功能、功能分頁與敏感資料三層；分頁開啟後包含該頁的匯入、匯出、修改、刪除及歷史，不再重複設定操作權限。
- `costView（顯示產品工價）`只套用於款號總表內的敏感工價欄位；成本設定、成本歷史與產品工價匯出使用各自分頁權限。
- 一般運算設定已改存於 `system/operationSettings（一般運算設定）`；薪資、保險、餐費及時薪改存於 `system/costSettings（成本設定）`。
- 舊 `system/settings（合併設定）`已改為只允許管理員讀取及轉換，禁止再寫入。
- 舊敏感成本快取及成本記錄快取會在本機快取結構升級時清除。
- 新訂單已停止寫入 `snapshotHr（平均時薪快照）`；管理員載入訂單時會自動清除既有訂單內未使用的舊欄位。
- 版本 `b3df86f（完善功能權限與工價資料保護）`已推送到 `main（主要分支）`。
- 使用者已於 2026 年 8 月 6 日在 Firebase（雲端資料庫）網站發布目前新版 Firestore Rules（雲端資料庫安全規則），並以 `sales（業務角色）`規則內容完成版本確認。
- 裁帶特殊 PDF（可攜式文件）匯出維持獨立本機工具；已限制正式系統及本機測試來源、單次要求容量，並加入啟動等待及轉檔逾時提示。
- 外部及雲端文字的共用安全顯示工具與自動測試已建立。
- Firebase Emulator Suite（Firebase 本機模擬器）安全規則自動測試已建立；目前 23 項未登入、角色、分頁完整操作、敏感工價、成本設定、款號增量、訂單工序、同步限制及操作紀錄測試通過。
- `operationLogs（獨立操作紀錄）`正式複合索引已啟用；舊出勤、舊員工、舊報工集合共 166 筆測試文件及其三筆舊複合索引已完成實體清除與重新載入驗收。

尚待處理或重新驗收：

- 2026 年 8 月 6 日完成的新權限架構及 `firestore.rules（雲端資料庫安全規則檔）`目前只完成本機測試，尚未推送或發布到正式 Firebase（雲端資料庫）；發布時必須先部署新安全規則，再發布新網頁程式。
- 新網頁與新安全規則發布後，必須用課長款號歷史、開發裁帶模板紀錄、文員訂單匯入及授權職務成本設定做正式畫面驗收。
- 使用完全未登入的外部請求，重新驗收所有正式資料路徑均被拒絕。
- 確認並清除 `system/accounts（舊系統帳號文件）`的雲端實體資料。
- 評估 App Check（應用程式驗證）；目前暫緩，不影響既有 Google（Google 帳號）登入。
- 公開網站加入 CSP（內容安全政策）及其他必要安全標頭。
- 建立登入失敗、權限拒絕、異常流量及雲端用量增加警示。
- 逐份確認並清除不再需要的舊 `system（系統設定）`文件；不得刪除整個 `system（系統設定集合）`，執行前仍須另行確認範圍及取得高風險刪除同意。

### 裁帶特殊 PDF（可攜式文件）轉檔工具目前定案

- 2026 年 8 月 6 日決定保留獨立本機轉檔方式，只服務裁帶特殊 PDF（可攜式文件）匯出。
- 主要實際調用位置為 OneDrive（雲端硬碟）的 `1MAY9/Cong cu chuyen doi PDF（PDF 轉換工具）`資料夾；工具、啟動器及 `cutting-cache（裁帶快取）`都維持 OneDrive（雲端硬碟）同步，不搬到其他本機路徑。
- `cutting-cache（裁帶快取）`是一個模板或模板版本一個資料夾，不是每次轉檔新增；2026 年 8 月 6 日檢查時共有 24 個模板快取、沒有重複版本，總容量約 25.9 MB（百萬位元組）。
- 專案與 OneDrive（雲端硬碟）的 `local-cutting-server.ps1（本機轉檔服務）`必須同步更新；2026 年 8 月 6 日已將本次來源限制、容量限制及轉檔功能更新同步到主要 OneDrive（雲端硬碟）執行位置。
- 正常順序固定為：確認或啟動工具、確認訂單文字、選擇儲存位置、使用本機模板快取轉檔、儲存完成檔案。
- 工具未啟動、來源不符、要求過大或等待逾時時，只停止本次匯出並顯示提示，不影響其他系統頁面。
- 這項方式暫時定案。後續架構檢查不再反覆討論是否把它改成網頁內建；只有匯出格式改變、工具故障、瀏覽器停止支援、安全問題，或使用者主動要求時才重新評估。

### 高風險項目進度：成本與工價資料保護已完成程式及規則更新，待實際帳號驗收

已完成：

- 成本設定頁固定只允許管理員使用；此固定權限會在權限頁顯示但不可操作。
- 課長、文員、開發、生管及業務皆可由管理員設定 `costView（查看工價與工資）`；各角色預設關閉。
- 未取得查看工價權限時，款號總表、工序明細、報表預覽及表格匯出都不產生金額欄位。
- 匯出功能仍可使用，但只匯出款號、客戶、名稱、尺寸、工序數、秒數及產量等非金額資料。
- `costlog（成本變動記錄）`必須同時具備 `costView（查看工價與工資）`，否則前端及雲端安全規則都拒絕。
- 一般運算設定與敏感成本設定已拆分；沒有查看工價權限的使用者只讀取一般運算設定。
- 登出、換帳號及本機快取升級時會清除記憶體、畫面欄位與舊敏感快取。
- JavaScript（網頁程式）語法、權限清單、安全規則欄位、無成本時的產量計算及敏感預設值檢查已通過。

尚待管理員實際操作及驗收：

1. 管理員登入後開啟「成本設定」，確認舊金額正確，再按一次「儲存並重新計算」，建立拆分後的兩份設定文件。
2. 管理員開啟「訂單進度」，讓系統自動清除既有訂單的 `snapshotHr（平均時薪快照）`欄位。
3. 管理員開啟「權限管理」，確認課長、文員、開發、生管及業務設定後按「儲存套用」，將 `costView（查看工價與工資）`欄位正式寫入各角色權限。
4. 使用非管理員角色的測試帳號，驗收款號總表、工序明細、匯出、成本記錄及直接資料讀取；沒有查看工價權限時不得取得任何敏感金額。
5. 若要開放某角色查看工價，必須由管理員明確勾選；不得因開放匯出功能而自動開放金額。

## 八、登入畫面目前狀態

- 登入畫面使用越文在上、中文在下的固定雙行排版。
- 背景使用 `login-background.webp（登入背景圖片）`，左側為柴犬、縫紉機及 M9（系統識別字樣），右側保留登入內容空間。
- 圖片內建外框已移除，畫面只保留網頁本身的圓角裁切。
- Google（Google 帳號）登入卡片已縮小，電腦版約為 `300 × 58 px（像素）`，手機版會依容器縮小。
- 登入畫面版本 `a2c7a2e（優化登入背景與按鈕比例）`已包含在目前主分支歷史中。
- 目前最新已推送版本為 `c3d63c0（完成架構基底、按需載入與安全驗收）`，位於 `main（主要分支）`。

## 九、目前工作區注意事項

- 2026 年 8 月 6 日檢查時，架構、快取、安全規則與測試程式已包含在版本 `c3d63c0（架構基底完成版本）`；工作區仍保留使用者原有的 `local-cutting-launcher.ps1（本機裁帶啟動程式）`、`裁切測試（裁切測試資料夾）`及其他未追蹤文件。
- 以上內容不得自行刪除、還原或全部加入版本；開始工作前應先確認本次允許修改及推送的檔案。
- `a2c7a2e（登入畫面優化版本）`除了登入畫面兩個檔案外，也包含當時已暫存的 `local-cutting-server.ps1（本機裁帶服務程式）`修改。尚未依使用者指示建立修正版本，不得自行重寫歷史或還原。
- 每次執行 Git（版本控制）提交前，必須先檢查已暫存清單，只提交使用者本次同意的檔案。

## 十、下次接手順序

1. 閱讀 `AGENTS.md（開發規則文件）`、`系統架構規劃.md（全系統架構規格）`、本文件及 `安全規則盤點.md（安全盤點文件）`。
2. 檢查目前 Git（版本控制）狀態及最新版本，不得覆蓋既有未提交內容。
3. 確認本次工作的檔案、目的、影響範圍及風險，取得使用者同意後再修改。
4. 發布前先確認本批實際要提交的程式、規則、索引、測試及文件範圍，不得把使用者原有未追蹤檔案一起加入。
5. 新版 `firestore.rules（安全規則檔）`已由管理員手動發布，`firestore.indexes.json（資料庫索引設定檔）`所需的操作紀錄索引也已啟用；後續不得重複建立相同索引，也不得為處理查詢問題而覆蓋或放寬安全規則。
6. 使用管理員、課長、文員、開發、生管及業務帳號逐頁確認功能首頁、可見功能、拒絕功能、權限頁、款號、訂單、成本與載入失敗提示；開發角色只顯示「開發」，業務角色必須出現在新增／修改帳號選項。
7. 以兩次開啟款號及展開同一張訂單確認 `lastProductReadMetrics（最近一次款號讀取量）`與 `lastOrderReadMetrics（最近一次訂單讀取量）`：第二次應命中本機快取，不應重讀整包資料。
8. 優先確認管理員是否已完成成本設定儲存、訂單舊時薪欄位清除及角色權限儲存；未完成時先協助這三項操作。
9. 使用無 `costView（查看工價與工資）`的課長或文員帳號，完成款號總表、明細、匯出、成本記錄及直接資料讀取拒絕測試。
10. Firebase（雲端資料庫）舊出勤、舊員工、舊報工集合與三筆舊複合索引已完成清除；後續若再發現舊資料，仍須先形成精確的保留／清除清單，不得直接刪除。
11. 確認正式頁面無錯後，再開始 `BOM（物料清單）`、庫存或產量與工時登記；新功能直接沿用中央登記、安全文字、獨立操作紀錄、按需讀取及本機測試基底。
12. 完成後先測試，再依使用者明確指示決定是否加入版本、提交及推送。

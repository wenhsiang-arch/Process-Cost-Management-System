# 系統開發交接

最後更新：2026 年 8 月 13 日

最新接手狀態：裁帶訂單匯入錯誤判定版本 `1aa27d1（裁帶訂單檢查版本）`已推送至 `main（主要分支）`；詳細規則記錄於第 7.1 節。UID（使用者識別碼）自動轉換底層已完成，功能版本 `4ad4f9b（UID 自動轉換版本）`及相同檔案內容的空白觸發版本 `222c684（重新部署版本）`均已推送至 `main（主要分支）`；第 319 次 GitHub Pages（GitHub 網站部署）已成功上線。Firebase Security Rules（雲端安全規則）已由管理員發布，正式網站權限亦由使用者確認正常。第 317、318 次部署是 GitHub Actions（GitHub 自動工作流程）服務異常留下的失敗或排隊紀錄，不影響目前上線版本。下一個工作優先處理第八節已盤點的失效程式。

最新本機開發狀態：產能主功能已新增獨立「工序修改」，完成款號僅新增匯入、完整版本歷史、人工確認同產品群組、正式標準修改、單張訂單例外、生產中訂單快照同步與 IE（工業工程）建議交接；舊「工序秒數同步」程式已移除。目前 151 項非雲端功能／架構測試全部通過，本批安全規則先前 47/47 通過；尚未執行 Git（版本控制）提交、推送或雲端安全規則發布。

## 一、文件用途

- 本文件只記錄目前仍有效的決定、尚未完成的工作及接手注意事項；已完成且不再影響後續開發的過程說明不保留。
- 開始工作前必須先閱讀 `AGENTS.md（開發規則文件）`、`系統架構規劃.md（全系統架構規格）`及本文件。
- 安全細節參考 `安全規則盤點.md（安全盤點文件）`；大型資料讀取方式參考 `各功能資料讀取方式與預估用量.md（大型功能讀取說明）`。
- 裁帶本機工具參考 `CUTTING_LOCAL_SERVER.md（本機裁帶服務說明）`及 `工具轉換系統.MD（工具轉換系統說明）`。

## 二、目前系統定位與架構

- 本系統是工廠輔助資料管理與效率工具，不取代正式採購、庫存、財務、人事或企業流程；主要透過 Excel（表格檔）匯入、系統登記、查詢、計算及匯出使用。
- 目前只開發及驗收桌機介面；手機與平板等桌機功能完成後，再依實際需要另外設計。
- 網站由 `index.html（主畫面檔案）`及 `js（功能程式資料夾）`組成，透過 GitHub Pages（GitHub 靜態網站服務）發布。
- 登入使用 Firebase Authentication（Firebase 身分驗證）與 Google（Google 帳號）；正式資料保存於 Cloud Firestore（雲端文件資料庫）。
- `js/features.js（中央功能程式）`統一管理導覽、功能分頁、權限、程式依賴、資料載入及開頁動作；功能程式與 Excel（表格檔）工具均採按需載入。
- 登入只讀取身分、帳號狀態、角色權限及輕量首頁設定，不預先下載款號、訂單、裁帶或成本資料。
- 業務資料依功能保存到 IndexedDB（瀏覽器本機資料庫），並以 `system/dataVersions（資料版本）`判斷是否更新；不同帳號、功能及資料版本不得混用快取。
- 款號已使用增量更新；訂單工序依單張訂單按需讀取。大型資料仍需持續避免登入預讀、整包監聽及無限制查詢。

## 三、帳號與權限現況

- 保留角色為管理員、課長、文員、開發、生管及業務；班長與員工登入、手機報工及舊審批流程已淘汰。
- 帳號使用 `userAccess（使用者存取權限）`管理；非管理員角色使用 `rolePermissions（角色功能權限）`管理功能。
- 權限層級固定為「主功能→功能分頁→敏感資料」。關閉主功能會隱藏全部下層；關閉分頁會隱藏並拒絕該分頁。
- 開啟功能分頁即包含查看、新增、修改、刪除、匯入、匯出及歷史，不再建立一般操作子開關。只有同一分頁混合一般資料與敏感資料時，才新增敏感資料子開關。
- `costView（顯示產品工價）`是款號總表目前保留的敏感資料子開關；成本設定、成本變動記錄與產品工價匯出使用各自分頁權限。
- 帳號管理與權限管理維持只限管理員。`orderImport（舊訂單匯入權限）`只作舊文件相容欄位，實際值跟隨 `progress（訂單資料分頁）`。
- `productionDevelopment（開發角色）`介面只顯示「開發」；`sales（業務角色）`已加入帳號與權限設定。
- 新功能必須同時更新中央功能清單、權限頁、角色欄位、Firestore Rules（雲端安全規則）及允許／拒絕測試；非管理員的新權限預設關閉。

## 四、目前主要底層成果

- `safe-dom.js（安全文字程式）`統一處理 Excel（表格檔）、雲端文字、搜尋高亮、行內參數及錯誤訊息。
- `operationLogs（獨立操作紀錄）`用於正式資料的匯入、匯出、刪除、撤銷、批次修改及重要設定異動；每次操作一份文件，建立後禁止網站修改或刪除。
- `history.js（歷史紀錄共用程式）`統一操作紀錄格式、查詢、工作階段快取及游標分頁；款號匯入歷史改成點開後才查詢，訂單調整歷史每次最多讀取 50 筆並可繼續載入。
- `file-io.js（檔案儲存共用程式）`統一另存位置、取消及失敗中止流程；表格檔、備份、裁帶模板與裁帶 PDF（可攜式文件）共用相同寫檔保護，但裁帶本機轉檔內容與服務沒有改動。
- 裁帶歷史分頁只在使用者點開後查詢最近 50 筆模板匯入、匯出及刪除紀錄，不在登入或裁帶主頁預讀。
- 功能頁第一次開啟正常載入；同一登入工作階段再次切換直接使用記憶體資料，一分鐘後才在背景檢查。登入、帳號及角色權限不使用這項業務資料快速切換代替正式驗證。
- 一般訂單、訂單工序及設定資料寫入已把 `dataVersions（資料版本）`放進同一個 WriteBatch／Transaction（雲端批次／交易）；資料與版本只能一起成功或一起取消，不再先寫業務資料後忽略版本失敗。
- 款號使用 `product-cache.js（款號快取程式）`及 `productChanges（款號增量變更）`；訂單使用 `order-process-cache.js（訂單工序快取程式）`。
- 款號匯入及刪除使用 Transaction（雲端交易），使款號、增量紀錄及版本一起成功或失敗；單次匯入上限為 498 款。
- 目前測試基底包含 53 項本機功能、架構及共用模組測試，以及 32 項 Firebase Emulator Suite（Firebase 本機模擬器）安全規則測試；本次全部通過。
- 仍保留的舊式相依：訂單明細會載入 `data.js（資料與報表程式）`取得工序分類文字；成本設定仍會重畫其他隱藏頁面。兩項均列入後續整理。

## 五、後續主要功能方向

- 舊員工報工、幹部審批、員工管理、獨立考勤及舊效率報表不再恢復。
- 新功能暫定為「產量與工時登記」，由文員依紙本資料統一登記；員工姓名只作選擇名冊，不是登入帳號。
- 同時支援系統逐筆輸入與 Excel（表格檔）整批匯入，兩種方式必須寫入相同資料結構並執行相同檢查。
- 標準產量至少包含日期、員工、訂單、款號、工序及數量；正式工序只能從款號工序表選擇。
- 返工、臨時工作及其他非標準作業可手寫說明與工時，納入有效工作時間，避免效率被錯誤拉低。
- 後續規劃包含 BOM（物料清單）、預料、庫存、缺料計算、採購單價、訂單用量金額及績效獎金；細節尚未定案，但必須沿用目前權限、快取、安全與操作紀錄架構。

## 六、雲端資料、安全規則與發布狀態

- Firestore Rules（雲端安全規則）採預設拒絕及最小權限；舊員工、報工及考勤集合的明確拒絕規則與拒絕測試必須保留。
- `attendance（舊出勤）`、`employees（舊員工）`及 `reports（舊報工）`共 166 筆測試文件，以及三筆相關舊複合索引已完成實體清除。
- `system/accounts（舊帳號）`、`system/permissions（舊權限）`及部分舊 `system（系統設定）`文件仍需逐份確認；不得直接刪除整個 `system（系統設定集合）`。
- `cuttingTemplateChunks（裁帶模板分段）`、`cuttingTemplates（裁帶模板）`、`orderLocks（訂單鎖定）`、`orderProcesses（訂單工序）`、`orders（訂單）`、`products（款號）`、`rolePermissions（角色功能權限）`及 `userAccess（使用者存取權限）`仍是現行資料，不得清除。
- `operationLogs（操作紀錄）`的 `permissionKey（功能代碼）`升冪加 `createdAt（建立時間）`降冪複合索引已在正式資料庫啟用。
- 新增的歷史動作篩選索引為 `permissionKey（功能代碼）`、`action（動作）`及 `createdAt（建立時間）`；正式索引尚未部署前，程式會自動使用既有索引的相容查詢，不會阻止歷史功能開啟。發布本次版本時應一併部署 `firestore.indexes.json（雲端索引設定檔）`。
- 已確認非管理員操作紀錄失敗的根因：舊規則每次業務請求同時判斷 UID（使用者識別碼）與 Email（電子信箱）權限文件，複雜功能會超過 Firebase Security Rules（雲端安全規則）單次 1,000 次運算限制；不是操作紀錄欄位或複合索引錯誤。
- 底層已改為「電子信箱只供預先核准，首次登入以同一筆 Transaction（雲端交易）自動建立 `userAccess/{UID}（使用者識別碼權限文件）`並刪除電子信箱邀請」。後續全部業務權限只查 UID；不需管理員手動填 UID。
- 本機已驗證管理員、課長、文員、開發、生管及業務，以及未核准、停用、冒用、權限提升與重複文件拒絕案例。版本 `4ad4f9b（UID 自動轉換版本）`已推送，Firebase Security Rules（雲端安全規則）亦已發布；管理員已在正式規則頁確認 `selfCreatesUidAccess（本人建立 UID 權限函式）`存在，且舊 `hasEmailAccessDocument（電子信箱備援判斷函式）`已不存在。
- 正式網站權限已由使用者完成實際驗收並確認正常。裁帶模板匯入、刪除及歷史操作紀錄若尚未逐項測試，仍應在後續修改該功能時一併驗收，不再視為 UID（使用者識別碼）權限底層的發布阻礙。
- 尚待安全項目：完全未登入的外部拒絕驗收、App Check（應用程式驗證）評估、CSP（內容安全政策）及異常登入／用量警示。

## 七、裁帶本機 PDF（可攜式文件）工具定案

- 裁帶特殊 PDF（可攜式文件）保留獨立本機轉檔方式，不改成網頁內建。
- 主要調用位置為 OneDrive（雲端硬碟）的 `1MAY9/Cong cu chuyen doi PDF（PDF 轉換工具）`；工具、啟動器及 `cutting-cache（裁帶快取）`維持 OneDrive（雲端硬碟）同步。
- `cutting-cache（裁帶快取）`按模板或模板版本保存，不應每次轉檔新增一份；專案與 OneDrive（雲端硬碟）的 `local-cutting-server.ps1（本機轉檔服務）`必須同步更新。
- 工具故障只停止裁帶匯出，不得阻止其他系統頁面。只有匯出格式改變、安全問題、瀏覽器停止支援或使用者主動要求時才重新評估架構。

### 7.1 裁帶訂單匯入錯誤判定（2026 年 8 月 8 日完成）

- 本次只修改裁帶的訂單匯入檢查；模板匯入規則沒有增加限制，模板是否可匯入仍由「匯入模板」功能頁檢查。
- 訂單必須能明確找到「款號」與「訂單數量」表頭；`PCS（件數）`可作為訂單數量表頭，不能寫在數量內容後方。數量內容只接受大於 0 的整數。
- 下列情況歸類為「訂單錯誤」：有數量但款號空白、有款號但數量空白、數量為 0、負數、小數、包含文字或單位、同一款號重複、表頭不存在或無法唯一確認、多個工作表、檔案格式不支援，以及檔案損壞或無法讀取。
- 同一份訂單中的款號必須唯一；重複款號不再自動合併數量。錯誤內容顯示工作表、第幾列、具體原因及處理方式，不使用表格欄位英文字母要求使用者自行判斷。
- 只要存在訂單錯誤，就禁止進入 PDF（可攜式文件）匯出；讀取新訂單失敗時會清除上一份訂單結果，避免誤匯出舊資料。
- 「缺少模板」維持獨立分類，不重複計入「訂單錯誤」；沒有訂單錯誤時，其他已有模板的有效款號仍可照原流程匯出。
- 本次相關語法、訂單規則、架構、快取及安全文字顯示共 28 項測試全部通過；程式版本為 `1aa27d1（裁帶訂單檢查版本）`。

## 八、全域代碼盤點：盤點完成，尚未處理

盤點日期：2026 年 8 月 6 日。現有主功能仍可使用；目前 53/53 本機功能與架構測試、32/32 Firebase Emulator Suite（Firebase 本機模擬器）安全規則測試及網頁程式語法檢查均通過，206 個頁面識別碼沒有重複，已檢查的畫面按鈕均有對應函式。本節只記錄其餘待處理內容，尚未修改下列程式。

### 8.1 訂單權限重新判定

- 原盤點認為「可以查看訂單」不應等於「可以修改或封存訂單」；但目前正式權限架構已明確決定，開啟訂單分頁就包含匯入、修改、封存及刪除等完整操作。
- 因此 `orders.js（訂單程式）`與 `firestore.rules（雲端安全規則）`目前不是權限衝突，不列為程式錯誤。
- 若未來確實需要「只看訂單、不能修改」的職務，必須先重新確認權限架構，再新增敏感動作或唯讀分頁；不得直接修改安全規則造成前端與雲端不一致。

### 8.2 可清除的失效或無效程式

- 舊獨立工序明細頁已不存在，可整理 `summary.js（款號摘要程式）`內的 `rDet（舊明細顯示函式）`、`auth.js（登入程式）`的 `dPage（舊明細頁碼）`、`utils.js（工具程式）`的 `goDP（舊明細換頁函式）`及其呼叫。款號表目前仍使用的「列內展開工序明細」不得刪除。
- `data.js（資料與報表程式）`內 4 個未使用的匯出表格小計樣式函式可刪除。
- `data.js（資料與報表程式）`的備份畫面仍計算已不存在的 `bk-stats（備份統計區）`，可移除無效計算，但不得影響備份匯出本身。
- `firebase.js（雲端資料庫程式）`的舊 `fbSave（舊設定儲存函式）`、`fbSaveWithStatus（舊狀態儲存函式）`及未使用對外介面，需再次確認沒有動態呼叫後分批刪除。
- `utils.js（工具程式）`的 `isOrderMutationLocked（訂單鎖定判斷）`目前沒有呼叫，可於訂單整理時刪除。

### 8.3 功能互相牽連

- `settings.js（成本設定程式）`輸入成本時仍同時重畫款號、舊明細、產品工價匯出及備份畫面，造成成本頁必須載入額外程式，款號增加後可能產生不必要延遲。
- 改善方向是只更新目前開啟的頁面，其他頁面等實際開啟時再計算；此項不改雲端資料，風險中等。
- 訂單明細依賴 `data.js（資料與報表程式）`的工序分類文字，待修改訂單或資料程式時再拆成小型共用工具，不進行一次全面重構。

### 8.4 安全與版本補強

- 訂單及訂單工序規則已限制必要資料類型，但尚未完整使用允許欄位清單；需補上額外欄位拒絕測試，避免授權帳號寫入系統未定義欄位。
- 成本設定仍以整包 JSON（資料交換格式）文字保存，安全規則只能限制文字大小，不能逐欄檢查薪資、匯率及範圍。未來調整資料結構時需規劃轉換與回復，不能直接改格式。
- `index.html（主畫面檔案）`的 Tabler Icons（圖示工具）仍使用 `@latest（永遠最新版）`，應改成測試過的固定版本，避免外部更新造成介面變動。

### 8.5 不得當作失效程式刪除

- 舊員工、報工、考勤集合的明確拒絕規則。
- 已淘汰班長與員工角色的拒絕測試。
- 舊雲端文件尚未確認清除前所需的安全相容處理。
- 裁帶本機轉檔工具、安裝式網頁設定與目前仍在使用的款號列內工序明細。

### 8.6 建議處理順序

1. 清理已確認無使用的舊明細頁、表格樣式、備份統計及共用函式。
2. 解除成本設定與其他隱藏頁面的重畫牽連。
3. 補強訂單欄位白名單、安全測試及圖示固定版本。
4. 成本設定資料格式轉換另立高風險任務，不與小型清理一起修改。

## 九、目前工作區與接手順序

- 工作區保留使用者原有的 `local-cutting-launcher.ps1（本機裁帶啟動程式）`、`裁切測試（裁切測試資料夾）`及未追蹤筆記；不得自行刪除、還原或加入版本。
- 每次修改前先確認檔案、目的、影響範圍及風險並取得同意；提交前只暫存當次相關檔案。
- 產能登記第一階段已完成；後續是否先做補充工序、考勤與效率，或回到第八節失效程式整理，由使用者決定。
- 新功能直接沿用中央功能登記、按需載入、資料版本、獨立快取、安全文字、分頁權限及操作紀錄；不得另外建立互相衝突的架構。
- 修改完成後先執行本機功能與安全規則測試，再依使用者指示決定是否推送及發布。

## 十、產能登記第一階段（2026 年 8 月 8 日本機完成）

- 新增 `production（產能）`主功能，包含 `productionEntry（生產登記）`、`productionRecords（生產紀錄）`及 `productionEmployees（產能員工資料）`三個分頁；非管理員權限預設關閉。
- 登記流程為生產日期、員工、訂單、款號、工序號與正整數生產數量。員工、訂單、款號採整段包含搜尋，唯一結果會自動選取；工序號輸入後約 0.4 秒精確自動選取，不需按 Enter（確認鍵）。
- 訂單限制可用款號，款號限制可用工序；切換訂單會清除原款號、工序與數量。儲存後保留日期、員工、訂單及款號，只清除工序與數量。
- 每次儲存建立一筆獨立 `productionEntries（生產登記）`，不實施同日唯一或自動累加限制。`productionProcessTotals（訂單工序累計）`以同一筆 Transaction（雲端交易）確保不同員工、不同時間的有效總量不超過訂單工序數量。
- 修改只允許改生產數量；日期、員工、訂單、款號或工序錯誤時作廢後重新登記。作廢會釋放工序累計量；正式生產紀錄與員工資料均禁止網站實體刪除。
- 每筆登記保存工序秒數、訂單數量及 `slPerHour（每小時產能）`快照；效率與考勤尚未開發，不在登記頁顯示效率。
- 新資料路徑為 `productionEmployees（產能員工）`、`productionEntries（生產登記）`及 `productionProcessTotals（訂單工序累計）`；舊 `employees（舊員工）`、`reports（舊報工）`及 `attendance（舊考勤）`仍維持全面拒絕。
- 登入不預載產能資料；開分頁後才載入。訂單與單張訂單工序沿用既有快取，產能員工與生產資料只保留工作階段記憶體，歷史依日期與 50 筆游標分頁查詢。
- 尚未處理補充工序、考勤、效率、員工匯入、生產匯出、年度封存及舊資料清理；這些都需另行確認後再開發。
- 安全規則及索引目前只在本機檔案與 Firebase Emulator Suite（Firebase 本機模擬器）驗收，尚未發布至正式 Firebase（雲端資料庫）。

## 十一、成本變動紀錄最小修正（2026 年 8 月 9 日本機完成）

- 問題原因是成本設定畫面的即時試算會先更新目前設定，按下儲存時再比較已經看不到舊值，因此誤判為沒有變動，未交給共用歷史程式寫入。
- `settings.js（成本設定程式）`現在會在一般運算設定與成本設定都載入完成後，保存一份「最後一次已載入或已成功儲存的值」作為比較基準；畫面即時試算不再改變這份基準。
- 儲存時以比較基準和畫面新值產生成本變動內容，再沿用既有 `history.js（歷史紀錄共用程式）`寫入 `operationLogs（操作紀錄）`；共用歷史程式、歷史資料格式及顯示方式均未修改。
- 成本設定成功儲存後才更新比較基準；設定儲存失敗時保留原基準，下一次重試仍能正確留下變動紀錄。
- `features.js（功能載入程式）`改由成本設定專屬載入函式依序完成資料載入與比較基準建立；`index.html（主畫面檔案）`與成本設定程式版本已同步更新，避免瀏覽器沿用舊快取。
- 本次沒有修改成本計算公式、款號工價、產品資料、雲端設定資料格式、Firebase Security Rules（雲端安全規則）或雲端索引。
- 新增成本歷史行為測試，並完成其餘非雲端模擬器測試，共 65/65 項通過。正式登入畫面的最後確認方式為：修改一項成本、儲存後開啟「成本變動紀錄」，確認出現操作者、時間、修改項目、修改前數值及修改後數值。

## 十二、成本變動紀錄表格化（2026 年 8 月 9 日本機完成）

- 成本變動紀錄已由卡片改為完整橫向表格；每筆儲存顯示時間、操作者、平均薪資、平均保險、餐費、每月總成本、平均時薪、美元匯率及台幣匯率。
- 有變動的設定只顯示更新後數值，下一行顯示正負百分比，不在畫面顯示「修改前 → 修改後」；未變動設定只顯示當時數值。修改前數值仍保存在操作紀錄中，用於百分比及歷史正確性。
- 每小時工作秒數及生產效率預設隱藏；表頭右側箭頭可向右展開，展開後箭頭改為向左收合。窄桌機展開時只讓表格內容橫向捲動，時間與操作者固定在左側。
- 新紀錄沿用 `operationLogs（操作紀錄）`既有 `changes（變動明細）`，每次保存九項完整成本設定；沒有新增雲端路徑或資料欄位。舊紀錄未保存的設定顯示橫線，不使用目前設定冒充歷史。
- 新增 `cost-log.js（成本歷史畫面程式）`並採按需載入；舊成本歷史畫面已從 `data.js（資料畫面程式）`移除，讓共用歷史讀寫、成本設定判定及成本歷史畫面各自維持單一責任。
- 頁面新增「重新整理」按鈕，固定查詢最近 50 筆成本紀錄。快取版本已同步更新。
- 本次未修改成本計算公式、款號工價、產品資料、功能權限、Firebase Security Rules（雲端安全規則）或雲端索引。
- 語法、完整成本紀錄、舊紀錄相容、表格展開、按需載入、三種桌機尺寸規則及其他非雲端模擬器測試共 70/70 項通過。瀏覽器所在環境無法連線本機測試服務，且安全政策禁止直接開啟本機檔案；未採用繞過方式，正式登入畫面保留給使用者最後確認。

## 十三、全系統緊湊介面模式（2026 年 8 月 10 日本機完成）

- 正式功能頁改以共用緊湊密度為預設：主內容內距 12 像素、平面分頁最小高度 46 像素、雙語按鈕最小高度 44 像素、直向操作方塊 100 × 68 像素、區塊標題最小高度 34 像素、一般表格列約 36 至 40 像素。
- 緊湊化已套用裁帶、訂單、款號、工序秒數同步、成本、帳號／權限及產能七組功能樣式；只縮減空白與固定高度，輸入框維持 36 像素，雙語字級不縮小，長文字、錯誤原因、匯入預覽、彈出視窗及計算值加百分比的資料列仍可自動增高。
- 共用表格新版本造成舊測試仍比對過期版本與舊欄位選單、凍結表頭結構，本輪已同步改由 `ui-table-controls.js（共用表格操作程式）`與 `ui-table.js（共用表格捲動程式）`作為驗收來源，未退回各功能自己的重複實作。
- 程式影響範圍為 `index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`styles/ui-core.css（共用介面核心樣式）`及七個功能樣式檔；測試影響 `tests/architecture.test.mjs（架構測試）`、`tests/production.test.mjs（產能測試）`與 `tests/ui-consistency.test.mjs（介面一致性測試）`。
- 資料、計算、匯入匯出、權限、安全規則、雲端索引及快取方式均未修改。`js/features.js（中央功能清單）`語法檢查、差異格式檢查及全部 89 項非雲端功能、架構與介面測試通過。
- 款號細節以 `款號管理.md（款號功能文件）`為準；產能進度與實際狀態以 `產能登記.md（產能功能文件）`第十八節為準，不引用本文件較早的產能說明判斷最新狀態。已登入業務頁仍待使用者以 1280 × 720、1366 × 768 及 1920 × 1080 像素完成實際操作驗收。

## 十四、全域語言顯示模式（2026 年 8 月 12 日本機完成）

- 全站已完成「雙語、只顯示越文、只顯示中文」三種模式。共用頂端選擇器是唯一入口，各功能不得另做自己的語言切換。
- `js/ui-runtime.js（共用介面執行程式）`統一管理模式及 UID（使用者識別碼）偏好；偏好保存在既有 IndexedDB（瀏覽器本機資料庫）的 `uiLanguagePreference` 範圍，沒有新增任何雲端資料。
- 登入完成身分與權限確認後、主畫面顯示前讀取偏好；登出時畫面恢復雙語但保留該 UID 偏好。同一瀏覽器的不同 UID 互相隔離，其他瀏覽器、設定檔、電腦或裝置不會自動同步。
- `js/ui-text.js（共用雙語文字程式）`、`js/ui-components.js（共用介面元件程式）`、`js/ui-table-controls.js（共用表格操作程式）`及 `styles/ui-core.css（共用介面核心樣式）`統一處理靜態與動態文字、單語緊湊尺寸及表頭下限；切換模式不覆蓋使用者自訂欄寬。
- 已同步款號、訂單、成本、帳號、同步狀態、產能登記、產能紀錄、出勤、員工、三個生產分析頁、系統監控及裁帶工具的動態顯示文字。業務資料、公式、權限、快取版本、Firebase（雲端資料庫）安全規則與雲端索引均未修改。
- 新增 `tests/ui-language.test.mjs（語言顯示測試）`，並更新架構、介面一致性、表格、安全文字及產能相關測試；全站 141 項非雲端自動測試全部通過。
- Firebase Emulator Suite（Firebase 本機模擬器）本次未啟動，因此沒有重跑安全規則模擬器測試；本次沒有修改 `firestore.rules（Firebase 雲端資料庫安全規則）`。仍需在可登入的桌機環境依三種正式尺寸及三種語言模式完成實際操作驗收。
- 回復時只需回復語言核心、主畫面選擇器、共用樣式、功能顯示文字及載入版本；忽略本機 `uiLanguagePreference` 後會回到雙語，無需轉換或回復任何業務資料。

## 十五、全域單語殘留盤點與修正（2026 年 8 月 13 日本機完成）

- 實際問題不是語言偏好失效，而是部分舊介面仍使用相鄰文字、斜線字串、自訂表頭或動態輔助屬性，沒有建立中央語言模式可辨識的越文與中文節點；表格重新建立後也可能再次出現舊結構。
- `js/ui-text.js（共用雙語文字程式）`現會處理已核准的摘要、設定、狀態、舊表頭及`title（滑鼠提示）`、`placeholder（輸入提示）`、`aria-label（輔助操作名稱）`。相容處理不掃描一般資料儲存格、使用者輸入、款號、姓名、備註、幣別代碼或計算結果。
- `js/ui-table-controls.js（共用表格操作程式）`會在表頭初始化及重新整理時統一確認雙語節點，保留排序、欄位顯示與使用者自訂欄寬。新頁面仍必須直接建立正式雙語結構，不得依賴舊字串自動猜測。
- 同步修正款號、訂單、成本歷史、帳號、產能登記，以及員工、IE（工業工程）與部門生產分析的已知動態殘留；載入版本已更新，避免瀏覽器沿用舊程式快取。
- 實際瀏覽器檢查確認：只顯示越文時，已載入的摘要、成本設定第二語言、成本歷史及動態輸入提示均未殘留中文；只顯示中文時未殘留越文。未登入環境無法建立需要正式身分與資料的完整功能內容，仍需使用者登入後檢查真實表格、長文字及按需載入頁面。
- 11 個本次介面程式通過語法檢查，17 個非雲端測試檔共 142 項全部通過；本次沒有修改業務資料、公式、登入、角色、權限、語言偏好保存方式、功能資料快取、Firebase（雲端資料庫）讀寫、安全規則或雲端索引。
- 回復時只需回復本節涉及的語言文字、共用表格控制、功能顯示文字、共用樣式及載入版本；不需要清除或轉換 IndexedDB（瀏覽器本機資料庫）偏好，也不需要回復任何正式資料。

## 十六、工序修改、款號版本與訂單快照（2026 年 8 月 13 日本機完成）

- 舊獨立「工序秒數同步」已從中央功能清單、導覽、主畫面及按需載入中移除，`js/sync.js（舊同步程式）`與 `styles/features/sync.css（舊同步樣式）`已刪除。舊 `secondSyncLogs（工序同步紀錄）`未實體刪除，但新安全規則將網站存取全部拒絕。
- 新「工序修改」位於產能主功能下，使用獨立 `productionProcessEdit（工序修改）`權限與按需載入模組。可新增、刪除、排序、改名稱、類別與秒數，並分為「正式標準修正」與「單張訂單例外」。
- 正式標準修正會保留開發原始工序，更新款號目前正式標準，並讓使用者選擇哪些生產中訂單要同步。快照修改前的 `productionEntries（產能登記）`保留原秒數與產能快照，不改寫歷史效率。
- 款號匯入改為只建立新款號；同碼相同或不同都列出後略過，永久取消覆蓋舊款號入口。每次實際異動同時建立可匯出的完整 `productVersions（款號版本）`與 `productVersionChunks（款號版本分段）`。
- 同產品群組依客戶、品名、工序數量、順序、類別與名稱提供候選，尺寸與秒數不影響候選，顏色暫不處理；必須人工確認才建立。切換群組尺寸時會保留已勾選的套用款號。
- IE（工業工程）分析的確認修正只將款號、工序號與建議秒數帶入新頁，不在分析頁直接寫入。
- 本批新資料路徑、權限、訂單鎖定、工作狀態與操作紀錄的 `firestore.rules（Firebase 雲端文件資料庫安全規則）`已在本機完成，尚未取得另行發布授權；正式網站要使用新功能前，仍需另行發布這批安全規則。

# 系統架構規劃

最後整理：2026-09-05

## 1. 文件定位

本文件是系統目前實際採用的全系統架構正式來源，只保存現行設計、資料路徑、載入方式、快取、安全邊界及驗收原則，不保存開發階段、提交編號、測試數量或已被取代的歷史方案。

- 全案工作規則：`AGENTS.md（全案代理規則）`。
- 共用介面規則：`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`。
- 各功能的業務、畫面與計算細節：各自功能文件。
- 實際程式入口、權限鍵與按需載入清單：`js/features.js（中央功能清單）`。
- 實際資料欄位與限制：現行程式、安全規則及自動測試。

文件與程式不一致時，不得以過期文件覆蓋現行正式行為；應先核對現行程式與業務需求，再同步修正正式文件。

## 2. 系統定位與核心原則

本系統管理訂單、款號、工序、產能、考勤、分析、績效獎金、成本、裁帶、裁片與管理功能。

共同原則：

1. 一項正式資料只有一個正式來源。
2. 使用者實際開啟功能時才載入該功能的程式與資料。
3. 固定身分與可修改顯示資料分開；名稱或代碼調整不得任意改變歷史關聯。
4. 重要資料與其操作紀錄必須一起成功或一起失敗。
5. 未鎖定資料使用目前正式來源；正式鎖定後保存當時完整結果。
6. 一般舊資料可以保留供稽核，但已淘汰路徑不得繼續成為正式 Runtime（執行程式）來源；舊款號修改／匯入歷史依第 13 節例外處理，不得恢復。
7. 使用者只需驗收操作方式、畫面與業務結果；技術識別碼、交易、快取與分批處理由系統負責。

## 3. 功能登記與功能文件

主要功能與正式文件：

| 功能 | 正式文件 |
|---|---|
| 訂單資料、訂單匯入、訂單項目與封存 | `訂單資料.md` |
| 款號管理、同產品群組、款號匯入與快速修改 | `款號管理.md` |
| 產能登記、員工績效、考勤與員工資料 | `產能登記.md` |
| 生產分析 | `生產分析.md` |
| 月績效獎金與獎金參數 | `績效獎金.md` |
| 系統監控 | `系統監控.md` |
| 備料／裁帶／裁片 | `備料.md`、`工具轉換系統.MD`、`CUTTING_LOCAL_SERVER.md`、`裁片出單.md`、`PIECE_CUTTING_LOCAL_SERVER.md` |
| 雲端費用參考 | `雲端資料庫計費方式.md` |

新功能必須先搜尋現有文件，避免建立重複規格。功能名稱、入口、權限鍵、程式及資料載入統一登記於 `js/features.js`。

## 4. 程式架構與按需載入

### 4.1 核心與功能程式

- 登入、權限、Firebase（雲端資料庫）連線、共用快取、共用訊息及中央介面元件屬於共用底層。
- 訂單、款號、產能、分析、獎金、裁帶、裁片及其他主要功能使用各自程式檔。
- 功能專屬邏輯不得塞入 `index.html（主畫面）`、`auth.js（登入權限）`、`firebase.js（雲端核心）`或其他無關功能。
- 跨功能只能透過公開且穩定的服務介面合作，不得直接操作其他功能的畫面狀態或內部變數。

### 4.2 載入生命週期

1. 登入時只載入身分、帳號狀態、角色權限及首頁必要設定。
2. 使用者開啟功能後才載入該功能程式與必要資料。
3. 相同頁面同時發出的相同查詢共用同一個 Promise（非同步工作承諾）。
4. 離開功能或登出時，停止該功能的監聽、計時器及背景工作。
5. 一般且向下相容的網站更新，使用 `runtime-version.json（網站版本狀態檔）`標記為 `notice（提醒更新）`，舊分頁可完成目前工作，並在側邊欄持續顯示更新提醒。
6. 不相容、資料格式或安全性更新必須標記為 `required（必須更新）`；舊分頁無法安全寫入時，必須阻止寫入並要求重新載入，不能寫出新舊混合資料。未標記或無法辨識的更新一律視為必須更新。
7. 網站執行版本只用於判斷網頁程式是否需要更新；使用者新增或修改業務資料及各功能資料版本變化，不得觸發全系統更新提醒。
8. 閒置登出以最後操作時間與目前時間的實際差值判斷，不依賴畫面倒數計時；瀏覽器背景休眠後恢復時立即核對，超過期限就登出。側邊欄不顯示倒數，登出後不得由網站自行重新登入。

## 5. 正式資料身分與來源

### 5.1 款號與工序

- `products/{productId}`是目前正式 Product Master（款號主檔）。
- `productId（款號固定識別碼）`在款號建立後固定。
- `processId（工序固定識別碼）`在工序建立後固定。
- 款號代碼由 `productCodeIndex（款號代碼索引）`唯一對應 `productId`；現行介面不允許修改既有款號代碼。
- 款號客戶、中文／越文品名、尺寸、工序號、分類、名稱、標準秒數及每小時產能由款號主檔提供。
- 每小時產能使用現行共用公式由標準秒數與正式工作秒數計算，不建立第二套獨立來源。
- `system/productsMeta（款號版本資料）`是款號快取唯一新舊判斷來源，並保存當前追蹤起點、增量起始序號及正式刪除標記。
- `productChangeBatches（款號修改批次）`保存單款、群組與 Excel（試算表）匯入的主摘要；`productChangeItems（款號修改明細）`保存每款完整修改前後內容。
- 現行流水帳以 `system/productsMeta`保存的追蹤起點為基準；起點以前的舊款號修改歷史不回填、不轉換，也不作為目前功能的備援來源。

### 5.2 訂單與訂單項目

- `orders（訂單）`保存訂單本身的客戶、PO、說明、交期、完成／出貨日期及備註。
- `orderItems（訂單項目）`使用固定 `orderItemId（訂單項目識別碼）`連結訂單、款號及該行數量。
- 同一訂單可有多行相同款號；不同 PO、顏色、交期或條件仍是不同訂單項目。
- 款號主檔更新不得覆蓋訂單自己擁有的業務欄位。

### 5.3 產能、累計與摘要

- `productionEntries（產能紀錄）`保存員工、日期、`orderItemId`、`productId`、`processId`、數量及狀態。
- `productionProcessTotals（工序累計）`保存同一訂單項目與工序的有效累計數量。
- `productionAttendance（考勤）`保存員工每日正常與加班工時。
- `productionDaySummaries（每日摘要）`及 `productionEmployeeMonths（員工月摘要）`是可由正式來源重建的衍生資料。
- `productionMonths（生產月份）`是月份狀態、來源修訂及摘要可用狀態的唯一控制來源。

### 5.4 同產品群組

- `productGroups（同產品群組）`保存群組主資料。
- `productGroupMembers（群組成員）`保存固定 `productId`與群組的關聯。
- 群組不是另一份款號內容；顯示與修改仍以目前款號主檔為準。
- 尺寸分頁由群組成員目前款號資料即時計算，不另外保存可互相衝突的尺寸群組。

### 5.5 績效獎金鎖定資料

- 未鎖定月份使用目前員工月摘要、公開獎金表與人工調整即時計算。
- `performanceBonusSnapshots（獎金凍結快照）`及 `performanceBonusSnapshotChunks（獎金快照分段）`保存鎖定時完整結果。
- 鎖定快照包含當時產能、考勤、款號／工序、訂單情境、分析、績效及最終獎金所需內容。
- 鎖定後再修改款號、工序或公式，不得改變已凍結結果。

### 5.6 操作紀錄

- `operationLogs（獨立操作紀錄）`保存匯入、匯出、批次修改、作廢、鎖定及重要設定異動。
- 紀錄至少包含操作者、時間、功能、動作、結果、目標或檔名及影響筆數。
- 一般網站功能不得修改或刪除已建立的操作紀錄。

### 5.7 備料主檔

- 裁帶沿用 `cuttingTemplates`及`cuttingTemplateChunks`，行為由裁帶正式規格管理。
- 裁片使用單一 `pieceCuttingTemplates/main`作為主檔版本與唯一 freshness source（資料新舊判斷來源），原始 Excel（表格檔）保存於`pieceCuttingTemplateChunks/main_00000...`。單一主檔會解析全部非空白工作表；圖片來源群組使用「工作表身分＋G 欄合併列範圍」識別，款號必須在整份主檔唯一，同一圖片群組內的布料＋裁片名稱也必須唯一。
- 裁片 PDF（可攜式文件）先以工作表身分及主檔 G 欄合併範圍建立圖片來源群組，再由獨立本機工具套用 Excel 明確保存的旋轉角度與裁切範圍；沒有旋轉設定時保持原圖，不依寬高猜測方向。工具以處理後圖片 SHA-256（內容驗證碼）辨識實際顯示內容，最後以「實際圖片內容＋布料完整名稱」建立固定 A4 橫式工作頁；不同來源群組的顯示圖片完全相同且布料相同時可合併，相同布料但圖片內容不同時分開，同組多款號共用一張固定框代表照片，數量固定為訂單數量乘以 `SỐ KIỆN`。裁片表採固定欄寬、完整列分頁，下方固定保留備註表與照片；多訂單於瀏覽器本機逐檔驗證後合併加總，相同正式訂單編號的檔案副本會阻擋，任一訂單款號缺少主檔時也會阻擋匯出，成功匯出仍只建立一筆操作紀錄。詳細排版、錯誤定位與用量預算以`裁片出單.md`為準。
- 裁帶與裁片共用既有 `preparationMain + cutting`權限組合，但資料路徑、功能程式、快取、本機服務與歷史動作完全分開。
- 主導覽以「備料出單」作為分類抬頭，裁帶出單與裁片出單各自使用直接子入口；中央功能清單仍以同一個備料模組登記兩個頁面，內容區不建立重複的跨功能切換列。此導覽調整不改變權限鍵、資料路徑、安全邊界或雲端用量。

## 6. 未鎖定與鎖定資料行為

### 6.1 未鎖定月份

Product Resolver（款號解析器）依 `productId + processId`把目前款號主檔套用到未鎖定訂單顯示、產能紀錄、摘要、分析、績效與月績效獎金。

修改款號或工序後，使用者不需要逐訂單、逐月份或逐筆產能按同步，也不需要手動重建摘要。歷史產能的員工、日期、訂單項目、工序身分、數量及狀態不得因顯示資料更新而改變。

### 6.2 鎖定月份

- 只有正式鎖定／匯出月績效獎金時，月份才凍結。
- 鎖定後來源產能、考勤與凍結結果不可由一般功能變更。
- 解除鎖定按鈕與權限保留，但現行功能只顯示「功能尚未接入」，不得改變狀態或資料。

## 7. 款號匯入與訂單匯入

### 7.1 款號匯入

- 正規化後的款號代碼是唯一判斷鍵。
- 新款號建立固定 `productId`；既有款號採完整覆蓋並保留原 `productId`。
- 同工序號沿用原 `processId`；新增工序建立新 `processId`；匯入檔未出現的舊工序不留在目前主檔。
- 既有未鎖定產能不大量複製新名稱與秒數，而是由解析器使用覆蓋後資料。
- 若移除的舊工序已有正式引用且沒有可唯一承接的新工序，必須阻止該款號覆蓋並列出原因。
- 寫入前顯示檔案、覆蓋／新增款號、工序變更及受影響報工明細；使用者確認後才執行。
- 讀取、分析、儲存及結果階段都必須顯示進度與逐款號結果。

### 7.2 訂單匯入

- 匯入建立 `orders`與`orderItems`，不得建立另一份款號／工序主檔。
- 訂單項目只保存訂單自己擁有的欄位及固定款號關聯。
- 匯入前重新核對款號版本，避免使用過期畫面建立錯誤關聯。
- 同檔重複、缺少欄位、無法唯一識別的款號及不合法數量必須在寫入前列出。

## 8. 完整寫入、同時操作與中斷

- 每次款號操作先原子建立一筆 `productChangeBatches`主摘要與開始 `operationLogs（獨立操作紀錄）`。
- 每個款號的主檔、新增時的代碼索引、`productChangeItems`明細及 `system/productsMeta`在同一筆 Transaction（雲端交易）一起成功或失敗。
- 每筆成功款號修改在同一筆交易取得唯一且單調增加的 `changeSequence（修改序號）`；群組逐款交易不要求序號連續，其他使用者的修改可穿插，增量同步仍依序號範圍取得全部差異。
- 全批結束時再原子結案主摘要與結果 `operationLogs`；群組與匯入允許已成功款號保留，失敗與未處理項目逐筆列明。
- 產能新增／作廢與工序累計、月份修訂、必要摘要及操作紀錄必須原子完成。
- 同一訂單項目同一工序的有效累計不得超過訂單項目數量；不同工序分開計算。
- 同時修改同一款號、同時逼近數量上限，或修改訂單數量與報工同時發生時，必須明確失敗或要求重試，不得發生 Lost Update（更新遺失）。
- 大批操作使用可重跑的固定識別碼、檢查點及明確結果；中斷後不得重複建立或重複累計。

## 9. 讀取與快取

- 登入階段不得預讀完整款號、訂單、產能、分析或獎金集合。
- 大型集合使用條件、日期範圍、數量限制或分頁。
- 持久快取只用於可證明能降低重複大量讀取的資料，並依 UID（使用者識別碼）隔離；裁帶模板與裁片主檔是經核准的同機共用例外。
- 款號快取只以 `system/productsMeta`作為正式新舊判斷來源，不建立重疊的款號版本來源。
- 款號版本相同時直接使用 UID（使用者識別碼）隔離的本機快取；版本不同且本機同步位置有效時，只查詢本機序號之後、目前序號以前的款號文件，再合併正式刪除標記。
- 既有第四版快取若版本正好等於雲端版本，可直接補記追蹤起點而不重讀款號；缺少同步位置、追蹤起點不同、筆數驗證失敗或同步期間持續變動時，才完整讀取目前 `active == true（有效）`的款號，不讀取已退役技術指向文件。
- 完整集合或快取更新只能在查詢明確成功、結果由雲端確認、讀取前後唯一版本位置相同，且可用正式摘要驗證筆數完整後發布；在此之前不得清空或取代畫面與既有快取。
- `0 筆`不是單獨的成功條件。只有雲端查詢成功、版本前後一致且正式摘要也為 0 時才是合法空資料；網路中斷、離線快取空白、部分結果、版本改變或無法驗證時，必須保留最後一次已驗證資料並提示同步失敗。
- 同一資料範圍有重疊背景請求時只允許最新請求發布；較舊請求即使晚回，也不得覆蓋已驗證的新資料。登出、換帳號或重設功能狀態時必須使尚未完成的舊請求失效。
- 驗證失敗不得在同一次背景工作自動重複完整集合查詢；保留舊資料並等下一次背景或使用者重試，避免短暫版本變動放大 Reads（文件讀取次數）。
- 正常程式直接讀取預算：版本未變約 1 Read、單款變更約 3 Reads、10 款變更約 12 Reads；若期間另有其他使用者修改，增加的只有那些實際不同款號。完整回復的冷讀取上限約為 `目前有效款號數 N + 2`，已有有效版本記憶時約為 `N + 1`；合法 0 筆查詢仍依雲端最低計費約 3 Reads。安全規則驗證所需的身分與角色文件可能另計費。
- 現行網站仍禁止永久刪除整個款號。未來若建立正式刪除流程，必須在同一交易更新版本、序號及累積刪除標記；未更新版本文件的人工雲端刪除不屬於支援操作。
- 產能日期範圍與摘要使用 `productionMonths`的對應版本與狀態判斷失效。
- 員工績效在同一次日期範圍查詢結果內進行週／月分頁；全部員工依七天、精確單一員工依月份，翻頁不得重新查詢雲端。
- 款號流水帳摘要輸入只搜尋已載入內容；使用者按 Enter（確認鍵）跨批次搜尋時，最多讀取 50 筆明細及其對應的 50 筆主摘要，單次直接讀取預算上限約 100 Reads（文件讀取次數）。
- 寫入後只更新或清除直接受影響的記憶體與快取，不讓無關功能整批重讀。
- 即時監聽必須有條件且只在功能開啟期間存在。
- 裁片頁開啟時只讀 `pieceCuttingTemplates/main`一筆；需要配對訂單且本機內容驗證碼不同時才讀裁片主檔分塊。多工作表解析、重複款號／裁片／訂單檢查與缺少主檔檢查都使用已載入的本機資料，不另增雲端讀寫；本機圖片快取以第三版工作表身分重建。正式分塊、快取與最低／一般／最壞用量預算以`裁片出單.md`第 5、8 節為準。

## 10. 權限與安全邊界

- 所有瀏覽器直接存取的正式資料都必須受登入、有效 `userAccess/{UID}`、角色功能權限及 Firestore Security Rules（雲端資料庫安全規則）保護。
- 非管理員角色的新功能權限預設關閉；缺少權限欄位時安全視為關閉。
- 前端隱藏或停用按鈕只負責操作體驗，不能取代安全規則。
- 安全規則負責身分、授權、不可變識別碼、重要狀態及必要格式；複雜可重建計算不在規則內重算。
- 正式資料與保密獎金參數必須使用不同權限邊界，公開月獎金頁不得讀取保密公式與公司損益資料。
- 款號修改流水帳由已開放 `productsMain（款號管理主功能）`的使用者共同查看；匯入依 `summary（款號總表）`驗證，單款、群組、工序與標準秒數修改統一依 `productionProcessEdit（工序修改，含群組）`驗證。
- 每次實際改變資料路徑、角色或安全責任時，必須同步更新權限頁、安全規則及允許／拒絕測試。

本文件不記錄某次安全規則是否已發布；發布狀態必須以當次雲端內容與本機版本實際比對。

## 11. 介面與語言

- 正式介面支援雙語、只顯示越文、只顯示中文三種中央模式。
- 固定寬度元件以越文在上、中文在下呈現；大量說明先完整越文，再完整中文。
- 表格表頭使用雙行文字，並保持桌機欄寬、對齊、水平捲動與狀態顏色一致。
- 動態載入、確認、進度、成功、失敗與空資料訊息同樣遵守中央語言規則。
- 批次修改、覆蓋匯入、鎖定與匯出必須提供修改前預覽、執行進度及完成結果。

## 12. 匯出與檔案儲存

- 開始產生或下載檔案前，先讓使用者選擇檔名與儲存位置。
- 取消選擇時停止後續處理，不留下空白或未完成檔案。
- 瀏覽器不支援系統儲存位置選擇時，明確提示並停止，不得偷偷改用預設下載。
- 儲存失敗不得以不完整內容覆蓋原檔。
- 畫面、凍結結果與匯出檔必須使用同一套正式計算來源。

## 13. 已淘汰 Runtime 路徑

正式 Runtime 不得再讀寫下列舊來源或重新加入相容備援：

- `orderProcesses`
- `productProcessStandards`
- `processEditJobs`
- `productionAnalysisSummaries`
- `productionDayChanges`
- `productionProcessAnalysisQueue`
- 舊款號／工序 Snapshot（快照）同步路徑
- 舊 Summary Migration（摘要轉換）路徑
- 舊工序優化／標準錯誤訂正模式
- `productHistory`、`productChanges`、`system/impHist`及其 `historyId`、`targetHistoryId`等舊款號歷史指向

一般舊雲端資料可以保留供稽核，但不得讓正式程式維護新舊雙軌。舊款號修改、舊匯入、舊歷史與對應操作紀錄是明確例外，不再保留；只保留固定 `productId`、`processId`所需的技術身分轉換紀錄。現行程式、測試與安全規則必須持續保持這些舊款號歷史路徑為 0 Reads / 0 Writes / 0 Compatibility Logic / 0 Rules Dependencies（零讀取／零寫入／零相容邏輯／零安全規則依賴）。

## 14. 測試與完成標準

每次修改依影響範圍驗收：

1. 管理員、全部有效非管理員角色、未授權、停用及無效角色。
2. 款號、訂單、產能、考勤、摘要、分析、績效與獎金的跨功能一致性。
3. 同時操作、數量上限、原子寫入及中斷重試。
4. 舊瀏覽器快取、舊分頁、重新登入及 Cold / Warm Cache（冷／暖快取）。
5. 三種語言模式與桌機版面。
6. 匯入、預覽、進度、結果、檔案取消及寫入失敗。
7. 月份鎖定後畫面、凍結資料與 Excel（試算表）結果一致。
8. 正式 Runtime 對已淘汰路徑零存取。
9. 單款修改、群組修改與 Excel 匯入都產生一層主摘要與一層直接明細，並可依款號跨批次查找。
10. 舊款號歷史重設前後，當前款號、工序及其固定識別碼的業務內容比對必須完全一致。
11. 閒置跨日與瀏覽器背景休眠後恢復時，依實際經過時間登出且不自動重新登入；畫面不顯示登出倒數。
12. 群組可保存一個以上固定成員但不得為空；群組快速修改的來源款號只作比較基準，可取消勾選。

文件只記錄現行要求與可重複驗收方式，不保存容易過期的單次測試數字、提交編號或發布宣告。

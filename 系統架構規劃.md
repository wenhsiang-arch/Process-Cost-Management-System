# 系統架構規劃

## 1. 文件定位

本文件是系統目前實際採用的全系統架構正式來源，只保存現行設計、資料路徑、載入方式、快取、安全邊界及驗收原則，不保存開發階段、提交編號、測試數量或已被取代的歷史方案。

- 全案工作規則：`AGENTS.md（全案代理規則）`。
- 共用介面規則：`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`。
- 各功能的業務、畫面與計算細節：各自功能文件。
- 實際程式入口、權限鍵與按需載入清單：`js/features.js（中央功能清單）`。
- 實際資料欄位與限制：現行程式、安全規則及自動測試。

文件與程式不一致時，不得以過期文件覆蓋現行正式行為；應先核對現行程式與業務需求，再同步修正正式文件。

## 2. 系統定位與核心原則

本系統管理訂單、款號、工序、產能、考勤、分析、績效獎金、成本、裁帶、裁片與管理功能。

共同原則：

1. 一項正式資料只有一個正式來源。
2. 使用者實際開啟功能時才載入該功能的程式與資料。
3. 固定身分與可修改顯示資料分開；名稱或代碼調整不得任意改變歷史關聯。
4. 重要資料與其操作紀錄必須一起成功或一起失敗。
5. 未鎖定資料使用目前正式來源；正式鎖定後保存當時完整結果。
6. 一般舊資料可以保留供稽核，但已淘汰路徑不得繼續成為正式 Runtime（執行程式）來源；舊款號修改／匯入歷史依第 13 節例外處理，不得恢復。
7. 使用者只需驗收操作方式、畫面與業務結果；技術識別碼、交易、快取與分批處理由系統負責。

## 3. 功能登記與功能文件

主要功能、中央頁面路徑與正式文件：

| 功能 | `page（中央頁面路徑）` | 正式文件 |
|---|---|---|
| 系統更新、最新公告與年度歷史 | `home` | `系統更新.md` |
| 訂單資料、匯入、出貨、封存及歷史操作紀錄 | `progress`、`shipped-orders`、`order-archive`、`order-history` | `訂單資料.md` |
| HUNTER 檢驗報告範本與依款號匯出 | `inspection-report-template`；匯出入口位於訂單頁 | `檢驗報告.md` |
| 款號總表、修改流水帳、群組與工序修改 | `summary`、`product-change-log`、`product-groups` | `款號管理.md` |
| 產能登記、員工績效、考勤與員工資料 | `production-entry`、`production-records`、`production-attendance`、`production-employees` | `產能登記.md` |
| 月績效獎金 | `production-bonus` | `績效獎金.md` |
| 生產分析 | `production-analysis` | `生產分析.md` |
| 成本設定、成本變動紀錄與產品工價匯出 | `settings`、`costlog`、`export` | `成本管理.md` |
| 績效獎金參數 | `performance-bonus-settings` | `績效獎金.md` |
| 帳號核准、管理與角色功能權限 | `accounts`、`permissions` | `帳號管理.md` |
| 系統監控 | `system-monitor` | `系統監控.md` |
| 備料分類、裁帶／裁片入口與共用權限 | `cutting`、`piece-cutting` | `備料.md` |
| 裁帶業務與紙張輸出規格 | `cutting` | `工具轉換系統.MD` |
| 裁帶工具安裝、啟動與故障排除 | 不屬於網站頁面 | `CUTTING_LOCAL_SERVER.md` |
| 裁片業務、主檔與紙張輸出規格 | `piece-cutting` | `裁片出單.md` |
| 裁片工具安裝、啟動與故障排除 | 不屬於網站頁面 | `PIECE_CUTTING_LOCAL_SERVER.md` |
| 雲端費用參考 | 不屬於網站頁面 | `雲端資料庫計費方式.md` |

`page（中央頁面路徑）`只作文件索引；功能入口、權限、程式、樣式及按需載入仍以 `js/features.js（中央功能清單）`為唯一執行來源。新增、改名或移除頁面時，必須在同一次任務同步核對本表與對應功能文件，不能在功能內另建第二套路徑清單。

新功能必須先搜尋現有文件，避免建立重複規格。功能名稱、入口、權限鍵、程式及資料載入統一登記於 `js/features.js`。

首頁更新日誌只說明使用者可感受到的改變。最新 5 則保留在 `index.html（主畫面）`，較早內容各自唯一保存於 `data/home-updates/{year}.json（年度公告檔）`；使用者點擊後才由 `js/features.js（中央功能清單）`載入 `js/home-updates.js（歷史公告程式）`及所選年度檔。首頁以 `home`登記按需資源，不加入業務模組或角色權限清單；沒有雲端資料、業務快取或另一套語言偏好。功能規格、維護方式與完整用量預算以 `系統更新.md`為準。功能規格與工具操作手冊各有責任，不因同屬備料就合成一份。

## 4. 程式架構與按需載入

### 4.1 核心與功能程式

- 登入、權限、Firebase（雲端資料庫）連線、共用快取、共用訊息及中央介面元件屬於共用底層。
- 訂單、款號、產能、分析、獎金、裁帶、裁片及其他主要功能使用各自程式檔。
- 功能專屬邏輯不得塞入 `index.html（主畫面）`、`auth.js（登入權限）`、`firebase.js（雲端核心）`或其他無關功能。
- 跨功能只能透過公開且穩定的服務介面合作，不得直接操作其他功能的畫面狀態或內部變數。

### 4.2 載入生命週期

1. 登入時只載入身分、帳號狀態、角色權限及首頁必要設定。
2. 使用者開啟功能後才載入該功能程式與必要資料。
3. 相同頁面同時發出的相同查詢共用同一個 Promise（非同步工作承諾）。
4. 離開功能或登出時，停止該功能的監聽、計時器及背景工作。
5. 一般且向下相容的網站更新，使用 `runtime-version.json（網站版本狀態檔）`標記為 `notice（提醒更新）`，舊分頁可完成目前工作，並在側邊欄持續顯示更新提醒。
6. 不相容、資料格式或安全性更新必須標記為 `required（必須更新）`；舊分頁無法安全寫入時，必須阻止寫入並要求重新載入，不能寫出新舊混合資料。未標記或無法辨識的更新一律視為必須更新。
7. 網站執行版本只用於判斷網頁程式是否需要更新；使用者新增或修改業務資料及各功能資料版本變化，不得觸發全系統更新提醒。
8. 非管理員的閒置登出以最後操作時間與目前時間的實際差值判斷，不依賴畫面倒數計時；瀏覽器背景休眠後恢復時立即核對，超過 30 分鐘就登出。管理員不啟動閒置自動登出，但手動登出、帳號停用、權限失效及其他登入攔截仍然有效。側邊欄不顯示倒數，登出後不得由網站自行重新登入。

## 5. 正式資料身分與來源

### 5.1 款號與工序

- `products/{productId}`是目前正式 Product Master（款號主檔）。
- `productId（款號固定識別碼）`在款號建立後固定。
- `processId（工序固定識別碼）`在工序建立後固定。
- 款號代碼由 `productCodeIndex（款號代碼索引）`唯一對應 `productId`；現行介面不允許修改既有款號代碼。
- 款號客戶、中文／越文品名、尺寸、工序號、分類、名稱、標準秒數及每小時產能由款號主檔提供。
- 款號工序分類只允許 `BL（備料）、TC（普工）、SX（生產）、HC（後整）、QC（品檢）、DG（包裝）`；單一款號可只使用實際需要的分類，不要求六種齊全。訂單生產進度仍只統計 `SX（生產）`工序。
- 每小時產能使用現行共用公式由標準秒數與正式工作秒數計算，不建立第二套獨立來源。
- `system/productsMeta（款號版本資料）`是款號快取唯一新舊判斷來源，並保存當前追蹤起點、增量起始序號及正式刪除標記。
- `productChangeBatches（款號修改批次）`保存單款、群組與 Excel（試算表）匯入的主摘要；`productChangeItems（款號修改明細）`保存每款完整修改前後內容。
- Excel 匯入覆蓋同款號時，工序固定身分以同款號內唯一的正規化越文工序名稱配對，不以工序號配對。新匯入檔同款號不可出現重複越文工序名稱。
- `productLegacyProcesses（舊工序參照）`只保存被新版正式工序移除、但仍被既有產能引用的固定工序資料。它不屬於正式 `ops（工序清單）`、不進入快取版本、工序數、工價或匯出，且只在解析已移除工序時按需讀取。
- 舊工序參照目前不設自動清除；未經使用者另行確認，不得新增定期刪除或與凍結結果相互修改的流程。
- 匯入影響查詢使用 `productionEntries（產能紀錄）`的 `processId + status + productionDate`複合索引。純排序、工序號、中文或款號基本文字變更不讀取產能；移除、分類或秒數改變才進入影響查詢。績效差異先讀取每月一筆的 `productionMonths（生產月份狀態）`，從最早未凍結月份起查詢有效產能；狀態不完整時回退完整歷史查詢。正式雲端用量預算與估算前提以 `款號管理.md` 第 12.1 節為準。
- 現行流水帳以 `system/productsMeta`保存的追蹤起點為基準；起點以前的舊款號修改歷史不回填、不轉換，也不作為目前功能的備援來源。

### 5.2 訂單與訂單項目

- `orders（訂單）`保存訂單本身的客戶、PO、說明、交期、完成／出貨日期及備註。
- `orderItems（訂單項目）`使用固定 `orderItemId（訂單項目識別碼）`連結訂單、款號及該行數量。
- 同一訂單可有多行相同款號；不同 PO、顏色、交期或條件仍是不同訂單項目。
- 款號主檔更新不得覆蓋訂單自己擁有的業務欄位。

### 5.3 產能、累計與摘要

- `productionEntries（產能紀錄）`保存員工、日期、`orderItemId`、`productId`、`processId`、數量及狀態。
- `productionProcessTotals（工序累計）`保存同一訂單項目與工序的有效累計數量。
- `orderProductionProgressVersions（訂單生產進度版本）`是一般產能新增／作廢時同步更新的小型失效標記；正式進度仍由訂單項目、目前款號生產工序與工序累計重建。
- `productionAttendance（考勤）`保存員工每日正常與加班工時。
- `productionDaySummaries（每日摘要）`及 `productionEmployeeMonths（員工月摘要）`是可由正式來源重建的衍生資料。
- `productionMonths（生產月份）`是月份狀態、來源修訂及摘要可用狀態的唯一控制來源。
- 未凍結產能平常先以目前正式工序解析；正式工序已移除才依 `productId + processId`讀取對應舊工序參照。已凍結月份仍只使用凍結快照。

### 5.4 同產品群組

- `productGroups（同產品群組）`保存群組主資料。
- `productGroupMembers（群組成員）`保存固定 `productId`與群組的關聯。
- 群組不是另一份款號內容；顯示與修改仍以目前款號主檔為準。
- 尺寸分頁由群組成員目前款號資料即時計算，不另外保存可互相衝突的尺寸群組。

### 5.5 績效獎金鎖定資料

- 未鎖定月份使用目前員工月摘要、公開獎金表與人工調整即時計算。
- `performanceBonusSnapshots（獎金凍結快照）`及 `performanceBonusSnapshotChunks（獎金快照分段）`保存鎖定時完整結果。
- 鎖定快照包含當時產能、考勤、款號／工序、訂單情境、分析、績效及最終獎金所需內容。
- 鎖定後再修改款號、工序或公式，不得改變已凍結結果。

### 5.6 操作紀錄

- `operationLogs（獨立操作紀錄）`保存匯入、匯出、批次修改、作廢、鎖定及重要設定異動。
- 紀錄至少包含操作者、時間、功能、動作、結果、目標或檔名及影響筆數。
- 一般網站功能不得修改或刪除已建立的操作紀錄。
- 功能內的歷史操作紀錄依中央功能清單的主功能權限範圍查詢；同一主功能的各分頁共用一個歷史入口，不以固定動作清單切割，也不得重複建立另一份歷史集合。各主功能可依自身資料設計摘要與欄位，不要求所有歷史頁使用相同版面。

### 5.7 備料主檔

- 裁帶沿用 `cuttingTemplates`及`cuttingTemplateChunks`，行為由裁帶正式規格管理。
- 裁片以 `pieceCuttingTemplates/main（裁片主檔）`作為唯一主檔版本與資料新舊判斷來源，原始表格分段保存於 `pieceCuttingTemplateChunks/main_00000...（裁片主檔分段）`。網站依版本按需載入，輸出交由獨立本機工具處理；主檔解析、唯一性、圖片處理、排版、錯誤定位及用量預算以 `裁片出單.md`為準，工具安裝與故障排除以 `PIECE_CUTTING_LOCAL_SERVER.md`為準。
- 裁帶與裁片共用既有 `preparationMain + cutting`權限組合，但資料路徑、功能程式、快取、本機服務與歷史動作完全分開。
- HUNTER 檢驗報告隸屬訂單模組，沿用 `progress（訂單資料）`單一權限；範本主檔為 `inspectionReportTemplates/main`，原始 `.xlsx`分段存於 `inspectionReportTemplateChunks/main_00000...`，不使用裁帶／裁片資料路徑，也不依賴獨立本機轉換工具。既存 `.xls`範本可下載或替換，不再用於匯出；新範本只接受 `.xlsx`。匯出時保留範本字型與排版，開啟位置回到最上方；功能文件與雲端用量預算以 `檢驗報告.md`為準。
- 主導覽以「備料出單」作為分類抬頭，裁帶出單與裁片出單各自使用直接子入口；中央功能清單仍以同一個備料模組登記兩個頁面，內容區不建立重複的跨功能切換列。此導覽調整不改變權限鍵、資料路徑、安全邊界或雲端用量。

## 6. 未鎖定與鎖定資料行為

### 6.1 未鎖定月份

Product Resolver（款號解析器）依 `productId + processId`把目前款號主檔套用到未鎖定訂單顯示、產能紀錄、摘要、分析、績效與月績效獎金。

修改款號或工序後，使用者不需要逐訂單、逐月份或逐筆產能按同步，也不需要手動重建摘要。歷史產能的員工、日期、訂單項目、工序身分、數量及狀態不得因顯示資料更新而改變。

### 6.2 鎖定月份

- 只有正式鎖定／匯出月績效獎金時，月份才凍結。
- 鎖定後來源產能、考勤與凍結結果不可由一般功能變更。
- 解除鎖定按鈕與權限保留，但現行功能只顯示「功能尚未接入」，不得改變狀態或資料。

## 7. 款號匯入與訂單匯入

### 7.1 款號匯入

- 正規化後的款號代碼是唯一判斷鍵。
- 新款號建立固定 `productId`；既有款號採完整覆蓋並保留原 `productId`。
- 既有款號匯入依同款號內唯一的正規化越文工序名稱沿用原 `processId`，不以工序號或排序配對；無法配對的新工序建立新 `processId`，匯入檔未出現的舊工序不留在目前主檔。
- 既有未鎖定產能不大量複製新名稱與秒數，而是由解析器使用覆蓋後資料。
- 移除的舊工序已有正式引用時，以獨立 `productLegacyProcesses（舊工序參照）`保留原固定身分與工序資料，不阻止新款號表覆蓋，也不把舊工序塞回正式主檔。
- 寫入前顯示檔案、覆蓋／新增款號、工序變更及受影響報工明細；使用者確認後才執行。
- 讀取、分析、儲存及結果階段都必須顯示進度與逐款號結果。

### 7.2 訂單匯入

- 訂單資料檔的 `.xlsx`與 `.xls`共用同一個 `ArrayBuffer（陣列緩衝區）`讀取與試算表解析入口；副檔名只用於接受格式檢查，不分成兩套欄位判斷。品檢、裁帶與裁片範本因需保存既定格式，仍依各自功能文件固定只接受 `.xlsx`。
- 匯入預覽資料只存在 `js/orders.js（訂單功能程式）`內，保存正式匯入所需欄位及 `processCount（工序數）`，不建立瀏覽器全域暫存，也不複製完整工序陣列。選檔後的大型拖放區縮成檔名與重新選擇單列；重新選檔、格式拒絕或關閉視窗時必須清除舊預覽。
- 重匯時以訂單號碼鍵查詢主資料，使用中與已封存訂單都提示已存在；永久刪除後可重匯。未完成的匯入先分批清除其明細、主資料及匯入鎖，再建立新訂單，不要求原帳號或原檔接續。中斷清理在下一次重匯時繼續；沒有主資料的舊孤立鎖若顯示零筆且查無明細，可留下修復紀錄後重新匯入；仍有明細或狀態不明時停止並核對。沿用原訂單權限與唯一快取版本，不建立新集合。

- 匯入建立 `orders`與`orderItems`，不得建立另一份款號／工序主檔。
- 訂單項目只保存訂單自己擁有的欄位及固定款號關聯。
- 匯入前重新核對款號版本，避免使用過期畫面建立錯誤關聯。
- 同檔重複、缺少欄位、無法唯一識別的款號及不合法數量必須在寫入前列出。
- 訂單匯入每批最多 8 筆明細，以現行全角色安全規則核對預算為依據；進行中匯入不向其他畫面發布正式訂單。`orderImportLocks（訂單號碼佔用紀錄）`防止同號重複建立，並與每批明細同交易記下已寫筆數供清理核對；不作為永久帳號歸屬或續跑來源。
- 最後結案才原子更新訂單可用狀態、匯入鎖、成功操作紀錄及既有 `dataVersions/orders（訂單快取版本）`。失敗清理另記不可變操作紀錄；未完成匯入不更新正式訂單快取版本。具體流程、清理用量及其他裝置快取失效預算以 `訂單資料.md`第 4.3、9.2 節為準。
- `orders`與 `orderItems`讀取統一使用既有 `canReadOrders（正式訂單讀取權限判斷）`，避免管理員被一般角色設定文件缺少誤拒；保留有效身分、角色、帳號啟用及母功能檢查。單次測試及雲端發布狀態應依當次版本確認，不以本文件中的舊紀錄判定。

- order-archive（已封存訂單）由中央清單登記並按需載入 js/order-archive.js，與訂單頁共用既有 orders 快取來源。archived 可還原；管理員自行判斷後進入 deleting，僅允許接續清除；開始、每批、完成均記錄 operationLogs。最後釋放 orderImportLocks，內部識別碼與顯示單號保持分離。不掃描或清除產能等關聯，不宣稱自動無關聯保護。具體資料、角色、限制、成本與恢復方式見訂單資料.md 第 10.2、9.3 節。
- shipped-orders（已出貨訂單）由中央清單登記並按需載入 `js/shipped-orders.js`，沿用 `progress（訂單資料）`權限、`orders`清單與 `dataVersions/orders`唯一版本來源。`shipmentStatus`只控制主表與已出貨分頁分類，確認或取消不改變 `lifecycleStatus`，因此不排除產能、績效獎金或凍結月份資料。舊訂單缺少欄位時視為尚未出貨；具體流程及雲端用量見 `訂單資料.md`第 6.4、9.4 節。

## 8. 完整寫入、同時操作與中斷

- 每次款號操作先原子建立一筆 `productChangeBatches`主摘要與開始 `operationLogs（獨立操作紀錄）`。
- 每個款號的主檔、新增時的代碼索引、`productChangeItems`明細及 `system/productsMeta`在同一筆 Transaction（雲端交易）一起成功或失敗。
- 每筆成功款號修改在同一筆交易取得唯一且單調增加的 `changeSequence（修改序號）`；群組逐款交易不要求序號連續，其他使用者的修改可穿插，增量同步仍依序號範圍取得全部差異。
- 全批結束時再原子結案主摘要與結果 `operationLogs`；群組與匯入允許已成功款號保留，失敗與未處理項目逐筆列明。
- 一般產能新增／作廢與工序累計、訂單生產進度版本、月份修訂、必要摘要及操作紀錄必須原子完成；補充工時不更新訂單進度版本。
- 同一訂單項目同一工序的有效累計不得超過訂單項目數量；不同工序分開計算。
- 同時修改同一款號、同時逼近數量上限，或修改訂單數量與報工同時發生時，必須明確失敗或要求重試，不得發生 Lost Update（更新遺失）。
- 大批操作使用可重跑的固定識別碼、檢查點及明確結果；中斷後不得重複建立或重複累計。

## 9. 讀取與快取

- 登入階段不得預讀完整款號、訂單、產能、分析或獎金集合。
- 大型集合使用條件、日期範圍、數量限制或分頁。
- 持久快取只用於可證明能降低重複大量讀取的資料，並依 UID（使用者識別碼）隔離；裁帶模板與裁片主檔是經核准的同機共用例外。
- 款號快取只以 `system/productsMeta`作為正式新舊判斷來源，不建立重疊的款號版本來源。
- 款號版本相同時直接使用 UID（使用者識別碼）隔離的本機快取；版本不同且本機同步位置有效時，只查詢本機序號之後、目前序號以前的款號文件，再合併正式刪除標記。
- 既有第四版快取若版本正好等於雲端版本，可直接補記追蹤起點而不重讀款號；缺少同步位置、追蹤起點不同、筆數驗證失敗或同步期間持續變動時，才完整讀取目前 `active == true（有效）`的款號，不讀取已退役技術指向文件。
- 完整集合或快取更新只能在查詢明確成功、結果由雲端確認、讀取前後唯一版本位置相同，且可用正式摘要驗證筆數完整後發布；在此之前不得清空或取代畫面與既有快取。
- `0 筆`不是單獨的成功條件。只有雲端查詢成功、版本前後一致且正式摘要也為 0 時才是合法空資料；網路中斷、離線快取空白、部分結果、版本改變或無法驗證時，必須保留最後一次已驗證資料並提示同步失敗。
- 同一資料範圍有重疊背景請求時只允許最新請求發布；較舊請求即使晚回，也不得覆蓋已驗證的新資料。登出、換帳號或重設功能狀態時必須使尚未完成的舊請求失效。
- 驗證失敗不得在同一次背景工作自動重複完整集合查詢；保留舊資料並等下一次背景或使用者重試，避免短暫版本變動放大 Reads（文件讀取次數）。
- 正常程式直接讀取預算：版本未變約 1 Read、單款變更約 3 Reads、10 款變更約 12 Reads；若期間另有其他使用者修改，增加的只有那些實際不同款號。完整回復的冷讀取上限約為 `目前有效款號數 N + 2`，已有有效版本記憶時約為 `N + 1`；合法 0 筆查詢仍依雲端最低計費約 3 Reads。安全規則驗證所需的身分與角色文件可能另計費。
- 現行網站仍禁止永久刪除整個款號。未來若建立正式刪除流程，必須在同一交易更新版本、序號及累積刪除標記；未更新版本文件的人工雲端刪除不屬於支援操作。
- 產能日期範圍與摘要使用 `productionMonths`的對應版本與狀態判斷失效。
- 訂單主表的生產進度快取依 UID（使用者識別碼）隔離，以 `orderProductionProgressVersions/{orderId}`及既有 `system/productsMeta`作為兩個不同資料責任的正式新舊判斷來源：前者只使單張訂單累計失效，後者只使款號工序與秒數失效。更新週期固定為台灣時間每日 06:00 至隔日 05:59，每個帳號與瀏覽器設定檔每期各有最多一次自動嘗試及一次使用者確認後的手動嘗試，兩者使用獨立本機標記；任一嘗試失敗仍計入該類型當期次數，且不自動重試。當期自動額度尚未使用時，畫面明確顯示計算中，完成後才呈現新結果；當期自動額度已使用時，登入或開頁直接讀取該次本機結果。同一帳號、週期與嘗試類型的同時載入共用單一工作；沒有尚未出貨訂單時不得讀取進度雲端資料，也不消耗手動次數。款號版本改變時依 `products.changeSequence（款號變更序號）`只讀上次之後改動的款號，並依固定 `processId（工序識別碼）`判斷秒數、分類及新增工序；只有新納入 `SX（生產）`且本機沒有累計的工序才補讀累計。訂單進度版本改變時依快取的前次版本時間，使用 `productionProcessTotals`的 `orderId + updatedAt`複合索引只讀後續更新的目前絕對累計；查詢期間若又有新報工，之後可能重讀同一筆絕對值但不會重複加總。舊快取缺少版本時間、時間異常或索引尚未完成時，只對受影響訂單退回每 30 個文件識別碼一批、最多三批並行的完整讀取，確保結果正確且不阻斷服務。沒有版本且沒有可信快取的舊訂單先按每 10 張一批確認正式工序累計；完全沒有累計時直接保存 0%。每次嘗試的各類回傳文件數、查詢批次及最低估計 Reads（文件讀取次數）保存在同一份本機快取，不新增雲端統計文件。進度只統計目前款號主檔的 `SX`工序，凍結月份的有效一般產能仍包含在正式累計；詳細公式與最低／一般／最壞用量以 `訂單資料.md`第 5、9.5 節為準。
- 訂單的 `productionProgressCompleted（完成狀態）`為 true 時，同一筆主資料必須保存 `productionProgressSnapshotPercent（完成進度快照）`；所有裝置顯示該快照與 100%，並把該訂單排除於自動及手動進度更新。取消完成只恢復一般統計，不增加或重置當期更新額度。完成／取消沿用 `progress（訂單資料）`權限及 `dataVersions/orders（訂單快取版本）`，各約 1 次直接讀取、3 次寫入、0 次刪除；詳細預算與驗收見 `訂單資料.md`第 6.5、9.5、10 節。
- 訂單主表完整沿用 `js/ui-table-controls.js（共用表格控制）`的雙語表頭、欄位勾選及欄寬拖曳，偏好依 UID（使用者識別碼）保存在本機；功能樣式不另存一套固定欄寬。列印直接複製已呈現表格、承接目前語言模式並移除未勾選欄位，「操作」欄固定不列印，也不重新讀取正式資料；表格上方的標題、更新資訊與未出貨總數量不屬於列印內容。欄位勾選、拖曳與列印的新增雲端讀取、寫入、刪除、傳輸及儲存均為 0，且不建立操作紀錄；詳細驗收以 `訂單資料.md`第 5、9.1、10 節為準。
- 員工績效在同一次日期範圍查詢結果內進行週／月分頁；全部員工依七天、精確單一員工依月份，翻頁不得重新查詢雲端。
- 款號流水帳摘要輸入只搜尋已載入內容；使用者按 Enter（確認鍵）跨批次搜尋時，最多讀取 50 筆明細及其對應的 50 筆主摘要，單次直接讀取預算上限約 100 Reads（文件讀取次數）。
- 寫入後只更新或清除直接受影響的記憶體與快取，不讓無關功能整批重讀。
- 即時監聽必須有條件且只在功能開啟期間存在。
- 裁片頁開啟時只讀 `pieceCuttingTemplates/main`一筆；需要配對訂單且本機內容驗證碼不同時才讀裁片主檔分塊。多工作表解析、重複款號／裁片／訂單檢查與缺少主檔檢查都使用已載入的本機資料，不另增雲端讀寫；本機圖片快取以第三版工作表身分重建。正式分塊、快取與最低／一般／最壞用量預算以`裁片出單.md`第 5、8 節為準。
- HUNTER 檢驗報告只在開啟範本分頁或從訂單表格點擊匯出時，讀取最多兩份範本摘要；匯出選定單張訂單後按需讀取其項目、缺少的款號及範本分段。不建立持久範本快取或新資料版本，因每張訂單通常只匯出一次；正式讀寫與跨裝置預算以 `檢驗報告.md`為準。

## 10. 權限與安全邊界

- 所有瀏覽器直接存取的正式資料都必須受登入、有效 `userAccess/{UID}`、角色功能權限及 Firestore Security Rules（雲端資料庫安全規則）保護。
- 非管理員角色的新功能權限預設關閉；缺少權限欄位時安全視為關閉。
- 前端隱藏或停用按鈕只負責操作體驗，不能取代安全規則。
- 安全規則負責身分、授權、不可變識別碼、重要狀態及必要格式；複雜可重建計算不在規則內重算。
- 正式資料與保密獎金參數必須使用不同權限邊界，公開月獎金頁不得讀取保密公式與公司損益資料。
- 款號修改流水帳由已開放 `productsMain（款號管理主功能）`的使用者共同查看；匯入依 `summary（款號總表）`驗證，單款、群組、工序與標準秒數修改統一依 `productionProcessEdit（工序修改，含群組）`驗證。
- 每次實際改變資料路徑、角色或安全責任時，必須同步更新權限頁、安全規則及允許／拒絕測試。

本文件不記錄某次安全規則是否已發布；發布狀態必須以當次雲端內容與本機版本實際比對。

## 11. 介面與語言

- 正式介面支援雙語、只顯示越文、只顯示中文三種中央模式。
- 固定寬度元件以越文在上、中文在下呈現；大量說明先完整越文，再完整中文。
- 表格表頭使用雙行文字，並保持桌機欄寬、對齊、水平捲動與狀態顏色一致。
- 動態載入、確認、進度、成功、失敗與空資料訊息同樣遵守中央語言規則。
- 批次修改、覆蓋匯入、鎖定與匯出必須提供修改前預覽、執行進度及完成結果。

### 11.1 中央文字角色與功能樣式分工

- `UI設計規範與參照/介面設計規範.md`是字級、字重、雙語、對比色、元件與桌機驗收的唯一規格；本文件只登記程式責任，不保存另一份平行視覺標準。
- `styles/ui-core.css（共用介面核心樣式）`在全域提供正文、表頭、標籤、控制項、提示、統計數字與雙語文字角色；不再依個別頁面的試作標記啟用。`styles/themes/default.css（預設主題）`提供文字及成功／警告／錯誤等語意色，字型來源仍為 `styles/fonts/default.css（預設字型設定）`。
- `styles/features/（功能專屬樣式資料夾）`共同引用中央角色，各檔只保留功能必要的欄寬、排列、狀態及特定內容呈現；實際載入清單以 `js/features.js（中央功能清單）`及主畫面的靜態載入為準，不在文件另外列一份容易失效的數量與清單。
- 側欄文字對齊由 `index.html`的側欄樣式負責，顏色引用預設主題的導覽角色；具體對齊與配色只在介面規範第 4.2、5.1 節定義，不更動其他元件的共用文字規則。
- `index.html（主畫面）`及各功能動態介面保留原識別碼與事件，使用既有 `js/ui-text.js（共用雙語文字程式）`、`js/ui-runtime.js（共用介面執行控制）`及共用元件支援三種語言模式；不建立另一套語言偏好、持久快取或功能載入機制。
- 功能樣式仍透過 `js/features.js（中央功能清單）`按需載入。全域整理不改業務資料路徑、權限、安全邊界、`schemaVersion（資料結構版本）`、資料版本或快取格式，不因介面更新清除或強制重建業務快取。
- `js/ui-table-controls.js（共用表格控制）`負責表頭全文提示及三語切換，不改欄寬偏好或排序。業務原文提示以 `data-ui-neutral-title（中性原文提示標記）`交由 `js/ui-text.js`保留，不套用介面翻譯拆分；數值與雙語狀態由所屬功能依介面規範分開呈現。
- 向下相容的介面更新採既有 `notice（提醒更新）`流程；實際版本以 `runtime-version.json（網站版本狀態檔）`及執行程式為準，不代表版本已提交、推送或發布。
- 來源守門由 `tests/ui-legibility.test.mjs（中央文字與樣式引用測試）`核對閱讀字級、字重、色值、變數定義及動畫消費；`tests/ui-language.test.mjs（語言模式測試）`核對正式雙語結構與語言生命週期。仍有正式使用者的相容名稱可保留，失效樣式標記與已移除畫面的文字、配色不得繼續保留。
- 離線畫面必須分開呈現「通過的自動檢查、明確失敗、人工待驗、未執行的功能狀態」。頁面骨架不代表資料列、功能對話框或資料操作已驗收；實際情境、呈現筆數、三語／寬度／側欄與漏網項目以當次驗收輸出為準，不在架構文件保存容易過期的通過總數。
- 驗收入口：`tests/ui-global-preview-server.cjs（受限來源的離線預覽服務）`、`tests/ui-global-preview.html（桌機驗收矩陣）`、`tests/ui-global-fixture.js（虛構資料夾具）`及 `tests/ui-global-checks.js（唯讀畫面判定器）`。判定器的反例測試由 `tests/ui-global-preview.test.mjs`保存。僅綁定本機、限制來源／路徑／請求方法、封鎖外部資料連線；只用於開發驗收，不是正式網站功能。
- 離線情境直接使用正式呈現來源，包含裁帶歷史的載入／空資料／資料列／錯誤、群組視窗、產能搜尋、匯入錯誤及共用連線提示；不得把一般資料儲存格加入全域猜測拆字。未使用正式帳號或業務資料，不取代正式流程、全部權限情境、列印成品、真實瀏覽器縮放或同事桌機的實體清晰度驗收。
- 離線樣式驗收提供全功能預載、單頁精確相依及依導航順序累積三種模式；相依來源只讀中央功能清單，不另存一份功能樣式清單。報告須保留實際寬高、載入樣式與導航順序，且樣式冷開驗收不等同程式或資料冷啟動驗收。

### 11.2 純介面調整的共通雲端用量預算

- 適用前提：只改靜態文字或樣式，驗收使用離線資料；不改資料讀寫、資料版本、唯一新舊判斷來源、快取格式或失效範圍。任一前提改變，須重新估算，不得沿用零增量結論。
- 在上述前提下，額外 Reads（文件讀取）、Writes（寫入）、Deletes（刪除）、業務資料傳輸及儲存均為 0；最低、一般及最壞情況相同。每個帳號／瀏覽器／裝置新增的業務快取重建量為 0，乘以使用人數及次數後仍為 0。
- 正式登入、開啟功能、查詢、修改及匯入匯出仍依各功能預算計費；靜態更新不代表正式使用免計費。本節只保存共通介面增量，各功能文件保留業務用量及本節引用，不重複複製零增量段落。
- 網站靜態檔案重新載入的傳輸另計，估算為實際下載大小乘以重新載入裝置及次數，不包含在上述業務傳輸中；剩餘雲端額度未取得時標示未知，不得據此啟動正式資料的大量驗收。

## 12. 匯出與檔案儲存

- 開始產生或下載檔案前，先讓使用者選擇檔名與儲存位置。
- 取消選擇時停止後續處理，不留下空白或未完成檔案。
- 瀏覽器不支援系統儲存位置選擇時，明確提示並停止，不得偷偷改用預設下載。
- 儲存失敗不得以不完整內容覆蓋原檔。
- 畫面、凍結結果與匯出檔必須使用同一套正式計算來源。

## 13. 已淘汰 Runtime 路徑

正式 Runtime 不得再讀寫下列舊來源或重新加入相容備援：

- `orderProcesses`
- `productProcessStandards`
- `processEditJobs`
- `productionAnalysisSummaries`
- `productionDayChanges`
- `productionProcessAnalysisQueue`
- 舊款號／工序 Snapshot（快照）同步路徑
- 舊 Summary Migration（摘要轉換）路徑
- 舊工序優化／標準錯誤訂正模式
- `productHistory`、`productChanges`、`system/impHist`及其 `historyId`、`targetHistoryId`等舊款號歷史指向

一般舊雲端資料可以保留供稽核，但不得讓正式程式維護新舊雙軌。舊款號修改、舊匯入、舊歷史與對應操作紀錄是明確例外，不再保留；只保留固定 `productId`、`processId`所需的技術身分轉換紀錄。現行程式、測試與安全規則必須持續保持這些舊款號歷史路徑為 0 Reads / 0 Writes / 0 Compatibility Logic / 0 Rules Dependencies（零讀取／零寫入／零相容邏輯／零安全規則依賴）。

## 14. 測試與完成標準

每次修改依影響範圍驗收：

1. 管理員、全部有效非管理員角色、未授權、停用及無效角色。
2. 款號、訂單、產能、考勤、摘要、分析、績效與獎金的跨功能一致性。
3. 同時操作、數量上限、原子寫入及中斷重試。
4. 舊瀏覽器快取、舊分頁、重新登入及 Cold / Warm Cache（冷／暖快取）。
5. 三種語言模式與桌機版面。
6. 匯入、預覽、進度、結果、檔案取消及寫入失敗。
7. 月份鎖定後畫面、凍結資料與 Excel（試算表）結果一致。
8. 正式 Runtime 對已淘汰路徑零存取。
9. 單款修改、群組修改與 Excel 匯入都產生一層主摘要與一層直接明細，並可依款號跨批次查找。
10. 舊款號歷史重設前後，當前款號、工序及其固定識別碼的業務內容比對必須完全一致。
11. 非管理員閒置跨日與瀏覽器背景休眠後恢復時，依實際經過時間登出且不自動重新登入；管理員不啟動閒置計時。畫面不顯示登出倒數。
12. 群組可保存一個以上固定成員但不得為空；群組快速修改的來源款號只作比較基準，可取消勾選。

文件只記錄現行要求與可重複驗收方式，不保存容易過期的單次測試數字、提交編號或發布宣告。

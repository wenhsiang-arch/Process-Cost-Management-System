# 系統架構規劃

最後更新：2026 年 8 月 6 日

## 一、文件定位

- 本文件是 Process Cost Management System（製程成本管理系統）的全系統正式架構規格。
- 本文件固定長期共用的底層規則，包括功能分工、程式載入、資料存取、快取、安全、權限、Excel（表格檔）匯入匯出、使用體驗、版本轉換及驗收方式。
- `BOM（物料清單）`欄位、獎金公式、報表欄位及其他業務細節可以在開發過程調整，但不得因此破壞本文件規定的共用架構。
- 開始新增或修改功能前，必須先閱讀 `AGENTS.md（開發規則文件）`、本文件及 `系統開發交接.md（開發交接文件）`。
- 若未來實際需求需要改變本文件，必須先說明原因、影響範圍、資料轉換、回復方式及風險，取得使用者明確同意後才能修改。

### 1.1 使用者只需要確認的五件事

技術細節由開發規則約束，使用者只需要確認：

1. 新功能先測試，再開放給正式使用者。
2. 新功能發生問題時可以單獨關閉，不影響其他工作。
3. 程式、權限及資料更新後都能回到上一個可用版本。
4. 匯入或更新失敗時繼續使用舊的完整資料，不使用半份新資料。
5. 高風險修改執行前，必須先說明影響、測試、回復方式及可能中斷時間。

若使用者的新指令與本文件衝突，不得自行忽略任何一方。必須先指出衝突、影響及安全替代方案；取得使用者確認後，才依確認結果開發並同步修正本文件。可能造成資料外洩、刪除正式資料、取消可信任登入或全面公開資料庫的做法不得直接執行，必須改用安全替代方案。

## 二、系統定位

- 本系統是工廠的輔助資料管理與工作效率工具，不取代工廠正式的採購、倉庫、財務、人事或企業流程。
- 主要使用方式是集中保存必要資料，透過 Excel（表格檔）匯入、比對、計算、預覽及匯出，減少人工整理與重複計算。
- 不因新增採購單價、庫存、預料或績效功能，自動擴充成完整採購審批、倉庫進出帳、薪資發放或企業資源規劃流程。
- 若未來要把輔助資料改成正式帳務或正式流程，必須另行評估資料完整性、權責、稽核、備份及法規，不得直接沿用輔助工具的假設。

## 三、架構核心原則

1. 登入只讀身分、帳號狀態、角色權限及首頁必要的小型設定，不讀取大型業務資料。
2. 使用者實際開啟功能時，才載入該功能的 JS（JavaScript 程式檔）與資料。
3. 每個主要功能擁有自己的資料路徑、版本、快取、權限、程式及驗收範圍。
4. 資料版本相同時使用 IndexedDB（瀏覽器本機資料庫）快取；版本不同時只更新相關功能或資料分區。
5. 大型資料禁止無條件完整讀取、無限制即時監聽及登入後預先載入。
6. 畫面限制與 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）必須同步；真正的資料保護由雲端安全規則執行。
7. 新功能沿用統一畫面、載入、錯誤、匯入、匯出及驗收方式，不另創互不相容的流程。
8. 現有功能不一次全面重構；未來修改某項功能時，再讓該功能逐步符合本架構。
9. 瀏覽器、畫面按鈕、本機快取及前端計算結果都不視為可信任的安全來源。
10. 每個新功能必須可以單獨開啟、關閉及回復，單一功能故障不得阻止其他功能使用。
11. 正式更新前先保留上一個可用的程式、權限及資料版本，確認新版可用後才完成切換。
12. 使用者工作不中斷的優先順序為：保護資料、停止錯誤寫入、繼續使用舊的完整版本、關閉故障功能、最後才停止整個系統。

## 四、功能模組與功能登記

### 4.1 主要功能模組

- 登入與帳號權限。
- 款號與工序表。
- 訂單與訂單工序。
- 人員名冊、文員產量與工時登記。
- 報表與統計。
- 裁帶統計與本機 PDF（可攜式文件）工具。
- `BOM（物料清單）`。
- 庫存資料。
- 預料與缺料計算。
- 採購單價資料。
- 員工績效與獎金計算。

### 4.2 功能身分資料

每個新功能在開始開發前，必須先完成以下登記：

- `id（功能代碼）`：全系統唯一且建立後不得任意更名。
- `label（功能名稱）`：越文在上、中文在下。
- `module（程式模組）`：實際開啟功能時才載入的程式檔。
- `permission（權限代碼）`：權限頁及安全規則共用的固定代碼。
- `dataPath（資料路徑）`：雲端資料集合、文件或資料分區。
- `cacheScope（快取範圍）`：IndexedDB（瀏覽器本機資料庫）內的獨立快取名稱。
- `dataVersion（資料版本）`：判斷是否需要重新下載的版本名稱。
- `importFormat（匯入格式）`及 `exportFormat（匯出格式）`：允許的 Excel（表格檔）結構。
- `lifecycle（功能生命週期）`：開啟、載入、顯示、離開及清理方式。
- `tests（驗收項目）`：角色、資料、安全、快取、網路及畫面測試。

同一功能的導覽、權限頁、程式載入、快取版本及資料路徑必須引用同一份中央功能清單，避免不同檔案重複建立互相矛盾的名稱。雲端安全規則仍必須逐項明確設定，不得由前端清單自動放行。

## 五、資料分類與儲存方式

### 5.1 小型設定資料

- 適用於角色權限、一般運算設定、成本設定及各功能資料版本。
- 每項設定使用獨立文件，不把敏感成本與一般設定混在同一份文件。
- 只讀取目前角色及目前功能需要的設定。

### 5.2 大型參考資料

- 適用於款號工序、`BOM（物料清單）`、庫存快照及採購單價。
- 使用「小型摘要文件＋自然分區或資料區塊」方式保存。
- 摘要至少包含 `version（版本）`、`schemaVersion（資料結構版本）`、`updatedAt（更新時間）`、`updatedBy（更新者）`、`rowCount（資料列數）`及 `chunkCount（資料區塊數）`。
- 資料依款號、材料、倉庫、月份、代碼區段或實際業務範圍分區，不使用單一無限制集合讀取。
- Excel（表格檔）整批快照可在完成檔案大小測試後，以約 200～500 列為一個資料區塊；實際上限以文件容量、網路速度及安全規則可驗證性決定，不把固定列數視為永久常數。
- 資料區塊只適用於由少數授權人員整批匯入、主要整批查閱且內容不需要逐筆權限控制的參考快照；不得把所有大型資料一律分塊。
- 需要精確查找少量款號或材料時，只讀取涉及的分區；需要完整匯出時，優先使用已驗證版本的本機快取。

### 5.3 持續增加的記錄資料

- 適用於員工工序登記、出勤、異動記錄及其他歷史資料。
- 每筆記錄獨立保存，依月份、日期、員工、訂單或狀態建立查詢條件。
- 畫面只讀取選定日期範圍及目前頁面，不把全部歷史保存到單一文件或一次下載。
- 歷史資料達到規定期間後可以封存，但封存規則及查閱方式必須先取得使用者同意。

### 5.4 計算產生的結果

- 預料、缺料、訂單用料金額及績效獎金優先在瀏覽器使用已授權且版本正確的資料計算。
- 不因畫面可以重新計算，就重複把所有明細寫回雲端。
- 只有需要再次查閱、追溯、共用或正式結算時，才保存結果。
- 保存結果時，必須同時記錄使用的來源版本、計算規則版本、時間及操作者，確保日後可以重現。

### 5.5 數量與敏感金額分離

- 庫存數量、`BOM（物料清單）`用量、採購單價、庫存買入金額及績效獎金不得因操作方便而全部放在同一個可讀文件。
- 沒有金額權限的角色只能取得完成工作所需的數量及非敏感資料。
- 匯出及本機快取也必須遵守相同分離原則。

### 5.6 儲存方式選擇規則

- `BOM（物料清單）`、款號工序及其他整批參考快照，可以使用「摘要文件＋資料區塊」。
- 庫存異動、員工工序登記、採購單價歷史、權限異動及操作記錄，每筆必須獨立保存，才能查詢、驗證、追溯及限制權限。
- 首頁數量與統計使用獨立的小型摘要文件，不得為了顯示數字讀取全部明細。
- 持續增加的記錄不得保存成接近文件容量上限的單一 JSON（資料交換格式）字串或大型陣列。
- 不需要共用及追溯的計算結果優先留在瀏覽器；需要保存時必須連同來源版本及計算版本保存。
- 大型欄位若不需要查詢，必須評估取消索引；實際查詢需要的索引則記錄在 `firestore.indexes.json（資料庫索引設定檔）`。

### 5.7 淘汰資料與索引清理規則

- 移除畫面、程式與安全規則存取，不等於已刪除 Firebase（雲端資料庫）的實體集合、文件或索引；交接文件必須分開記錄「已停用」與「已實體清除」。
- 淘汰集合先由安全規則明確拒絕，再盤點文件數量、現行程式引用、轉換需求、備份及回復方式；取得使用者另一次高風險同意後才可實體刪除。
- `system（系統設定集合）`等同時包含新舊文件的集合只能逐份清理，不得因部分文件淘汰而刪除整個集合。
- Firebase（雲端資料庫）集合與複合索引必須分開盤點；索引只在目前程式查詢與 `firestore.indexes.json（資料庫索引設定檔）`都確認不再使用後才可刪除。
- 清理完成後必須驗收現行登入、權限、款號、訂單、裁帶、成本及操作歷史查詢，並更新 `系統開發交接.md（開發交接文件）`與 `安全規則盤點.md（安全盤點文件）`。

## 六、資料讀取與免費額度控制

### 6.1 登入階段

- 只讀取 Firebase Authentication（Firebase 身分驗證）、`userAccess（使用者存取權限）`、目前角色的 `rolePermissions（角色功能權限）`及首頁必要的小型設定。
- 不因角色具有某功能權限，就預先讀取該功能資料。
- 首頁需要的統計數字使用小型彙總文件，不掃描大型集合計算。

### 6.2 功能開啟階段

1. 通過角色與功能權限檢查。
2. 載入該功能程式。
3. 讀取該功能的小型版本文件。
4. 版本相同時使用 IndexedDB（瀏覽器本機資料庫）。
5. 版本不同時只下載需要的資料分區或頁面。
6. 完成後更新該功能快取，不影響其他功能。

### 6.3 查詢限制

- 一般表格預設每頁最多 50 筆；確有必要時可以調整，但不得取消分頁後完整讀取大型集合。
- 分頁使用查詢游標，不使用會讓前面資料仍被計入讀取的跳過筆數方式。
- 查詢優先使用單一明確範圍，例如月份、日期、狀態、員工、訂單或材料分類，避免複雜且大量掃描的條件。
- 相同頁面同時要求相同資料時，必須共用同一個 Promise（非同步工作承諾）。
- 即使查詢結果為空也屬於一次查詢，不得用大量空查詢猜測資料是否存在。
- 安全規則為確認角色而需要的文件讀取不得省略；不得為了節省讀取量降低資料安全。

### 6.4 即時監聽

- `BOM（物料清單）`、工序表、庫存、採購單價、預料及歷史記錄預設不使用即時監聽。
- 只有報工等待處理或其他確實需要即時變化的畫面才能使用。
- 監聽必須有明確條件，離開功能、登出或換帳號時立即停止。

### 6.5 內部用量目標

- 已有有效快取的功能，再次開啟時原則上只需要讀取 1～2 份小型版本或權限文件。
- 正常翻頁只讀取該頁需要的資料。
- 完整資料更新必須是使用者明確匯入、重新整理或資料版本變更的結果，不得成為日常每次開頁行為。
- 以 Firebase（雲端資料庫）免費額度為上限，系統內部應設定低於官方額度的警戒比例；達到 50%、75%及 90%時分級提醒管理員檢查來源。
- 官方額度可能調整，文件不固定寫死永久數字；每次正式擴大使用人數或資料量前重新確認官方限制。

## 七、IndexedDB（瀏覽器本機資料庫）快取

- 每個功能使用獨立 `cacheScope（快取範圍）`，不得共用一份無法局部失效的大型快取。
- 快取鍵至少包含網站來源、UID（使用者識別碼）、功能及資料版本。
- 不同 UID（使用者識別碼）、瀏覽器或網站來源不得共用業務快取。
- 資料版本相同才可使用快取；無法確認登入、帳號狀態或權限時不得先顯示敏感快取。
- 寫入或匯入後只更新或清除相關功能快取。
- 本機快取維持 1 GB（十億位元組）動態上限；這是全部快取的絕對容量上限，不是每個功能的使用目標。接近上限時優先清除最久未使用的非必要快取。
- 不快取圖片、附件、匯出檔、重複歷史副本及不需要跨瀏覽器重啟保存的資料。
- 容量不足時停止新增快取並改由雲端按需讀取，不得讓主要功能故障。
- 採購單價、庫存金額、成本、員工資料及績效獎金等敏感資料，預設不永久快取；確有工作需要時，必須先確認是可信任電腦並設定較短保存期限。
- 敏感快取在登出、換帳號、失去權限、帳號停用或快取結構升級時清除，並提供使用者主動清除本機資料的方式。
- 目前使用 localStorage（瀏覽器簡易儲存空間）的較大業務快取，後續修改該功能時逐步轉移到 IndexedDB（瀏覽器本機資料庫），不一次全面搬移。

## 八、JS（JavaScript 程式檔）架構與按需載入

### 8.1 登入時允許載入的核心程式

- 共用畫面與格式工具。
- 登入、登出及權限檢查。
- 導覽與功能登記。
- Firebase（雲端資料庫）核心連線。
- IndexedDB（瀏覽器本機資料庫）快取管理。
- 統一載入、成功、錯誤及確認訊息。

### 8.2 功能程式

- `bom.js（物料清單功能程式）`。
- `inventory.js（庫存功能程式）`。
- `material-plan.js（預料功能程式）`。
- `purchase-price.js（採購單價功能程式）`。
- `employee-entry.js（文員工序登記程式）`。
- `performance-bonus.js（績效獎金功能程式）`。

以上名稱是功能邊界示例；正式建立時仍依功能身分資料確認。使用者開啟功能並通過權限後才進行 lazy loading（按需載入）。

### 8.3 第三方工具

- Excel（表格檔）解析、壓縮、PDF（可攜式文件）及其他大型工具，只在使用相關功能時載入。
- 外部套件版本必須固定，不使用永遠指向最新版的設定。

### 8.4 程式責任

- 畫面程式只處理操作、驗證及顯示，不直接散落 Firebase（雲端資料庫）讀寫。
- 每個功能由自己的資料存取程式負責查詢、寫入、版本、快取及錯誤轉換。
- `firebase.js（Firebase 雲端資料庫程式）`後續只保留登入、共用連線、資料版本、共用快取介面及安全封裝，不再加入每個功能的專屬邏輯。
- 不同功能不得直接修改彼此的內部變數；跨功能操作使用明確公開介面。
- 同一功能內高度相關且一起載入的程式可以保留在同一檔案，不過度拆成零碎檔案。
- 單一程式約超過 800 行、混合多種職責或修改時容易牽連無關功能時，必須先評估拆分；行數只是提醒，功能邊界才是主要判斷。

### 8.5 功能生命週期

每個主要功能至少提供以下一致行為：

1. `init（初始化）`：建立一次性事件及基本狀態。
2. `load（載入）`：檢查權限、版本、快取及雲端資料。
3. `render（顯示）`：依目前狀態更新畫面。
4. `destroy（離開清理）`：停止監聽、計時器、未完成背景工作及敏感記憶體。

### 8.6 底層實作現況（2026 年 8 月 6 日）

- 已新增 `js/features.js（中央功能登記與載入程式）`，統一登記主功能、功能分頁、權限欄位、程式依賴、資料載入及開頁動作。
- `index.html（主畫面檔案）`登入時只預先載入 `utils.js（共用工具程式）`、`data-cache.js（資料快取程式）`、`features.js（中央功能程式）`、`auth.js（登入與權限程式）`及 `firebase.js（雲端資料庫核心程式）`；登入成功先顯示不讀取業務資料的輕量功能首頁，其他功能程式在使用者實際開頁後才載入。
- 權限管理畫面、側邊導覽及頁面切換已共用中央功能清單；新增功能仍需同步更新雲端安全規則，不得把前端清單視為資料庫授權。
- Excel（表格檔）工具已改成實際選擇匯入檔案，或確認匯出儲存位置後才載入；未使用的 JSZip（壓縮工具）已從首頁移除。
- 本機功能程式的登入預載量由 13 個、約 `290,332 bytes（位元組）`降為 6 個、約 `89,499 bytes（位元組）`，減少約 `69.2%`；此為四階段完成後的最終原始檔大小，只代表網頁程式下載量，不代表 Firebase Firestore（雲端文件資料庫）讀取筆數下降。
- 已新增 `safe-dom.js（安全文字顯示程式）`，統一處理 Excel（表格檔）、雲端文字、搜尋高亮、行內參數及錯誤訊息；舊功能不得再各自建立不一致的跳脫方式。
- 匯入、匯出及成本設定已改用 `operationLogs（獨立操作紀錄）`，每次操作一份文件，依功能權限查詢最新紀錄，建立後禁止網站修改或刪除。
- 款號快取已由 localStorage（瀏覽器簡易儲存空間）移至依 UID（使用者識別碼）隔離的 IndexedDB（瀏覽器本機資料庫）；版本改變時優先依 `productChanges（款號增量變更）`補讀實際變動款號，缺漏時才完整重讀。
- 款號寫入改為 Transaction（雲端交易），同時更新款號、增量紀錄及版本；最多一次 498 款，避免超過交易上限及多台電腦互相覆蓋。
- 訂單頁開啟時不再讀取全部 `orderProcesses（訂單工序）`；工序依單張訂單按需讀取及快取，匯入、數量調整與秒數同步只使受影響訂單的工序快取失效。
- 已建立 Firebase Emulator Suite（Firebase 本機模擬器）規則測試基底與共用功能測試；目前 23 項安全規則及 17 項本機功能測試通過，其中架構測試為 8 項。
- `firebase.js（雲端資料庫程式）`目前仍是核心程式；`data.js（資料與報表程式）`仍同時提供訂單明細需要的工序分類文字。已先拆出中央功能、安全文字、款號快取及訂單工序快取，其他內容只在下次修改相應功能時逐步移出，避免一次全面重寫。

## 九、統一使用體驗

### 9.1 功能頁結構

主要功能頁原則上依序呈現：

1. 功能標題與簡短說明。
2. 主要操作按鈕。
3. 搜尋、日期及其他篩選。
4. 資料摘要。
5. 表格或主要內容。
6. 分頁。
7. 載入、空資料、錯誤及成功狀態。

### 9.2 雙語與版面

- 介面只使用越文與中文，越文為主、中文為輔。
- 固定寬度元件預設越文在上、中文在下，使用兩個獨立文字區塊。
- 表格表頭、按鈕、提示、狀態及錯誤訊息沿用同一雙語樣式。
- 現階段只開發及驗收桌機介面，不需要處理手機或平板排版，也不得為了相容小畫面而犧牲桌機工作效率。
- 桌機系統全部開發完成後，再由使用者決定是否需要手機或平板功能；若決定開放，只針對確認需要的功能，依實際使用方式另外設計適合該裝置的介面，不直接縮小套用完整桌機介面。
- 桌機支援寬度內的文字不得截斷、擠壓或任意換行。

### 9.3 狀態與訊息

- 新功能使用統一的系統載入、成功、警告、錯誤及確認元件，不再各自建立不同風格。
- 載入中必須顯示正在處理的內容，完成或失敗後一定結束載入狀態。
- 權限不足、網路失敗、資料不存在、格式錯誤及系統錯誤使用不同訊息。
- 可重試的錯誤提供重試；不可重試的錯誤提供具體處理方式。
- 危險操作必須明確顯示影響範圍，不能只使用含糊的確認文字。

### 9.4 表格與表單

- 表格預設支援搜尋、必要篩選、排序及分頁，但不得為了畫面功能先讀取全部資料。
- 表單使用一致的必填、格式、數值範圍及錯誤位置提示。
- 儲存時防止重複點擊；失敗時保留使用者尚未送出的內容。
- 畫面與匯出使用相同的篩選及計算來源，避免看到的資料與匯出不同。

### 9.5 導覽與帳號狀態

- 所有桌面角色登入後，管理功能選單預設收起。
- 使用者只能進入已授權功能；失去權限、帳號停用或登入身分不一致時立即停止功能並登出。
- 換帳號時不得保留上一個帳號的頁面狀態、敏感資料或展開狀態。

## 十、權限與安全規則

- 新功能必須在同一次開發中同步完成權限頁、中央功能清單、角色預設值及 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）。
- 課長、文員、開發、生管及業務的新權限預設關閉，舊權限文件缺少新欄位時安全視為關閉。
- 權限層級固定為「主功能→功能分頁→敏感資料」；職務開啟功能分頁後，即可使用該頁的查看、新增、修改、刪除、匯入、匯出及歷史，不再為這些操作建立隱藏權限。只有同一分頁混合一般資料與敏感資料時，才新增敏感資料子開關。
- 管理員也只能存取安全規則明確列出的資料路徑，未知路徑維持拒絕。
- 每項寫入除了確認登入與角色，還要驗證欄位、資料類型、長度、數值範圍、識別碼、版本、建立者及允許的狀態轉換。
- 前端隱藏按鈕只能改善操作體驗，不能當成資料安全。
- App Check（應用程式驗證）可以降低非正式網站及自動化濫用，但不能取代登入、角色及安全規則。
- 新功能未完成未登入者、管理員、課長、文員、開發、生管、業務、未授權角色及跨使用者存取測試前，不得宣稱安全上線。
- 權限判斷順序固定為「固定安全限制 → 帳號是否啟用 → 主功能權限 → 功能分頁權限 → 敏感資料權限」；任何一層拒絕，最後結果就是拒絕。帳號管理、權限管理及不可改寫的操作紀錄等固定安全限制，不因功能分頁開啟而解除。
- 角色只作為預設權限組合。除固定管理員功能外，不得在個別畫面另外寫死「課長隱藏」或「文員顯示」，避免與權限頁衝突。
- 瀏覽器及前端計算結果不視為可信任資料來源。輔助計算可以在瀏覽器執行；若未來結果要作為正式薪資、付款或帳務依據，必須另行加入可信任伺服器端驗證。

## 十一、Excel（表格檔）匯入規則

所有新匯入功能使用相同階段：

1. 選擇檔案。
2. 檢查副檔名、容量、工作表、欄位、資料列數、資料類型及必要唯一值。
3. 明確列出工作表、列、欄、識別碼及錯誤原因。
4. 顯示匯入預覽與新增、更新、忽略及錯誤數量。
5. 使用者確認後才開始雲端寫入。
6. 先寫入新版本資料，再於全部成功後切換摘要文件的有效版本。
7. 失敗時保留上一個完整版本；未完成的新版本不得被一般查詢使用。
8. 成功後更新資料版本及相關快取。
9. 保存來源檔名、內容雜湊值、資料結構版本、時間及操作者；不得保存不需要的原始敏感內容。

匯入功能必須設定可調整的檔案容量、解壓後總容量、工作表數、欄數、資料列數、儲存格長度、處理時間及單次寫入上限。超過限制時停止並要求分檔，不得讓瀏覽器或雲端服務無限制處理。

- 匯入檔案內的公式、巨集、外部連結及可執行內容不得自動執行；不需要時拒絕，需要顯示時只當成一般文字。
- Excel（表格檔）、使用者及雲端來源的文字都視為不可信任內容，不得直接插入網頁程式或網頁標籤。
- 相同內容雜湊值及相同匯入工作識別碼的重試必須安全且可重複執行，不得建立重複資料。
- 新版本啟用後，未完成版本及孤立資料區塊必須依保留期限安全清理；清理不得影響上一個可回復版本。

## 十二、Excel（表格檔）及其他匯出規則

- 產生或下載前先顯示系統儲存位置選擇視窗。
- 使用者取消時停止產生、下載及寫入，不留下空白或未完成檔案。
- 瀏覽器不支援選擇儲存位置時，以越文與中文明確提示並停止，不改用預設下載位置。
- 儲存失敗時不得以未完成內容覆蓋原檔。
- 匯出欄位依目前權限產生；沒有金額權限時，檔案內不得存在隱藏金額欄位或敏感中間資料。
- 匯出內容記錄資料版本、篩選條件及產生時間，方便追溯。
- 匯出優先使用已確認版本的本機快取；快取過期時先更新需要的資料，不無條件讀取其他功能。
- 匯出使用者或外部來源文字時，必須處理可能被 Excel（表格檔）解讀為公式的開頭字元，避免開啟檔案時執行非預期公式。

### 12.1 匯入與匯出歷史操作記錄

- 後續新增或修改任何匯入、匯出功能時，必須在同一次開發中建立歷史操作記錄，不得只完成檔案功能而省略記錄。
- 使用者確認開始操作後才建立記錄；只開啟檔案或儲存位置選擇視窗但尚未確認，不計為正式操作。
- 每次操作使用獨立記錄，不得把持續增加的歷史累積在單一大型 JSON（資料交換格式）字串、陣列或文件。
- 歷史記錄至少包含：操作識別碼、功能、匯入／匯出、操作者 UID（使用者識別碼）、雲端時間、檔名、資料版本、資料筆數、篩選範圍及成功／失敗／取消狀態。
- 匯入記錄另外保存來源內容雜湊值、新增／更新／忽略／錯誤筆數及啟用後的資料版本；匯出記錄另外保存實際輸出欄位範圍及是否包含敏感金額。
- 失敗記錄只保存安全的錯誤代碼與摘要，不保存完整檔案內容、密碼、權杖、電腦完整路徑或不必要的敏感資料。
- 一般使用者不得修改或刪除歷史記錄；管理員也不得透過一般畫面改寫內容。只有依明確保存期限執行的受控清理程序可以刪除。
- 查看匯入／匯出歷史必須跟隨對應功能權限；課長、文員、開發、生管及業務預設不因具有其他功能而自動取得，管理員固定開放。即使可以查看，也只能取得目前功能及敏感資料權限允許的紀錄。
- 歷史記錄的建立、查看及受控清理必須同步受到 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）保護並完成允許與拒絕測試。
- 歷史記錄寫入失敗時必須顯示明確的越文與中文提示，不得把操作顯示為已完整完成；是否停止檔案操作依資料敏感性及功能規格決定，但不得靜默遺失記錄。

## 十三、計算、日期、幣別及單位

- 全系統共用數量、金額、幣別、小數位、四捨五入、日期及時間格式，不由各功能自行發明。
- 工廠業務日期使用哪一個時區必須由全系統設定統一決定；未決定前不得在不同功能混用使用者電腦時間與雲端時間。
- 材料單位、換算比例、損耗、庫存數量、採購單價及工序秒數必須明確保存單位。
- 預估金額、庫存參考成本、實際使用金額及績效獎金分開命名及顯示，不得混成同一個「成本」。
- 重要計算使用共用純計算函式；相同輸入與相同規則版本必須得到相同結果。
- 保存計算結果時記錄來源資料版本及 `calculationVersion（計算規則版本）`。

## 十四、同時操作、版本及完整寫入

- 修改或匯入前重新檢查目前資料版本，避免其他電腦已更新後仍用舊資料覆蓋。
- 同一功能的同一份資料同時只能有一個有效版本。
- 大型匯入若需要多批寫入，先完成所有新資料，再最後切換有效版本；不得在中途讓使用者讀到新舊混合資料。
- 同一筆資料的識別碼建立後不可因顯示名稱改變而更換。
- 刪除預設採封存或停用；真正刪除需依資料用途、歷史關聯及使用者明確要求決定。
- 失敗時必須能回到上一個有效版本，不依賴使用者重新匯入修復半份資料。

## 十五、資料結構版本與轉換

- 每個主要功能保存 `schemaVersion（資料結構版本）`。
- 新增欄位必須定義舊資料的安全預設值，不得因缺少欄位而載入失敗或自動開放權限。
- 欄位更名、資料拆分、資料合併及單位改變必須先建立轉換方案、驗證方式及回復方式。
- 敏感資料轉換先確認新資料可用，再停止舊資料讀取；不得先刪除舊資料。
- 資料轉換及快取升級各自管理，不能把清除本機快取誤當成完成雲端資料轉換。

## 十六、錯誤、離線及部分失敗

- 網路失敗、權限拒絕、資料版本衝突、容量不足及格式錯誤必須分開處理。
- 無法確認資料版本時，只能使用明確允許的離線快取；畫面必須標示資料可能不是最新版本。
- 敏感功能無法完成當次登入與權限確認時，不顯示永久快取。
- 寫入失敗不得讓畫面顯示成功，也不得只更新本機畫面而沒有正式資料。
- 背景工作必須可以安全結束；離開功能後不得繼續重試或消耗資源。
- 所有載入流程必須在成功或失敗後解除載入狀態，避免永久停留在等待畫面。

## 十七、監控與用量管理

- 每日檢查 Firebase（雲端資料庫）的文件讀取、寫入、刪除、拒絕請求及異常增加。
- 為每個大型功能記錄載入來源、快取命中、雲端讀取筆數、查詢條件及耗時；記錄不得包含正式敏感資料內容。
- 發現單一功能讀取量異常時，只停用或修正該功能，不要求其他無關快取全部失效。
- 新增大型資料功能前，先以預估使用人數、每日開啟次數、每頁資料量及快取命中率估算最壞讀取量。
- 免費額度接近警戒值時，優先找出重複查詢、完整集合讀取、未停止監聽及版本反覆失效，不先降低安全規則。

## 十八、測試與驗收

每個主要功能完成前至少驗收：

- 管理員、課長、文員、開發、生管、業務及未授權帳號的允許與拒絕。
- 未登入者及直接資料庫請求的拒絕。
- 第一次載入、快取命中、版本變更、快取容量不足及清除後重新載入。
- 正常網路、離線、連線中斷及寫入途中失敗。
- 空資料、少量資料、大量資料、錯誤 Excel（表格檔）及超出限制的檔案。
- 重複點擊、相同查詢同時發生及兩台電腦同時更新。
- 桌機版的雙語文字、按鈕、表格、載入及錯誤畫面；手機與平板留待桌機系統完成後另案評估及驗收。
- 匯出欄位與目前權限一致，取消與儲存失敗不留下錯誤檔案。
- 修改功能沒有讓其他無關資料重新下載或重新載入程式。

安全規則必須使用 Firebase Emulator Suite（Firebase 本機模擬器）完成自動允許與拒絕測試，不以正式資料試錯。專案必須逐步建立並保存 `firebase.json（Firebase 專案設定檔）`、`firestore.indexes.json（資料庫索引設定檔）`及安全規則測試設定；新增或修改資料路徑但測試未通過時不得發布。

## 十九、發布與快取版本

- 修改功能程式時同步更新該功能的程式快取版本，不無條件改變所有功能版本。
- 發布前檢查實際提交檔案，只包含本次核准範圍。
- 發布後驗證新程式已載入、舊快取能安全升級、登入與權限正常、主要功能沒有錯誤。
- 不使用 Git（版本控制）操作修改或覆蓋未經使用者同意的文件、本機工具、測試資料或其他工作區內容。
- 新功能預設保持關閉，先由管理員或指定測試帳號驗收，再逐步開放給正式角色。
- 發布前保留上一個可用版本及回復步驟；登入、權限、主要資料讀取或寫入發生重大錯誤時，立即關閉相關功能或切回上一版。
- 診斷頁、測試資料、本機工具及開發專用檔案必須列入發布排除清單，不得因批次加入檔案而公開部署。

## 二十、現有系統逐步調整順序

後續開發時依需要逐步處理，不立即全面重構：

1. 已完成：新增中央功能清單，統一導覽、權限、按需載入及頁面生命週期；快取與資料路徑仍依各功能逐步接入。
2. 已完成：首頁只保留核心程式，Excel（表格檔）、裁帶及其他功能程式改為按需載入，並移除未使用的 JSZip（壓縮工具）。
3. 已完成：建立共用安全文字顯示、獨立操作紀錄及 Firebase Emulator Suite（Firebase 本機模擬器）安全規則自動測試基底。
4. 已完成：款號表導入 UID（使用者識別碼）隔離快取、增量補讀、交易寫入及讀取量記錄。
5. 已完成：訂單工序改為單張訂單按需讀取與獨立快取，進頁不再下載全部工序。
6. 新增的 `BOM（物料清單）`、庫存、預料、採購單價、文員工序登記及績效獎金從第一天遵守本架構。
7. 修改既有報表、裁帶或其他舊功能時，再逐步拆出功能資料存取程式及功能生命週期。
8. 將仍使用 localStorage（瀏覽器簡易儲存空間）的其他大型業務快取逐步移到 IndexedDB（瀏覽器本機資料庫）。
9. 統一舊功能的載入、成功、錯誤、確認及空資料畫面，但每次只處理本次功能範圍。

## 二十一、新功能開發完成檢查表

完成前逐項確認：

- 已建立功能身分資料。
- 已使用獨立功能程式及按需載入。
- 已定義資料路徑、分區、版本及快取。
- 登入階段沒有新增大型資料讀取。
- 已設定查詢條件、分頁、單次上限及共用 Promise（非同步工作承諾）。
- 已同步權限頁與 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）。
- 已完成 Excel（表格檔）匯入預覽、完整寫入、錯誤及回復。
- 已完成匯入／匯出歷史操作記錄、獨立查看權限及安全規則測試。
- 已完成儲存位置選擇及取消流程。
- 已記錄計算規則與來源資料版本。
- 已完成離開功能及登出清理。
- 已檢查桌機版的越文及中文顯示；手機與平板不列入現階段完成條件。
- 已完成角色、安全、快取、網路、大量資料及同時操作驗收。
- 已確認只讓相關功能快取及程式版本失效。
- 已確認外部、Excel（表格檔）及雲端文字使用安全方式顯示，沒有直接插入可執行網頁內容。
- 已完成 Firebase Emulator Suite（Firebase 本機模擬器）安全規則自動測試。
- 已確認新功能可以單獨關閉，並已驗證上一版程式、權限及資料的回復方式。
- 已檢查第三方工具、外部服務及本機服務的版本、來源、容量限制、失敗及逾時處理。
- 已更新 `系統開發交接.md（開發交接文件）`的完成狀態與待辦事項。

## 二十二、瀏覽器與內容安全

- 使用者輸入、Excel（表格檔）、Firebase（雲端資料庫）及外部服務取得的文字一律視為不可信任內容。
- 一般文字優先使用 `textContent（安全文字輸出屬性）`或建立文字節點顯示，不得直接放入 `innerHTML（直接插入網頁內容屬性）`。
- 確實需要顯示格式化網頁內容時，只能經過全系統共用且經測試的清理工具，不得由各功能自行組合安全規則。
- 公開網站必須逐步加入 CSP（內容安全政策）、防止被其他網站嵌入、內容類型保護、來源資訊限制及不必要瀏覽器能力限制。
- 前端 Firebase（雲端服務）公開連線設定可以存在網頁中，但管理員憑證、服務帳號、私密金鑰、外部服務密碼及可繞過安全規則的權杖不得放入網頁、版本庫或匯出檔。
- 若未來 Service Worker（離線背景程式）開始快取程式，登入、權限、安全修正及首頁程式必須能強制更新；不得讓舊安全程式長期離線使用。

## 二十三、第三方工具、外部服務及本機服務

- CDN（內容傳遞網路）及第三方工具必須固定明確版本，不得使用 `@latest（永遠取得最新版）`或其他會無預警改變的來源。
- 可以使用 SRI（第三方檔案完整性驗證）時必須驗證檔案；無法驗證且屬重要功能時，應評估保存已檢查的本機版本。
- 第三方工具升級前先記錄舊版本、測試匯入匯出結果並保留回復方式，不因外部發布新版就自動更新。
- 匯率及其他外部資料必須經過統一存取介面，設定逾時、回傳格式驗證、失敗提示及最後成功時間；外部服務失敗不得以空值覆蓋原本有效資料。
- 本機服務即使只監聽 `127.0.0.1（本機位址）`，仍必須限制 Origin（網站來源）、請求方法、Content-Type（內容類型）、請求容量及處理時間。
- 超過容量或來源不符的本機請求，必須在完整讀取內容前拒絕；本機服務不得因單一請求長時間占用而影響其他工作。

### 裁帶特殊 PDF（可攜式文件）轉檔工具目前定案

- 2026 年 8 月 6 日決定：裁帶特殊 PDF（可攜式文件）匯出保留為獨立本機轉檔工具，不改成一般網頁匯出，也不併入 Firebase（雲端資料庫）後台。
- 主要實際調用位置固定在 OneDrive（雲端硬碟）的 `1MAY9/Cong cu chuyen doi PDF（PDF 轉換工具）`資料夾；工具程式與 `cutting-cache（裁帶快取）`都保留在此位置同步，不改成本機獨立路徑。
- `cutting-cache（裁帶快取）`以模板及模板版本建立資料夾，不是每次轉檔建立；版本相同時重複使用同一份 JSON（版面資料）及 PNG（模板圖片）。
- 專案內的 `local-cutting-server.ps1（本機轉檔服務）`是開發來源，OneDrive（雲端硬碟）同名檔案是實際執行版本；修改轉檔服務後必須同步兩邊並重新啟動工具，未同步前不得宣稱實際工具已更新。
- 此工具只服務裁帶特殊匯出；其他功能不得直接依賴或共用其內部轉檔程式。
- 固定呼叫方式為：確認或啟動本機工具、確認訂單文字、選擇儲存位置、使用本機模板快取轉檔、將完成檔寫入使用者選擇的位置。
- 模板第一次使用或版本改變時才傳給本機工具；版本相同時使用本機模板快取，避免每次重傳大檔案。
- 本機工具只接受正式系統網站及本機測試頁面呼叫；單次要求設有容量上限，網頁等待逾時時必須停止等待並顯示錯誤。
- 本機工具故障時只停止裁帶特殊匯出，不得影響款號、訂單、成本或其他系統功能。
- 此項視為目前已定案的特殊例外。後續全系統分檔、按需載入或資料架構討論不再反覆評估是否取消或改寫；只有匯出格式改變、工具故障、瀏覽器停止支援、安全問題，或使用者主動要求時才重新評估。

## 二十四、稽核、備份與復原

- 權限、敏感金額、重要設定、匯入版本及刪除／封存等高風險異動，使用每筆獨立且預設不可修改的稽核記錄。
- 稽核記錄至少保存操作者 UID（使用者識別碼）、雲端時間、功能、動作、資料識別碼、修改前後摘要及資料版本；不得保存密碼、權杖或不必要的完整敏感內容。
- 稽核記錄不得持續累積在單一大型 JSON（資料交換格式）字串或大型陣列。
- 資料版本回復不等於備份。正式使用前必須決定備份頻率、保存期間、存取權限及安全儲存位置，並定期實際驗證能否還原。
- 至少保留上一個完整可用資料版本；未確認新版本完整前，不得刪除唯一可回復版本。
- 帳號停用、員工資料及歷史資料必須定義保留與刪除原則；真正刪除前先檢查關聯、備份及法律或公司保存需求。

## 二十五、錯誤更新與工作不中斷

更新或安全限制影響正式使用者時，依下列順序處理：

1. 立即停止相關功能的新寫入與自動重試，避免錯誤擴大。
2. 關閉發生問題的單一功能，不停止其他沒有問題的功能。
3. 保留現有完整資料，切回上一版程式、權限或有效資料版本。
4. 向使用者顯示清楚的越文與中文狀態，不把權限拒絕、網路失敗及資料錯誤混成同一個提示。
5. 使用已驗證的 Excel（表格檔）匯入匯出或舊版資料作為臨時工作方式，不使用半完成資料。
6. 記錄原因、影響範圍、處理結果及再次上線前的驗收項目。

- 系統必須保留至少一個已驗證且受安全規則保護的管理員回復管道，但不得建立繞過登入或權限的隱藏後門。
- 權限限制過嚴時，恢復上一份已驗證權限設定；權限意外過寬時，先關閉功能或收緊規則，再檢查期間內的稽核記錄。
- 雲端用量接近上限時，優先停止非必要背景讀取、即時監聽及重複查詢；不得為了維持功能而取消登入、放寬資料權限或使用不完整資料。

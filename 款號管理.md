# 款號管理

最後更新：2026 年 8 月 13 日

## 一、文件定位

- 本文件是「Quản lý mã hàng / 款號管理」功能的唯一正式功能文件。
- 保存款號總表、款號工序資料、介面操作、資料來源、權限、快取、測試、進度及交接狀態。
- 全系統架構引用 `系統架構規劃.md（全系統架構規格）`；共用介面引用 `UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`，本文件不重複建立另一套共用規則。

## 二、功能身分資料

- `id（功能代碼）`：`products（款號管理主功能）`。
- `page（功能頁）`：`summary（款號總表）`。
- `module（程式模組）`：`js/summary.js（款號總表程式）`。
- `style（功能樣式）`：`styles/features/products.css（款號功能樣式）`。
- `permission（權限代碼）`：主功能使用 `productsMain（款號管理主權限）`，頁面使用 `summary（款號總表權限）`；工價欄另受 `costView（產品工價顯示權限）`限制。
- `dataPath（資料路徑）`：`products（款號資料集合）`、`system/productsMeta（款號版本資料）`及 `productChanges（款號增量變更）`。
- `cacheScope（快取範圍）`：款號資料使用依 `UID（使用者識別碼）`隔離的 IndexedDB（瀏覽器本機資料庫）快取，由 `js/product-cache.js（款號快取程式）`管理。
- `lifecycle（功能生命週期）`：通過權限後按需載入款號程式、樣式、設定與款號資料；版本相同使用本機快取，版本改變時優先補讀增量；開啟頁面後由 `rSum（顯示款號總表函式）`產生表頭、資料列與分頁。

## 三、款號總表介面規格

- 表頭與資料列使用同一張固定欄位配置表格；款號、客人、中文名稱、越文名稱、尺寸、工序數、總工價及操作欄各自使用固定比例，桌機尺寸變化時不分開移動。
- 可以排序的欄位使用三段滑鼠排序：未排序、遞增、遞減。排序箭頭固定在越文標題同一行的右側，中文標題固定在第二行。
- 本階段不加入 Enter（確認鍵）或空白鍵切換排序，也不讓排序表頭進入鍵盤 Tab（跳格鍵）順序。
- 表格左上角提供「Chọn cột hiển thị / 選擇顯示欄位」，包含全選、部分選取狀態及恢復預設；全部欄位取消時顯示雙語空欄位提示。
- 工價欄只在 `canViewCosts（可查看工價判斷）`通過時建立表頭及資料內容；未授權時不顯示工價欄，其欄位選項也保持隱藏。前端隱藏只負責介面，正式資料仍由既有登入、角色權限及 Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）保護。
- 款號資料列沿用既有粗體款號；本次沒有再次調整款號字重。

## 四、本次完成內容

- 將全部可排序表頭改成「越文標題＋右側排序箭頭」第一行及中文第二行，保留既有三段排序結果與分頁回到第一頁的操作。
- 新增款號總表欄位選擇功能、全選、部分選取狀態、恢復預設及全部隱藏提示。
- 新增表頭與資料列共用的欄位識別及固定比例；長文字使用省略顯示，工序數及工價維持數字對齊，操作欄置中。
- 展開工序明細時會依目前顯示欄位更新跨欄數量，不因隱藏欄位造成明細走位。
- 工價欄選項跟隨既有 `costView（產品工價顯示權限）`，沒有新增權限代碼或改變角色預設值。

## 五、修改檔案與影響

- 程式與介面：`index.html（主畫面檔案）`、`js/summary.js（款號總表程式）`、`styles/features/products.css（款號功能樣式）`及 `js/features.js（中央功能清單）`。
- 測試：`tests/ui-consistency.test.mjs（介面一致性測試）`。
- 文件：本文件及 `系統架構規劃.md（全系統架構規格）`的功能文件路徑登記。
- 資料與快取：沒有新增或修改資料集合、文件、欄位、查詢、排序來源、寫入交易、資料版本或 IndexedDB（瀏覽器本機資料庫）快取內容。
- 權限與安全：沒有修改登入、角色、功能權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引。
- 回復方式：回復上述款號程式、樣式、主畫面及中央載入版本即可；不需要轉換或回復正式資料。

## 六、測試與待驗收

- JavaScript（網頁程式）語法檢查通過。
- 產能與介面一致性合計 30 項自動測試全部通過，包含固定欄位配置、排序箭頭位置、欄位選擇、全選狀態、工價欄權限顯示及按需載入版本。
- 尚待登入後在 1280 × 720、1366 × 768 及 1920 × 1080 像素實際驗收款號表頭、長文字、省略顯示、排序、欄位選擇、展開明細、工價授權與未授權畫面。
- 本次屬中風險介面調整；已知剩餘風險為真實長款號、客人名稱及雙語名稱可能需要依登入後資料微調欄寬，但不得改變資料或權限邏輯。

## 七、全系統緊湊模式套用（2026 年 8 月 10 日）

- 款號總表沿用全系統共用緊湊模式：頁面、分頁、操作區、區塊標題、表格儲存格及分頁器減少多餘空白，表格標題區最小高度調整為 44 像素。
- 款號欄粗體、欄位比例、排序、欄位選擇、工價權限、省略顯示、展開工序明細及分頁資料邏輯均維持原設定；本次沒有修改 `summary.js（款號總表程式）`的資料處理。
- 輸入框與一般控制仍維持 36 像素高度，雙語字級沒有縮小；長名稱與展開明細可依內容增高，不以固定緊湊列高截斷內容。
- 影響檔案為共用 `styles/ui-core.css（共用介面核心樣式）`、`styles/features/products.css（款號功能樣式）`、`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、介面測試及本文件。
- 資料、快取、權限與安全沒有變更；只提高樣式載入版本。全部 89 項非雲端功能、架構與介面測試通過，仍待使用者登入後以三種正式桌機尺寸確認真實長名稱的閱讀密度。

## 八、款號總表使用者欄寬試點（2026 年 8 月 10 日）

- 款號總表是第一批可自行調整欄寬的正式表格。使用者可拖曳表頭右側分隔線調整欄寬，雙擊分隔線可依目前表頭與已顯示資料自動符合內容；各欄仍受最小可讀寬度與最大合理寬度限制。
- 自訂欄寬只保存在目前瀏覽器本機；重新進入款號總表時沿用，按欄位選單的「Mặc định / 恢復預設」會同時恢復預設欄位及預設欄寬。拖曳分隔線不會觸發排序、換頁或重新讀取款號資料。
- 款號總表各欄已補上正式最小、建議及最大寬度。文字欄可承接較多空間，序號、尺寸、工序數、工價及操作欄維持合理上限；欄位超過可視寬度時沿用共用浮動水平捲軸。
- 實際修改範圍：`index.html（主畫面檔案）`、`js/summary.js（款號總表程式）`、`js/ui-table-controls.js（共用表格操作程式）`、`styles/ui-core.css（共用介面核心樣式）`、`js/features.js（中央功能清單）`、相關介面測試、共用介面規範及本文件。
- 資料、權限、快取與安全影響：沒有新增或修改雲端集合、文件欄位、查詢、寫入、資料版本、角色權限、安全規則或雲端索引；瀏覽器本機只保存非敏感的欄寬數值。
- 自動驗收：共用控制的拖曳、雙擊自動符合、保存、恢復預設及排序隔離測試通過。仍需由使用者在登入狀態以 1280 × 720、1366 × 768、1920 × 1080 像素驗收真實長名稱、欄位顯示切換、排序與浮動水平捲軸。

## 九、排序與欄寬操作修正（2026 年 8 月 10 日）

- 款號總表只在點擊越文標題右側的排序箭頭時切換排序；點擊表頭文字、空白位置及欄寬調整把手都不再觸發排序。
- 原本的長直線調整區改為欄界上的小型左右箭頭把手，靠近時顯示、拖曳時反白目前表頭；資料、換頁及排序結果不因拖曳而改變。
- 欄位可縮小的下限改以完整雙語表頭計算。越文標題、排序箭頭及中文標題都能完整顯示後即可停止縮小，較長資料仍依既有規則省略並可查看完整內容；操作欄保留按鈕正常使用所需寬度。
- 欄位選單仍顯示原本的「Mặc định／恢復預設」，功能不變，會同時恢復預設顯示欄位與預設欄寬。
- 本次只調整共用表格操作與外觀，不修改款號資料、排序規則、讀取、快取、權限或安全規則。共用排序、拖曳下限、保存與恢復測試，以及全系統 94 項非雲端回歸測試均已通過；仍需登入後確認真實表頭長度與滑鼠拖曳手感。

## 十、全系統表格主視窗浮動捲軸（2026 年 8 月 10 日最新本機修正版）

- 款號總表沿用全系統單一規則：表格超寬且目前頁面內容不足一個可視畫面時，水平捲軸顯示在整個主內容可視區最下緣，不放在款號資料區、分頁器、空欄位提示或表格底框內。
- 款號資料使主內容垂直超過一個畫面時，頁面位於頂端不顯示浮動捲軸；使用者開始向下捲動後，才在主內容最下緣顯示。回到頂端、表格離開畫面、表格不再超寬或離開功能時隱藏。
- 前一版依資料區或表格框底部定位的程式、樣式及測試已淘汰，不保留相容分支；功能框內的原始水平捲軸由共用控制接管並隱藏，畫面只保留一條水平操作。
- 本次只調整共用表格定位與外觀，不修改款號資料、排序、分頁、讀取、快取、權限或安全規則。共用捲軸與介面兩組 27 項測試及全部 98 項非雲端回歸測試通過；仍需登入後確認短內容與長內容兩種實際位置。

## 十一、款號匯入虛框上下間距修正（2026 年 8 月 11 日）

- 移除 `index.html（主畫面檔案）`內已淘汰的 `.detail-import-drop（款號匯入框舊樣式）`尺寸規則，避免舊的下外距及內距覆蓋 `ui-file-picker（共用檔案入口）`。
- 款號匯入虛框改為完整沿用共用檔案入口：四邊外距統一為 4 像素，內容使用共用內距並隨操作列高度垂直置中；點擊、拖入、格式提示及匯入流程維持不變。
- 影響只限款號匯入虛框外觀；沒有修改款號資料、匯入格式、讀取、寫入、快取、權限、安全規則或雲端索引。
- `tests/ui-consistency.test.mjs（介面一致性測試）`新增舊樣式不得重新覆蓋共用上下間距的回歸驗證；仍需登入後於三種正式桌機尺寸確認虛框上下留白一致。

## 十二、全域語言顯示模式套用（2026 年 8 月 12 日）

- 款號總表、明細、匯入狀態、空資料提示與共用表頭已接入中央三種語言模式，不新增款號頁自己的切換器或偏好。
- 單語模式只縮減雙語預留高度、內距與目前可見表頭所需寬度；款號總表既有欄寬拖曳、保存與恢復預設功能不變，切換語言不會覆蓋使用者欄寬。
- 本次沒有修改款號資料、匯入格式、版本、公式、Firebase（雲端資料庫）讀寫、權限或安全規則。
- 共用語言、表格、架構及介面測試已納入款號情境；仍需在登入後以三種語言模式檢查總表、明細與匯入流程的桌機畫面。

## 十三、全域語言殘留修正（2026 年 8 月 13 日）

- 款號總表由程式建立的可排序表頭及客人、尺寸等明細標籤改為正式越文與中文節點，修正只顯示越文時仍看見中文表頭或明細文字的情況。
- 共用表格控制會在表頭重新建立後再次確認語言結構；語言切換只更新目前文字所需下限，不改變欄位選擇、排序、拖曳、雙擊自動符合、保存或恢復預設行為。
- 本次不修改款號資料、匯入格式、分頁、資料版本、IndexedDB（瀏覽器本機資料庫）內容、工價權限、安全規則或雲端索引。
- 11 個本次介面程式通過語法檢查，全站 142 項非雲端自動測試全部通過；仍需登入後使用真實長款號、客人及雙語名稱完成三種語言模式畫面確認。

## 十四、僅新增匯入、完整版本歷史與同產品群組（2026 年 8 月 13 日）

- 款號 Excel（表格檔）匯入固定只建立系統尚未存在的新款號。已存在且完全相同的款號、已存在但工序名稱、順序、數量或秒數不同的款號，都會列出分類與差異後略過，畫面不再提供「覆蓋舊款號」。
- 每次實際新增、修改或刪除款號後，系統會建立當時全部款號的完整且不可改寫版本，保存於 `productVersions（款號版本）`與 `productVersionChunks（款號版本分段）`；可從「工序修改」頁查看版本與匯出 Excel（表格檔）。
- 系統依客戶、越文品名，以及每道工序的工序號、順序、類別與越文名稱判斷候選群組；中文品名、中文工序名稱、尺寸與秒數不參與判斷，顏色暫不處理。系統不自動建立群組，必須由使用者確認後才寫入 `productGroups（款號群組）`與 `productGroupMembers（群組成員）`。既有群組保存的舊版候選特徵由款號模型相容讀取，不批次改寫正式群組資料。
- 正式工序修改會保留第一次修改前的開發人員原始工序，之後只更新目前正式標準，避免原始設定被覆蓋後無法追溯。
- 本批只完成程式、本機安全規則與測試；`firestore.rules（Firebase 雲端文件資料庫安全規則）`尚未由本批發布。

# 款號管理

最後整理：2026-08-25

## 1. 文件定位

本文件是款號主檔、款號匯入、同產品群組、完整編輯及群組快速修改的現行正式功能規格。共同架構與介面規則分別以 `系統架構規劃.md`及`UI設計規範與參照/介面設計規範.md`為準。

## 2. 功能與權限

| 頁面或操作 | 權限 | 用途 |
|---|---|---|
| 款號總覽 | `summary（款號總覽）` | 查詢、展開及匯入款號 |
| 款號修改流水帳 | `productsMain（款號管理主功能）` | 查詢全部使用者的款號修改 |
| 工序修改（含群組） | `productionProcessEdit（工序修改，含群組）` | 修改款號正式欄位、工序結構、標準秒數及群組；每小時產能只依秒數自動計算，不可直接修改 |
| 成本查看 | `costView（成本查看）` | 查看核准的成本內容 |

非管理員缺少新權限欄位時視為關閉。畫面入口不能取代資料層與安全規則的權限檢查。

## 3. 款號主檔與固定身分

### 3.1 正式來源

`products/{productId}`是款號及其完整工序的唯一正式來源，包含：

- 款號代碼與客戶。
- 中文／越文品名。
- 尺寸。
- 工序號、分類、中文／越文工序名稱。
- 標準秒數與依現行公式計算的每小時產能。
- 其他正式款號欄位。

未鎖定功能不得另存可過期的款號名稱、工序名稱或秒數作為正式顯示來源。

### 3.2 固定識別碼

- `productId（款號固定識別碼）`建立後不變。
- `processId（工序固定識別碼）`建立後不變。
- 現行介面與正式儲存服務禁止修改既有款號代碼。
- 修改客戶、品名、尺寸、工序號、分類、名稱或秒數不會建立另一個款號或工序。
- 系統不提供自動合併不同工序的功能。

## 4. 款號總覽與展開

- 使用者實際進入款號頁後才載入款號資料，不在登入時預讀完整集合。
- 支援中央智慧搜尋、Enter（確認鍵）執行、排序、欄位選擇、欄寬與水平捲動；搜尋依款號、客戶、中／越文品名及尺寸判定相關度，不要求使用者輸入完全相同的連續字串。
- 上方款號總數與工序總筆數固定顯示目前已載入的完整資料總數；搜尋及客戶篩選只改變下方表格，不改變這兩個總數。
- 款號列可展開目前完整工序，直接查看工序號、分類、中文／越文名稱、秒數及每小時產能。
- 款號代碼只讀；客戶、中文／越文品名、尺寸及展開後工序的可修改欄位可開啟共用群組快速修改。
- 操作欄「編輯」先開啟目前單一款號的完整編輯面板，不預先混入群組成員。

## 5. Excel（試算表）款號匯入

### 5.1 唯一與完整覆蓋

- 系統只在檔案前 20 個實際列尋找完整表頭；表頭上方的標題或說明列直接忽略。
- 匯入欄位依表頭名稱辨識，不依固定欄位位置。款號、客人、中文名稱、越文名稱、尺寸、工序號、加工分類、工序中文、工序越文及秒數必須各出現一次；缺少或重複時停止匯入，不從資料內容猜測欄位。
- 欄位可以調整順序並加入不相關欄位；每小時數量欄只辨識後忽略，正式資料仍由秒數自動計算。
- 正規化後的款號代碼是匯入唯一鍵。
- 新款號建立新的固定 `productId`。
- 相同款號採完整覆蓋並保留原 `productId`；內容相同時跳過，不產生無意義寫入。
- 匯入檔有 11 道工序就以 11 道為準，有 13 道就以 13 道為準，舊主檔未出現於新檔的工序不殘留。
- 同工序號沿用原 `processId`，新增工序才建立新 `processId`。
- 若準備移除的舊工序已有正式引用，但新檔沒有相同工序號或其他可唯一承接關聯，該款號不得覆蓋並列出原因；不得讓歷史報工失去可解析工序。

### 5.2 既有報工影響

- 匯入不把新名稱、分類或秒數大量複製回每筆產能。
- 既有未鎖定報工保留員工、日期、訂單項目、`productId`、`processId`、數量及狀態，再由 Product Resolver（款號解析器）取得覆蓋後的最新主檔內容。
- 同工序號保留固定身分時，既有報工會自動使用新名稱、分類、秒數及每小時產能。
- 已鎖定月份仍使用鎖定快照，不受後續匯入影響。

### 5.3 預覽、進度與結果

匯入流程分成三段：

1. 讀取與驗證：顯示讀取進度，檢查重複款號、必要欄位、型別及工序。
2. 影響預覽：抬頭顯示檔名、覆蓋款號、新增款號、工序變更及受影響報工數；下方列出確實受影響的報工工序明細。
3. 使用者確認後執行：顯示逐款號進度；結果區分覆蓋、新增、相同跳過、失敗與原因。

使用者確認前不得寫入。讀取或分析期間需停用重複觸發，避免因等待而連按多次。

### 5.4 原子寫入與操作紀錄

每次匯入先原子建立一筆批次主摘要與開始操作紀錄。每一個款號的下列資料必須在同一筆 Transaction（雲端交易）一起成功或失敗：

- `products（款號主資料）`
- `productCodeIndex（款號代碼索引）`，只在新增款號時建立
- `productChangeItems（款號修改明細）`
- `system/productsMeta（款號版本資料）`

單一款號失敗不得留下半份主檔。全批結束時再原子結案主摘要與結果 `operationLogs（獨立操作紀錄）`，包含檔名、操作者、成功、失敗及未處理數量。

## 6. 單一款號完整編輯

### 6.1 畫面

- 開啟時只顯示所選款號的客戶、中文／越文品名、尺寸及完整工序。
- 款號代碼以鎖定樣式顯示，不能輸入或修改。
- 不顯示獨立「排序」欄；工序號就是排序。
- 既有工序可拖曳；新增工序先加入尾端，也可拖到指定位置。
- 工序移到已存在的位置時，其他工序依位置順移，固定 `processId`不變。
- 款號與工序目前不提供刪除或停用按鈕。

### 6.2 與群組快速修改共用

完整編輯本身只處理目前款號。使用者點擊客戶、中文／越文品名、尺寸、工序號、分類、名稱或秒數時，才開啟與款號總覽及生產登記共用的群組快速修改面板。

尚未儲存的新增或拖曳存在時，必須先完成或取消單款結構變更，才可開啟群組修改，避免兩個流程互相覆蓋。

## 7. 同產品群組

### 7.1 群組定位

- 群組只保存固定 `productId`關聯，不保存另一份款號內容。
- 群組允許只有一個成員，作為之後加入同產品款號的起點；空群組仍禁止建立。
- 同一款號同一時間只能屬於一個有效群組。
- 款號主檔修改或匯入後，群組不會因名稱或工序內容變更而斷掉；群組仍連到相同 `productId`，畫面改用最新主檔內容。
- 群組沒有永久刪除流程；停用只讓群組退出一般使用，不能刪除款號、訂單、產能或歷史。

### 7.2 已有群組

點擊修改時，如果目前款號已有有效群組：

- 右上狀態顯示目前群組。
- 下方只顯示該群組成員，不再推薦其他款號。
- 成員依尺寸分頁顯示。
- 來源款號與資料一致成員預設勾選，差異成員預設不勾選；包含最初點入的來源款號在內，使用者都可取消不想修改的成員。

### 7.3 沒有群組時的推薦

點擊修改時，如果目前款號沒有群組，系統立即載入候選，不必先進入預覽或先建立群組。

推薦規則：

1. 客戶及越文品名都必須相同；越文品名不同時先修正品名，不列入推薦。
2. 每個尺寸分頁各自比較，不使用其他尺寸作為共同基準。
3. 來源款號所在尺寸直接以來源款號為比較基準；其他尺寸以該尺寸最多款號共同使用的「工序數量＋工序號＋越文工序描述＋標準秒數」組合作為主要版本。
4. 屬於主要版本的款號標示「高度符合」並預設勾選；少數不同版本逐項顯示差異且預設不勾選，但使用者仍可自行選擇。已有群組開啟快速修改時也使用相同預設勾選規則，不因已在同群組就自動全選。
5. 同尺寸版本票數相同而沒有明確主要版本時，標示「存在多種版本・請確認」，不自行判定高度符合。
6. 已在其他群組的款號顯示實際群組名稱且不得勾選。
7. 即使某個尺寸只有一個候選，也要保留該尺寸分頁並顯示該款號，但標示「無同尺寸款號可比較」。

每個尺寸分頁固定依「資料一致、狀態異常、已在其他群組」排序；同一狀態內保留原候選順序，來源款號固定列在可選區最上方。

### 7.4 尺寸分頁與一致性狀態

- 不同尺寸不得混在同一頁。
- 分頁標題顯示「尺寸／該尺寸款號數量」。
- 比對只在同尺寸候選內進行，避免不同尺寸無基準時全部被誤判異常。
- 每個尺寸的勾選、輸入值與主要版本判定彼此獨立；切換分頁不會把某個尺寸的修改值套到其他尺寸。
- 明細在款號右側顯示越文品名，再顯示尺寸、總工序數量、相關工序欄位及差異狀態。
- 推薦表格優先在彈窗內容寬度內緊湊呈現；確實超寬時，以表格最大可視框作為捲動區，水平捲軸固定留在該框底部，不必先捲到全部資料末端。
- 每個款號可展開完整工序明細，不需離開面板查款號表。
- 表格沿用共用欄寬控制；款號、越文品名、尺寸及越文工序描述使用符合內容的預設比例，長內容可調整欄寬查看，差異底色在滑鼠移入時仍固定顯示。

狀態依實際差異顯示：

- `資料一致`
- `工序數量不同`
- `越文描述不同`
- `標準秒數不同`
- 同時存在多項差異時逐項列出。

差異只作提醒，不阻止使用者勾選並執行；只有完全沒有對應工序的該項目無法憑空修改，其他可處理款號仍可繼續。

## 8. 共用群組快速修改

### 8.1 共用入口

下列入口使用同一套面板、資料準備、預覽、儲存及結果服務：

- 款號總覽欄位點擊。
- 單一款號完整編輯內的欄位點擊。
- 生產登記的工序號、工序名稱及秒數；報工款號代碼只讀。
- 生產分析的正式秒數修改入口。

開啟過程顯示載入進度並停用重複點擊。

### 8.2 欄位與明細

面板依修改欄位顯示直接相關內容：

| 修改項目 | 必須顯示 |
|---|---|
| 客戶／中文品名／越文品名／尺寸 | 款號、尺寸、目前值、修改後值、群組狀態 |
| 工序號 | 款號、尺寸、總工序數、目前／修改後工序號、越文名稱、秒數、衝突或順移結果 |
| 分類 | 款號、尺寸、工序號、越文名稱、目前／修改後分類 |
| 中文／越文工序名稱 | 款號、尺寸、工序號、目前／修改後名稱、秒數 |
| 標準秒數 | 款號、尺寸、工序號、越文名稱、目前／修改後秒數、修改後每小時產能 |

數字欄採一致對齊與固定最小間距，避免總工序數量與工序號看成同一欄。

來源款號只作為同尺寸比較基準，不強制成為修改目標；使用者取消來源款號後，仍可只修改其他已勾選款號。

### 8.3 預覽與結果

使用者按下預覽後，對每個款號顯示：

- 款號與尺寸。
- 工序號及越文工序名稱。
- 被修改欄位的「目前值 → 修改後值」。
- 相關款號主檔內容與差異狀態。
- 可執行或無法處理的原因。

執行期間顯示逐款號進度。完成結果保留同樣的業務欄位與「目前值 → 修改後值」，另外顯示成功、失敗及說明，不能只顯示「已完整儲存」。

每次只儲存目前開啟的尺寸分頁。使用者關閉修改結果後：

- 只有一個尺寸時直接結束，不再詢問。
- 有多個尺寸時才另外顯示「是否繼續修改其他尺寸」。
- 選擇繼續會回到同一個修改面板，可切換其他尺寸，也可重新訂正剛修改的尺寸。
- 選擇結束才關閉修改流程；結果視窗與後續詢問不得同時出現。
- 系統不判定使用者是否已完成全部尺寸，也不顯示「所有尺寸已完成」。

### 8.4 儲存規則

- 快速修改與完整編輯共用 `product-master-service.js（款號正式儲存服務）`。
- 每個款號各自原子儲存；一個款號失敗不會留下半份資料。
- 一次儲存只建立目前尺寸分頁內已勾選款號的修改請求，其他尺寸不寫入。
- 其他已成功款號不因單一失敗而被描述成未完成，結果逐筆列明。
- 主檔修改後，所有未鎖定功能透過固定識別碼與解析器使用最新內容，不建立手動同步按鈕。

## 9. 款號修改流水帳

- 畫面只有「主摘要」與「明細」兩層，不另建第三層。
- 主摘要顯示時間、操作者、單款／群組／Excel 匯入、款號數、成功／失敗／未處理數及狀態。
- 摘要搜尋支援款號、操作者及檔名的智慧比對與相關度排序；輸入期間只篩選已載入摘要，不產生雲端讀取，按 Enter（確認鍵）後才跨批次查找款號明細。
- 每筆主摘要都可在原列下方獨立展開或收合；同時展開其他紀錄不會自動關閉目前內容。明細抬頭在該展開區可視期間保留收合操作，收合後回到原摘要位置。
- 明細第一次最多讀取 100 筆，使用者按「載入更多明細」後再接續讀取最多 100 筆；已載入內容在本次頁面保留，重新收合與展開不重複讀取。
- 展開區可搜尋目前已載入的款號、工序號、越文工序名稱、修改項目及修改前後內容；搜尋不額外讀取尚未載入的明細，畫面必須明確標示搜尋範圍。
- 摘要列與展開區的收合按鈕沿用中央按鈕樣式、固定寬度與對齊，不建立獨立外觀。
- 已載入款號使用自然順序顯示；單款與群組的同款明細先顯示款號基本欄位，再依工序號數字順序顯示工序號、越文工序名稱、秒數及其他實際變更。中文名稱不作為排序依據，但實際變更仍保留於流水帳。
- 單款與群組修改明細直接列出款號、尺寸、總工序數、工序號、工序名稱、修改項目及修改前後值；分類不另做固定顯示欄。
- Excel 匯入對每款顯示「套用前全部工序」與「套用後全部工序」，工序號、名稱、秒數的不同格使用底色標示；原有 11 道變成 10 或 12 道時，移除與新增列分別標示。
- Excel 匯入的 100 筆限制只作用於一次載入的受影響款號數；每個已載入款號仍完整顯示整套套用前／套用後工序，不改變原本配對與差異底色。
- 有 `productsMain`權限的使用者都可查看；新增或修改流水帳只能由真正的款號寫入流程完成，不提供人工編輯或刪除。

### 9.1 新追蹤起點

- 正式重設時直接以當前 `products`作為新起點，不將舊修改歷史轉進新流水帳。
- 重設只更換追蹤標記、版本基準及清除舊指向；款號、客戶、品名、尺寸、工序、秒數、`productId`及 `processId`必須完全不變。
- 舊 `productHistory`、`productChanges`、`system/impHist`、舊款號匯入／修改操作紀錄及對應指向要實體刪除；固定身分所需的技術轉換紀錄保留。
- 正式刪除前必須先完成本機、安全規則與款號內容比對；刪除後再驗證舊歷史 0 讀取、0 寫入、0 相容邏輯、0 安全規則依賴。

## 10. 資料路徑

| 路徑 | 用途 |
|---|---|
| `products` | 目前正式款號與完整工序 |
| `productCodeIndex` | 正規化款號代碼到固定 `productId`的唯一索引 |
| `system/productsMeta` | 款號資料版本與快取核對 |
| `productChangeBatches` | 單款、群組與 Excel 匯入的主摘要 |
| `productChangeItems` | 每個款號的完整修改前後與直接明細 |
| `productGroups` | 群組主資料與有效狀態 |
| `productGroupMembers` | 群組與固定 `productId`關聯 |
| `operationLogs` | 匯入、完整編輯、快速修改與群組操作紀錄 |

`productHistory`、`productChanges`、`system/impHist`、`orderProcesses`、`productProcessStandards`、`processEditJobs`、舊款號快照及舊版本分段不是現行正式來源，不得在新功能重新加入。

## 11. 主要程式責任

| 檔案 | 責任 |
|---|---|
| `js/product-model.js` | 款號與工序正規化、固定身分及共用計算 |
| `js/product-change-log-store.js` | 款號修改批次、完整前後快照與直接明細 |
| `js/product-change-log.js` | 流水帳摘要、款號搜尋、一般明細與匯入對照畫面 |
| `js/product-master-store.js` | 主檔、代碼索引、流水帳明細與版本寫入計畫 |
| `js/product-master-service.js` | 匯入、完整編輯、快速修改與群組共用正式交易 |
| `js/product-resolver.js` | 依固定識別碼解析目前款號主檔 |
| `js/product-import-impact.js` | 匯入前受影響報工彙總計數 |
| `js/product-master-editor.js` | 單一款號完整編輯及群組修改入口 |
| `js/product-quick-edit.js` | 共用群組快速修改、推薦、預覽、進度與結果 |
| `js/product-group-store.js` | 群組與成員資料存取 |
| `js/product-group-runtime.js` | 群組推薦、尺寸分頁、差異與排序 |
| `js/production/product-groups.js` | 群組管理頁面 |
| `js/production/process-group-ui.js` | 生產功能使用的共用群組介面元件 |
| `js/production/product-seconds-adapter.js` | 生產登記與分析的共用修改轉接 |

## 12. 讀取與快取

- 進入款號功能後才讀取主檔與群組。
- `system/productsMeta`是款號快取唯一版本來源；版本相同才可使用本機快取。
- 每次成功修改取得唯一且單調增加的修改序號；群組逐款修改允許其他使用者序號穿插，不要求群組序號連續。
- 版本改變時，只查詢本機序號之後到目前序號的款號並合併；缺少安全同步位置或驗證不一致時，才完整讀取有效款號。
- 既有第四版快取若版本與雲端相同，直接補記追蹤起點；不同版本且缺少追蹤起點時完整讀取一次，避免漏掉修改。
- 正常程式直接讀取預算：單款修改後同步約 3 Reads（文件讀取），10 款修改後同步約 12 Reads；期間若有其他使用者修改，只增加那些實際不同款號。完整回復讀取約 1,333 Reads（依有效款號總數變動）。安全規則驗證的身分與角色文件可能另計費。
- 現行功能不允許永久刪除整個款號；快取可套用正式刪除標記移除已不存在文件，但不新增刪除入口。
- 群組開啟時載入必要成員與候選；已在群組的款號不掃描無關候選。
- 匯入影響數只對實際變更且已有關聯風險的工序執行 Aggregate count（彙總計數），不下載整批報工明細。
- 快速修改開啟時共用同一個載入工作，避免連按造成重複查詢。
- 修改完成只更新直接相關的主檔、群組及目前工作階段資料，不要求全站重新下載。
- 流水帳只在使用者開啟該頁時載入，主摘要每頁 50 筆；明細在點開時才讀取，每次最多 100 筆並由使用者決定是否載入更多。展開 184 筆的批次若全部查看約讀取 184 筆明細；同一頁面重開已載入批次使用記憶內容，不重複讀取。摘要輸入只查本機已載入資料；按 Enter（確認鍵）跨批次搜尋時，明細查詢最多 50 Reads（文件讀取），再讀取對應主摘要最多 50 Reads，最壞單次約 100 Reads，不建立另一套持久快取。

## 13. 驗收重點

1. 既有款號代碼在所有入口均不可修改。
2. 匯入 11／12／13 道工序能完整取代，不殘留舊工序。
3. 同工序號保留固定身分，未鎖定報工顯示覆蓋後內容。
4. 已有群組只顯示現有成員；無群組時立即顯示推薦。
5. 推薦預設勾選一致項，差異項不預選，其他群組成員不可選。
6. 來源尺寸以來源款號判定一致性，其他尺寸以自己的多數版本判定，彼此不得影響；單一款號與票數相同時不得誤報異常或高度符合。
7. 尺寸分頁、總工序數量、展開明細及差異狀態正確。
8. 每次只寫入目前尺寸；關閉結果後才詢問是否繼續修改其他尺寸，且可返回訂正目前尺寸。
9. 所有欄位入口共用同一面板、預覽、進度與結果格式。
10. 同時修改同一款號時明確拒絕或要求重試，不覆蓋他人結果。
11. 三種語言模式與桌機表格欄寬、數字對齊及水平捲動正常。
12. Runtime 對所有已淘汰款號、工序及快照路徑為零讀寫。
13. 單款修改、群組修改與 Excel 匯入都正確建立一筆主摘要及對應款號明細。
14. 匯入完整顯示全部前後工序，工序數不同、內容全換、新增與移除的底色都正確。
15. 有款號管理權限的全部現行角色可讀流水帳，未授權、停用、無效角色與未登入者不可讀。
16. 新追蹤起點前後款號內容雜湊完全相同，固定識別碼不變，舊歷史實體刪除後為零讀取、零寫入、零相容邏輯與零安全規則依賴。
17. 流水帳明細在原摘要列下方可同時展開、個別收合、每次最多載入 100 筆，已載入搜尋與重新展開不增加讀取；Excel 匯入仍完整呈現每款整套前後工序。
18. 單一款號可建立群組，空群組仍拒絕；來源款號取消勾選後，可只儲存其他選定款號。
19. 款號總表、群組清單、快速修改及流水帳的智慧搜尋均支援 Enter（確認鍵），輸入期間不產生額外雲端查詢。

# 裁帶統計與 PDF 轉換系統正式規格

## 一、文件定位

- 本文件是裁帶統計、模板匯入、訂單比對、模板儲存及 PDF（可攜式文件）轉換的現行正式規格。
- 2026 年 7 月 29 日以前與本文件衝突的舊規則停止適用。
- 2026 年 7 月 29 日完成的新模板改版內容，以及 2026 年 7 月 30 日的程式檢查結果，已統一整理在本文件。
- 舊模板不再使用，也不再要求相容。
- 後續修改若要改變本文件規格，必須先取得使用者同意。

## 二、功能範圍

裁帶功能是一個獨立功能，範圍包含：

- `.xlsx`（Excel 表格檔）裁帶模板匯入與檢查。
- `.xlsx`、`.xls`（Excel 表格檔）訂單匯入。
- 款號、數量及模板比對。
- Firebase Firestore（Firebase 雲端文件資料庫）模板資料儲存。
- IndexedDB（瀏覽器本機資料庫）模板檔案快取。
- 本機模板索引、圖片索引及 PDF（可攜式文件）轉換。
- PDF（可攜式文件）前方統計報告。

不得因修改裁帶功能而變更登入、訂單、出勤、報工或其他不相關功能。

## 三、新模板固定結構

- 系統只接受 A 欄到 K 欄固定位置的新版 `.xlsx`（Excel 表格檔）模板。
- 固定欄位如下：
  - A 欄：圖片或圖片欄段別文字。
  - B 欄：款號。
  - C 欄：顏色。
  - D 欄：帶子規格。
  - E 欄：工序。
  - F 欄：裁切規格。
  - G 欄：訂單數量。
  - H 欄：`SỐ KIỆN`（每件條數）。
  - I 欄：`SL:CẮT THỰC TẾ`（實際裁切總數）。
  - J 欄：缺料數量。
  - K 欄：備註。
- 每一組由自己的表頭列開始，下一個表頭列代表下一組開始。
- 每個工作表及每個組別的表頭必須分開保存，禁止套用第一個工作表的表頭。
- 欄位寬度比例必須依照模板原始欄寬，禁止自行平均分配。

## 四、款號規則

- B 欄內容是完整且唯一的正式款號。
- 款號可以由任何英文字母、數字及符號組成。
- 系統只清除款號前後空白，並統一英文字母大小寫。
- 不得限制成特定英文字母或數字格式。
- 純數字款號必須正常支援。
- 新規格不再自動展開範圍款號；模板與訂單必須使用相同的完整款號。
- 同一款號若出現在不同模板，必須阻止覆蓋並明確指出衝突模板。

## 五、模板匯入與錯誤規則

- 每個工作表都必須獨立檢查 B、G、H、I 欄固定表頭。
- 找不到表頭、找不到款號、H 欄空白或 H 欄為 0，均屬於錯誤。
- 出現錯誤時禁止確認及匯入模板。
- 錯誤訊息必須明確顯示工作表、儲存格、款號與原因，方便使用者回到原模板修改。
- 同一原始組別內若 H 欄的 `SỐ KIỆN`（每件條數）不同，必須彈出提醒並列出位置，但仍允許匯入。
- 舊規格模板可以保留在既有資料中，但必須標示停用，不得參與訂單配對。

## 六、原始組別與合併儲存格

- 組別只能依照匯入模板的原始表頭位置判斷。
- 不得因為圖片、文字、工序、規格或數量相同而自行合併不同組別。
- 相同版面模組可以共用，但每個原始組別的資料、表頭及位置必須獨立保存。
- 原模板有合併儲存格，PDF（可攜式文件）必須依照相同範圍合併顯示。
- 原模板沒有合併儲存格，PDF（可攜式文件）必須逐格顯示。
- 不得只用文字是否相同判斷是否合併。
- 顏色、規格、工序、裁切規格、數量、每件條數、總數、缺料與備註欄，都必須尊重模板實際合併狀態。

## 七、數量計算

- 訂單數量為 0 的款號不顯示。
- G 欄的 `SL:PO PCS`（訂單數量）等於訂單匯入數量。
- H 欄的 `SỐ KIỆN`（每件條數）代表每一件產品在該工序需要裁切的條數。
- I 欄的 `SL:CẮT THỰC TẾ`（實際裁切總數）等於訂單數量乘以每件條數。
- 計算關係：實際裁切總數 = 訂單數量 × 每件條數。
- 同一份訂單內重複出現的相同款號，訂單數量必須加總。
- 匯出前必須驗算訂單數量、每件條數及實際裁切總數。

## 八、A4 紙張與組別高度

- PDF（可攜式文件）固定使用 A4（紙張）直式、一頁寬。
- 模板欄位必須依原始比例放大或縮小到一頁寬。
- 同一組有效款號為 1～5 個時，組別高度固定為 4.95 公分。
- 1～5 個款號平均分配內容區高度。
- 五格時的內格高度是標準列高。
- 從第 6 個款號開始，每增加一個款號，組別增加一個標準列高。
- 第 6 個款號以上不得繼續壓縮列高或過度縮小文字。
- 每張 A4（紙張）的組別數量依實際高度安排，不再強制固定六組。
- 同一組別必須完整留在同一頁，禁止拆到兩頁。
- 若剩餘空間不足，下一組完整移到下一頁。
- 不同模板可以接續排版，不得因切換模板強制換頁。
- 目前業務前提是不會出現單一組別超過一整張 A4（紙張）。

## 九、組別總計與統計報告

- 裁帶內容組別內不顯示 `TỔNG CỘNG`（總計）列。
- 不得預留組別總計列高度。
- 每個款號自己的訂單數量、每件條數與實際裁切總數必須保留。
- PDF（可攜式文件）最前方的統計報告必須保留。
- 統計報告保留已完成款號、缺少模板款號、訂單數量及裁切總數統計。
- 「移除組別總計」不得影響統計報告的總數。

## 十、文字顯示

- 文字不得超出格子、重疊或使用 `...`（省略符號）截斷。
- 文字應依格子寬度與高度自動縮放，優先完整顯示。
- 同一份 PDF（可攜式文件）的相同欄位應使用一致字級，避免同欄文字忽大忽小。
- 原模板有手動分行時，必須保留原分行。
- 尺寸乘法格式如 `15MM*1.2MM`（15 毫米乘 1.2 毫米）、`15MMx1.2MM`（15 毫米乘 1.2 毫米）或 `15MM×1.2MM`（15 毫米乘 1.2 毫米）不得從中間錯誤拆行。
- 文字包含 `ĐẦU TRÁI PHẢI`（頭左右）或 `DAU TRAI PHAI`（頭左右）時，允許在 `ĐẦU`（頭）或 `DAU`（頭）後分行。
- 不得依文字意思自行猜測顏色、規格或備註的拆分方式。

## 十一、圖片與索引

- 圖片必須保持原比例，禁止裁切、拉伸或變形。
- 圖片在圖片欄可用範圍內自動放大或縮小。
- 圖片欄的段別文字必須保留。
- 圖片優先從 `.xlsx`（Excel 表格檔）內部 `xl/media`（內部圖片資料夾）讀取，不使用不可靠的畫面截圖方式。
- 相同圖片以內容雜湊值判斷，只儲存一份圖片檔，再由不同組別共用圖片索引。
- 版面模組、組別資料與圖片資料必須分開保存。
- 索引至少要能定位模板、工作表、原始組別、款號列、欄位、合併範圍、欄寬及圖片。
- 索引版本改變時，舊索引必須自動失效並重建。
- 2026 年 7 月 30 日合併儲存格修正後，本機索引版本為第 15 版。

## 十二、速度與快取

- 日常目標是第一次建立索引，後續直接使用索引產生 PDF（可攜式文件）。
- 不得每次開啟 Microsoft Excel（微軟表格程式）重新掃描模板。
- 同一模板只分析一次結構。
- 同一圖片不得重複提取或儲存。
- 本機已有有效快取時，網頁不得再次傳送完整模板。
- 100 個款號的 PDF（可攜式文件）產生時間目標為 1 分鐘內。
- 速度優化不得犧牲組別、欄寬、表頭、合併儲存格、圖片或文字規則。

## 十三、暫存與安全刪除

- 每次轉換成功或失敗後，都必須清除本次建立的暫存資料。
- 只能刪除名稱符合本工具專用格式的暫存資料夾。
- 禁止刪除使用者其他檔案。
- 啟動時可以清除超過 24 小時的舊裁帶暫存資料。
- 禁止使用 `taskkill`（強制結束程序）關閉全部 Microsoft Excel（微軟表格程式）。
- 模板索引快取與單次轉檔暫存資料必須分開管理。

## 十四、2026 年 7 月 29 日完成紀錄

- 新模板改為固定 A～K 欄。
- 支援純數字及包含符號的完整款號。
- 每個工作表及組別使用自己的表頭。
- 原始組別不再依相同內容自行合併。
- 1～5 個款號維持固定組高，第 6 個起自動增加高度。
- 組別依實際高度安排頁面，同組不跨頁。
- 裁帶內容移除組別總計列，統計報告保留。
- 圖片改用內容雜湊索引，相同圖片不重複儲存。
- 本機工具直接讀取 `.xlsx`（Excel 表格檔）內部結構，不再依賴 Microsoft Excel COM（Excel 本機自動化介面）。
- 模板錯誤會指出工作表與儲存格；結構錯誤禁止匯入。
- 同組每件條數不同時顯示提醒，但允許匯入。
- 網頁匯入與本機 PDF（可攜式文件）已統一解析合併儲存格；合併範圍內的內容與寫入位置都以左上角來源格為準。
- 同一款號重複落在相同的合併 H 欄時，每件條數只計算一次；不同款號共用同一個合併 G 或 I 欄時，輸出前會顯示工作表與合併範圍並禁止錯誤輸出。
- 修正前建立的模板分析紀錄會標示「Cần nhập lại / 需要重新匯入」；使用者只需以相同檔名重新匯入一次，原始模板檔內容不會被程式改寫。
- 新索引模式第一次建立索引後，後續轉換可直接使用快取。

## 十五、2026 年 7 月 30 日檢查結果

### 已確認正常

- `js/cutting.js`（裁帶統計程式）與 `js/cutting-store.js`（裁帶模板儲存程式）語法檢查通過。
- `local-cutting-server.ps1`（本機 PDF 轉檔工具）語法檢查通過。
- 專案路徑與 OneDrive（雲端同步資料夾）內的本機轉檔工具內容完全相同。
- 原始表頭分組、動態組高、同組不跨頁、圖片共用、統計報告及移除組別總計的核心規則均已存在。
- 網頁模板分析與本機轉檔工具已使用一致的合併儲存格來源規則，B、G、H、I 欄均會解析到合併範圍左上角。

### 待修正的程式衝突

- 一般大小的模板在「新分段加主資料」不超過單一批次限制時，會由 Firebase Firestore（Firebase 雲端文件資料庫）的 `writeBatch`（原子批次寫入）一次提交；一般上傳中斷不會留下新舊分段混合。
- 仍需防護的情況是：模板大到必須拆成多個 `writeBatch`（原子批次寫入）、同一模板被兩個人同時覆蓋、或後續清理舊分段失敗。這些情況不應被描述成「一般上傳中斷」。
- 單一 `writeBatch`（原子批次寫入）仍受 Firebase Firestore（Firebase 雲端文件資料庫）請求容量限制；目前尚未在上傳前主動估算容量，過大的模板可能整批失敗，但不等同於新舊資料混合。
- 模板檔目前只有長度檢查，尚未使用完整內容雜湊驗證。
- 本機服務目前允許所有來源呼叫，也沒有請求容量限制；正式使用前應限制正式網站與本機測試網址。
- 刪除雲端模板目前被綁定為必須先啟動本機 PDF（可攜式文件）工具，兩個操作應解除依賴。
- 擁有裁帶功能權限的課長與文員目前都能匯入、覆蓋及刪除模板，正式上線前要確認是否符合管理需求。
- 專案內沒有 Firebase Firestore（Firebase 雲端文件資料庫）安全規則檔，無法從程式庫確認正式資料權限。
- 頁面說明的啟動檔名稱與實際 `啟動PDF工具.bat`（PDF 工具啟動批次檔）名稱不同。

### 已確認可清除的多餘程式

- 網頁傳送但本機工具未使用的 `orderCells`（訂單儲存格資料）。
- 未被呼叫的單行文字繪圖函式。
- 未被呼叫的報告格線繪圖函式。
- 沒有作用的單行顯示參數。
- 已不再回傳但仍保存在記憶體中的舊計時紀錄集合。
- 網頁資料列內未被使用的偵測方式與信心分數欄位。

合併儲存格衝突已完成修正；其餘項目目前只完成檢查與紀錄，尚未修改程式。

## 十六、正式上線前檢查

- 驗證 B～K 欄合併與未合併的模板，並確認合併 H 欄不重複計算。
- 驗證 1、5、6 個以上款號的組別高度。
- 驗證不同工作表表頭、不同圖片及相同圖片共用。
- 驗證同一組每件條數不同的提醒。
- 驗證錯誤模板確實禁止匯入，且錯誤位置明確。
- 驗證一般大小模板在單一 `writeBatch`（原子批次寫入）提交失敗時仍保留上一個完整版本。
- 壓力測試超大模板、多使用者同時覆蓋及舊分段清理失敗的情況。
- 驗證正式網站可由實際使用的 Chrome（谷歌瀏覽器）及 Edge（微軟瀏覽器）連接本機工具。
- 驗證第一次轉檔與使用快取後的第二次轉檔速度。
- 確認 Firebase Firestore（Firebase 雲端文件資料庫）正式安全規則及模板備份方式。
- 確認哪些角色可以匯入、覆蓋與刪除模板。

## 十七、曾發生問題與不得再犯

- PDF（可攜式文件）曾因內部物件順序及 `xref`（交叉參照表）錯誤而整頁空白；修改 PDF 封裝時必須驗證每個物件編號。
- 圖片曾因 `CopyPicture`（複製圖片）與 `Chart.Export`（圖表匯出）只產生空白小圖；圖片必須優先讀取原始內嵌檔案。
- 不同工作表曾錯用第一頁表頭；版面模組可以共用，但組別表頭不得共用。
- 不同原始組別曾因內容相似而被合併；組別必須以原始表頭位置為準。
- 款號曾受格式限制而無法識別；固定 B 欄內的完整非空白內容就是款號。
- 合併儲存格曾被當成空白；網頁分析與本機轉檔必須使用一致的合併解析方式。
- 任何速度優化都不得以破壞模板原始組別、欄寬、表頭、合併狀態、圖片或文字為代價。

## 十八、2026 年 7 月 30 日目前進度

### 本次已完成

- 網頁模板分析已解析 B、G、H、I 欄的合併儲存格，合併範圍內一律使用左上角來源內容與來源位置。
- 同一款號重複落在相同合併 H 欄時，每件條數只計算一次。
- 合併 G、I 欄只寫入左上角來源格，不會因合併範圍內的空白格重複寫入。
- 不同款號或不同欄位共用同一個合併寫入格時，會顯示工作表、合併範圍及款號並禁止錯誤輸出。
- 本機 PDF（可攜式文件）工具已同步解析合併表頭、合併資料列及缺少實體 XML（可延伸標記語言）列的情況。
- 索引版本已升為第 15 版，修正前的本機索引會自動失效並重建。
- 修正前建立的網頁模板分析紀錄會顯示「Cần nhập lại / 需要重新匯入」，避免舊分析資料繼續參與訂單比對。
- 已完成 `js/cutting.js`（裁帶統計程式）語法檢查、兩份本機工具語法檢查、合併數量回歸測試及共用合併位置攔截測試。
- 專案路徑與已獲使用者同意同步的 OneDrive（雲端同步資料夾）工具內容雜湊完全相同。

### 上線後要做

- 重新啟動本機 PDF（可攜式文件）工具，讓索引版本 15 的程式生效。
- 以相同檔名重新匯入修正前建立的模板一次。
- 使用正式模板驗證 1、5、6 個以上款號的組高、圖片、文字大小及同組不跨頁。
- 觀察實際上傳容量、多人同時覆蓋及舊分段清理情況，再決定是否追加容量檢查與完整內容雜湊。

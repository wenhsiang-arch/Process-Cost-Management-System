# 裁帶 PDF 轉換工具固定規則

此文件記錄裁帶 PDF 轉換工具的核心規則。後續任何修改，只要與本文件規則衝突，都不得進行。

## 一、欄位寬度規則

- 組別內每一個欄位的寬度比例，必須依照匯入的 Excel 模板。
- 模板欄位寬度代表生產現場觀看資訊的重要程度。
- 寬度較大的欄位表示較重要，寬度較小的欄位表示較不重要。
- 程式不得自行平均分配欄位寬度。
- 程式不得用固定欄寬取代模板欄寬比例。

## 二、A4 寬度規則

- 模板本身是按照 A4 紙張寬度設計。
- PDF 輸出時，寬度必須設定為 1 頁 A4 寬。
- 如果模板原始寬度未滿 A4 寬，轉換工具必須依照每個欄位的原始比例放大到滿版。
- 如果模板原始寬度超過 A4 寬，轉換工具必須依照每個欄位的原始比例縮小到 1 頁寬。
- 放大或縮小時，只能等比例調整欄位寬度，不得改變欄位之間的重要比例。

## 三、組別高度規則

- 每一組的輸出高度必須固定為 4.95 公分。
- 4.95 公分包含表頭與內容區。
- 每張 A4 直式紙張必須以 6 組為滿版高度目標。
- 不得因為寬度縮放、模板差異、圖片大小或文字內容，自行改變每組高度。

## 四、款號列高度規則

- 訂單數量為 0 的款號必須隱藏。
- 隱藏數量 0 的款號後，只顯示有訂單數量的款號。
- 組別總高度仍固定為 4.95 公分，不得改變。
- 扣除表頭高度後，剩下的內容高度必須由有數量的款號平均分配。
- 例如原模板有 10 個款號，但訂單只有 2 個款號有數量，輸出時只顯示 2 列，且這 2 列平均分配內容區高度。

## 五、接續排版規則

- 不同模板的資料必須可以接續輸出。
- 不得因為切換模板就強制跳到新頁。
- 若前一頁仍有可放置組別的空間，下一個模板的組別必須接續排入。
- 紙張使用效率是必要條件，但不得犧牲模板欄位比例。

## 六、文字縮放規則

- 文字大小必須依照實際欄位寬度與格子高度自動調整。
- 文字不得超出格子邊界。
- 文字不得互相重疊。
- 款號、顏色、規格、數量、備註、段別等欄位，都必須依照各自格子大小調整。
- 不得只針對單一欄位名稱做特殊修補，避免其他模板跑版。

## 七、圖片規則

- 圖片必須依照圖片欄位框位自動放大或縮小。
- 圖片必須保持原比例。
- 圖片不得裁切。
- 圖片至少要有一邊盡量貼近欄位可用範圍。
- 圖片不得因縮放造成欄位高度改變。

## 八、修改限制

- 任何裁帶 PDF 排版修改前，必須先確認是否符合本文件規則。
- 若新需求與本文件衝突，必須先向使用者確認是否修改本文件規則。
- 未經使用者同意，不得修改本文件規則。

## 九、讀取速度與快取優化規則

- 裁帶 PDF 轉換時間優化非常重要。
- 實際訂單可能會有 100 個款號，因此轉換工具必須高度優化讀取速度。
- 100 個款號的 PDF 產生時間，目標最好能在 1 分鐘內完成。
- 若無法在 1 分鐘內完成，必須優先分析並減少重複讀取、重複判斷與重複呼叫 Excel（表格程式）的次數。
- 同一個模板檔內，若不同分頁使用相同表格模式，程式不得每一組都重新分析整張表。
- 同一個模板檔內，若不同分頁使用相同照片，程式不得每一組都重新尋找或重新匯出相同照片。
- 每個模板應優先只分析一次表格結構，包含分頁名稱、欄位範圍、組別列範圍、照片位置與可輸出範圍。
- 每個分頁應優先只建立一次組別位置表，後續應用訂單款號所在列直接判斷所屬組別。
- 前端若已傳入有訂單數量的儲存格位置，後台應優先使用這些位置判斷要保留的款號列，不應再逐格掃描整張表。
- 速度優化不得破壞既有排版規則，包含欄位寬度比例、A4（紙張）滿版、每組 4.95 公分、每頁 6 組、圖片不裁切與文字不跑版。
- 速度優化不得改成 Excel（表格程式）常駐，也不得在程式內 taskkill（強制關閉程序）全部 Excel（表格程式），除非使用者明確同意。

## 十、模板測試資料夾分析紀錄

- 已分析工作路徑內的 `裁切測試`（裁切測試資料夾）。
- 測試資料夾內有 4 個 `.xlsx`（Excel 表格檔）模板。
- 分析結果顯示，同一個模板檔內，每個分頁的照片高度重複，多數情況是同一張照片重複使用。
- 多數模板分頁寬度固定在 A:Q（A 欄到 Q 欄），少數分頁為 A:P（A 欄到 P 欄）。
- 同一模板內，各分頁的表格列位置具有固定規律。
- 因此後續優化應以「模板層級快取」與「分頁層級組別位置表」為主，不應每個款號或每一組都重新讀取整份 Excel（表格檔）。
- 建議後台流程：
  - 每個模板只分析一次表格結構。
  - 每個分頁只建立一次組別位置表。
  - 每組只做必要動作：套用數量、隱藏 0 數量列、輸出原模板版面。
  - 相同照片只確認一次位置，不重複尋找。
  - 優先使用前端傳入的訂單儲存格位置，減少後台逐格掃描。

## 十一、模板 / 款號 / 圖片索引資料庫方向

- 長期正確方向應改為「第一次建立索引，日常依索引產生 PDF」。
- 不應長期依賴每次開啟 Excel（表格程式）、掃描模板、逐組截圖、再貼到新 Excel（表格程式）輸出 PDF。
- 第一次匯入或更新模板時，工具應分析 Excel（表格檔）並建立索引資料。
- 索引資料應包含：
  - 款號對應的模板檔。
  - 款號對應的分頁。
  - 款號對應的組別。
  - 款號所在列。
  - 款號欄、顏色欄、數量欄、件數欄、備註欄等欄位位置。
  - 組別的起始列與結束列。
  - 欄位寬度比例。
  - 列高與合併格位置。
  - 圖片框位位置與大小。
  - 圖片檔案索引。
  - 圖片與款號、組別或模板的對應關係。
- 多個款號若共用同一張圖片，圖片只能儲存一次，後續以圖片索引重複使用。
- 日常匯入訂單時，工具應依款號查詢索引，直接取得模板、分頁、組別、列位置與圖片，不應重新掃描整份 Excel（表格檔）。
- 產生 PDF 時，應優先使用索引資料直接排版，減少或避免 Excel（表格程式）即時參與。
- 此索引方向仍必須保留所有固定排版規則，不得因加速而犧牲版面。

## 十二、索引模式下的自動排版規則

- 索引模式仍必須保留文字自動調整大小。
- 文字大小必須依照格子寬度與高度計算，避免切字、溢出或重疊。
- 款號、顏色、規格、數量、備註、段別等欄位，都必須依照各自格子大小自動調整。
- 索引模式仍必須保留圖片自動調整。
- 圖片必須依照圖片框位自動放大或縮小。
- 圖片必須保持原比例。
- 圖片不得裁切。
- 圖片不得拉伸變形。
- 圖片至少要有一邊盡量貼近框位可用範圍。
- 欄位寬度仍必須依照 Excel（表格檔）模板原始欄寬比例。
- 每組高度仍必須固定為 4.95 公分。
- 每張 A4（紙張）仍必須以 6 組為滿版高度目標。
- 隱藏 0 數量款號後，組別總高度仍不得改變，剩下的內容列必須平均分配高度。
- 索引模式的目標是提高速度與穩定性，不是改變輸出格式。

## 十三、暫存檔與殘留資料清除規則

- 每次轉換 PDF 完成後，不論成功或失敗，都必須清除本次轉換產生的暫存資料。
- 暫存資料包含臨時 Excel（表格檔）、臨時圖片、臨時 PDF、臨時資料夾與本次轉換中間檔。
- 清除暫存資料時，只能清除本工具建立的專用暫存資料夾，不得清除使用者其他檔案。
- 本機後台啟動時，必須自動檢查舊的工具暫存資料。
- 舊暫存資料若超過 1 天，啟動時應自動清除。
- 清除舊暫存資料不得使用 taskkill（強制關閉程序）全部 Excel（表格程式）。
- 清除舊暫存資料不得影響索引資料庫，除非該索引明確判定為失效或使用者要求重建。

## 十四、索引模式下的模組與圖片共用規則

- 索引模式下，相同模組只應建立一份模組資料。
- 多個款號若使用相同模組，應透過款號對模組的對應表共用同一份模組資料。
- 索引模式下，相同圖片只應儲存一份圖片檔。
- 多個款號、分頁或組別若使用同一張圖片，應透過圖片索引共用同一份圖片檔。
- 不得因為多個款號共用同一張圖片，就在索引資料庫中重複儲存多份相同圖片。
- 圖片是否相同，應優先用圖片內容雜湊值判斷，而不是只看檔名。
- 模組是否相同，應依欄位結構、欄寬比例、列高、合併格、表頭位置、圖片框位等版面資料判斷。
- 款號索引應只記錄它對應哪個模組、哪個圖片、哪個分頁、哪個組別與哪一列。
- 產生 PDF 時，應依款號查索引，再共用對應模組與圖片，不應為每個款號重建模組或重複載入圖片。

## 十五、分頁組別索引建立規則

- 每個 Excel（表格檔）內，同一個分頁的組別格式預設視為相同。
- 每個 Excel（表格檔）內，不同分頁的組別格式也可能相同。
- 建立索引時，應優先只檢測每個分頁的第一個組別，用來建立該分頁的模組資料。
- 若後續分頁被判定與既有模組相同，應共用既有模組，不應重新建立一份模組。
- 不應為同一分頁內的每個組別重複讀取欄寬、列高、合併格、表頭、圖片框位等版面資料。
- 不同組別的版面格式可以相同，但數值資料不同，因此每個組別仍需記錄自己的款號列、數量列、分頁、組別位置與資料對應。
- 索引建立時，版面資料與數值資料應分開：
  - 版面資料：由第一個組別建立模組索引。
  - 數值資料：每個組別只記錄不同的款號、數量、件數、總數與所在列。
- 這樣可以避免整份 Excel（表格檔）重複掃描，提高 100 個款號以上訂單的產生速度。

## 十六、SL 與總數欄位規則

- SL（訂單數量欄）預設必須清 0。
- 只有訂單有數量的款號，才把訂單數量寫入 SL（訂單數量欄）。
- SL（訂單數量欄）的數量等於訂單匯入的數量。
- `SL:PO PCS`（訂單數量欄）代表訂單匯入的數量。
- `SỐ KIỆN`（件數欄 / 該工序裁幾段）代表該款號在該工序每件需要裁幾段。
- `SL:CẮT THỰC TẾ`（實際裁出數量欄）代表實際需要裁出的總數。
- `SL:CẮT THỰC TẾ`（實際裁出數量欄）應由 `SL:PO PCS`（訂單數量欄）帶入後，依 `SỐ KIỆN`（件數欄 / 該工序裁幾段）自動計算。
- 一般計算關係為：`SL:CẮT THỰC TẾ`（實際裁出數量欄） = `SL:PO PCS`（訂單數量欄） × `SỐ KIỆN`（件數欄 / 該工序裁幾段）。
- 若模板內有公式欄位，應優先保留公式邏輯，不得用錯誤固定值覆蓋公式。
- 若前端已經負責計算並傳入 `SL:CẮT THỰC TẾ`（實際裁出數量欄），轉換工具不得重複計算造成不一致。
- 若模板本身已有公式可自動計算 `SL:CẮT THỰC TẾ`（實際裁出數量欄），轉換工具應只寫入 `SL:PO PCS`（訂單數量欄），並保留公式。
- 隱藏 0 數量款號時，應以 SL（訂單數量欄）是否大於 0 作為主要判斷。

## 十七、範圍款號與總計列規則

- 範圍款號是指模板顯示一個文字，但實際代表多個款號，例如 `HAC0101~05`（範圍款號文字）代表 `HAC0101`（款號）、`HAC0102`（款號）、`HAC0103`（款號）、`HAC0104`（款號）、`HAC0105`（款號）。
- 索引建立時，工具內部必須把範圍款號展開成可查詢的款號清單，用來匹配訂單款號。
- PDF（可攜式文件）輸出時，款號顯示文字必須維持 Excel（表格檔）模板原樣，例如仍顯示 `HAC0101~05`（範圍款號文字），不得強制拆成多列。
- 多個訂單款號若對應同一個範圍款號列，`SL:PO PCS`（訂單數量欄）必須加總後填入同一列。
- 多個訂單款號若對應同一個範圍款號列，圖片、模組、欄位、列高與版面都應共用，不得重複建立。
- `TỔNG CỘNG`（總計列）是否顯示，不得只看模板原本有沒有總計列，必須依隱藏 0 數量款號後的實際顯示列數判斷。
- 隱藏 0 數量款號後，如果同一組只剩 1 列有效款號，PDF（可攜式文件）不顯示 `TỔNG CỘNG`（總計列）。
- 隱藏 0 數量款號後，如果同一組有 2 列以上有效款號，PDF（可攜式文件）才顯示 `TỔNG CỘNG`（總計列），總計值等於所有有效列的 `SL:CẮT THỰC TẾ`（實際裁出數量欄）加總。
- `TỔNG CỘNG`（總計列）不得改變每組 4.95 公分固定高度；有總計列時，總計列與有效款號列一起平均分配內容區高度。
- 第一版索引不得假設每個分頁永遠只有「有總計」或「無總計」一種狀態，因為總計列是否顯示取決於訂單實際留下幾列有效款號。

## 十八、最近問題紀錄與不得再犯規則

- 最近重做 PDF（可攜式文件）工具時，曾出現「PDF 可以產生但整頁空白」問題；原因是 PDF（可攜式文件）內部物件索引順序錯誤，導致已畫好的頁面圖片沒有正確顯示。後續修改 PDF（可攜式文件）封裝時，必須確認每個物件編號與 `xref`（交叉參照表）位置一一對應。
- 最近曾出現快取圖片是空白小圖的問題；原因是使用 Excel（表格程式）的 `CopyPicture`（複製圖片）加 `Chart.Export`（圖表匯出）擷取圖片不可靠，可能只輸出 200-300 bytes（位元組）的空白圖或邊框圖。後續圖片索引應優先從 `.xlsx`（表格檔）內部的 `xl/media`（內部圖片資料夾）讀取原始圖片，不應優先依賴截圖。
- 最近曾出現「所有模板排版都變成同一種格式」問題；原因是工具用固定欄位順序重畫表格，沒有完整保留 Excel（表格檔）模板原本的欄位順序、欄寬比例與欄位存在狀態。後續索引模式不得重新排列欄位，必須依原模板欄位順序輸出。
- 模板欄位可能不同，例如有些模板是「圖片、規格、款號、顏色、裁切規格、數量、件數、總數、備註」，有些模板是「圖片、款號、數量、工段、規格、裁切規格、件數、總數、備註」。工具不得假設所有模板欄位順序相同。
- 模板若有未被工具識別成標準欄位的欄位，也不得直接丟棄；索引應保留原始欄位位置、欄寬與該列固定文字，避免 PDF（可攜式文件）少欄位或欄位空白。
- 圖片欄上方可能有 `ĐOẠN A`（段 A）、`ĐOẠN B`（段 B）等段別格；這些格子不是圖片本身，但屬於圖片欄的一部分，PDF（可攜式文件）輸出必須呈現，不得只顯示圖片。
- 索引資料應分開保存「版面資料」與「資料值」：版面資料包含欄位順序、欄寬、表頭、圖片框位；資料值包含款號、數量、件數、總數、顏色、規格、工段、備註與其他固定格內容。
- 快取索引版本改變時，必須讓舊索引自動失效並重建，避免沿用舊版空白圖片、錯誤欄位順序或錯誤 PDF（可攜式文件）排版。
- 若 PDF（可攜式文件）內容異常，排查順序應先看：
  1. `index.json`（索引資料）是否有 groups（組別）、items（款號列）、columns（欄位）、images（圖片）。
  2. `images`（圖片資料夾）內圖片檔大小是否合理；若只有數百 bytes（位元組），通常是空白快取。
  3. 暫存頁面圖是否有畫面；若暫存圖有內容但 PDF（可攜式文件）空白，優先查 PDF（可攜式文件）封裝。
  4. 若 PDF（可攜式文件）有內容但版面錯，優先查欄位順序、欄寬比例與固定文字是否被索引保留。
- 任何加速優化都不得犧牲「模板原樣排版」；速度優化只能減少重複讀取，不得把不同模板強制統一成同一種畫面。
# 最新補充：PDF 版面輸出規則

## 字體顯示規則
- PDF 輸出時，文字不能用 `...` 省略呈現。
- 欄位文字必須依照格子大小自動縮放，盡量完整顯示。
- 適用欄位包含規格、款號、顏色、裁切規格、備註與其他模板欄位。
- 若文字太長，優先縮小字體，不得直接截斷文字。
- 第一層級：原 Excel 儲存格有分行時，PDF 必須依照原本分行展示。
- 原分行後，每一行仍必須以單行縮放呈現，不得再被自動折成第三行或更多行。
- 第二層級：原 Excel 儲存格沒有分行時，只要文字中出現 `MM` 且 `MM` 後面還有內容，PDF 必須在第一個 `MM` 後分行。
- 第二層級分出的每一行也必須以單行縮放呈現。
- 原 Excel 儲存格沒有分行，且文字到 `MM` 就結束時，PDF 必須以單行展示。
- 工具不得依照文字意思或關鍵字自行拆行，例如不得猜測 `MÀU / 顏色` 欄的主色或輔助說明。

## 原模板合併儲存格規則
- PDF 輸出必須依照原 Excel 模板的合併狀態。
- 原 Excel 合併儲存格，PDF 也要合併展示。
- 原 Excel 分開儲存格，PDF 也要分開展示。
- 不可以只用「文字是否相同」判斷是否合併。
- `MÀU / 顏色`、`GHI CHÚ / 備註`、規格欄都要依照原模板合併狀態輸出。
- 若同一欄原本是合併展示，例如整組共用同一個顏色或備註，PDF 必須整組合併顯示。
- 若原模板該欄是每個款號分開填寫，PDF 必須逐列分開顯示。

## 總數列合併規則
- `TỔNG CỘNG / 總數` 列可以做合併展示。
- 合併範圍不得碰到圖片欄。
- 總數文字應放在圖片欄右側到總數欄左側之間。
- 總數數字欄仍要保留原本的數字欄位置與底色。

## 影響範圍與風險
- 本段規則只影響 PDF 輸出版面規則。
- 不改前端、不改 Firebase、不改訂單資料格式。
- 修改說明文件本身屬低風險。

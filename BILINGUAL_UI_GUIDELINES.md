# 中越雙語介面排版規範

## 目的

統一系統內越文與中文的顯示方式，提升閱讀性並節省畫面空間。

基本原則：

- 越文優先顯示。
- 中文作為輔助說明。
- 不使用「中文在左、越文在右」的排列方式。

## 適用範圍

本規範適用於後續所有新增或修改的使用者介面。

新增或調整頁面、彈窗、按鈕、欄位、表格、提示、警告及錯誤訊息時，必須依照本規範選擇對應的中越雙語排版方式。

修改既有介面時，只調整本次修改涉及的文字；未經確認，不進行全系統文字排版重構。

## 1. 表格表頭

適用於表格欄位名稱。為了節省橫向空間，採用：

- 上方顯示越文。
- 下方顯示中文。
- 中文字體可以稍小、顏色稍淡。

範例：

| 越文 | 中文 |
|---|---|
| Khách hàng | 客人 |
| Số đơn hàng | 訂單號碼 |
| Số lượng | 數量 |
| Tiến độ | 生產進度% |
| Hoàn thành | 實際完成日 |

HTML 格式：

```html
<th>
  Số đơn hàng
  <br>
  <span class="label-zh">訂單號碼</span>
</th>
```

## 2. 功能名稱與短欄位名稱

適用於功能按鈕、頁面標題、選擇欄位及簡短狀態名稱。

當顯示位置足夠寬時，採用：

- 左側顯示越文。
- 右側顯示中文。
- 中間使用 `/` 分隔。

範例：

```text
Tiến độ đơn hàng / 訂單進度
Quản lý đơn hàng / 訂單管理
Nhập đơn hàng Excel / 匯入訂單 Excel
Chọn đơn hàng / 選擇訂單
Số lượng báo công / 報工數量
```

若文字放入表格表頭後會增加欄位寬度，應改用「上方越文、下方中文」。

## 3. 說明文字、警告與完整敘述

適用於操作說明、匯入進度、警告訊息、錯誤訊息及確認內容。

採用：

- 上方顯示完整越文段落。
- 下方顯示完整中文段落。
- 不要每一句越文後立即使用 `/` 接中文。
- 越文與中文之間保留適當間距。

範例：

```text
Đơn hàng có dữ liệu lớn, hệ thống sẽ tự động chia thành 3 đợt nhập.
Đang nhập đợt 1/3, vui lòng không đóng trang.

訂單資料較大，系統將自動拆分為 3 個批次匯入。
正在匯入第 1/3 批，請勿關閉頁面。
```

警告區塊範例：

```text
⚠️ Lưu ý

- Chỉ cập nhật đơn hàng đã chọn, không ảnh hưởng đơn hàng khác.
- Không đóng trang trong khi hệ thống đang xử lý.

⚠️ 注意

- 只更新所選訂單，不影響其他訂單。
- 系統處理期間請勿關閉頁面。
```

## 快速判斷規則

| 內容類型 | 排版方式 |
|---|---|
| 表格表頭 | 上方越文、下方中文 |
| 功能名稱、按鈕、簡短欄位 | 左側越文、右側中文 |
| 警告、說明、進度、錯誤訊息 | 整段越文在上、整段中文在下 |
| 空間不足的短欄位 | 改為上方越文、下方中文 |

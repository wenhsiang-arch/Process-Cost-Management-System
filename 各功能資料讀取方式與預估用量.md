# 各功能資料讀取方式與預估用量

最後更新：2026 年 8 月 6 日

## 一、文件用途

- 本文件記錄每個大型功能目前真正採用的登入、開頁、主頁、子頁、搜尋、分頁、匯入、匯出、版本、快取及預估讀取量。
- 預估值只計算程式直接取得的 Firebase Firestore（Firebase 雲端文件資料庫）文件；安全規則相依讀取、重試及正式計費以 Firebase（雲端服務）用量畫面為準。
- 畫面顯示 20 筆不代表雲端只讀 20 筆；必須分清楚「雲端分頁」與「完整下載後在本機分頁」。
- 本文件以簡單方式協助討論。正式共用規則仍以 `系統架構規劃.md（全系統架構規格）`為準。

## 二、全系統登入

登入成功後固定先顯示輕量功能首頁，不自動打開款號、訂單或其他業務功能。

| 步驟 | 直接讀取內容 | 預估文件讀取 |
|---|---|---:|
| 確認帳號 | `userAccess（使用者存取權限）` | 已綁定 UID（使用者識別碼）通常 1；舊電子信箱尚未綁定最多 2 |
| 載入角色權限 | 一般角色只讀自己的 `rolePermissions（角色功能權限）` | 1 |
| 管理員載入權限管理基底 | 課長、文員、開發、生管、業務共 5 份角色權限 | 5 |
| 初始化 IndexedDB（瀏覽器本機資料庫） | 整理本機快取及清除已淘汰快取 | 0 |
| 顯示功能首頁 | 不讀取大型業務資料 | 0 |

因此，一般角色登入的程式直接讀取約 2～3 份，管理員約 6～7 份；安全規則可能增加少量相依文件讀取。使用者實際點開功能後，才加入以下各功能讀取。

## 三、款號表

### 3.1 現行結構

1. `products（款號資料集合）`每款一份文件，該款全部工序放在同一份文件。
2. `system/productsMeta（款號版本資料）`保存版本、資料結構版本、款號數、工序數及變更序號。
3. `productChanges（款號增量變更）`每次匯入或刪除建立一份小型紀錄，只保存變更序號及受影響款號。
4. 完整款號依網站來源、UID（使用者識別碼）及版本保存於 IndexedDB（瀏覽器本機資料庫）；不同帳號不共用。
5. 畫面仍維持完整本機搜尋、排序、客人篩選、每頁 20 款及展開工序，使用體驗不變。

### 3.2 點開款號表時

| 情況 | 實際處理 | 款號相關直接讀取 |
|---|---|---:|
| 同一頁面 15 秒內重複要求 | 使用記憶體中的版本及款號 | 通常 0 |
| IndexedDB（瀏覽器本機資料庫）版本相同 | 讀版本，完整款號由本機取得 | 約 1 |
| 版本改變且增量紀錄連續 | 讀版本、變更紀錄及真正改變的款號 | 約 `1 + L + C` |
| 沒有快取、快取損壞或增量紀錄缺漏 | 讀版本、全部款號及完成後版本重驗 | 約 `N + 2` |
| 讀取期間另一台電腦更新 | 自動再讀一次完整款號並重驗版本 | 最壞約 `2N + 3～4` |

- `N（款號數）`：全部款號文件數。
- `L（變更紀錄數）`：本機版本到最新版本之間的增量紀錄數。
- `C（實際變更款號數）`：新增或修改後需要補讀的款號數；刪除款號不需要再讀原文件。
- 增量序號不連續、資料數量不符或紀錄超過安全範圍時，自動放棄增量並完整重讀，避免顯示半份資料。

### 3.3 主頁與操作

| 使用者動作 | 雲端讀取 |
|---|---:|
| 顯示第一頁 20 款、翻頁、搜尋、篩選、排序 | 0，使用已驗證的本機資料 |
| 展開某款工序 | 0，工序已包含在該款資料 |
| 切換越盾、美元、台幣 | 0，本機重新計算 |
| 最新操作紀錄 | 款號頁目前載入最新 50 筆以內的授權操作紀錄；依實際筆數計算 |
| 匯出、備份、預覽 Excel（表格檔） | 款號版本不變時 0，使用本機資料 |
| 確認匯入 | 交易內重驗 1 份版本；最多寫入 498 款、1 份增量紀錄、1 份新版本 |
| 刪除一款 | 交易內重驗 1 份版本；刪除 1 款並寫入增量紀錄及新版本 |
| 強制重新整理 | 約 `N + 2` |

款號寫入採用 Transaction（雲端交易）：款號、增量紀錄及版本必須一起成功；另一台電腦已更新時會拒絕舊版本寫入，不會互相覆蓋。

### 3.4 讀取量比較範例

以畫面曾顯示約 1,150 款、另一台電腦只更新 10 款且產生 1 份變更紀錄為例：

| 狀況 | 舊方式 | 新方式 |
|---|---:|---:|
| 版本改變後開頁 | 約 1,151 | 約 12（1 版本＋1 紀錄＋10 款） |
| 減少比例 | — | 約 98.96% |
| 第一次使用或快取遺失 | 約 1,151 | 約 1,152；多 1 次完成後版本重驗以保護資料一致 |

最近一次實際載入方式與文件數會保存於 `lastProductReadMetrics（最近一次款號讀取量）`，供開發驗收使用，不保存正式資料內容。

### 3.5 仍需注意

- 目前每頁 20 款是本機分頁，不是雲端分頁；第一次使用仍需完整下載。
- 當款號接近數萬筆、單次完整下載可能接近免費額度前，必須再改成依款號／客人分區或雲端搜尋與游標分頁。
- 目前方案優先保留使用者快速搜尋、任意排序與匯出的體驗，同時把日常「少量變更後整包重讀」改成增量補讀。

## 四、訂單與訂單工序

### 4.1 現行結構

- `orders（訂單資料）`保存訂單摘要、款號數、總數量、工序數、款號清單及單張訂單工序版本。
- `orderProcesses（訂單工序）`每道訂單工序一份文件。
- 訂單摘要仍使用 UID（使用者識別碼）隔離的 IndexedDB（瀏覽器本機資料庫）整體快取。
- 訂單工序改為「每張訂單一份獨立快取」，一張訂單變動不會讓其他訂單工序快取失效。

### 4.2 登入、開頁與展開

| 使用者動作 | 舊方式 | 現行方式 |
|---|---|---|
| 登入停在功能首頁 | 不固定 | 不讀訂單及工序 |
| 第一次點開訂單頁 | 全部訂單 `O`＋全部工序 `P` | 全部訂單 `O`，工序 0 |
| 訂單版本相同再次開頁 | 使用整體快取 | 讀小型版本後使用訂單快取，工序 0 |
| 顯示進度主表 | 依全部已下載工序計數 | 使用訂單摘要的 `processCount（工序數）` |
| 選擇或展開一張訂單 | 工序已整包下載 | 只查詢該訂單的 `Pᵢ（該單工序數）`；有效快取時 0 |
| 搜尋款號 | 在全部工序本機搜尋 | 新訂單先用摘要款號清單；只對符合或缺少舊摘要的訂單補讀工序 |
| 調整數量或同步秒數 | 完成後可能重讀全部工序 | 只更新並重新讀取受影響訂單的工序 |
| 匯入新訂單 | 完成後重讀全部工序 | 只建立並快取新訂單的工序 |

- `O（訂單文件數）`：目前訂單摘要數。
- `P（全部工序文件數）`：所有訂單工序總數。
- `Pᵢ（單張訂單工序數）`：目前點開訂單的工序數。

例如 100 張訂單、每張 30 道工序，舊方式第一次開頁約讀 3,100 份；新方式第一次只讀約 100 份訂單摘要，使用者再展開一張時補讀約 30 份，開頁階段約減少 96.8%。

最近一次訂單摘要來源、訂單文件數、工序查詢數、工序文件數及快取命中次數，會保存於 `lastOrderReadMetrics（最近一次訂單讀取量）`供開發驗收。

### 4.3 仍需注意

- 訂單摘要在快取失效時仍是完整集合讀取；它比訂單工序小很多，但長期累積到數萬張時仍需改成日期範圍、狀態及游標分頁。
- 舊訂單若沒有 `processCount（工序數）`或款號清單，主表先顯示「—」，第一次展開或搜尋後會在本機補齊；不會為了補舊欄位在開頁時重讀全部工序。
- 工序秒數同步仍會在真正確認同步時查詢該張訂單最新工序，這是避免用舊快取覆蓋正式資料的必要讀取。

## 五、操作歷史

- 款號匯入、款號備份匯出、產品工價匯出、訂單匯入、成本設定、裁帶模板匯入及裁帶匯出，都改為 `operationLogs（獨立操作紀錄）`每次操作一份文件。
- 一般畫面只查詢目前功能權限允許的最新 50 筆，不再讀寫一份持續變大的大型 JSON（資料交換格式）。
- 建立後一般使用者與管理員都不能透過網站修改或刪除；清理需未來另建受控程序。
- 舊 `system/impHist（舊匯入紀錄）`與 `system/cLog（舊成本紀錄）`保留管理員唯讀，禁止再寫入。
- `operationLogs（獨立操作紀錄）`查詢所需的 `permissionKey（功能權限代碼）`升冪加 `createdAt（建立時間）`降冪複合索引已在正式 Firebase（雲端資料庫）啟用。

### 5.1 已淘汰集合的讀取現況

- 2026 年 8 月 6 日已從正式 Firebase（雲端資料庫）實體清除 `attendance（舊出勤集合）`、`employees（舊員工集合）`及 `reports（舊報工集合）`，共 166 筆舊流程測試文件；重新載入後三個集合已不再顯示。
- 一般使用者登入、開啟首頁、款號、訂單、成本或裁帶時，本來就不會查詢上述三個舊集合；清除後其正常操作預估讀取量仍為 0，主要改善是減少測試資料與管理混亂。
- `cuttingTemplateChunks（裁帶模板分段）`、`cuttingTemplates（裁帶模板）`、`orderLocks（訂單鎖定）`、`orderProcesses（訂單工序）`、`orders（訂單）`及 `products（款號）`仍是現行功能資料，不得因名稱較舊而清除。

## 六、BOM（物料清單）

目前尚未開發。開發時需分開記錄款號搜尋、單張 BOM（物料清單）、完整匯入、版本、快取及預料調用，不得在進頁時完整讀取全部 BOM（物料清單）。

## 七、庫存

目前尚未開發。必須分開「目前庫存摘要」與「入庫／出庫歷史」；主頁只讀摘要或選定材料，不得為了顯示庫存總數掃描全部歷史。

## 八、產量與工時登記

舊員工報工、班長審批、員工登入及獨立考勤已淘汰。新功能尚未開發，未來由文員逐筆或 Excel（表格檔）匯入，並依日期、員工、訂單及狀態查詢，不得進頁讀取全部歷史。

## 九、其他大型功能固定記錄格式

每個新大型功能必須記錄：

1. 雲端集合、摘要、版本及快取範圍。
2. 登入、開主頁、搜尋、分頁、子頁、匯入、匯出及刪除的讀寫量。
3. 一般使用、快取命中、版本改變、第一次使用及最壞情況。
4. 是否即時監聽，以及離開功能後如何停止。
5. 實際測量欄位、優化前後比較及仍需關注的成長門檻。

## 十、官方計算參考

- [Firebase（雲端服務）官方讀取與免費額度說明](https://firebase.google.com/docs/firestore/pricing)
- [Firebase（雲端服務）官方游標分頁說明](https://firebase.google.com/docs/firestore/query-data/query-cursors)

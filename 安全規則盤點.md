# 系統安全規則盤點

> **目前版本：2026 年 8 月 6 日更新。** 本頁上半部是目前有效狀態；文件下方保留 2026 年 8 月 4 日身分驗證改造前的歷史盤點，只供追溯，不代表現在仍存在相同問題。

## 目前結論

- 核心安全改造已完成，系統不再使用舊帳號密碼作為登入入口。
- 已啟用 Firebase Authentication（Firebase 身分驗證）與 Google（Google 帳號）登入，並要求已驗證的 Google 電子信箱。
- 未經管理員預先核准的帳號無法使用系統；帳號由 `userAccess（使用者存取權限）`管理，課長、文員、開發、生管與業務的功能權限由 `rolePermissions（角色功能權限）`管理。
- Firestore Rules（雲端資料庫安全規則）已改為預設全部拒絕，只有明確列出的路徑、角色及功能可以存取。
- 班長與員工手機端已確定淘汰，目前維持全部拒絕，不列為重新開放項目。
- 目前程式來源的剩餘風險評估為「中等」：核心登入、規則測試、文字安全、本機工具限制、新版規則、操作紀錄索引及舊報工資料清理已有保護；但正式環境尚未完成未登入外部驗收、舊 `system（系統設定）`文件確認、App Check（應用程式驗證）與安全標頭，因此不能標記為全部安全工作完成。

## 已完成的核心安全項目

- Google（Google 帳號）登入已啟用，系統會先確認既有登入狀態，再決定是否開放登入按鈕。
- 管理員可預先核准 Google 電子信箱；使用者首次登入後自動綁定 UID（使用者識別碼）。
- 管理員、課長、文員、開發、生管及業務必須具有有效且啟用的 `userAccess（使用者存取權限）`文件才能進入桌機系統。
- 管理員可管理全部核准功能；課長、文員、開發、生管及業務依 `rolePermissions（角色功能權限）`決定可使用功能。
- Firestore Rules（雲端資料庫安全規則）已保存於專案的 `firestore.rules（雲端資料庫安全規則檔）`，並已由管理員手動發布。
- `system/accounts（舊系統帳號資料）`與 `system/permissions（舊字串權限資料）`已禁止網站讀取及寫入。
- 未列出的資料路徑與未知角色全部拒絕，管理員也不能自動存取未明確核准的路徑。
- `system/dataVersions（各功能資料版本）`已加入欄位與功能權限驗證，不可任意修改其他功能的資料版本。
- 登入後不再自動下載全部業務資料；資料依功能開啟時讀取，並使用本機快取降低重複讀取。
- Firebase Emulator Suite（Firebase 本機模擬器）安全規則自動測試已建立；目前 18 項未登入、角色、欄位、款號增量、訂單工序及操作紀錄測試通過。
- `safe-dom.js（安全文字顯示程式）`及 4 項自動測試已建立，用於雲端文字、Excel（表格檔）、搜尋高亮、行內參數及錯誤訊息。
- `operationLogs（獨立操作紀錄）`建立後禁止網站修改及刪除；讀取依對應功能權限限制。
- 本機裁帶工具已取消允許全部來源，改為正式網站與核准本機來源，並設定 200 MB（百萬位元組）單次要求上限。

## 近期發布與驗收項目

### 1. 本批規則與索引狀態

- 本批新增 `productChanges（款號增量變更）`、`operationLogs（獨立操作紀錄）`、訂單摘要欄位限制、開發角色顯示名稱修正及 `sales（業務角色）`。
- 本機測試已通過；`firestore.rules（安全規則檔）`已由管理員於 Firebase（雲端資料庫）網站手動發布，並以 `sales（業務角色）`內容確認為目前新版。
- `firestore.indexes.json（資料庫索引設定檔）`所需的複合索引已於 2026 年 8 月 6 日在正式 Firebase（雲端資料庫）啟用：`operationLogs（獨立操作紀錄）`的 `permissionKey（功能權限代碼）`升冪加 `createdAt（建立時間）`降冪，索引識別碼為 `CICAgJim14AK（操作紀錄索引識別碼）`。

### 2. 未登入外部驗收

- 使用完全未登入的外部請求，重新測試 `userAccess（使用者存取權限）`、`employees（員工資料）`、`orders（訂單資料）`及其他正式資料。
- 預期結果必須是拒絕，不可只用「看不到登入後畫面」代替資料庫直接驗收。
- 完成測試並保存不含正式資料內容的結果後，才可以標記完成。

### 3. 清除舊帳號文件

- `system/accounts（舊系統帳號文件）`目前已禁止網站存取，但仍需確認雲端文件本身是否存在。
- 清除前先確認現行管理員、課長、文員、開發、生管及業務均已轉換到 Google（Google 帳號）登入與 `userAccess（使用者存取權限）`。
- 確認不再需要後，備份必要的舊帳號名稱與角色，再刪除舊文件；不得備份或移轉舊密碼與密碼雜湊。
- 不得誤刪現行 `userAccess（使用者存取權限）`、`rolePermissions（角色功能權限）`或 Firebase Authentication（Firebase 身分驗證）使用者。

### 4. 盤點舊集合與舊索引

- 2026 年 8 月 6 日取得高風險刪除同意後，已實體清除 `attendance（舊出勤集合）`11 筆、`employees（舊員工集合）`19 筆及 `reports（舊報工集合）`136 筆，共 166 筆舊流程測試文件；Firebase（雲端資料庫）資料頁重新載入後三個集合均已消失。
- `system/accounts（舊系統帳號）`、`system/permissions（舊字串權限）`及 `system/employeeUserHistory（舊員工帳號歷史）`列入同一批盤點；`system/impHist（舊匯入紀錄）`、`system/cLog（舊成本紀錄）`與 `system/settings（舊合併設定）`需先確認轉換及追溯需求。
- 不得刪除整個 `system（系統設定集合）`，也不得誤刪 `cuttingTemplateChunks（裁帶模板分段）`、`cuttingTemplates（裁帶模板）`、`orderLocks（訂單鎖定）`、`orderProcesses（訂單工序）`、`orders（訂單）`、`products（款號）`、`rolePermissions（角色功能權限）`及 `userAccess（使用者存取權限）`等現行資料。
- Firebase（雲端資料庫）「資料」頁與「索引」頁已分開驗收。已刪除 `attendance（舊出勤集合）`的員工加日期索引，以及 `reports（舊報工集合）`的狀態加建立時間、員工加建立時間索引；正式索引頁目前只保留已啟用的操作紀錄索引。

## 日後強化項目

以下項目不要求現在一次完成，待正式使用量、公開流量、付費服務或相關功能擴大時再評估：

- 評估 App Check（應用程式驗證），正式強制前先觀察正常流量。
- 加入 Content Security Policy（內容安全政策）及其他瀏覽器安全標頭。
- 固定仍使用 `@latest（永遠最新版）`的圖示套件版本並加入完整性檢查；未使用的 JSZip（壓縮工具）已移除。
- 持續把舊的 `innerHTML（插入網頁內容）`改接共用安全文字工具，並為新功能加入相同測試。
- 為匯入檔案增加明確容量、資料筆數與內容格式限制。
- 建立登入失敗、權限拒絕、異常流量與雲端用量增加警示。

## 本機快取安全補充

- `IndexedDB（瀏覽器本機資料庫）`快取會保存在同一台電腦的瀏覽器設定檔中，關閉瀏覽器或重新啟動電腦後仍可能存在。
- 快取依網站來源及 UID（使用者識別碼）隔離，登入驗證、帳號啟用狀態及角色權限不得只依賴快取。
- 不得把密碼、權限文件、登入憑證、匯出檔、圖片或附件放入永久業務快取。
- 共用電腦、離職設備、送修設備或交接設備，應清除網站資料或使用系統日後提供的「清除本機快取」功能。
- 「清除本機快取」屬日後強化項目；在功能完成前，只應在受信任的公司電腦與個人瀏覽器設定檔使用系統。

## 目前驗收狀態

| 項目 | 狀態 |
|---|---|
| Google（Google 帳號）登入 | 已完成並由管理員實際登入確認 |
| 未核准帳號拒絕 | 已完成基本畫面與權限確認 |
| `userAccess（使用者存取權限）` | 已啟用 |
| `rolePermissions（角色功能權限）` | 已啟用 |
| 目前正式 `firestore.rules（雲端資料庫安全規則檔）` | 本批新版已由管理員手動發布並確認 |
| `firestore.indexes.json（資料庫索引設定檔）` | `operationLogs（獨立操作紀錄）`複合索引已啟用 |
| 未列出路徑全部拒絕 | 已寫入規則 |
| 班長與員工手機端 | 目前全部拒絕 |
| 未登入外部資料庫直接驗收 | 待處理 |
| `system/accounts（舊系統帳號文件）`實體清除 | 待處理 |
| `attendance（舊出勤）`、`employees（舊員工）`、`reports（舊報工）`實體清除 | 已完成，共刪除 166 筆測試文件並重新載入驗收 |
| 舊複合索引清理 | 已完成，刪除 3 筆舊索引，只保留操作紀錄索引 |
| 本機裁帶工具來源與容量限制 | 已完成 |
| App Check（應用程式驗證） | 日後評估 |
| 自動化安全規則測試 | 已完成基底，18 項通過 |
| 共用安全文字測試 | 已完成，4 項通過 |

## 後續開發持續規則

- 新增任何資料集合、文件、欄位、登入方式、角色、讀取、寫入、匯入、匯出或刪除功能時，必須同步設計及檢查安全規則。
- 前端畫面限制只能改善操作體驗，不得視為資料安全保護。
- 若發現功能未受安全規則保護，必須立即告知使用者資料路徑、影響範圍、風險高低與建議方案。
- 修改登入、權限、Firebase（雲端資料庫）安全規則或正式資料存取方式，屬於高風險修改，必須先取得使用者明確同意。

---

<details>
<summary>2026 年 8 月 4 日身分驗證改造前歷史盤點（點擊展開）</summary>

> 以下內容是改造前的歷史紀錄，其中「未使用 Firebase Authentication（Firebase 身分驗證）」、「未登入可讀資料」及「專案沒有安全規則檔」等描述已經過期，不可當成目前狀態。

## 一、文件目的

- 本文件記錄 2026 年 8 月 4 日針對公開網站、登入流程、Firebase（雲端資料庫）、角色權限、本機 PDF（可攜式文件）工具及瀏覽器安全所做的只讀盤點結果。
- 本次只進行讀取、比對及未登入存取測試，沒有修改系統程式、雲端資料或安全規則，也沒有進行任何寫入攻擊測試。
- 本文件不得記錄或公開正式帳號、密碼、密碼雜湊、員工個人資料及訂單內容。

## 二、整體結論

- 目前安全風險等級為「嚴重／最高優先」。
- 公開網域上的未登入外部請求，目前可以直接讀取部分正式 Firebase Firestore（Firebase 雲端文件資料庫）資料。
- 現行登入與角色權限主要在瀏覽器內判斷，不能作為雲端資料的真正安全防線。
- 系統不能只新增一份 Security Rules（安全規則）就直接上線；必須先導入可信任的身分驗證，再讓安全規則依登入身分與角色限制資料。

## 三、已確認的安全問題

### 1. 未登入者可以讀取正式資料

使用完全未登入、沒有使用者身分的外部請求進行只讀測試，結果如下：

| 測試資料 | 結果 |
|---|---|
| `system/accounts（系統帳號資料）` | HTTP（網頁傳輸協定）200，讀取成功 |
| `employees（員工資料）` | HTTP（網頁傳輸協定）200，讀取成功 |
| `orders（訂單資料）` | HTTP（網頁傳輸協定）200，讀取成功 |

本次只確認回應狀態與回應長度，沒有顯示或保存資料內容，也沒有測試新增、修改或刪除。

影響：知道 Firebase（雲端資料庫）專案識別資訊的人，可能繞過登入畫面直接讀取正式資料。

風險：嚴重。

### 2. 登入不是可信任的雲端身分驗證

- `js/auth.js（登入與權限程式）`內存在預設管理員帳號與密碼。
- 帳號密碼由瀏覽器下載後，在使用者電腦內使用 bcrypt（密碼雜湊工具）比對。
- `js/firebase.js（Firebase 雲端資料庫程式）`會在登入前讀取帳號、設定及全部員工資料。
- 即使密碼使用雜湊，只要密碼雜湊可以被未登入者下載，仍可能遭到離線猜測。
- 目前沒有使用 Firebase Authentication（Firebase 身分驗證），因此 Cloud Firestore Security Rules（雲端文件資料庫安全規則）無法取得可信任的使用者身分。

影響：無法可靠判斷真正的管理員、課長、文員、班長與員工。

風險：嚴重。

### 3. 畫面權限不能保護雲端資料

- `js/permissions.js（權限管理程式）`主要控制功能是否顯示及是否可以從畫面操作。
- 攻擊者不需要使用系統畫面，可以直接呼叫 Firebase Firestore（Firebase 雲端文件資料庫）。
- 權限設定目前以 JSON（資料交換格式）字串保存，安全規則無法直接把該字串當成可信任的細項權限使用。

影響：隱藏按鈕或封鎖頁面不能阻止直接讀寫資料庫。

風險：嚴重。

### 4. 專案缺少安全規則檔與自動測試

- 目前版本追蹤檔案中沒有 `firestore.rules（雲端資料庫安全規則檔）`。
- 沒有發現 Firebase Emulator Suite（Firebase 本機模擬器）的安全規則測試。
- 無法從專案本身確認正式環境目前部署的完整安全規則內容。

影響：規則沒有和程式一起接受檢查，容易發生新增功能後忘記保護資料的情況。

風險：高。

### 5. 本機工具允許所有網頁來源呼叫

- `local-cutting-server.ps1（本機 PDF 轉檔程式）`只綁定 `127.0.0.1（本機回送位址）`，不會直接對外網路開放，這一點正確。
- 但目前 `Access-Control-Allow-Origin（允許呼叫來源）`設定為 `*（全部來源）`。
- 任何使用者開啟的惡意網站，都可能嘗試從瀏覽器呼叫本機工具。
- 本機工具會一次讀取完整請求內容，目前沒有明確的請求容量上限。

影響：可能被惡意網站濫用本機快取、轉檔功能、記憶體及處理器資源。

風險：高。

### 6. 公開網站缺少瀏覽器安全防護

公開網站已使用 HTTPS（加密網頁連線），但檢查時沒有發現以下安全標頭：

- Content Security Policy（內容安全政策）。
- X-Frame-Options（禁止外部框架嵌入設定）。
- X-Content-Type-Options（內容類型保護設定）。
- Referrer-Policy（來源資訊傳送規則）。
- Permissions-Policy（瀏覽器功能權限規則）。

此外，系統從外部 CDN（內容傳遞網路）載入表格、壓縮、密碼及圖示套件；圖示套件使用 `@latest（永遠最新版）`，版本沒有固定。

影響：第三方套件異常或畫面資料未妥善處理時，可能擴大跨網站指令攻擊及供應鏈風險。

風險：中高。

### 7. 畫面資料插入與檔案容量需要加強

- 多個畫面使用 `innerHTML（插入網頁內容方式）`呈現雲端資料或 Excel（表格檔）內容。
- 部分位置已有跳脫處理，但尚未確認所有帳號、訂單、員工、備註及錯誤訊息都經過安全處理。
- 訂單及模板匯入主要檢查副檔名，沒有完整的檔案容量限制及內容安全限制。

影響：惡意資料可能造成跨網站指令攻擊、瀏覽器當機、雲端用量增加或轉檔工具資源耗盡。

風險：高。

## 四、目前已存在的正確保護

- 公開網站使用 HTTPS（加密網頁連線）。
- 本機 PDF（可攜式文件）工具只監聽 `127.0.0.1（本機回送位址）`。
- 新密碼已使用 bcrypt（密碼雜湊工具）保存，不再只保存明文密碼；但仍需移出可被瀏覽器直接讀取的雲端文件。
- 高風險刪除畫面已有再次輸入名稱的確認流程；但真正的刪除權限仍必須由雲端安全規則執行。
- 下載與匯出功能已要求先選擇檔名與儲存位置。

## 五、建議處理順序

### 第一階段：緊急控制

1. 先備份正式 Firebase Firestore（Firebase 雲端文件資料庫）資料及目前安全規則。
2. 決定是否能接受系統暫時停止服務；若正式資料已在使用，最安全的緊急方案是先設定全部拒絕。
3. 變更已公開或可能被猜到的預設管理員密碼。
4. 在完成正式身分驗證前，不得宣稱目前系統適合公開使用。

注意：現在直接設定全部拒絕，現有登入及資料功能會立即停止，因為目前程式尚未使用 Firebase Authentication（Firebase 身分驗證）。

### 第二階段：建立可信任登入

1. 導入 Firebase Authentication（Firebase 身分驗證）。
2. 每位使用者使用 UID（使用者識別碼）對應自己的帳號資料。
3. 密碼交由 Firebase Authentication（Firebase 身分驗證）管理，不再把密碼或密碼雜湊保存於一般資料文件。
4. 管理員、課長、文員、班長及員工角色，改由可信任的 Custom Claims（自訂權限聲明）或受保護的使用者權限文件管理。
5. 角色與啟用狀態只能由管理員或受信任的伺服器修改，使用者不得修改自己的角色。

#### 舊帳號文件清除提醒

- `system/accounts（舊系統帳號文件）`目前已由安全規則禁止網站讀取及寫入，但文件本身仍可能保留於 Firebase Firestore（Firebase 雲端文件資料庫）。
- 完成 Firebase Authentication（Firebase 身分驗證）、Google（Google 帳號）登入、新帳號管理及權限轉換後，後續每次進行安全規則檢查時，都必須確認此舊文件是否仍存在；若仍存在，必須提醒管理員完成清除。
- 清除前必須先確認舊管理員、課長與文員帳號均已完成轉換，並依需要備份舊帳號名稱與角色；不得保留或移轉舊密碼及密碼雜湊。
- 確認不再需要舊帳號資料後，刪除 `system/accounts（舊系統帳號文件）`，並再次驗收網站無法讀取該路徑。
- 清除時不得誤刪目前使用中的 `userAccess/{UID}（使用者權限文件）`，也不得誤刪 Firebase Authentication（Firebase 身分驗證）內的現行 Google（Google 帳號）管理員使用者。
- 本提醒在 `system/accounts（舊系統帳號文件）`完成備份、刪除及驗收前，不得標記為完成。

### 第三階段：建立資料庫安全規則

安全規則必須採用「預設全部拒絕，需要時才逐項開放」：

| 資料範圍 | 建議權限 |
|---|---|
| 帳號、角色及系統設定 | 只有管理員可以修改；一般使用者只能讀取自己的必要資料 |
| 員工資料 | 員工只能讀取自己的必要資料；管理角色依工作需求管理 |
| 訂單及工序 | 已登入且具有訂單權限的角色才能讀取或修改 |
| 報工資料 | 員工只能新增及查看自己的資料；班長只能審批授權範圍 |
| 考勤資料 | 員工只看自己的資料；授權管理角色才能新增或修改 |
| 裁帶模板及模板分段 | 只有具有裁帶權限的角色可以讀取；匯入、覆蓋及刪除角色需由使用者確認 |
| 成本、歷史及同步記錄 | 依管理角色限制，不得向所有已登入使用者全面開放 |
| 未列出的資料路徑 | 全部拒絕 |

每個寫入規則還要驗證欄位名稱、資料類型、長度、數值範圍、不可變識別碼、建立者及狀態轉換，不能只檢查是否已登入。

### 第四階段：增加多層保護

> **目前狀態：暫緩處理、尚未啟用。** App Check（應用程式驗證）目前先列為後續安全強化項目；因管理員目前尚未了解其用途與設定方式，現階段不進行設定，也不影響目前的 Google（Google 帳號）登入與 Firestore Rules（資料庫安全規則）。應在系統準備正式使用、公開流量明顯增加，或升級 Blaze（用量計費方案）前，再提醒管理員了解並評估是否啟用。

1. 啟用 App Check（應用程式驗證），建議使用 reCAPTCHA Enterprise（企業版自動風險驗證）；先觀察正常流量，再正式強制執行。
2. 本機工具只允許正式網站及核准的本機測試網址呼叫。
3. 本機工具依實際模板大小設定請求容量上限，並考慮加入一次性驗證資訊。
4. 公開網站加入 Content Security Policy（內容安全政策）及其他安全標頭。
5. 外部套件固定版本並加入完整性檢查，或改為保存於專案內；不得使用 `@latest（永遠最新版）`。
6. 所有來自使用者、Excel（表格檔）及雲端資料的文字，顯示前必須安全跳脫。
7. 為登入失敗、資料拒絕、異常流量及用量增加建立監控與警示。

## 六、安全規則驗收標準

正式上線前至少要通過以下測試：

- 未登入讀取帳號、員工、訂單及其他正式資料時，必須收到 HTTP（網頁傳輸協定）403 拒絕結果。
- 未登入者不得新增、修改或刪除任何正式資料。
- 員工只能讀取及建立自己的授權資料，不能查看其他員工的個人資料。
- 班長只能審批授權部門或範圍的資料，不能自行擴大權限。
- 課長與文員只能操作管理員核准的功能。
- 一般使用者不得修改角色、權限、成本設定及帳號狀態。
- 已通過或已封存資料不得被一般使用者任意改回其他狀態。
- 所有安全規則必須在 Firebase Emulator Suite（Firebase 本機模擬器）測試允許與拒絕情況。
- 正式部署後，再以未登入外部請求進行只讀驗收，確認不再出現成功回應。
- 本機工具必須拒絕非核准網站來源及超出容量限制的請求。

## 七、後續開發強制規則

- 新增任何資料集合、文件、欄位、登入方式、角色、讀取、寫入、匯入、匯出或刪除功能時，必須同步設計及測試安全規則。
- 前端畫面限制只能改善操作體驗，不得視為資料安全保護。
- 若發現功能未受安全規則保護，必須立即告知使用者其資料路徑、影響範圍、風險高低與建議方案。
- 未完成可信任登入、安全規則及未授權測試前，不得宣稱功能可以安全公開上線。
- 修改登入、權限、Firebase（雲端資料庫）安全規則或正式資料存取方式，屬於高風險修改，必須先取得使用者明確同意。

## 八、官方參考資料

- [Firebase（雲端資料庫）安全檢查表](https://firebase.google.com/support/guides/security-checklist)
- [Cloud Firestore（雲端文件資料庫）安全規則入門](https://firebase.google.com/docs/firestore/security/get-started)
- [Cloud Firestore（雲端文件資料庫）規則條件](https://firebase.google.com/docs/firestore/security/rules-conditions)
- [Firebase Authentication（Firebase 身分驗證）網站登入說明](https://firebase.google.com/docs/auth/web/start)
- [Custom Claims（自訂權限聲明）與角色控制](https://firebase.google.com/docs/auth/admin/custom-claims)
- [App Check（應用程式驗證）網站保護](https://firebase.google.com/docs/app-check/web/recaptcha-enterprise-provider)
- [Firebase Emulator Suite（Firebase 本機模擬器）安全規則測試](https://firebase.google.com/docs/rules/emulator-setup)
- [Firebase API Key（Firebase 應用程式識別金鑰）說明](https://firebase.google.com/docs/projects/api-keys)

</details>

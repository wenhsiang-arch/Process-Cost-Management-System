# 產能登記

最後整理：2026-08-24

## 1. 文件定位

本文件是產能登記、員工績效、月績效獎金入口、考勤及員工資料的現行正式功能規格。款號與工序內容以 `款號管理.md`為準，獎金以 `績效獎金.md`為準。

## 2. 功能與權限

產能主功能包含：

| 頁面 | 權限 | 用途 |
|---|---|---|
| 生產登記 | `productionEntry（產能登記）` | 一般產能與補充工時 |
| 員工績效 | `productionRecords（員工績效）` | 日／月績效及明細 |
| 月績效獎金 | `performanceBonus（月績效獎金）` | 月獎金、人工調整、鎖定與匯出 |
| 考勤 | `productionAttendance（考勤）` | 正常與加班工時 |
| 員工資料 | `productionEmployees（員工資料）` | 員工及部門管理 |
| 款號／工序快速修改 | 對應款號權限及 `processSecondsEdit` | 開啟共用款號修改面板 |

非管理員缺少權限欄位時視為關閉。所有頁面仍須通過自己的資料權限，不能靠其他頁面入口繞過。

## 3. 固定身分與正式來源

### 3.1 產能紀錄關聯

一般產能使用：

- `orderId（訂單固定識別碼）`
- `orderItemId（訂單項目固定識別碼）`
- `productId（款號固定識別碼）`
- `processId（工序固定識別碼）`
- 員工、日期、數量及狀態

顯示款號代碼、工序號、名稱、分類、秒數及每小時產能時，未鎖定資料由 Product Resolver（款號解析器）取得目前 `products（款號主檔）`內容，不讀取舊訂單工序或舊標準集合。

### 3.2 訂單項目

- 同一訂單可有多行相同款號，每行使用不同 `orderItemId`。
- 訂單的 PO、顏色、數量、說明、交期、完成／出貨日期及備註不受款號主檔覆蓋。
- 報工必須選到明確訂單項目，不能只依訂單號與款號猜測。

## 4. 生產登記

### 4.1 一般產能流程

1. 選擇生產日期與員工。
2. 系統顯示該員工當日考勤；當日考勤必須大於 0 才能新增一般產能。
3. 選擇訂單、訂單項目／款號與目前有效工序。
4. 輸入大於 0 的安全整數數量。
5. 儲存前重新讀取月份、訂單、訂單項目、款號與工序累計。
6. 交易成功後更新當日表格、工序累計及直接相關摘要。

同一張訂單款號可以由不同員工、不同日期及多筆產能完成，不限制人員、日期或報工筆數。

### 4.2 數量上限

- 只限制同一 `orderItemId + processId`的累計有效數量不得超過該訂單項目數量。
- 不同工序分開累計。
- 兩名員工同時逼近上限時，由同一筆雲端交易重新核對累計；只能讓合法結果成功。
- 訂單數量修改與報工同時發生時，系統必須明確拒絕或要求重試，不得超量或覆蓋其中一筆結果。

### 4.3 補充工時

- 補充工時不綁定訂單項目與工序，也不增加 `productionProcessTotals（工序累計）`。
- 同一員工同一天的有效補充工時不得超過當日考勤總工時。
- 補充工時只加入效率分子一次。

### 4.4 儲存的原子範圍

一般產能新增至少同步處理：

- `productionEntries（產能紀錄）`
- `productionProcessTotals（工序累計）`
- `productionMonths（月份狀態與修訂）`
- 受影響的每日／員工月摘要
- `operationLogs（獨立操作紀錄）`

上述必要資料必須一起成功或失敗，不得顯示成功卻只有部分累計或摘要完成。

## 5. 作廢與管理員永久刪除

- 一般業務修正使用作廢：原紀錄改為 `voided（已作廢）`並保留操作者、時間、原因及修訂號。
- 作廢在同一交易扣回工序累計、更新月份與摘要並建立操作紀錄。
- 已作廢紀錄不能再次作廢。
- 管理員永久刪除是現行受限能力，必須與作廢相同地原子扣回累計及摘要並建立操作紀錄；不得成為一般使用者清理歷史的方式。
- 已鎖定月份禁止作廢或刪除來源產能。

## 6. 員工指定日期紀錄

- 生產登記下方依目前員工及日期範圍顯示紀錄。
- 款號、工序號、工序名稱、秒數與每小時產能使用目前主檔解析；員工、日期、訂單項目、數量與狀態使用產能正式紀錄。
- 支援狀態篩選、搜尋、日期／週分頁及展開必要明細。
- 無法解析固定身分的資料列入異常，不得偷偷回退舊快照或錯配另一工序。

## 7. 效率與員工績效

### 7.1 標準有效工時

一般產能：

`標準有效工時 = 完成數量 ÷ 目前每小時產能`

等價公式：

`標準有效工時 = 完成數量 × 標準秒數 ÷ 正式每小時工作秒數`

標準缺少、為 0 或無法解析時列為異常，不能當成 0 秒正常資料。

### 7.2 當日效率

`當日效率 =（一般產能標準有效工時 + 補充工時）÷（正常工時 + 加班工時）× 100%`

- 分母為 0 時不顯示正常效率。
- 不平均每筆效率百分比。
- 顯示值可四捨五入，內部彙整保留完整精度。

### 7.3 未鎖定與鎖定

- 未鎖定月份的員工績效、日／月摘要、分析與獎金使用最新款號主檔解析結果。
- 月績效獎金正式鎖定後，使用鎖定快照，不受後續款號或公式修改影響。

## 8. 考勤

- 每位員工每天一筆有效考勤，保存正常工時與加班工時。
- 儲存考勤時同步更新月份修訂、每日摘要、員工月摘要及操作紀錄。
- 當日已有產能時不得刪除考勤；必須先處理產能，避免留下有產能但無考勤。
- 修改考勤與報工同時發生時，交易使用最新狀態，不能產生一半新、一半舊結果。
- 已鎖定月份不得修改或刪除考勤。

## 9. 員工與部門

- 員工使用固定工號及固定內部身分；工號一旦有歷史業務資料，永久不得分配給另一人。
- 有歷史產能、考勤或摘要的員工只能停用，不能永久刪除。
- 只有管理員可永久刪除建立錯誤、已停用且完全沒有正式引用的員工。
- 部門修改不得改寫員工既有生產紀錄；歷史顯示依正式資料與現行篩選規則處理。

## 10. 工序快速修改

### 10.1 入口

生產登記表格的工序號、工序名稱與秒數，可依權限開啟款號管理共用快速修改面板。

- 款號代碼只讀且不作為修改入口，不能從報工頁改碼。
- 開啟面板時重新讀取目前款號、群組及必要候選。
- 若款號已有群組，只顯示現有群組；沒有群組才顯示推薦候選。
- 沒有群組時，各尺寸分頁以該尺寸多數款號共同使用的越文品名、工序數量、工序號、越文描述及秒數判定高度符合，不與其他尺寸混合比較。
- 每次只修改目前開啟尺寸內已勾選的款號，其他尺寸的勾選與輸入值保持獨立。
- 開啟、預覽、執行及結果都使用共用載入與進度畫面。
- 使用者關閉結果後，只有存在其他尺寸分頁時才詢問「是否繼續修改其他尺寸」；選擇繼續回到原面板，選擇結束才關閉。

### 10.2 修改後同步

- 成功修改後更新目前頁已載入的款號與工序資料，不重新查詢整張舊訂單工序集合。
- 未鎖定既有產能、員工績效、分析及獎金下次解析時使用最新主檔。
- 使用者不需要逐筆同步報工或手動重建摘要。
- 完整面板與結果規格以 `款號管理.md`為準。

## 11. 月份、摘要與重建

- `productionMonths/{YYYY-MM}`是月份開放／鎖定、來源修訂及摘要狀態的唯一控制文件。
- 正常新增、作廢及考勤修改會同步更新直接相關的日／月摘要。
- 摘要是可重建衍生資料，不是另一份原始產能。
- 正式 Runtime 不提供一般使用者「建立月份摘要」、「同步產能」或「執行 Migration（資料轉換）」按鈕。
- 只有摘要確實缺少、損壞或需修復時，管理流程才可依原始來源重建；重建期間使用月份修訂防止過期結果誤標完成。
- 分析與獎金遇到摘要未就緒或版本不一致時停止顯示正式結果，不退回無限制掃描完整歷史。

## 12. 資料路徑

| 路徑 | 用途 |
|---|---|
| `productionEmployees` | 員工主資料 |
| `productionDepartments` | 部門主資料 |
| `orders` | 訂單主資料 |
| `orderItems` | 訂單項目、款號關聯與數量 |
| `products` | 目前款號與工序正式來源 |
| `productionEntries` | 一般產能與補充工時紀錄 |
| `productionProcessTotals` | 訂單項目＋工序有效累計 |
| `productionSupplementTotals` | 員工每日補充工時累計 |
| `productionAttendance` | 員工每日考勤 |
| `productionDaySummaries` | 員工每日摘要 |
| `productionEmployeeMonths` | 員工月份摘要與每日明細 |
| `productionMonths` | 唯一月份狀態、修訂及摘要版本 |
| `operationLogs` | 新增、作廢、刪除、考勤及管理操作紀錄 |

`orderProcesses`、`productProcessStandards`、`productionDayChanges`及舊摘要轉換路徑不是現行 Runtime 正式來源。

## 13. 主要程式責任

| 檔案 | 責任 |
|---|---|
| `js/production/production-entry.js` | 生產登記畫面、輸入、當日表格與快速修改入口 |
| `js/production/linked-entry-store.js` | 訂單項目、款號解析、產能新增／作廢／刪除與工序累計交易 |
| `js/production/linked-summary-store.js` | 每日、員工月摘要及款號解析來源版本 |
| `js/production/production-guard-store.js` | 月份、考勤及摘要狀態共用檢查 |
| `js/production/report-store.js` | 依條件讀取產能紀錄 |
| `js/production/production-records.js` | 員工績效及紀錄明細 |
| `js/production/attendance-store.js` | 考勤交易、摘要與快取失效 |
| `js/production/employee-store.js` | 員工與部門資料及引用限制 |
| `js/production/product-seconds-adapter.js` | 生產功能到共用款號快速修改的轉接 |
| `js/order-item-store.js` | 訂單項目與工序累計識別 |
| `js/order-service.js` | 訂單匯入、數量修改與狀態操作 |

## 14. 讀取與快取

- 進入產能功能後才讀取員工、訂單清單及所選日期資料。
- 訂單項目按所選訂單讀取，不預先載入全部訂單明細。
- 生產紀錄依員工、日期範圍、狀態與分頁查詢，不讀取全年完整集合。
- 款號快取以 `system/productsMeta`核對；月份相關快取以 `productionMonths`核對。
- 同一頁相同查詢共用 Promise（非同步工作承諾）。
- 新增、作廢或考勤修改後，只更新受影響日期、員工、訂單項目與月份快取。
- 離開頁面或登出時停止背景工作。

## 15. 驗收重點

1. 同訂單多行相同款號仍能選到正確 `orderItemId`。
2. 多人、多日、多筆可報工，但同項目同工序永不超量。
3. 同時報工、訂單改量與款號修改不發生更新遺失或混合資料。
4. 作廢／管理員刪除正確扣回累計、摘要並保留操作紀錄。
5. 考勤與補充工時限制正確，補充工時不污染訂單工序累計。
6. 款號修改後，未鎖定紀錄、績效與分析顯示最新主檔；固定業務資料不變。
7. 鎖定月份不能修改來源，凍結結果不受後續主檔變更影響。
8. 快速修改所有入口共用同一面板與儲存結果；各尺寸獨立比較及儲存，結果關閉後才詢問是否繼續修改其他尺寸。
9. 三種語言模式、桌機表格、搜尋、日期範圍與進度訊息正常。
10. Runtime 對所有已淘汰產能、訂單工序與摘要路徑為零讀寫。

# 產能登記功能規劃

## 文件用途

- 功能名稱：`Ghi nhận sản lượng / 產能登記`
- 文件狀態：生產登記、員工績效、考勤與員工資料已進入整合測試。本次在本機把原生產紀錄分頁改為員工績效、把修改與作廢移至生產登記當日表、縮窄考勤表、移除備註顯示並保留舊備註資料相容、加入績效跳回當日登記及原日期標籤分組；尚未提交或推送，仍待已登入桌機畫面驗收。
- 建立日期：2026-08-08。
- 本文件是產能登記功能的主要需求與進度紀錄。
- 後續每次確認需求、修改程式或完成驗收，都必須同步更新本文件的「開發進度」與「變更紀錄」。
- 尚未確認的內容必須保留在「待確認事項」，不得直接當成正式需求實作。

## 一、功能目的

本功能由文員快速登記員工每日完成的訂單、款號、工序與生產數量，並以獨立考勤分頁登記正常班及加班工時。登記頁只顯示目前員工與生產日期的考勤總工時；全部員工的每日效率顯示在員工績效分頁。

本功能以「快速輸入、少按鍵、資料清楚、避免雲端紀錄過度累積」為主要原則，不在登記頁呈現全部員工的效率統計明細。

## 二、預計分頁

1. `Ghi nhận sản xuất / 生產登記`
2. `Hiệu suất nhân viên / 員工績效`
3. `Chấm công / 考勤`
4. `Dữ liệu nhân viên / 員工資料`

後續若需要跨員工彙總，再另行規劃：

5. `Thống kê hiệu suất / 效率統計`

所有畫面依全系統介面規則，以越文在上、中文在下的雙語方式顯示，只規劃桌機操作。

## 三、生產登記頁

### 3.1 主要流程

- 一般生產：`工號 → 日期 → 訂單 → 款號 → 工序號 → 生產數量 → 儲存`
- 補充工時：`工號 → 日期 → 工序號 0 → 原因 → 小時 → 儲存`；訂單與款號可一起留空。

### 3.2 快速輸入規則

- 工號、訂單、款號及工序都使用「可直接輸入，也可展開選擇」的搜尋下拉欄位；向下箭頭統一放在輸入框內側右方。
- 只點擊輸入框不直接展開完整清單；輸入文字後才在下方顯示包含該文字的匹配結果，必須由文員選取。
- 點擊輸入框內的向下箭頭才展開目前欄位的完整可選清單；款號只顯示所選訂單內的款號，工序只顯示所選款號內的工序。
- 完整清單超過固定高度時使用可拖動的垂直捲軸；滑鼠離開輸入框與選單的整個範圍後自動收起。
- 選單面板必須緊貼對應輸入框下緣，不得留下滑鼠移動斷層；滑鼠從輸入框移入結果清單時不可提前收起。
- 搜尋採整段文字包含比對，不限於開頭文字。
- 例如輸入 `1234`，可以找到工號 `M91234`。
- 搜尋結果即使只有一筆也不自動選取，選取後不自動跳到下一個輸入欄位；唯一例外是工序號已完整輸入且只有完全相符的工序時，按 `Enter（確認鍵）`直接選定該工序並移到生產數量。
- 搜尋下拉欄位按向下鍵時展開選單，上下鍵移動目前選項，`Enter（確認鍵）`選定，`Esc（退出鍵）`收回。
- `Tab（欄位切換鍵）`及 `Shift＋Tab（反向欄位切換鍵）`只依「工號、日期、訂單、款號、工序號、生產數量」循環，不停在下拉箭頭、日期圖示、日期切換鍵或欄位選單。
- 工號選取後，自動顯示員工姓名與部門。
- 工序號輸入採整段包含比對；例如輸入 `1`，列出該款號內工序號包含 `1` 的結果供文員選取。
- 工序選中後，不在上方增加工序資訊橫條；工序名稱、工序秒數及訂單數量只顯示於下方表格。
- 生產日期預設使用電腦目前日期，保持自動模式時跨日會自動切換；日曆圖示開啟日期選擇，上鍵切換前一天、下鍵切換後一天。下鍵與日曆最多只能選到今天，未來日期停用；文員手動補登後會維持該日期，切回今天即恢復自動模式。
- 生產數量只接受正整數，不設定額外的一萬件上限；真正上限由訂單工序剩餘數量決定。
- 工序號輸入 `0` 時立即切換為補充工時模式，不需要按確認鍵或另外點選；原「工序」欄改為可輸入的「原因」，原「數量」欄改為「小時」。
- 補充工時原因為必填，最多 200 字；單筆工時只接受 0.5～24 小時，並以 0.5 小時為單位。
- 補充工時可以不選訂單與款號；若已選訂單，必須同時選擇該訂單內的款號，不允許只有訂單或只有款號。
- 補充工時儲存在獨立 `supplementHours（補充工時）`欄位，不寫入 `quantity（生產數量）`，也不建立或改變訂單工序累計。
- 員工資訊區右側提供「Hướng dẫn / 說明」按鈕，以完整越文區塊再完整中文區塊說明工序 0 的使用方式。
- 儲存開始後按鈕持續鎖定到雲端交易完成，且成功或失敗後至少間隔 1 秒才可再次操作，避免連按重複登記。
- 登記頁上方保留一個白色「生產登記」抬頭，抬頭右側內嵌淡灰員工資訊區；工號以標籤加輸入框呈現，姓名與部門使用具名稱的單行文字，不再建立第二張員工資訊卡片。
- 欄位依內容長度配置：訂單號碼最寬、款號次寬，日期、工序號與生產數量維持窄欄。日曆圖示放在生產日期標籤右側，前後日期鍵放在日期輸入框內側，不另占大型按鈕寬度。
- 訂單工序載入狀態顯示於生產登記區標題旁，不使用會短暫推動下方表格的大型提示框。

### 3.3 同一員工快速切換訂單

- 儲存後預設保留日期、員工、訂單與款號。
- 清空工序號及生產數量，游標回到工序號，方便繼續登記同一訂單。
- 點擊訂單或款號輸入框只定位游標；輸入文字後顯示匹配結果，點擊框內箭頭才展開完整可選資料。
- 切換訂單後保留日期與員工，清空款號、工序及生產數量。
- 下方表格持續顯示該員工當日的所有訂單，不因切換訂單而只剩目前訂單。

### 3.4 下方表格

目前欄位：

- 訂單
- 款號
- 工序號
- 工序名稱
- 生產數量
- 補充工時
- 訂單數量
- 工序秒數
- 每小時數量
- 狀態
- 操作

登記頁顯示所選員工與生產日期的有效及已作廢紀錄，不顯示效率百分比。具有原 `productionRecords（員工績效／生產紀錄權限代碼）`權限者可在下方當日表修改有效紀錄或作廢；管理員在系統測試期間另可永久刪除有效或已作廢紀錄。員工績效分頁不提供修改、作廢或刪除。

表格標題左側圖示可展開欄位顯示選單：瀏覽用途的訂單、款號、工序號、工序名稱／原因、生產數量、補充工時、訂單數量及工序秒數都可自由顯示或隱藏；管理員另可控制操作欄。恢復預設放在選單抬頭右側並一次開啟全部可用欄位；全部關閉時顯示緊湊空欄提示。此選擇只改變目前畫面，不改變查詢、資料、計算、匯出或權限。

## 四、生產數量限制

### 4.1 計算單位

同一工序的訂單完成量依以下條件合計：

`訂單＋款號＋工序號`

同一工序可以由不同員工、不同時間共同完成，但全部有效生產數量加總不得超過該訂單的訂單數量。

工序 0 補充工時不屬於訂單工序完成量，不讀取、建立或變動 `productionProcessTotals（訂單工序累計資料）`，也不受訂單數量上限計算。

### 4.2 儲存檢查

儲存時必須以雲端交易重新取得最新資料並確認：

`目前已登記數量＋本次生產數量 ≤ 訂單數量`

工序選定後只讀取該工序既有的 `productionProcessTotals（訂單工序累計資料）`單一文件，在生產數量標籤右側顯示「輸入後預計數量／訂單數量」。輸入造成超量時立即改成紅色並先阻止儲存；點擊數量卡片時留在生產登記頁，於下方當日表尋找並醒目顯示目前工序，不再跳到員工績效。此預覽只協助提早發現問題，雲端交易仍是多人同時操作時的最終判斷。

若超量，拒絕儲存並顯示：

- 訂單數量
- 已登記數量
- 剩餘可登記數量
- 本次輸入數量

修改數量時只計算前後差額；作廢時釋放該筆生產數量。多人同時登記時，也必須透過同一筆雲端交易避免超量。

### 4.3 跨功能界線

產能登記功能只能讀取既有訂單、款號與工序資料，不得為了配合產能計算而自行修改訂單頁、款號頁或其他既有功能。

如果既有訂單之後被刪除或把數量降到已登記產量以下，產能功能先標示資料異常並停止後續登記。是否在訂單功能增加阻擋，必須另外說明影響並取得同意。

## 五、雲端資料量控制

### 5.1 正式保存方式

每次按下儲存會建立一筆獨立的 `productionEntries（生產登記）`文件，不強制同一天、同員工、同訂單、同款號、同工序只能一筆，也不自動合併每日數量。

目前實際作業通常是每位員工、每個生產日統一登記一次；若文員確實再次儲存相同組合，系統仍視為另一筆有效紀錄，兩筆數量一起計入訂單工序完成量。若填錯，使用生產紀錄分頁修改或作廢。

另以 `productionProcessTotals（訂單工序累計）`每道訂單工序只保存一份目前有效總量，供多人、不同時間共同登記時做訂單數量上限交易；這份累計不是生產明細。

### 5.2 生產明細內容

- 日期
- 員工工號、姓名及部門快照
- 訂單識別資料及訂單號碼快照
- 款號快照
- 工序號、工序名稱及工序秒數快照
- 當時採用的每小時產能快照
- 本筆生產數量
- 建立時間、修改時間及操作者
- 狀態

### 5.3 不重複保存

- 效率後續依有效生產紀錄與考勤即時計算，不另外保存重複效率明細。
- 一般新增登記不另外建立操作紀錄；修改與作廢會建立不可改寫的操作紀錄。
- 只有修改、作廢、永久刪除、批次調整及其他重要操作，才新增不可任意修改的操作紀錄。
- 不把整月或整年的所有資料塞入同一份大型雲端文件，避免容量限制、多人更新衝突及查詢困難。
- 暫不自動刪除一年以前的正式資料。

### 5.4 員工識別與計算規則

- 員工工號是唯一且建立後不可變更的正式識別資料；姓名與部門不得取代工號作為資料關聯。
- 個人產量歸屬依員工工號、訂單、款號、工序及生產日期確認。
- 訂單工序累計依訂單、款號及工序合計全部員工的有效生產數量，不把員工工號加入訂單數量上限的累計條件。
- 員工效率與未來績效獎金依員工工號及生產日期計算；姓名與部門只供畫面、報表及歷史快照顯示，不得作為關聯、分組或計算條件。
- 修改員工姓名不改變原工號的歷史產量、效率及未來績效獎金歸屬；畫面可依員工工號顯示員工資料中的最新姓名，原生產紀錄仍保留登記時的姓名快照供追溯。
- 正式使用時員工離職或停止使用採停用處理，不刪除工號資料；停用後仍可依原工號查詢歷史紀錄及進行既有資料計算。

### 5.5 未來封存

如果日後確實需要限制保存年限，再另外規劃「年度封存」：先匯出並驗證完整歷史資料，再由管理員決定是否清除雲端舊資料。封存與刪除屬高風險功能，不包含在目前第一階段。

## 六、效率計算

### 6.1 官方計算基準

效率直接使用款號表畫面顯示的整數「每小時產能」計算，不在產能畫面顯示或說明每小時有效工作 50 分鐘。

每筆有效生產紀錄的標準工時：

`標準工時＝生產數量÷每小時產能快照`

員工每日效率：

`每日效率＝全部有效標準工時合計÷當日考勤總工時×100%`

工序 0 補充工時以 100% 效率計入：`2 小時補充工時＝2 小時有效標準工時`。補充工時只增加效率分子的有效標準工時，不增加考勤分母，也不使用工序秒數或每小時產能計算。

### 6.2 快照規則

- 新登記使用款號表目前顯示的最新每小時產能。
- 儲存時把整數每小時產能一併保存在生產資料中。
- 款號資料日後更新時，新登記採用新數值；舊紀錄保留原快照，避免歷史效率跟著改變。
- 工序秒數保留作為畫面顯示及追溯資料，不直接作為正式效率公式。

## 七、考勤工時

### 7.1 輸入方式

考勤已使用每日表格式批次登記：

- 選擇日期
- 依部門篩選
- 搜尋員工
- 批次填入正常班工時
- 批次填入加班工時
- 個別修改請假、半天或其他例外員工
- 儲存當日考勤

正式欄位：工號、姓名、部門、正常班工時、加班工時、總工時、備註、狀態及管理員測試刪除操作。

- 同一考勤日期、同一員工只保存一筆，以 `日期__工號`作為固定文件識別碼；再次儲存是修改該日總工時，不新增重複紀錄。
- 正常班與加班各允許 0～24 小時，最小單位為 0.5 小時，兩者合計不得超過 24 小時。
- 批次套用只修改目前部門與搜尋條件下可見的員工，套用後仍需按儲存才寫入雲端。
- 新考勤只允許啟用員工；既有考勤可在員工停用後繼續修正，姓名與部門會依同一工號更新快照。
- 考勤資料依日期按需讀取，不在登入時讀取整個集合。

### 7.2 效率狀態

- 有生產、沒有考勤：顯示「考勤未登記」，不自行假設為 8 小時。
- 有考勤、沒有生產：效率為 0%。
- 考勤為 0 小時但有生產：標示資料異常，不計算效率。
- 正常班與加班合計為效率分母；目前只計算整日效率，不拆分正常班效率與加班效率。

## 八、員工資料

已建立本功能專用的新員工資料，不重新啟用舊的員工資料路徑。

基本欄位：

- 工號
- 姓名
- 部門
- 啟用狀態
- 建立者與建立時間
- 最後修改者與修改時間

工號必須唯一，建立後不得直接更改。正式使用時，離職或停用員工採用停用方式；系統測試期間暫時允許管理員永久刪除沒有任何生產紀錄的員工。歷史生產紀錄仍保留原本的員工資料快照。

新增員工與修改員工使用不同流程：新增時若工號已存在，系統必須拒絕並提示使用員工列表原有的編輯功能，不得用新增表單覆蓋既有資料；修改時工號維持唯讀。

新增員工一律自動設為啟用，新增／編輯表單不顯示狀態勾選欄；既有員工的停用與重新啟用只由下方員工列表的操作按鈕處理，停用不刪除歷史資料。

部門改用 `productionDepartments（產能部門）`獨立清單管理：

- 新增或修改員工時，部門只能由下拉選單選擇，不提供任意文字寫入。
- 儲存員工時會再次確認新選擇的部門仍存在且為啟用狀態，避免另一個視窗停用部門後仍建立錯誤資料；既有員工保留原本停用或舊部門時仍可修改姓名及狀態。
- 員工資料頁的部門下拉選單底部只保留一個「Quản lý bộ phận / 部門管理」入口；開啟後的部門管理視窗內包含新增、改名、停用／啟用及刪除功能，不再於下拉選單右側重複放置新增與管理按鈕。
- 未被任何員工使用的部門可以修改名稱或刪除。
- 已被員工使用的部門不得修改名稱或刪除，必須先將相關員工改到其他部門。
- 部門可以停用或重新啟用；停用後不出現在新增員工選單，但既有員工仍保留原部門並可正常顯示與編輯。
- 新增、改名、停用／啟用及刪除部門都建立 `operationLogs（獨立操作紀錄）`。

## 九、修改、作廢與刪除

### 9.1 生產資料

- 數量填錯：修改該筆生產數量，記錄修改前後數值、操作者及時間。
- 補充工時填錯：修改該筆 `supplementHours（補充工時）`，仍限制 0.5～24 小時及 0.5 小時單位，並留下操作紀錄；補充原因建立後不直接改寫，錯誤時作廢後重新登記。
- 日期、員工、訂單、款號或工序錯誤：作廢錯誤資料，再重新登記正確資料。
- 作廢時同步扣回訂單工序累計數量；考勤與效率完成後，再由有效紀錄重新計算標準工時與效率。
- 系統測試期間暫時允許管理員永久刪除生產紀錄，不要求輸入識別資料，只使用一般確認視窗。
- 永久刪除有效紀錄時，必須在同一筆雲端交易扣回 `productionProcessTotals（訂單工序累計）`；扣回後為零時，同時清除該累計文件。已作廢紀錄不再次扣回數量。
- 補充工時作廢或永久刪除只處理該筆明細及操作紀錄，不得扣回、建立或刪除任何訂單工序累計。
- 課長、文員及其他非管理員角色仍禁止實體刪除。
- 正式上線前必須依使用者指示移除刪除按鈕，並把 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）恢復為禁止實體刪除；不得只隱藏畫面按鈕。

### 9.2 考勤資料

- 尚未儲存的內容可以直接清空。
- 工時填錯可以修改，並保留修改紀錄。
- 日期或員工錯誤時作廢原考勤，再重新建立正確資料。
- 作廢考勤不再計入效率分母，但不影響生產紀錄。

### 9.3 員工資料

系統測試期間，管理員可永久刪除沒有任何生產紀錄的員工；若仍有有效或已作廢生產紀錄，必須先刪除相關紀錄。其他角色只允許停用與重新啟用。

## 十、員工績效頁

- 正式畫面名稱為 `Hiệu suất nhân viên / 員工績效`；程式、頁面及權限識別碼仍沿用 `production-records`與 `productionRecords`，避免破壞既有權限、安全規則及網址關聯。
- 日期範圍每次最多 31 天；只在實際開啟分頁時按日期讀取有效生產紀錄與考勤，不在登入時預先載入全年資料。
- 同一生產日期、同一員工只顯示一列，整合考勤總工時、有效標準工時、補充工時、當日效率及績效狀態。
- 員工搜尋只比對工號、目前姓名及部門；績效狀態可篩選已計算、考勤未登記、考勤時數異常及缺少標準產能。
- 員工工號欄預設隱藏，需要時由表格欄位選單開啟；姓名與部門優先顯示員工主資料，找不到主資料時才使用歷史快照。
- 多日資料沿用原日期標籤設計：同一連續日期只在第一位員工列顯示日期與星期，其餘員工列保留空白日期格；下一日期以藍色上框線分隔。此方式不使用真正合併儲存格，不影響搜尋與欄位操作。
- 點擊某位員工的「Mở đăng ký / 前往登記」，直接切到生產登記並帶入該員工與生產日期。
- 員工績效屬彙總查看頁，不提供生產紀錄修改、作廢或永久刪除；這些操作統一在生產登記的員工當日表處理。

## 十一、架構與程式分工原則

本功能必須先依 `系統架構規劃.md（全系統架構規格）`檢查現有架構，再開始編寫。不得把新功能邏輯塞入不相關的既有功能，也不得為了方便建立一個包辦全部工作的共用檔案。

### 11.1 第一階段實際功能檔案

- `production-entry.js（生產登記頁程式）`：登記畫面、快速輸入、當日紀錄修改／作廢、管理員測試刪除，以及員工績效帶入員工與日期的入口。
- `production-records.js（員工績效頁程式）`：依日期彙總全部員工的考勤與有效生產、績效篩選、原日期標籤分組及返回當日登記。
- `production-attendance.js（產能考勤頁程式）`：日期、部門與員工篩選、批次套用、個別編輯、狀態及管理員永久刪除畫面。
- `production-employees.js（員工資料頁程式）`：員工維護、部門下拉與部門管理，以及管理員永久刪除畫面。
- `employee-store.js（員工資料存取程式）`：員工查詢、分開的新增／修改、停用、部門資料管理、關聯檢查與永久刪除。
- `entry-store.js（生產資料存取程式）`：一般生產與補充工時的輸入驗證及儲存分流、單筆工序累計讀取、數量上限交易、修改、作廢、永久刪除與一般生產工序累計同步。
- `report-store.js（產能報表資料存取程式）`：員工當日有效／全部狀態查詢、單日查詢及最多 31 天的員工績效日期範圍查詢。
- `attendance-store.js（考勤資料存取程式）`：考勤驗證、日期查詢、批次交易、資料版本、本機快取、刪除及每日效率計算。
- `production.css（產能功能樣式）`：四個產能分頁共同使用的桌機樣式。

目前每日效率屬生產紀錄與考勤資料的直接計算，不另外建立 `production-efficiency.js（效率統計頁程式）`。若後續新增跨部門、跨日期或績效獎金統計，再依獨立責任評估新增檔案，不塞入登記頁。

本輪完成後 `production-entry.js（生產登記頁程式）`約 1320 行，已超過拆分評估提醒；本次再次依責任檢查，內容仍集中於同一生產登記頁的欄位互動、日期、一般生產／工序 0 輸入模式、儲存前預覽、當日表格與該表格的修改／作廢操作，資料驗證及交易仍由既有 `entry-store.js（生產資料存取程式）`負責，員工績效計算也留在考勤資料層及績效頁，未塞入登記頁。目前不建立零碎功能檔或全域共用模組；若登記頁再增加獨立子流程，必須先提出依介面、表格操作或頁面狀態責任拆分的方案。

### 11.2 共用程式原則

- 優先使用系統現有的雙語介面、提示、確認視窗、安全畫面輸出、雲端資料庫介面、本機快取、分頁、日期格式及防止重複點擊功能。
- 產能功能專屬的搜尋、數量限制、考勤及效率邏輯留在產能功能內。
- 只有兩個以上不同主要功能確定使用完全相同的能力時，才提出全系統共用模組方案。
- 目前不建立 `production-core.js（產能核心共用程式）`，避免形成難以維護的混合檔案。
- 目前不獨立建立 `production-calculation.js（產能計算程式）`；每日效率公式集中在 `attendance-store.js（考勤資料存取程式）`的可測試純函式中。

### 11.3 載入與快取

- 登入時不預先讀取產能完整資料。
- 使用者進入產能主功能後，只載入目前分頁需要的程式與資料。
- 訂單摘要與單張訂單工序沿用既有版本檢查及 IndexedDB（瀏覽器本機資料庫）快取。
- 產能員工名冊與部門清單只在相關分頁實際開啟後讀取，不在登入階段預先讀取；兩者分別使用 `productionEmployees（產能員工快取範圍）`及 `productionDepartments（產能部門快取範圍）`。
- 開頁時先讀取 `dataVersions（資料版本）`；版本相同使用依 `UID（使用者識別碼）`隔離的 `IndexedDB（瀏覽器本機資料庫）`，版本不同才重新下載對應集合。
- 員工或部門新增、修改、狀態切換及刪除時，在同一筆交易更新對應資料版本，並只讓該集合快取失效，不要求其他功能重新下載。
- 使用者已確認產能、考勤及分析屬內部非敏感作業資料；快取仍依 UID（使用者識別碼）、電腦、瀏覽器設定檔及網站來源隔離，但登出與重新啟動電腦後保留，減少每天重複下載。帳號失去對應權限、停用或快取結構升級時不得顯示並依規則清除。
- 考勤依「日期」及「日期＋工號」分開保存於 IndexedDB（瀏覽器本機資料庫）；讀取某日只使用或更新該日快取，新增、修改或刪除只更新受影響日期與員工，不再因資料版本改變讓全部已讀日期失效。
- 生產登記與生產紀錄的日期／員工查詢使用獨立範圍快取；資料版本相同時再次開啟相同條件不重讀明細，版本改變時只讓產能查詢重新取得，不影響訂單、款號、員工、部門或考勤快取。
- 員工績效依使用者選定的日期範圍讀取，單次最多 31 天；生產登記只讀取所選員工與日期，不建立無限制的整批即時監聽。
- 離開功能或登出時，停止該功能的監聽、計時器與背景工作。

## 十二、資料路徑

第一階段正式使用：

- `productionEmployees（產能員工資料）`
- `productionDepartments（產能部門資料）`
- `productionEntries（生產登記明細）`
- `productionProcessTotals（訂單工序累計資料）`
- `productionAttendance（產能考勤資料）`
- `productionDayChanges（產能日期變更標記）`：產能或考勤成功異動後，以不阻止正式交易的方式記錄受影響員工日期，供生產分析增量補讀；不保存生產數量或考勤工時內容。

`productionEntries（生產登記明細）`以 `recordType（紀錄類型）`區分：

- `standard（一般生產）`：保留訂單、款號、工序、生產數量、訂單數量、工序秒數及每小時產能快照，並同步工序累計。
- `supplement（補充工時）`：固定 `processNo（工序號）`為 `0`，保存 `supplementReason（補充原因）`與 `supplementHours（補充工時）`；訂單與款號可一起留空，不保存生產數量、工序秒數、每小時產能或訂單工序識別碼，也不寫入工序累計。
- 不需要轉換既有一般生產資料，也不新增資料集合或雲端索引。

`productionAttendance（產能考勤資料）`固定以 `attendanceDate__employeeId（考勤日期__員工工號）`作為文件識別碼，保存考勤日期、工號、姓名與部門快照、正常班工時、加班工時、備註、修訂次數、建立與修改資訊及結構版本。總工時與效率均即時計算，不另存重複結果；本輪查詢只使用單文件讀取或單一日期條件，不需要新增複合索引。

效率統計未來由有效生產明細與考勤計算，不先建立重複的永久效率明細。不得重新開放或重用目前已停用的舊 `employees（舊員工資料）`、`reports（舊報表資料）`、`attendance（舊考勤資料）`路徑。

## 十三、權限與安全

- 目前權限為 `productionMain（產能主功能）`、`productionEntry（生產登記）`、`productionRecords（員工績效；沿用原生產紀錄權限代碼）`、`productionAttendance（產能考勤）`及 `productionEmployees（產能員工資料）`。
- 員工績效沒有獨立寫入資料；能開啟員工績效的角色只讀取有效生產紀錄與對應考勤後在畫面計算。原 `productionRecords`權限仍同時控制生產登記當日表的修改與作廢入口，以符合現有安全規則。
- 管理員以外的新增權限預設關閉，必須由管理員明確開啟。
- 畫面隱藏不能代替雲端安全規則。
- 產能功能只讀取必要的訂單、款號與工序資料，不寫入其他功能資料。
- 補充工時沿用 `productionEntry（生產登記）`與 `productionRecords（生產紀錄）`既有權限，不新增角色權限；沒有訂單時只比對啟用員工，有訂單時另外驗證款號屬於該訂單。
- `productionAttendance（產能考勤）`對全部非管理員角色預設關閉；只有管理員明確開啟後才能建立或修改考勤。生產登記與員工績效可以依各自權限讀取考勤摘要及效率所需資料，但不能因此寫入考勤。
- 雲端安全規則必須驗證資料欄位、型別、數值範圍、不可變識別資料及合法狀態轉換。
- 課長、文員及其他非管理員禁止實體刪除；系統測試期間只暫時允許管理員永久刪除產能資料。
- 修改、作廢、批次調整及重要操作寫入 `operationLogs（獨立操作紀錄）`。
- 部門新增、改名、狀態切換及刪除必須使用 `productionEmployees（產能員工資料）`分頁權限並留下操作紀錄；沒有該分頁權限的角色不得直接讀寫部門集合。
- 必須測試未登入者、一般使用者、班長、課長、文員、管理員及跨帳號存取的允許與拒絕結果。

## 十四、第一階段實際影響範圍

- 主畫面的產能入口及四個目前分頁容器。
- 中央功能清單、五個權限欄位及按需載入設定。
- 八份產能功能程式及一份專用樣式。
- 五個產能資料集合的雲端安全規則與既有生產紀錄查詢索引；考勤只以固定文件或日期條件查詢，不新增索引。
- 快速搜尋、正整數、訂單工序限制、多人共用數量上限、修改、作廢、權限及架構測試。
- 交接、安全盤點及讀取量文件。

未另行取得同意前，不修改既有訂單、款號、同步、登入、摘要、設定或其他功能的內部邏輯。

## 十五、待確認事項

- 員工資料第一階段是否需要表格檔匯入，或先只提供手動新增。
- 生產紀錄是否需要匯出，以及允許匯出的日期範圍與權限。
- 年度封存是否需要在後續階段開發，以及正式資料保存年限。

## 十六、開發進度

| 階段 | 狀態 | 說明 |
|---|---|---|
| 第一階段需求盤點 | 已完成 | 以快速登記、員工名冊、員工績效與訂單工序上限為範圍。 |
| 架構與資料設計 | 已完成 | 依中央功能清單按需載入；員工與部門各自接入資料版本及依 UID（使用者識別碼）隔離的本機快取，未修改訂單、款號或登入流程。 |
| 目前畫面 | 本輪程式完成，待實際畫面驗收 | 分頁順序為生產登記、員工績效、考勤、員工資料。員工績效同日顯示全部員工並沿用原日期標籤與藍色分隔；考勤表移除備註顯示並縮窄；修改、作廢與測試刪除集中在資料來源功能。 |
| 第一階段程式 | 既有版本已推送；補充工時本機完成 | 搜尋、鍵盤、日期與數量上限沿用既有流程；工序 0 新增原因／小時切換、說明視窗、獨立儲存欄位，以及當日與歷史表格的補充工時欄。補充工時不寫入一般生產數量或工序累計。 |
| 權限與安全規則 | 沿用既有狀態，本輪無異動 | 本次沒有新增權限代碼、資料欄位、安全規則或雲端索引；員工績效讀取及當日表修改／作廢仍使用既有權限與交易。 |
| 測試與驗收 | 本機自動測試完成 | 16 項產能專項測試及全系統 99 項非安全規則回歸測試全部通過；已登入桌機實際畫面仍待驗收。 |
| 管理員測試清除 | 本機驗收完成 | 生產登記當日表、考勤及員工資料暫時保留管理員永久刪除入口；員工績效為彙總結果，不另設刪除。正式上線前必須移除測試入口並恢復安全規則。 |
| 後續階段 | 部分完成 | 工序 0 補充工時、每日考勤及跨員工每日績效已在本機完成；績效獎金、匯入／匯出及年度封存尚未開始。 |
| 上線 | 部分完成 | 既有版本已推送；本次員工績效、當日登記操作與考勤表調整尚未提交或推送，且沒有新的安全規則或索引需要發布。 |

## 十七、變更紀錄

| 日期 | 版本 | 內容 | 狀態 |
|---|---|---|---|
| 2026-08-08 | 初版 | 建立產能登記初步模型，整合快速輸入、訂單切換、數量上限、考勤、效率、作廢及雲端資料量控制。 | 已記錄 |
| 2026-08-08 | 第一階段開發版 | 完成生產登記、生產紀錄、員工資料三頁；改採每次儲存一筆獨立登記，不做每日唯一或自動合併；完成工序累計交易、權限、安全規則、索引與本機測試。 | 本機完成，未發布 |
| 2026-08-09 | 管理員測試清除版 | 取消測試模式規劃；暫時在產能三個分頁加入管理員永久刪除，刪除生產紀錄時同步扣回或清除工序累計，員工仍有生產紀錄時禁止刪除。 | 程式已推送；安全規則已發布 |
| 2026-08-09 | 員工與快速選擇版 | 新增工號重複拒絕覆蓋；部門改為可新增、選擇、管理、停用與受使用關聯保護；訂單與款號點入即展開，款號只限目前訂單；完成74項功能／架構及38項安全規則測試。 | 程式已推送；安全規則已發布 |
| 2026-08-09 | 員工與部門快取版 | 員工及部門清單接入獨立 IndexedDB（瀏覽器本機資料庫）快取與 dataVersions（資料版本）；異動與版本同交易更新，只使相關快取失效，登出或失去權限時清除敏感快取。 | 程式已推送；安全規則已發布 |
| 2026-08-09 | 搜尋下拉手動選取版 | 四個搜尋欄位改用輸入框內的共用箭頭；輸入只列出匹配結果，箭頭展開完整清單，移除唯一結果自動選取及選取後跳欄，滑鼠離開後收起；新增員工固定啟用並移除表單狀態欄。 | 程式已推送，74 項功能／架構測試通過，待登入驗收 |
| 2026-08-09 | 搜尋下拉連續操作修正版 | 四個搜尋選單移入輸入框控制區並緊貼下緣，取消輸入框與清單之間的滑鼠移動斷層；未修改搜尋、選取或資料讀取邏輯。 | 本機 75 項功能／架構測試通過，待登入驗收 |
| 2026-08-09 | 生產登記緊湊分區版 | 員工資訊與生產登記拆成兩個緊湊區塊；日期快速操作改為標籤旁圖示；訂單、款號、工序與數量依內容長度分配欄寬；訂單工序載入改為標題旁緊湊狀態；當日表格加入固定核心欄與可選輔助欄。 | 本機 76 項非雲端功能／架構測試通過，待登入畫面驗收 |
| 2026-08-09 | 生產登記快速鍵與紀錄分組版 | 單一生產登記抬頭內嵌員工資訊；完成 Tab（欄位切換鍵）輸入順序、搜尋下拉鍵盤操作、工序 Enter（確認鍵）直選、數量 Enter（確認鍵）儲存、整日日期切換與未來日期限制；加入工序數量超量預覽、可自由隱藏全部瀏覽欄位、生產紀錄日期視覺分組及目前工序篩選銜接。 | 76 項非雲端功能／架構測試通過；程式已以 `ae6feb7` 推送至 `main（主要分支）`；未修改安全規則、資料格式或既有雲端交易，待登入畫面驗收 |
| 2026-08-09 | 生產登記員工資訊與儲存入口調整版 | 淡灰員工資訊區改為上、下、右側貼合外框，工號輸入框統一既有欄位外觀並縮短；移除右側儲存產量按鈕，保留生產數量按 Enter（確認鍵）儲存及防重複送出。 | 76 項非雲端功能／架構測試通過；程式已以 `286c97d` 推送至 `main（主要分支）`；未修改雲端資料、權限、安全規則或儲存交易 |
| 2026-08-09 | 生產登記輸入欄位寬度調整版 | 員工工號下拉框再縮短；訂單與款號改用固定緊湊寬度，日期欄完整顯示，生產數量標題簡化為「Số lượng / 數量」，數量預覽不再省略；輸入區靠左且不撐滿底框，右側保留後續欄位空間。 | 76 項非雲端功能／架構測試通過；程式已以 `90ca9cc` 推送至 `main（主要分支）`；未修改資料、權限、安全規則或儲存流程 |
| 2026-08-09 | 生產登記桌機欄位與數量上限顯示優化版 | 員工下拉只顯示工號；日期與訂單欄縮短；工序號與工序名稱拆開，工序號輸入框只保留兩位數寬度；已登記數量／訂單數量上限移到當日紀錄標題右側的大字底色框；欄位選單加入全選；數字表頭與數值固定同軸對齊。 | 76 項非雲端功能／架構測試通過；程式已以 `81ebe27` 推送至 `main（主要分支）`；未修改資料、權限、安全規則、上限計算或儲存流程；待登入後三種桌機尺寸實際驗收 |
| 2026-08-09 | 生產紀錄員工姓名同步顯示版 | 生產紀錄改為依員工工號優先顯示員工主資料最新姓名；找不到員工主資料時才顯示登記時的姓名快照。未改寫歷史生產資料，姓名仍不參與產量、效率或獎金計算。 | 修改 `production-records.js（生產紀錄頁程式）`、中央載入版本與產能測試；77 項非雲端功能／架構測試通過；未修改雲端資料、資料格式、快取、安全規則、索引或計算方式；待已登入畫面驗收 |
| 2026-08-09 | 生產登記當日表格欄位穩定版 | 當日表格改用固定欄位配置；訂單、款號與工序名稱靠左，工序號置中，數量欄在各自的緊湊欄內靠右，操作欄固定置中；表頭與內容使用相同欄寬，避免桌機尺寸調整時各自偏移。 | 28 項產能與介面一致性測試通過；未修改資料、權限、安全規則、計算或儲存流程；待登入後三種桌機尺寸實際驗收 |
| 2026-08-09 | 工序 0 補充工時本機開發版 | 輸入工序 0 即切換原因／小時；單筆 0.5～24 小時且以 0.5 小時為單位；訂單款號可一起留空；新增說明按鈕、獨立表格欄位、修改／作廢／刪除分流與安全規則。 | 78 項非雲端測試及 39 項安全測試通過；未提交、未推送、未發布安全規則，待已登入桌機畫面驗收 |
| 2026-08-10 | 考勤與每日效率程式推送版 | 新增考勤分頁、每日固定考勤文件、0.5 小時批次與個別輸入、考勤摘要、每日效率、員工工號／姓名拆欄、登記抬頭與淡藍員工列調整、依 UID 隔離快取、獨立權限及安全規則。 | 程式已以 `b5d8410` 推送；考勤新增安全規則尚未發布，待已登入桌機畫面驗收 |
| 2026-08-10 | 綜合搜尋與操作區單排本機版 | 生產紀錄把工號、訂單、款號與工序等表格內容合併為單一綜合搜尋；日期箭頭縮小；生產紀錄、考勤與員工資料操作區保持單排；部門改為下拉選單內單一管理入口；主按鈕雙語文字提高對比。 | 本次產能與介面兩組共 38 項測試及全系統 96 項非雲端回歸測試通過；未修改雲端資料、安全規則、索引或計算，尚未提交與推送，待桌機畫面驗收 |

## 十八、新視窗交接（2026 年 8 月 9 日）

第 18.1～18.8 節保留先前開發批次的歷史交接；工序 0 補充工時完成後，以第 18.9 節及目前程式狀態為最新依據。

### 18.1 接手結論

- 第一階段三個分頁、生產登記快速操作、員工資訊／儲存入口、輸入欄位寬度及本輪桌機欄位、數量上限顯示、欄位全選與表格對齊均已完成程式修改、本機自動測試及推送。新視窗不要重新實作，先由使用者登入正式頁面逐項驗收。
- 最近相關提交為 `81ebe27（產能登記桌機欄位顯示優化版本）`，已推送至 `main（主要分支）`。
- `UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`已整理為只保存共用規則；本節與本文件是產能功能狀態、操作細節及後續驗收的正式來源。
- 本輪只調整產能登記頁欄位寬度、員工／工序下拉文字、工序名稱顯示、數量上限卡、欄位全選、表格對齊、產能程式／樣式快取版本及相關測試；未修改 Enter（確認鍵）儲存流程、數量上限計算、雲端資料、登入、角色、權限或 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）。

### 18.2 最近一次程式修改檔案

- `index.html（主畫面檔案）`：工序號與工序名稱拆開；已登記數量／訂單數量上限卡移到當日紀錄標題右側；欄位選單加入全選；更新中央功能程式載入版本。
- `js/production/production-entry.js（生產登記頁程式）`：員工與工序下拉改為單行識別碼；工序名稱自動同步及清除；全選狀態支援全選、全部取消與部分選取；數量卡顯示已登記數量／訂單數量上限，既有預計超量阻擋與點擊切換紀錄保留。
- `js/features.js（中央功能程式）`：只更新產能登記程式與產能樣式的按需載入快取版本，沒有改變功能、導覽或權限登記。
- `styles/features/production.css（產能功能樣式）`：縮短日期與訂單欄；工序號輸入框縮到兩位數寬度且保留完整雙語抬頭；新增工序名稱顯示框、數量上限底色卡、全選排版及表格數字同軸對齊。
- `tests/production.test.mjs（產能功能測試）`及 `tests/ui-consistency.test.mjs（介面一致性測試）`：驗證本輪欄位結構、寬度、單行下拉、數量卡、全選、表格對齊及快取版本。

### 18.3 資料、權限、快取與安全影響

- 正式資料集合仍是第十二節列出的四個路徑，沒有新增、搬移或轉換資料。
- 數量預覽只讀取目前工序的一份 `productionProcessTotals（訂單工序累計資料）`文件；畫面預覽不能取代儲存時的雲端交易。
- 沒有修改權限代碼、角色預設、Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）或雲端索引。
- 沒有改變員工／部門快取範圍、資料版本結構或登出清除規則；只更新程式載入版本，避免瀏覽器沿用舊介面程式。
- 沒有改變生產明細格式、工序累計交易、作廢、修改或管理員測試刪除規則。

### 18.4 已完成驗收與限制

- 76 項非雲端功能／架構測試通過，程式語法、介面結構、鍵盤操作規則及既有功能回歸均通過。
- 本輪未修改安全規則，因此未重新執行 Firebase Emulator Suite（Firebase 本機模擬器）；既有 38 項安全測試結果維持先前狀態。
- 內建瀏覽器可載入最新本機版本，但沒有產能系統登入狀態；依 Google 登入限制未代替使用者登入，因此不能宣稱已看到真實資料畫面，三種桌機尺寸的最後畫面確認保留給使用者。
- 文件整理後尚未提交或推送；一般程式推送預設排除 `MD（標記文件）`，除非使用者另外明確指定文件推送。

### 18.5 新視窗驗收順序

1. 在 1366 × 768 像素先確認單一白色抬頭、淡灰員工資訊區上／下／右側貼合外框且左側位置不變；員工下拉只顯示工號；日期、訂單與款號寬度緊湊；工序號輸入框只容納兩位數且抬頭完整，工序名稱另框顯示；輸入區靠左且右側保留空間；確認右側儲存按鈕及深藍區域已移除。
2. 依工號、日期、訂單、款號、工序號及生產數量測試 Tab（欄位切換鍵）與 Shift＋Tab（反向欄位切換鍵），確認略過全部箭頭、圖示及欄位選單。
3. 測試搜尋下拉的向下鍵展開、上下鍵移動、Enter（確認鍵）選定與 Esc（退出鍵）收回；完整工序號按 Enter（確認鍵）後應直接選定並移到生產數量。
4. 測試日期上鍵前一天、下鍵後一天、日曆選擇及今天上限；任何入口都不得選到明天。
5. 測試已登記數量／訂單數量上限卡：選取工序後在當日紀錄標題右側以大字顯示已登記數量／訂單數量上限；達標使用成功底色，輸入後若預計超量則改為危險底色、顯示雙語超量數字並阻止儲存；點擊卡片後應切到生產紀錄並帶入訂單、款號及工序條件。
6. 測試生產數量按 Enter（確認鍵）只觸發一次儲存；成功後保留日期、員工、訂單與款號，清空工序及數量並回到工序欄。
7. 測試當日表格全部可用欄位可顯示或隱藏；左上全選可一次勾選或取消目前角色可用欄位，部分勾選時顯示部分選取狀態；管理員操作欄仍受權限控制；全部關閉時顯示緊湊空欄提示。切換欄位後，數字抬頭與數值仍須使用同一右側對齊軸。
8. 在生產紀錄查看至少兩個日期，確認同日只在首列顯示日期與星期、下一日有清楚上框線，且篩選、分頁與資料列操作不受影響。
9. 完成 1366 × 768 像素後，再檢查 1280 × 720 與 1920 × 1080 像素；不得出現文字截斷、底框變形、內層水平捲軸或主要操作被遮住。

### 18.6 後續邊界與風險

- 使用者回報的真實畫面問題可以在產能功能範圍內逐項修正；若需要改變共用元件或全域規範，必須先重新列出受影響頁面並取得同意。
- 補充工序、考勤、效率、員工匯入、生產匯出及年度封存仍未開始，不得與本輪畫面驗收一起順手加入。
- `production-entry.js（生產登記頁程式）`目前 907 行，已超過拆分評估提醒，但責任仍集中於同一登記頁；本輪只調整同頁欄位呈現、選單顯示與欄位可見性控制，並沿用既有 Enter（確認鍵）儲存與數量上限流程，不需要為此拆檔。新增不相關責任前仍必須先提出依責任拆分方案。
- 管理員永久刪除仍是測試期間功能；正式上線前必須同時移除畫面入口並恢復禁止實體刪除的安全規則，不能只隱藏按鈕。
- `系統開發交接.md（全系統交接文件）`目前仍保存較早的產能內容，部分搜尋與發布狀態已過時；未取得修改該文件的同意前，以本文件第十八節及目前程式為準，不得依舊交接回復已淘汰行為。

### 18.7 生產紀錄員工姓名同步修正

- 完成內容：生產紀錄的員工欄位依工號查找員工主資料，優先顯示最新姓名；員工主資料不存在時，才使用生產紀錄內的原姓名快照。
- 實際修改檔案：`js/production/production-records.js（生產紀錄頁程式）`、`js/features.js（中央功能載入程式）`、`index.html（主畫面檔案）`及 `tests/production.test.mjs（產能功能測試）`。
- 資料與計算：未修改 `productionEntries（生產明細）`、員工資料、姓名快照、工序累計、效率或績效獎金計算規則；不需要資料轉換。
- 權限、安全、快取與版本：未修改權限、安全規則、雲端索引或資料快取；只提高前端載入版本，避免瀏覽器繼續使用舊程式。
- 驗收結果：77 項非雲端功能／架構測試全部通過；未修改安全規則，因此未重跑 Firebase Emulator Suite（Firebase 本機模擬器）安全測試。
- 待處理事項：由使用者在已登入畫面修改員工姓名後，重新進入生產紀錄分頁，確認同一工號立即顯示新姓名且歷史產量不變。

### 18.8 生產登記當日表格欄位穩定調整

- 完成內容：當日表格採固定欄位配置，訂單與款號各占 12%、工序號占 7%、工序名稱占 29%、生產數量與訂單數量各占 12%、秒數與操作各占 8%；同一欄位的表頭與內容共用相同比例，桌機尺寸調整時不再分別移動。
- 對齊規則：訂單、款號及工序名稱靠左；工序號置中；生產數量、訂單數量及秒數在自己的緊湊欄位內靠右；操作欄恢復正常表格儲存格並固定置中。過長的訂單、款號或工序名稱使用省略顯示，避免撐動其他欄位。
- 實際修改檔案：`styles/features/production.css（產能功能樣式）`、`js/features.js（中央功能載入程式）`及 `tests/production.test.mjs（產能功能測試）`。`js/production/production-entry.js（生產登記頁程式）`既有欄位識別已足夠，本次重新確認後不需修改。
- 資料、權限、快取與安全影響：沒有改變正式資料路徑、讀取查詢、資料格式、數量上限計算、儲存交易、角色權限、安全規則或雲端索引；只提高產能樣式載入版本，避免瀏覽器沿用舊版排版。
- 驗收結果：程式語法與 28 項產能及介面一致性測試全部通過，差異格式檢查通過。本機頁面可載入但沒有已登入狀態，依 Google 登入限制未代替使用者登入；1280 × 720、1366 × 768 與 1920 × 1080 像素的真實資料表格仍待使用者登入後驗收。
- 已知風險：屬產能登記單一畫面的中風險介面調整，樣式已限定在當日表格，不影響款號表、其他產能分頁或共用表格。若實際長文字資料需要完整顯示，可在登入驗收後再調整工序名稱比例，不改動資料邏輯。

### 18.9 工序 0 補充工時（最新本機狀態）

- 操作：工序號輸入 `0` 後立即進入補充工時模式，不需按 Enter（確認鍵）或再點選；工序名稱欄轉為「Lý do / 原因」，生產數量欄轉為「Giờ / 小時」。原因必填且最多 200 字；單筆補充工時只接受 0.5～24 小時，並以 0.5 小時為單位。
- 訂單：返工可保留訂單與款號；其他作業可讓兩者一起留空。若選擇訂單，款號必須同時存在且屬於該訂單；切換訂單仍會清除原款號、工序與輸入值。
- 說明：員工資訊區右側加入「Hướng dẫn / 說明」按鈕，內容依共用長文規則先完整越文、再完整中文，說明輸入方式、工時範圍、訂單款號條件及效率用途。
- 資料：仍使用 `productionEntries（生產登記明細）`，以 `recordType:'supplement'`、`processNo:'0'`、`supplementReason（補充原因）`及 `supplementHours（補充工時）`保存。補充工時不寫入 `quantity（生產數量）`、工序秒數、每小時產能、訂單數量快照或訂單工序識別碼；不新增集合、不轉換舊資料、不新增索引。
- 累計與效率：建立、修改、作廢及永久刪除補充工時都不讀寫 `productionProcessTotals（訂單工序累計資料）`。未來效率計算將有效補充工時直接以 100% 效率加入標準工時分子，不增加考勤分母。
- 紀錄：當日表格與生產紀錄表格新增獨立「Giờ bổ sung / 補充工時」欄；補充原因顯示在工序名稱位置，一般生產數量／訂單數量／工序秒數位置顯示橫線。生產紀錄可修改補充工時、作廢；測試期間管理員可永久刪除。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/production-entry.js（生產登記頁程式）`、`js/production/entry-store.js（生產資料存取程式）`、`js/production/production-records.js（生產紀錄頁程式）`、`js/production/report-store.js（產能報表資料存取程式）`、`styles/features/production.css（產能功能樣式）`、`firestore.rules（Firebase 雲端資料庫安全規則）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`及 `tests/firestore.rules.test.mjs（安全規則測試）`。
- 權限與安全：沒有新增權限代碼或角色預設值，沿用 `productionEntry（生產登記）`與 `productionRecords（生產紀錄）`。安全規則採欄位白名單，拒絕超出工時範圍、非 0.5 單位、訂單款號不配對、多餘生產數量欄位、未授權角色及一般使用者永久刪除；補充工時修改／作廢不允許變動其他識別資料。
- 快取與版本：沒有新增業務資料快取或 `dataVersions（資料版本）`欄位；只提高產能相關按需載入版本，避免瀏覽器繼續使用舊程式與樣式。
- 驗收：全部 78 項非雲端功能／架構測試通過；全部 39 項 Firebase Emulator Suite（Firebase 本機模擬器）安全測試通過。安全測試涵蓋無訂單與有訂單建立、0.5 與 24 小時允許、0.25 與 24.5 小時拒絕、錯誤款號拒絕、修改、作廢、管理員刪除及不建立工序累計。
- 發布狀態：本輪沒有執行 Git（版本控制）提交或推送，也沒有發布 Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）。正式頁面使用工序 0 前，必須先由使用者發布包含本輪內容的安全規則；一般程式推送仍不得自動包含安全規則或本文件。
- 待驗收：在已登入桌機畫面依序確認工序 0 自動切換、原因欄可輸入、小時欄 0.5 單位、說明視窗、訂單款號可選／可留空、Enter（確認鍵）儲存、當日表格及生產紀錄的補充工時顯示；並確認一般工序選取、數量上限與累計完全不受影響。

### 18.10 當日表格與輸入區介面調整（最新本機狀態）

- 當日表格新增「Số lượng mỗi giờ / 每小時數量」欄位及欄位選擇項目。一般生產紀錄只顯示建立登記時已保存的 `hourlyCapacitySnapshot（每小時數量快照）`整數，不依工序秒數重新計算或四捨五入；舊資料缺少快照及補充工時紀錄顯示橫線。
- 工序號與生產數量的資料值使用內容寬度淡藍圓角底框；表頭不套用底框，補充工時紀錄沒有生產數量時仍顯示橫線。
- 當日表格的款號資料使用粗體顯示，只改變款號辨識度，不改變訂單號碼、工序名稱或其他欄位字重。
- 輸入區六個欄位統一由同一個欄位配置控制寬度與 12 像素水平間距；輸入控制跟隨各自欄位寬度，工序號仍維持適合兩位數的窄欄。移除 1366 像素寬度下另一套欄位尺寸，避免桌機縮放跨越條件時整排欄位突然走位；內容區保持靠左，不延伸填滿右側保留空間。
- 當日表格重新分配十個欄位比例，表頭與資料仍使用同一張固定配置表格；保留既有補充工時欄位、欄位選擇、管理員操作欄及數量上限顯示。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/production/production-entry.js（生產登記頁程式）`、`styles/features/production.css（產能功能樣式）`、`js/features.js（中央功能清單）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`及本文件。
- 資料、權限、快取與安全影響：不新增集合、欄位、查詢、計算、寫入、角色權限、安全規則或雲端索引；只讀取既有登記文件內的每小時數量快照並提高按需載入版本。沒有修改 `entry-store.js（生產資料存取程式）`或 `report-store.js（產能報表資料存取程式）`。
- 自動驗收：產能與介面一致性合計 30 項測試全部通過，並完成相關 JavaScript（網頁程式）語法檢查及差異格式檢查。
- 風險與待驗收：屬產能登記單一畫面的中風險介面調整。完成自動測試後，仍需在登入狀態以 1280 × 720、1366 × 768 及 1920 × 1080 像素驗收輸入框對齊、欄位間距、表格底框、每小時數量及欄位選擇；一般工序儲存、工序 0 補充工時、數量上限與刪除操作不得受影響。

### 18.11 父框縮放、表格超寬與工序號 Tab 修正（最新本機狀態）

- 父框縮放：員工資訊、員工工號、姓名、部門、說明按鈕及六個登記欄位都改由父框完整寬度、內容比例與最大合理寬度共同控制。控制項可隨父框縮小，並使用 `min-width:0（允許欄位縮小）`與 `max-width:100%（不得超過欄位）`限制；員工工號輸入仍以 112 像素為最大建議寬度，登記列在寬畫面最多使用 1040 像素，避免內容短卻產生過長輸入框。
- 說明按鈕與視窗：說明按鈕改為圖示、越文、中文上下排列，寬度跟隨自己的欄位且不超出員工資訊底框。工序 0 說明視窗依目前 `.mn（主內容區）`實際可視矩形動態置中；側欄展開／收合、視窗尺寸及瀏覽器縮放改變時，由 `ResizeObserver（尺寸變更觀察器）`與視窗事件重新定位，不使用固定側欄偏移。
- 工序號鍵盤流程：在工序號欄按正向 `Tab（欄位切換鍵）`時，先執行與 Enter（確認鍵）相同的完整工序確認。完全相符的一般工序完成選取後才移到數量；工序 0 進入補充工時後才移到原因；空白或無完全相符工序時留在原欄並顯示錯誤。`Shift＋Tab（反向欄位切換鍵）`仍直接回到上一欄，不強制重新確認。
- 表格寬度：當日表格與生產紀錄表格使用固定最小總寬、父框建議寬度及最大合理寬度，表頭與資料列仍是同一張固定配置表格。所有欄位到達最低可讀寬度後仍放不下時，只在表格資料範圍提供單一水平捲動；表格外框、操作區及整頁不跟著左右走位。生產紀錄的操作欄恢復正常表格儲存格，長員工、訂單、款號及工序文字使用省略顯示，滑鼠停留可查看完整內容。
- 共用規範：`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`已新增「任何控制項不得超出父底框」、表格各欄最小／建議／最大寬度、長文字完整查看方式、表格範圍單一水平捲動，以及說明視窗依主內容可視範圍動態置中的正式規則與驗收項目；後續介面不得再使用會因縮放而超出底框的固定寬度總和。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/production-entry.js（生產登記頁程式）`、`js/production/production-records.js（生產紀錄頁程式）`、`styles/features/production.css（產能功能樣式）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`、`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`及本文件。
- 資料、權限、快取與安全影響：沒有新增或修改集合、文件欄位、查詢、寫入、數量計算、角色權限、安全規則或雲端索引；只提高產能登記、生產紀錄、產能樣式與中央功能清單的前端載入版本，避免瀏覽器沿用舊檔。
- 自動驗收：相關 JavaScript（網頁程式）語法檢查通過，產能與介面一致性合計 30 項測試全部通過，差異格式檢查通過。
- 待使用者驗收：本機頁面可正常載入，但開發代理依 Google 登入限制不得代替使用者登入業務頁，因此仍需由使用者在登入狀態以 1280 × 720、1366 × 768、1920 × 1080 像素及 80%、100%、125%、150% 瀏覽器縮放驗收：全部上方元件不超出底框、側欄切換後說明視窗重新置中、兩張表格只在資料範圍水平移動，以及工序號正向 Tab 完成確認後才移到下一欄。

### 18.12 當日表格凍結表頭動態定位（最新本機狀態）

- 問題：當日表格使用主內容卷軸向下移動時，原本的凍結表頭會在主內容可視上緣下方留下空隙，造成上方露出半列資料；桌機縮放或側欄寬度改變後也可能出現定位偏差。
- 修正：保留當日表格原本的 `thead（表頭區）`，不複製表頭、不建立第二張表格，也不重新計算欄寬。程式依 `.ct（主內容捲動區）`實際可視上緣，只調整原表頭的垂直位移；左右捲動、欄位顯示切換、表頭及資料列仍由同一張表格共同定位。
- 動態更新：主卷軸移動、視窗尺寸、瀏覽器縮放、側欄寬度及表格尺寸改變時重新計算。表格到達底部後，表頭受表格高度限制並跟著離開；離開產能登記分頁時移除卷軸、縮放與尺寸觀察，避免背景工作繼續執行。
- 顯示：凍結表頭使用完整不透明底色與較高顯示層級，避免資料文字從下方透出；凍結位置直接貼齊主內容可視上緣，不再保留可露出半列資料的固定空隙。
- 共用規範：`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`已補充水平捲動表格可在不拆分欄位的前提下動態移動原表頭，並要求功能列、側欄、桌機尺寸及縮放改變後重新定位。
- 實際修改檔案：`js/production/production-entry.js（生產登記頁程式）`、`styles/features/production.css（產能功能樣式）`、`js/features.js（中央功能清單）`、`index.html（主畫面檔案）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`、`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`及本文件。
- 資料與安全影響：沒有修改雲端集合、文件欄位、查詢、寫入、計算、快取、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引；只提高產能登記、產能樣式及中央功能程式載入版本。
- 自動驗收：相關 JavaScript（網頁程式）語法檢查通過，產能與介面一致性合計 31 項測試全部通過，差異格式檢查通過。本機網站可正常載入，但只有 Google（Google 帳號）登入畫面；依登入限制未代替使用者進入業務頁，也未使用繞過登入的測試方式。
- 風險與待驗收：屬產能登記單一畫面的中風險介面修正。仍需由使用者在已登入畫面，以三種桌機尺寸、80%～150% 縮放、側欄展開／收合、欄位顯示切換及表格左右捲動，確認表頭不露出半列且與資料欄位保持對齊。

### 18.13 全系統緊湊模式套用（最新本機狀態）

- 產能登記、產能紀錄與員工資料已接上全系統共用緊湊密度。登記抬頭最小高度為 56 像素、當日表格標題區為 44 像素；操作區、篩選區、區塊標題、空資料提示及一般表格列同步減少多餘內距。
- 六個登記欄位仍維持單列、依內容長度分配與父框同步縮放；日期固定保留完整日期所需寬度，訂單、款號、工序號及數量依內容與上下限調整，工序名稱承接主要剩餘空間。輸入控制仍維持 36 像素高度，不自動改成兩列。
- 當日表格與產能紀錄維持滿寬配置、共用欄位選擇、浮動水平捲軸及原表頭凍結；長文字、補充工時、數量上限、工序號 Tab（欄位切換鍵）確認及 Enter（確認鍵）儲存行為沒有改變。
- 實際修改檔案：`styles/ui-core.css（共用介面核心樣式）`、`styles/features/production.css（產能功能樣式）`、`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`tests/architecture.test.mjs（架構測試）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`、`UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`及本文件。
- 資料、權限、快取與安全影響：沒有新增或修改雲端集合、文件欄位、查詢、寫入、計算、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引；只提高前端樣式與載入版本。
- 自動驗收：`js/features.js（中央功能清單）`語法檢查、差異格式檢查及全部 89 項非雲端功能、架構與介面測試通過。已登入業務資料與實際操作仍須由使用者依第十八節既定三種桌機尺寸完成最後驗收。

### 18.14 當日表格與產能紀錄使用者欄寬試點（最新本機狀態）

- 產能當日表與產能紀錄表加入第一批使用者欄寬調整。使用者可拖曳表頭右側分隔線調整同欄表頭與資料格，雙擊分隔線可依目前表頭與已顯示資料自動符合內容；每欄仍受最小可讀寬度與最大合理寬度限制。
- 產能當日表的訂單、款號、工序號、工序名稱、生產數量、補充工時、訂單數量、工序秒數、每小時數量及操作欄已建立同一份最小、建議與最大寬度來源。隱藏欄位或權限使操作欄不可用時，剩餘欄位的最低總寬會重新計算。
- 自訂欄寬只保存在目前瀏覽器本機；重新進入功能時沿用，欄位選單的「Mặc định / 恢復預設」會同時恢復預設欄位與預設欄寬。拖曳分隔線不會觸發排序、資料儲存或雲端讀取。
- 欄位總寬超過可視範圍時沿用既有浮動水平捲軸；表頭凍結仍使用原表頭，不複製表頭、不建立內層垂直捲軸，也不改變工序號 `Tab（欄位切換鍵）`確認與數量 `Enter（確認鍵）`儲存流程。
- 實際修改範圍：`index.html（主畫面檔案）`、`js/production/production-entry.js（生產登記頁程式）`、`styles/features/production.css（產能功能樣式）`、`js/ui-table-controls.js（共用表格操作程式）`、`styles/ui-core.css（共用介面核心樣式）`、`js/features.js（中央功能清單）`、相關產能與介面測試、共用介面規範及本文件。
- 資料、權限、快取與安全影響：沒有新增或修改雲端集合、文件欄位、查詢、寫入、數量計算、資料版本、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引；瀏覽器本機只保存非敏感的欄寬數值。
- 自動驗收：共用控制的拖曳、雙擊自動符合、保存、恢復預設及排序隔離測試通過；產能功能既有資料、鍵盤與版面測試通過。仍需由使用者在登入狀態以三種正式桌機尺寸驗收真實資料、欄位顯示、管理員與非管理員操作欄、凍結表頭及浮動水平捲軸。

### 18.14.1 共用排序與欄寬操作修正（最新本機狀態）

- 產能當日表與產能紀錄表只在點擊越文標題右側的排序箭頭時切換排序；點擊表頭文字、空白位置及欄寬調整把手都不再觸發排序。
- 原本的長直線調整區改為欄界上的小型左右箭頭把手，靠近時顯示、拖曳時反白目前表頭。欄位最小寬度依完整雙語表頭計算，資料比表頭長時沿用省略與完整內容提示；操作欄仍保留按鈕正常使用所需寬度。
- 欄位選單仍顯示原本的「Mặc định／恢復預設」，功能不變，會同時恢復預設顯示欄位與預設欄寬。
- 實際修改範圍：`js/ui-table-controls.js（共用表格操作程式）`、`styles/ui-core.css（共用介面核心樣式）`、`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、共用與產能介面測試、共用介面規範及本文件。
- 資料、權限、快取與安全影響：沒有修改雲端資料、查詢、計算、鍵盤登記流程、角色權限、安全規則或雲端索引。共用排序、拖曳下限、保存與恢復測試，以及全系統 94 項非雲端回歸測試均已通過；仍需登入後確認真實表頭長度與滑鼠拖曳手感。

### 18.15 考勤與每日效率（2026 年 8 月 10 日最新本機狀態）

- 畫面：分頁順序改為生產登記、生產紀錄、考勤、員工資料。生產登記移除員工列左側重複的「生產登記」區塊，整列使用與表頭相同的淡藍底色，並在部門右側依所選員工及生產日期顯示正常班、加班與總工時；未登記時明確顯示「Chưa chấm công / 考勤未登記」。
- 生產紀錄：員工工號與姓名拆成兩欄；每一列依工號及生產日期顯示同一天的效率。有生產但沒有考勤時顯示考勤未登記，考勤為 0 且有生產時顯示時數異常，一般工序缺少每小時產能快照時顯示缺少標準產能。
- 考勤操作：以日期、部門及工號／姓名搜尋員工，可批次套用正常班與加班，也可逐人修改工時及備註。工時以 0.5 小時為單位，正常班與加班各為 0～24 小時且合計不得超過 24 小時；批次套用後先標示尚未儲存，再由文員一次儲存目前修改。
- 資料：新增 `productionAttendance（產能考勤資料）`，同一日期與工號固定一份文件。寫入交易同時驗證員工、建立操作紀錄並更新 `dataVersions.productionAttendance（考勤資料版本）`；沒有建立重複的效率集合，也沒有修改訂單、款號、訂單工序、工序累計或既有生產登記資料格式。
- 效率：正式公式為 `(一般生產數量÷每小時產能快照＋有效補充工時)合計÷(正常班工時＋加班工時)×100%`。計算時讀取該員工當日全部有效生產紀錄，不受生產紀錄每頁 50 筆限制；同一日資料以每次 200 筆分頁讀完。
- 快取與讀取量：考勤按日期或固定文件按需讀取，不在登入階段讀取整個集合。依 UID（使用者識別碼）隔離的 IndexedDB（瀏覽器本機資料庫）最多保存最近 31 個已讀日期與 300 筆單筆查詢；考勤版本變更只使考勤快取失效。
- 權限與安全：新增 `productionAttendance（產能考勤）`權限，管理員以外全部角色預設關閉。具有生產登記或生產紀錄權限的角色只可讀取摘要與效率所需考勤，只有考勤權限可新增或修改；永久刪除仍只限管理員。規則驗證欄位白名單、工時範圍、0.5 單位、固定識別碼、員工快照、建立者、修改者、修訂次數、資料版本及操作紀錄。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/production-entry.js（生產登記頁程式）`、`js/production/production-records.js（生產紀錄頁程式）`、`js/production/report-store.js（產能報表資料存取程式）`、新增 `js/production/production-attendance.js（產能考勤頁程式）`、新增 `js/production/attendance-store.js（考勤資料存取程式）`、`styles/features/production.css（產能功能樣式）`、`firestore.rules（Firebase 雲端資料庫安全規則）`、`tests/architecture.test.mjs（架構測試）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`、`tests/firestore.rules.test.mjs（安全規則測試）`、本文件及 `系統架構規劃.md（全系統架構規格）`。
- 未修改範圍：沒有修改 `UI設計規範與參照/介面設計規範.md（使用者介面設計規範）`，因本輪直接沿用既有操作面板、雙語文字、表格、欄寬與桌機驗收規則；沒有修改 `firestore.indexes.json（Firebase 雲端資料庫索引設定）`，目前考勤查詢不需要新增複合索引。
- 自動驗收：相關 JavaScript（網頁程式）語法檢查、差異格式檢查、本輪 59 項功能／架構／介面測試及全系統 93 項非雲端回歸測試全部通過；使用 demo（示範）專案執行的全部 41 項 Firebase Emulator Suite（Firebase 本機模擬器）安全測試通過，沒有接觸正式業務資料。
- 待驗收與已知風險：仍需由使用者在已登入桌機畫面確認四個分頁順序、淡藍員工列、考勤摘要、批次與個別儲存、工號姓名拆欄及效率狀態。管理員測試期間若要永久刪除已有考勤的員工，必須先刪除該員工考勤；本輪沒有擴大修改既有員工永久刪除關聯檢查，正式上線前必須連同測試刪除入口一起重新驗收。

### 18.16 綜合搜尋與三個操作區單排調整（2026 年 8 月 10 日本機狀態）

- 生產登記：日期輸入框縮短，原本佔滿右側高度的上下按鈕改成輸入框內的小型上下箭頭；日期切換、未來日期限制及自動日期模式不變。
- 生產紀錄：日期範圍與狀態保留獨立欄位；原工號、訂單、款號與工序四個輸入欄合併成單一「Tìm trong bảng / 表格搜尋」。搜尋可比對目前已載入列中的日期、工號、目前姓名、歷史姓名快照、部門、訂單、款號、工序號、工序名稱、補充原因、生產數量、補充工時、訂單數量、工序秒數、每小時產能、效率及狀態；多個空白分隔文字採全部符合。
- 讀取量：歷史資料仍只依日期範圍每次最多讀取 50 筆並使用既有游標載入更多；綜合搜尋在瀏覽器內篩選目前已載入資料，不因每次輸入文字新增雲端讀取。原本可選的單一員工雲端查詢改為日期查詢後本機綜合篩選，因此查看更多搜尋結果時仍需按既有「載入更多」。
- 考勤與員工資料：兩個淡藍操作面板的輸入欄位與操作按鈕改為同一排；按鈕依父框空間縮小，不得超出外框。員工部門右側的新增與設定按鈕移除，部門下拉底部只留一個管理入口，新增部門仍在原部門管理視窗內完成。
- 共用外觀：啟用中的深藍主要按鈕，其越文與中文都繼承白色文字，避免中文呈現灰色而看不清；停用狀態仍沿用既有淡色處理。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/report-store.js（產能報表資料存取程式）`、`js/production/production-records.js（生產紀錄頁程式）`、`js/production/production-employees.js（員工資料頁程式）`、`styles/features/production.css（產能功能樣式）`、`styles/ui-core.css（共用介面核心樣式）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`及本文件。
- 資料、安全與快取影響：沒有新增或修改集合、文件欄位、資料格式、寫入交易、效率公式、工序累計、角色權限、安全規則、雲端索引或快取範圍；只改變生產紀錄本機篩選方式與桌機版面。
- 自動驗收：相關 JavaScript（網頁程式）語法檢查、產能／介面兩組共 38 項測試及全系統 96 項非雲端回歸測試全部通過；仍需已登入桌機畫面驗收。

### 18.17 全系統表格主視窗浮動捲軸（2026 年 8 月 10 日最新本機修正版）

- 產能當日表沿用全系統單一規則：表格超寬且目前頁面內容不足一個可視畫面時，水平捲軸顯示在整個主內容可視區最下緣，不放在「員工當日生產紀錄」功能框、空資料提示或表格底框內。
- 當產能資料列使主內容垂直超過一個畫面時，頁面剛開啟並位於頂端不顯示浮動捲軸；使用者開始向下捲動後，才在主內容最下緣顯示。回到頂端、表格離開畫面、表格不再超寬或離開功能時隱藏。
- 前一版依資料區或表格框底部定位的程式、樣式及測試已淘汰，不保留相容分支；功能框內的原始水平捲軸由共用控制接管並隱藏，畫面只保留一條水平操作。
- 實際修改範圍：`js/ui-table.js（共用表格程式）`、`styles/ui-core.css（共用介面核心樣式）`、`index.html（主畫面檔案）`、共用介面測試、共用介面規範及本文件。
- 資料、權限、快取與安全影響：沒有修改雲端資料、查詢、計算、鍵盤登記流程、角色權限、安全規則或雲端索引。共用捲軸與介面兩組 27 項測試及全部 98 項非雲端回歸測試通過；仍需登入後確認短內容與長內容兩種實際位置。

### 18.18 員工績效與當日登記操作整合（2026 年 8 月 10 日本機狀態）

- 分頁名稱：原 `Lịch sử sản xuất / 生產紀錄`改為 `Hiệu suất nhân viên / 員工績效`；程式、頁面與權限識別碼保留 `production-records`及 `productionRecords`，不建立第二套權限或資料來源。
- 員工績效：選定日期範圍後整合全部有效生產紀錄與逐日考勤，同一日期、同一員工只顯示一列。搜尋改為工號、姓名與部門；狀態可篩選已計算、考勤未登記、考勤時數異常及缺少標準產能。日期範圍單次最多 31 天。
- 日期外觀：依使用者最後確認恢復原本日期標籤與藍色分隔。每個日期只在第一位員工列顯示日期及星期，同日其他員工列保留空白日期格，下一日期使用藍色上框線區隔。
- 表格欄位：員工工號預設隱藏，需要時由欄位選單開啟；顯示姓名、部門、考勤總工時、有效標準工時、補充工時、當日效率、績效狀態與前往登記。
- 頁面銜接：點擊績效列的「Mở đăng ký / 前往登記」會開啟生產登記，直接帶入該員工與生產日期。績效頁不再提供修改、作廢或永久刪除。
- 生產登記當日表：改為載入有效及已作廢紀錄並顯示狀態。具有原 `productionRecords`權限者可修改有效紀錄或作廢；管理員測試期間另可永久刪除。點擊已登記數量／訂單數量上限卡片時留在原頁，尋找並醒目顯示目前工序。
- 考勤：表格移除畫面上的備註欄並縮窄至桌機可讀寬度；舊 `note（備註）`資料仍保留，文員後續修改工時時會原值寫回，不清除雲端歷史資料。生產登記員工資訊列只顯示考勤總工時，例如 `11.5 giờ / 11.5 小時`，不再顯示正常班與加班算式。
- 管理員測試刪除：保留在實際資料來源功能，即生產登記當日表、考勤與員工資料；員工績效是計算結果，不另設刪除入口。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/report-store.js（產能報表資料存取程式）`、`js/production/production-records.js（員工績效頁程式）`、`js/production/production-entry.js（生產登記頁程式）`、`js/production/production-attendance.js（考勤頁程式）`、`styles/features/production.css（產能功能樣式）`、`tests/production.test.mjs（產能功能測試）`、`tests/ui-consistency.test.mjs（介面一致性測試）`及本文件。
- 資料、安全與快取影響：沒有新增或修改集合、文件欄位、資料格式、效率公式、工序累計、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引。新增的是受 31 天限制的日期範圍讀取介面；當日修改、作廢與管理員刪除仍呼叫既有資料交易及操作紀錄。
- 架構評估：`production-entry.js（生產登記頁程式）`約 1320 行，雖已超過拆分提醒，但本次新增內容仍屬同一當日表操作，資料與計算沒有回塞畫面檔。若後續再加入獨立子流程，必須先提出依介面、表格操作或頁面狀態拆分的方案。
- 自動驗收：四份產能 JavaScript（網頁程式）語法檢查、差異格式檢查、16 項產能專項測試及全部 99 項非安全規則回歸測試通過。尚待使用者在已登入桌機畫面驗收日期分組、全員績效、績效跳轉、當日修改／作廢／刪除、工序醒目顯示、考勤表寬及總工時摘要。

### 18.19 產能桌機版面與工序明細檢視調整（2026 年 8 月 10 日本機狀態）

- 員工績效：表格改為個別緊湊寬度，工號與部門預設隱藏，需要時再由欄位選單開啟；日期、姓名、考勤總工時、有效標準工時、補充工時、當日效率與狀態保留在主要畫面，降低不必要的欄距與水平捲動。
- 工序明細：生產登記的「已登記數量／訂單數量上限」卡片不再捲動或醒目定位單一列。點擊後按需讀取同一 `orderProcessId（訂單工序識別碼）`的全部有效登記，暫時只顯示該訂單、款號與工序的紀錄，包含不同員工及不同生產日期；再次點擊卡片即回到目前員工、目前生產日期的當日表。
- 工序明細讀取：只在使用者點擊卡片時查詢 `productionEntries（生產登記資料）`，使用單一欄位條件並在本機排除作廢紀錄；同一個進行中的查詢共用既有非同步工作，不在登入或開啟頁面時預讀全部資料。沒有新增集合、欄位、複合索引或永久快取。
- 日期控制：生產登記、員工績效的開始／結束日期及考勤日期統一使用日曆按鈕與輸入框內的小型上下箭頭。日期輸入框縮短，但原本日期限制、搜尋與載入時機維持不變。
- 生產登記員工資訊：考勤總工時只顯示數字，例如 `11.5`，不再附加「小時」文字；字體大小與粗細跟員工姓名、部門一致。
- 員工資料：取消與新增／儲存按鈕調整為相同大小；部門下拉底部文字由 `Quản lý bộ phận / 部門管理`改為 `Chỉnh sửa bộ phận / 編輯部門`，功能與資料處理不變。
- 考勤：操作列與表格整體重新分配欄寬，正常工時、加班工時、總工時、狀態及操作欄保持合理間距；表格使用較窄的功能專屬寬度，避免短字欄位造成整頁不必要的水平捲動。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/report-store.js（產能報表資料存取程式）`、`js/production/production-entry.js（生產登記頁程式）`、`js/production/production-records.js（員工績效頁程式）`、`js/production/production-attendance.js（考勤頁程式）`、`js/production/production-employees.js（員工資料頁程式）`、`styles/features/production.css（產能功能樣式）`、`tests/production.test.mjs（產能功能測試）`及本文件。
- 資料、安全與快取影響：沒有修改寫入資料格式、效率公式、工序累計、考勤規則、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或 `firestore.indexes.json（Firebase 雲端資料庫索引設定）`；風險為中低，主要影響產能四個桌機分頁的顯示與工序明細按需讀取。
- 自動驗收：產能專項 16 項測試與相關 JavaScript（網頁程式）語法檢查通過。全系統非安全規則測試目前只有另一視窗正在修改的 `tests/ui-consistency.test.mjs（介面一致性測試）`仍固定檢查舊版產能樣式網址，待該視窗完成或取得重疊修改同意後更新；本視窗未覆蓋該檔案。
- 待桌機驗收：確認四種日期控制外觀、員工績效主要欄位可在一頁閱讀、工序卡片切換全員／全日期明細、再次點擊返回當日表、考勤純數字摘要、考勤表欄距及員工資料按鈕大小。

### 18.20 員工績效日期入口、版面與員工編輯彈窗（2026 年 8 月 10 日本機狀態）

- 員工績效：移除最右側重複的「生產登記」欄，日期方塊改成可操作入口。日期卡使用淡藍、虛線與輕微透明玻璃外觀；點擊後只帶入生產日期並開啟生產登記，不預先指定員工，文員可在該日期重新選擇任何員工。
- 日期分組：同一日期仍只在第一位員工列顯示日期卡；不同日期之間恢復清楚的藍色分隔線。同一天有多名員工時仍全部顯示，不會因日期入口而隱藏其他員工。
- 日期查詢修正：員工績效的開始或結束日期只要使用日曆或上下箭頭改變，就立即依新範圍重新載入。新查詢開始時先清除舊結果，並使用查詢序號防止較慢的舊查詢覆蓋最後選定日期，因此開始與結束均選 10 日時不再保留 8 日、9 日舊列。
- 績效欄位：考勤總工時及員工姓名欄縮窄，使主要資訊向左靠並減少約四分之一的空白；在當日效率右側新增 `Thưởng hiệu suất / 績效獎金`欄。獎金計算規則及金額來源尚未確認，本階段固定顯示 `—`，不得自行推算或寫入資料。
- 日期與考勤版面：生產登記、員工績效與考勤的日期欄縮短為一致的緊湊寬度；績效與考勤操作列使用相同間距。考勤表改為使用完整資料框寬度並重新分配欄位，移除表格右側無意義空白。
- 生產登記空狀態：移除「選擇員工後顯示當日紀錄」大卡片。尚未選員工時保留下方表格結構，不再顯示多餘的空白提示卡。
- 員工資料：新增員工操作列改為左側新增、右側取消。列表的修改按鈕改為開啟共用彈窗，不再把資料帶回頁面上方；彈窗顯示不可變更的工號及狀態，可修改姓名與部門，儲存仍沿用既有員工更新函式。頁面上方表單只負責新增員工，重複工號仍會拒絕，不會覆蓋既有資料。
- 實際修改檔案：`index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/production-records.js（員工績效頁程式）`、`js/production/production-entry.js（生產登記頁程式）`、`js/production/production-employees.js（員工資料頁程式）`、`styles/features/production.css（產能功能樣式）`、`tests/production.test.mjs（產能功能測試）`及本文件。
- 資料、安全與快取影響：沒有新增或修改雲端集合、欄位、寫入格式、效率公式、考勤規則、工序累計、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引；本輪只調整既有讀取觸發、頁面銜接、桌機版面及員工修改入口，風險為中等。
- 架構評估：`production-entry.js（生產登記頁程式）`仍超過 800 行，但本輪只擴充既有頁面銜接的一個日期情境，沒有加入新的不相關責任、資料來源或跨功能內部依賴，因此本輪不進行大規模拆分。員工編輯彈窗仍保留在員工功能程式內，沒有新增零碎共用檔案。
- 自動驗收：三份本輪 JavaScript（網頁程式）語法檢查與 16 項產能專項測試通過。全系統 99 項非安全規則測試中 98 項通過；唯一未通過項目是另一視窗正在修改的 `tests/ui-consistency.test.mjs（介面一致性測試）`仍檢查舊的中央功能網址版本，本視窗依多視窗規則沒有覆蓋該檔案。仍需登入桌機畫面確認日期卡手感、欄距、彈窗內容及考勤表實際寬度。

### 18.21 考勤列底線與績效表滿寬修正（2026 年 8 月 10 日本機狀態）

- 考勤表：操作欄原本受到一般按鈕容器的彈性排列樣式影響，空白操作格沒有維持標準表格儲存格高度，造成同一列右側底線與其他欄位高低不同。考勤操作欄現已固定使用表格儲存格排列，空白與有刪除按鈕時都跟整列同高。
- 員工績效表：移除原本 `1100px（像素）`最大顯示寬度，表格在可用空間足夠時填滿整個資料框，不再留下右側空白。使用者拖曳調整的欄寬仍保存並作為欄寬基準；欄寬總和大於畫面時維持原寬並使用既有水平捲軸，不強制壓縮。
- 實際修改檔案：`styles/features/production.css（產能功能樣式）`、`js/features.js（中央功能清單）`、`index.html（主畫面檔案）`、`tests/production.test.mjs（產能功能測試）`及本文件。
- 資料、安全與架構影響：沒有修改資料讀取、寫入、考勤內容、效率公式、欄寬本機保存格式、角色權限、安全規則或雲端索引；只修正考勤與員工績效兩張桌機表格的顯示方式，風險低。

### 18.22 員工績效個人工序入口調整（2026 年 8 月 10 日本機狀態）

- 日期欄：日期只負責標示及區分日期群組，不再作為生產登記入口。日期卡取消虛線、透明玻璃與點擊效果，恢復一般實線卡片；同一日期仍只在第一位員工列顯示，跨日藍色分隔線維持不變。
- 姓名欄：每一列員工姓名改成淡藍底、虛線與輕微透明玻璃樣式的個人工序入口。點擊姓名會同時帶入該列工號與生產日期後開啟生產登記，因此同一天有多位員工時可分別查看各自的當日工序。
- 實際修改檔案：`js/production/production-records.js（員工績效頁程式）`、`styles/features/production.css（產能功能樣式）`、`js/features.js（中央功能清單）`、`index.html（主畫面檔案）`、`tests/production.test.mjs（產能功能測試）`及本文件。
- 資料、安全與架構影響：沿用既有生產登記頁的員工與日期帶入介面，沒有新增雲端查詢、集合、欄位、寫入、快取、效率公式、權限、安全規則或雲端索引；只調整員工績效畫面的入口位置與外觀，風險低。

## 十九、新視窗接手摘要（2026 年 8 月 10 日）

### 19.1 接手範圍與正式來源

- 新視窗主線仍是「產能登記」，包含 `productionEntry（生產登記）`、`productionRecords（員工績效）`、`productionAttendance（考勤）`及 `productionEmployees（員工資料）`四個分頁。不得順便修改訂單、款號、裁帶、成本或其他功能架構。
- 接手前必須先閱讀 `AGENTS.md（全案代理規則）`、`系統架構規劃.md（全系統架構規格）`及本文件；產能最新規則以本文件第十八、十九節與目前程式為準，不得引用 `系統開發交接.md（全系統交接文件）`較早的產能第一階段內容覆蓋新狀態。
- 最新已推送的產能程式版本為 `e798c56（員工姓名個人工序入口版本）`，已存在 `main（主分支）`及遠端。其後若出現其他功能提交，必須保留，不得為了回到產能版本而重設或還原分支。
- 本文件是本機功能文件，一般程式推送預設排除；除非使用者另外明確要求推送文件，否則只在本機持續更新。

### 19.2 四個分頁目前狀態

- 生產登記：文員選擇生產日期後，以工號、訂單、款號及工序快速輸入。訂單、款號、工序必須依現有資料逐層限制；工序號 `0`進入補充工時，原因必填、工時以 0.5 小時為單位且單筆為 0.5～24 小時。當日表格提供既有紀錄的修改、作廢及管理員測試刪除；點擊工序累計卡片可切換查看同工序的全部有效登記，再次點擊返回目前員工當日資料。
- 員工績效：依日期範圍整合有效生產與考勤，同一日期、同一工號只顯示一列。日期只負責日期群組顯示，不可點擊；跨日使用藍色分隔線。每一列姓名使用淡藍底虛線入口，點擊時帶入該列工號與生產日期，開啟該員工當日生產登記。工號與部門預設隱藏，需要時由欄位選單開啟。
- 考勤：依考勤日期、部門與員工搜尋顯示正常工時、加班工時、總工時、狀態與操作；支援批次套用及逐人修改，工時以 0.5 小時為單位。畫面已移除備註欄，但舊備註值仍由資料程式原樣保留。管理員測試期間保留永久刪除入口。
- 員工資料：工號是不可變且唯一的正式識別碼，新增相同工號必須拒絕，不能覆蓋。姓名與部門由編輯彈窗修改；停用員工保留工號與歷史關聯。部門使用下拉選擇，下拉底部只有「Chỉnh sửa bộ phận / 編輯部門」入口，新增、改名、停用與測試刪除都在部門編輯視窗處理。

### 19.3 不得改變的資料與計算規則

- 生產、效率及未來績效獎金的員工關聯一律使用 `employeeId（員工工號）`；姓名與部門只供畫面及報表顯示，不得作為資料關聯、工序累計或計算條件。修改姓名後，歷史資料仍屬於原工號並顯示目前姓名；員工資料不存在時才使用登記快照作為顯示備援。
- 每次生產登記保存獨立明細；`productionProcessTotals（訂單工序累計）`依訂單工序統計不同員工、不同日期及不同時間的全部有效數量，交易必須防止有效累計超過訂單工序數量。作廢或永久刪除有效生產時必須同步釋放累計量。
- 一般生產數量只接受正整數；補充工時不寫入生產數量或工序累計。一般工序的效率標準使用建立登記時保存的 `hourlyCapacitySnapshot（每小時產能快照）`，不得改用工序秒數重新推算。
- 當日效率公式固定為「一般生產數量÷每小時產能快照」加上有效補充工時，再除以正常班與加班的考勤總工時，最後乘以 100%。補充工時以 100% 效率計入有效標準工時；沒有有效考勤時不得顯示正常效率。
- `Thưởng hiệu suất / 績效獎金`欄目前只顯示 `—`。獎金級距、金額來源、四捨五入、上限、結算週期與作廢後重算方式尚未確認，新視窗不得自行建立公式或寫入雲端資料。

### 19.4 最近完成內容

- 員工績效日期卡已取消點擊、虛線與透明玻璃效果，恢復一般實線卡片；同一天多名員工仍共用一個日期標籤。
- 淡藍底、虛線與輕微玻璃外觀已移到每一列員工姓名。點擊姓名呼叫既有 `PCMSProductionEntry.setPendingContext（設定生產登記待帶入內容）`，同時傳入 `employeeId（員工工號）`與 `productionDate（生產日期）`，不新增第二套查詢或頁面狀態。
- 本次已修改並推送 `index.html（主畫面檔案）`、`js/features.js（中央功能清單）`、`js/production/production-records.js（員工績效頁程式）`、`styles/features/production.css（產能功能樣式）`及 `tests/production.test.mjs（產能功能測試）`；沒有推送本文件，也沒有修改或發布安全規則與雲端索引。

### 19.5 尚待處理與上線前提醒

- 優先由使用者在已登入桌機畫面驗收：日期卡不可點擊、每位姓名都有個人工序入口、同一天三位員工可分別開啟正確工號與日期、跨日藍線仍正常、長姓名不破壞欄寬。
- 績效獎金計算尚未開始；必須先由使用者確認正式規則，再依現有員工工號、日期、考勤及有效生產資料設計，不得只依姓名或畫面百分比計算。
- 管理員永久刪除目前是測試用途，生產登記、考勤、員工及部門相關入口仍可能開放；正式上線前必須由使用者另行確認哪些刪除入口關閉、哪些只保留停用／作廢，不能由新視窗自行關閉或擴大刪除。
- 員工與生產資料批次匯入、正式匯出、長期封存及年度資料清理尚未完成；若開始規劃，必須先確認資料量、保留年限、操作紀錄、回復方式及安全規則，不得直接批次刪除雲端資料。
- `production-entry.js（生產登記頁程式）`已超過 800 行。小型錯誤與同頁操作可在原功能內修正；若再增加獨立子流程，必須先評估依畫面、資料或表格操作拆分，不得繼續加入不相關責任。

### 19.6 驗收與工作區狀態

- 最新一輪已通過 `tests/production.test.mjs（產能功能測試）`16 項、`tests/architecture.test.mjs（架構測試）`19 項、相關 JavaScript（網頁程式）語法檢查及差異格式檢查。
- 獨立本機預覽可載入登入畫面，但沒有使用者的已登入狀態；依 Google（Google 帳號）登入限制未代替使用者登入，也未讀取正式業務資料，因此真實員工資料的桌機視覺與點擊結果仍以使用者驗收為準。
- 工作區目前同時有其他視窗修改 `AGENTS.md（全案代理規則）`、介面規範、安全規則、共用測試與其他文件。新視窗開始前必須重新執行檔案狀態與差異檢查，只修改使用者指定的產能檔案，保留所有其他視窗內容。
- 一般程式推送必須排除所有 `MD（標記文件）`、`firestore.rules（Firebase 雲端資料庫安全規則）`及 `firestore.indexes.json（Firebase 雲端資料庫索引設定）`；只有使用者針對文件、安全規則或索引另外明確授權後才可處理。

### 19.7 新視窗開始順序

1. 完整讀取 `AGENTS.md（全案代理規則）`、`系統架構規劃.md（全系統架構規格）`及本文件第十八、十九節。
2. 檢查 `git status（版本狀態）`、目前分支與最近提交，確認 `e798c56（員工姓名個人工序入口版本）`仍在歷史中，並辨識其他視窗尚未提交的檔案。
3. 先聽取使用者的新問題並只記錄，不得看到交接中的待辦就自行開發；使用者要求開始修改後，再列出確切檔案、目的、影響與風險並取得同意。
4. 修改前重新讀取本文件與相關程式最新差異；完成每一批產能修改後立即同步更新本文件，但一般程式推送仍不包含本文件。

### 19.8 本視窗最後補充與表格共用分工（2026 年 8 月 10 日）

- 長表格水平操作問題：產能紀錄資料很多時，原始水平捲軸位於表格實際底部，使用者必須先用主卷軸拉到最下方才能左右移動。正式改善方向是沿用介面規範的唯一共用浮動水平捲軸：只要超寬表格有任何部分位於主內容可視區，就在主內容可視區最下緣顯示並與原表格左右位置雙向同步；表格沒有超寬、完全離開畫面或功能關閉時隱藏，不建立第二條上下卷軸，也不得遮住「再載入 50 筆」等底部操作。此問題已記錄，尚未由本視窗修改程式。
- 產能表縮放問題：款號總表在寬畫面或較小瀏覽器縮放比例下會填滿資料框，但生產登記當日表仍維持固定內容寬度並靠左，造成右側大片空白，兩者使用不同縮放規則。正式改善方向是讓產能表在空間足夠時至少填滿父框；訂單、工序號、數量、秒數及操作等短欄維持最小／建議／最大合理寬度，剩餘空間優先分配給工序名稱等長文字欄，不得把全部欄位平均無限制拉寬；畫面不足最小總寬時才由共用浮動水平捲軸控制。此問題已記錄，尚未由本視窗修改程式。
- 分工界線：使用者已另外建立「全系統表格共用」任務，統一處理浮動水平捲軸、表格滿寬與縮放、欄寬配置、排序、欄位選擇及凍結表頭等共同行為。新的產能視窗先繼續記錄與處理產能專屬畫面問題；除非使用者重新指定，不得在產能程式內另做一套共用表格控制，也不得覆蓋表格共用任務正在修改的介面規範、`tests/ui-consistency.test.mjs（介面一致性測試）`或 `tests/ui-table.test.mjs（共用表格測試）`。
- 版本與驗收：目前 `main（主分支）`及遠端最新整體提交為 `ee225b8（裁帶 PDF 頁碼版本）`，最新產能提交 `e798c56（員工姓名個人工序入口版本）`仍完整保留在歷史中；本次確認產能相關程式沒有未提交差異，並重新執行 `tests/production.test.mjs（產能功能測試）`，16 項全部通過。這不取代登入後三種桌機尺寸、側欄展開／收合、80%～150% 縮放及實際操作驗收。
- 本次交接只更新本文件，沒有修改 `AGENTS.md（全案代理規則）`、產能程式、資料讀寫、快取、計算、角色權限、Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）或雲端索引，也沒有執行 Git（版本控制）暫存、提交或推送。

## 二十、2026 年 8 月 12 日快取與生產分析增量更新

- 考勤快取由「最多 31 日／300 筆、版本改變整批失效」改為每個日期與每筆員工考勤獨立保存；修改一天不再讓其他日期重新下載。
- 生產登記與生產紀錄查詢加入依 UID（使用者識別碼）隔離的日期／條件快取；相同資料版本與條件再次開啟時直接使用本機結果。
- 產能與考勤成功新增、修改、作廢或刪除後，額外建立 `productionDayChanges（產能日期變更標記）`，讓生產分析只補讀受影響的員工日期。標記寫入採容錯處理，失敗不會阻止原正式交易。
- 登出不再清除產能、考勤及生產分析快取；不同 UID、電腦、瀏覽器設定檔及網站來源仍完全分開。失去權限或帳號停用時不得顯示。
- 實際修改檔案：`js/data-cache.js（資料快取程式）`、`js/firebase.js（雲端資料庫核心程式）`、新增 `js/production/change-store.js（產能變更標記程式）`、`js/production/attendance-store.js（考勤資料存取程式）`、`js/production/entry-store.js（產能登記資料程式）`、`js/production/report-store.js（生產紀錄查詢程式）`、`js/production/production-attendance.js（考勤畫面程式）`、`js/production-analysis/analysis-store.js（生產分析資料程式）`、中央載入、測試、安全規則及本文件。
- 安全影響：新增變更標記集合的欄位、操作者、功能權限及禁止刪除驗證；Firebase Emulator Suite（Firebase 本機模擬器）測試已通過。新規則尚未由本次工作發布，發布前分析會在無法取得可靠標記時安全退回完整讀取。
- 回復方式：可停止建立變更標記並讓分析回到完整版本讀取；既有產能與考勤格式沒有轉換或覆寫，不需回復正式資料。

## 二十一、2026 年 8 月 12 日分析異常定位考勤

- 考勤頁新增接收「日期＋工號」的畫面定位入口，供員工分析的「考勤時數異常」快捷處理使用。
- 開啟後套用指定日期、清除部門篩選、填入員工工號，載入完成後捲動到該員工並將游標放在正常工時欄位；只做畫面定位，不會自動新增、修改或儲存考勤。
- 員工沒有考勤頁權限時不顯示可點擊入口；本次不修改考勤資料格式、雲端查詢、快取、計算、權限內容或安全規則。
- 實際修改程式：`js/production/production-attendance.js（考勤頁程式）`、`js/production-analysis/employee-analysis.js（員工分析頁程式）`及中央載入版本；相關測試與本文件同步更新。
- 驗收結果：考勤定位包含在 17 項生產分析測試及 130 項全站非雲端安全規則測試內，全部通過。

## 二十二、2026 年 8 月 12 日當日生產紀錄欄位順序

- 「員工當日生產紀錄」的訂單數量移到款號與工序號之間，主要順序改為「訂單、款號、訂單數量、工序號、工序名稱」。
- 表頭、欄位選單及每筆資料列使用相同順序；既有欄位顯示／隱藏、排序與拖曳欄寬功能維持不變。
- 本次只改畫面欄位排列與載入版本，不修改生產資料、訂單數量快照、公式、快取、權限、安全規則或雲端查詢。
- 實際修改程式：`index.html（主畫面檔案）`、`js/production/production-entry.js（產能登記頁程式）`及`js/features.js（中央功能清單）`；`tests/production.test.mjs（產能功能測試）`與本文件同步更新。
- 驗收結果：產能、架構及介面一致性共 61 項測試全部通過，並確認表頭、欄位選單設定及資料列順序一致。

## 二十三、2026 年 8 月 12 日當日生產紀錄緊湊排版

- 依照短欄位的實際文字與數值長度，重新縮小訂單、款號、訂單數量、工序號、生產數量、補充工時、工序秒數、每小時數量、狀態及操作欄；工序名稱保留主要閱讀空間。
- 表頭與資料格左右內距由 10 像素縮為 6 像素，上下內距由 5 像素縮為 4 像素；工序號／生產數量標籤、狀態標籤及操作按鈕同步縮小，但仍保留可辨識文字與操作範圍。
- 當日模式的最低欄寬合計由約 1,310 像素降為約 1,126 像素，減少高縮放比例下不必要的水平捲動；工序明細模式仍保留日期、工號及姓名所需欄寬。
- 本次只修改「員工當日生產紀錄／工序登記明細」的桌機排版與載入版本，不修改資料、公式、欄位功能、權限、安全規則或其他功能表格。
- 實際修改程式：`js/production/production-entry.js（產能登記頁程式）`、`styles/features/production.css（產能樣式）`、`js/features.js（中央功能清單）`及`index.html（主畫面檔案）`；相關測試與本文件同步更新。
- 驗收結果：61 項產能、架構及介面相關測試，以及 130 項全站非雲端安全規則測試全部通過；程式語法與檔案差異檢查亦通過。

## 二十四、全域語言顯示模式套用（2026 年 8 月 12 日）

- 產能登記、當日生產紀錄、產能紀錄、出勤與員工頁的按鈕、狀態、錯誤、空資料、星期、部門選項及表頭已統一套用中央「雙語、只顯示越文、只顯示中文」模式。
- 產能功能仍在實際開啟頁面時按需載入；中央語言功能不會在登入時預先讀取產能資料。按需載入後才建立的動態文字也會自動套用目前模式。
- 單語模式縮減雙語表頭與控制列預留空間，但保留輸入、工序操作、日期、工號、姓名及主要動作的安全最低尺寸；切換模式不覆蓋產能表格的自訂欄寬。
- 本次只修改顯示文字與介面尺寸，不修改生產日、效率、數量、秒數、工序、出勤計算、匯入、雲端資料、權限或安全規則。
- 產能、共用語言、表格、架構及介面相關自動測試已通過；仍需登入後以三種語言模式及三種正式桌機尺寸完成高頻登記操作驗收。

## 二十五、全域語言殘留修正（2026 年 8 月 13 日）

- 產能登記的動態表頭、輸入提示、滑鼠提示及輔助操作名稱改用中央語言文字來源，修正只顯示越文或只顯示中文時仍殘留另一語言的情況。
- 當日紀錄及其他動態建立表格會在每次重新整理表頭時確認越文與中文節點；切換語言不覆蓋欄位顯示、排序或使用者保存的欄寬。
- 本次只調整顯示文字及共用表頭處理，不修改生產數量、工時、效率、日期、工序、匯入、資料讀取、快取、權限、安全規則或雲端索引。
- 11 個本次介面程式通過語法檢查，連同全站功能、架構、介面、語言及表格共 142 項非雲端自動測試全部通過；仍需使用者在登入後以真實資料確認長表頭與高頻登記畫面。

## 二十六、工序修改與訂單快照（2026 年 8 月 13 日）

- 「工序修改」為產能主功能內的獨立功能頁，不塞入產能登記子分頁。舊「工序秒數同步」入口、畫面、`js/sync.js（舊同步程式）`與 `styles/features/sync.css（舊同步樣式）`已移除，不沿用舊頁面設定。
- 新頁使用 `js/production/process-edit.js（工序修改畫面）`、`js/production/process-edit-store.js（工序修改資料）`與 `styles/features/production-process-edit.css（工序修改樣式）`，並由 `js/features.js（中央功能清單）`按需載入。
- 選擇款號後會列出已確認群組的其他尺寸；可切換檢視某個尺寸，並獨立勾選要一起套用的款號。新增、刪除、調整順序、名稱、類別與秒數都使用同一份工序表驗證。
- 正式標準修改會更新被勾選款號的目前正式工序，保留開發原始工序，並列出還在生產的訂單供使用者選擇是否同步新快照。
- 單張訂單例外只修改該張訂單，不改款號正式標準、其他訂單或未來訂單。訂單快照修改使用 `processEditJobs（工序修改工作）`鎖定單張訂單、分批更新與失敗重試；鎖定期間不允許新增該單產能紀錄。
- 訂單快照同步前的產能紀錄繼續保留當時秒數與每小時產能；同步後的新紀錄才使用新快照，不回頭改寫歷史效率。
- 新權限 `productionProcessEdit（工序修改）`對非管理員預設關閉；新資料路徑、訂單鎖定、款號版本及操作紀錄均已納入本機安全規則與允許／拒絕測試；雲端規則尚未由本批發布。

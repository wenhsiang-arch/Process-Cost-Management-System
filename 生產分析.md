# 生產分析功能規格

最後更新：2026 年 8 月 13 日

## 一、文件用途與目前狀態

- 功能名稱：`Phân tích sản xuất / 生產分析`。
- 功能位置：主功能「產能」分類下，與 `Ghi nhận sản lượng / 產能登記` 同層，不放入產能登記的四個分頁內。
- 內部分頁：`Phân tích nhân viên / 員工分析`、`Phân tích IE / IE 分析`、`Hiệu suất bộ phận / 部門效率`。
- 目前狀態：本機程式、公式測試、架構測試及 Firebase Emulator Suite（Firebase 本機模擬器）安全規則測試已完成；尚待使用者以已登入桌機及正式資料驗收畫面，`firestore.rules（Firebase 雲端資料庫安全規則）`也尚未發布。
- 本文件是生產分析唯一功能文件；產能登記、考勤、員工資料及每日績效獎金仍以 `產能登記.md（產能登記功能規格）`為正式來源。

## 二、功能邊界

本功能只讀取既有產能資料並在瀏覽器計算分析結果，不修改款號表秒數、不改寫產能登記、不保存每日回推時間，也不計算或發放績效獎金。

- 每日員工績效與獎金：仍留在原 `員工績效`頁，維持簡單呈現。
- 員工分析：供幹部查看員工是否維持自己的歷史狀態，以及在全產線同工序中屬於低、中或高位。
- IE（工業工程）分析：供 IE 找出標準秒數差異較大的工序，安排現場量測與除錯優先順序。
- 部門效率：供幹部查看部門本期綜合效率及與部門歷史平均的差異。
- 波動異常、薪資或時薪、超額獎金與月底獎金結算不在本次範圍。

## 三、資料來源與版本

### 3.1 雲端資料

- `productionEntries（產能登記資料）`：數量、補充工時、款號、工序及登記時的標準秒數／每小時產能快照。
- `productionAttendance（產能考勤資料）`：正常工時及加班工時。
- `productionEmployees（產能員工名冊）`：工號、姓名及目前部門資料；歷史分析優先使用產能及考勤當時保存的部門快照。
- `productionDayChanges（產能日期變更標記）`：保存有異動的員工、日期及資料種類，供分析快取只補讀受影響日期；不保存生產數量或考勤工時。
- `system/dataVersions（資料版本文件）`：使用 `productionEntries`、`productionAttendance`、`productionEmployees`三個版本判斷快取是否有效。
- `operationLogs（獨立操作紀錄）`：只在成功匯出或開啟列印時寫入；動作為 `productionAnalysisExport（生產分析匯出）`或 `productionAnalysisPrint（生產分析列印）`。

### 3.2 工序版本

同一統計工序的識別條件為：

`款號 + 工序號 + 登記時標準秒數快照 + 登記時每小時產能快照`

款號表秒數修改後，新登記使用新快照並開始新的統計版本；舊登記保留舊快照，不混入新版本。計算規則版本為 `production-analysis-v1（生產分析第一版）`。

## 四、共用計算規則

### 4.1 標準有效工時與當日效率

`單筆標準有效工時 = 生產數量 ÷ 登記時每小時產能快照`

`員工當日效率 =（當日標準有效工時合計 + 補充工時合計）÷ 當日考勤總工時 × 100%`

此公式沿用產能登記的正式每日績效公式；工序秒數只用於版本識別與 IE 秒數比較，不另把 15 秒換算成 60 分鐘版本。

### 4.2 每日工序時間回推

`當日可分配生產時間 = 考勤總工時 − 補充工時`

`該工序回推加工時間 = 當日可分配生產時間 × 該工序標準有效工時 ÷ 當日全部工序標準有效工時`

每日回推時間只在後台計算，不顯示於員工或 IE 表格。大量資料可以降低偶然差異，但不能證明同一天一起生產的各工序分攤一定等於真實時間，因此 IE 建議秒數仍需現場查核。

### 4.3 回推秒數

`單筆回推秒數 = 該工序回推加工時間 × 3,000 秒 ÷ 生產數量`

使用 3,000 秒，是因款號表每小時產能以 50 分鐘有效生產時間計算。例如標準秒數 15 秒、每小時產能 200，若常規回推為 18 秒，IE 確認正確後可直接把款號表標準秒數改為 18 秒，不必再換算成 60 分鐘版本。

### 4.4 全產線常規值

- 1 名員工：暫時顯示該員工資料。
- 2～9 名員工：取所有員工效率或回推秒數的中位數。
- 10 名以上員工：依個人同工序效率排序，排除最高 20%及最低 20%，平均中間 60%。

極端強者與長期低效率者仍保留於個人歷史，不直接拉高或拉低全產線常規值。

### 4.5 員工高、中、低位置

直接比較該員工同工序表現與該工序全產線常規效率：

- 低：低於全產線常規效率的 90%。
- 中：介於全產線常規效率的 90%～110%。
- 高：高於全產線常規效率的 110%。

這是相對位置，不使用固定 40%～120%人員範圍，也不以員工人數決定是否顯示。

### 4.6 可信度

可信度只依該工序目前版本的累積標準有效工時判定，不以員工數或有效生產日作為門檻；無論資料多少都顯示結果、員工人數及累積有效工時。

- 低：少於 20 小時。
- 中：20 小時以上、少於 50 小時。
- 高：50 小時以上。

模擬參考值代表回推秒數落在實際值 ±10%內的預估機會，不是正確率保證：

| 累積有效工時 | 預估比例 |
|---:|---:|
| 5 小時 | 約 58% |
| 10 小時 | 約 70% |
| 20 小時 | 約 80% |
| 30 小時 | 約 85% |
| 50 小時 | 約 93% |
| 100 小時 | 約 97% |
| 200 小時 | 約 99% |

表格中的其他工時使用相鄰兩點做直線估算，例如顯示 `可信度中（約 76%）`；少於 5 小時顯示低且小於約 58%，超過 200 小時最高顯示約 99%。

## 五、分頁內容

### 5.1 員工分析

主表每位員工每天只顯示一列，主要欄位為：展開、日期、員工、當日效率、個人歷史平均及與平常相比；個人分析不顯示部門，也不在表格內重複顯示算法欄。

展開員工每日主列後顯示當日完整工序明細：款號／工序、數量、當日工序效率、該員工該工序歷史平均、該工序全產線常規平均及高／中／低位置。

- 個人歷史使用歷史累計有效工時除以歷史累計考勤工時，不直接平均每天百分比。
- 同工序個人歷史只使用該員工、該款號工序及相同秒數／產能版本的先前資料。
- 可依日期、員工、款號／工序、低於自己歷史及高／中／低位置篩選；款號／工序或位置篩選會保留符合條件的員工每日主列及工序明細。
- 沒有考勤、產能或標準產能時，畫面顯示實際原因，不以錯誤的 0%代替，也不列入與歷史的效率比較。
- 「考勤時數異常」在具備考勤頁權限時可直接開啟該日考勤並篩選、定位到該員工；「未登記產能」在具備產能登記權限時可直接開啟該日產能登記並選好該員工。沒有目標頁權限時維持一般狀態文字，不提供跳轉。
- 「與平常相比」的黃色或綠色標籤只包住文字內容，字級與一般表格一致，不填滿整個儲存格。
- 畫面合併為員工每日主列，Excel（表格檔）匯出及列印仍逐筆保留完整工序明細。
- 不顯示每日工序回推加工時間或個人回推秒數。

### 5.2 IE 分析

主要欄位：款號／工序、目前標準秒數、常規回推建議秒數、差異秒數／百分比、全部綜合效率、常規效率、累積有效工時、員工人數、可信度及優先級。

- 預設只顯示建議秒數與目前秒數差異至少 15%的工序，使用者可修改門檻。
- 差異絕對值 30%以上標示高優先，其餘符合門檻者標示中優先。
- 可依日期、部門、款號／工序、差異方向及可信度篩選。
- 可勾選部分工序；列印或匯出時有勾選就只處理勾選資料，未勾選則處理目前全部篩選結果。
- 列印查核表另留「實測秒數、原因、建議、查核人／日期」手寫欄位。
- 建議秒數只是 IE 現場除錯參考，系統不自動修改款號表。

### 5.3 部門效率

`部門效率 =（部門標準有效工時合計 + 部門補充工時合計）÷ 部門考勤工時合計 × 100%`

- 先加總工時再計算，不平均個人百分比。
- 保留全部員工，不排除效率特別高或特別低的人員。
- 顯示本期效率、所選開始日期以前的歷史平均、差異、員工人數及各類工時。
- 可依日期與部門篩選；不判定部門高／中／低。

## 六、說明、匯出與列印

- 三個分頁的表格不顯示說明／算法欄，也不在每列重複放置查看算法按鈕；所有使用者說明與中文公式集中在各分頁上方的 `Hướng dẫn / 使用說明`。
- 使用說明以一般使用者能理解的方式解釋結果用途，公式區再用中文數學式說明資料來源與計算方式。
- Excel（表格檔）匯出在產生檔案前先要求使用者選擇檔名與儲存位置；取消即停止，不建立空白檔，不支援位置選擇的瀏覽器會提示並停止。
- 匯出檔包含報表工作表與 `Cách tính / 計算說明`工作表，後者保存計算版本、公式、常規值方法、可信度表及使用限制。
- 列印使用瀏覽器列印視窗；成功開啟匯出或列印後建立不可修改的 `operationLogs（獨立操作紀錄）`。

## 七、載入、快取與讀取量

- 登入時不載入生產分析程式或歷史資料，只有實際開啟本功能才按需載入。
- `productionAnalysis（生產分析快取範圍）`使用依 UID（使用者識別碼）隔離的 IndexedDB（瀏覽器本機資料庫），快取結構為 `production-analysis-cache-v2`。
- 第一次開啟、快取損壞或無法證明變更標記完整時，以每批 200 筆游標分頁建立完整 `productionEntries（產能登記）`與 `productionAttendance（產能考勤）`歷史基準。
- 資料版本相同時直接使用快取；產能或考勤版本改變時，讀取上次游標以後的 `productionDayChanges（產能日期變更標記）`，只重讀受影響的員工日期並取代該日舊資料。變更超過 500 個員工日期、標記讀取失敗或標記不存在時，安全退回完整讀取。
- 產能、考勤及分析是使用者確認的內部非敏感作業資料；快取依 UID、電腦、瀏覽器設定檔及網站來源隔離，登出與重新啟動電腦後保留。換帳號不會共用，失去權限或帳號停用時不得顯示。
- 這仍是跨年度稀有工序的受控完整基準；資料量明顯增長時必須改為月份封存或雲端彙總，不得長期依賴反覆完整讀取。
- 畫面表格每頁最多 50 筆；相同載入工作共用同一個 Promise（非同步工作承諾）。

## 八、權限與安全

- 權限代碼：`productionAnalysis（生產分析權限）`。
- 管理員可使用；課長、文員、開發、生管、業務及未來非管理員角色預設關閉，必須由管理員明確開啟。
- 缺少 `productionAnalysis`欄位的舊角色權限文件安全視為關閉。
- 有此權限者只能讀取 `productionEmployees`、`productionEntries`、`productionAttendance`及`productionDayChanges`，不能透過分析功能新增、修改或刪除正式產能與考勤資料。
- 有此權限者可建立及讀取自己功能所屬的匯出／列印操作紀錄；操作紀錄建立後不可修改或刪除。
- 本機安全測試已驗收管理員、全部五種現行非管理員角色、未登入、停用帳號、未核准帳號、淘汰角色、停用角色設定、缺少新欄位及禁止產能資料寫入。

## 九、程式與檔案責任

- `js/production-analysis/analysis-calculations.js`：純計算公式。
- `js/production-analysis/analysis-store.js`：版本、雲端讀取與 IndexedDB 快取。
- `js/production/change-store.js`：跨產能與分析使用的員工日期變更標記存取；正式產能交易失敗時不建立標記，標記失敗時不阻止正式交易。
- `js/production-analysis/analysis-export.js`：Excel（表格檔）匯出、列印與操作紀錄。
- `js/production-analysis/employee-analysis.js`：員工分析分頁。
- `js/production-analysis/ie-analysis.js`：IE 分析分頁。
- `js/production-analysis/department-analysis.js`：部門效率分頁。
- `js/production-analysis/production-analysis.js`：主畫面、三分頁生命週期及共用說明視窗。
- `styles/features/production-analysis.css`：生產分析專屬桌機樣式。
- `tests/production-analysis.test.mjs`：公式測試。
- `tests/architecture.test.mjs`：中央清單、按需載入、檔案來源及快取架構測試。
- `tests/firestore.rules.test.mjs`：角色與安全規則測試。

## 十、驗收結果與待辦

已完成：

- 11 項生產分析公式、員工每日分組與增量快取規則測試。
- 19 項全系統架構測試，加上 11 項生產分析測試，合計 30 項通過。
- 全系統一般程式、公式、架構及介面測試共 122 項通過。
- 45 項 Firebase（雲端資料庫）安全規則測試通過。
- 全部新增 JavaScript（網頁程式）檔案通過語法檢查。

尚待處理：

- 使用者以已登入桌機與真實資料驗收欄寬、文字長度、篩選結果、列印與匯出內容。
- 正式使用前先發布本次 `firestore.rules（Firebase 雲端資料庫安全規則）`，再發布會讓產能登記同步更新 `dataVersions.productionEntries（產能登記資料版本）`的前端程式；順序不可顛倒。
- 本次不修改 `firestore.indexes.json（Firebase 雲端資料庫索引設定）`，因目前查詢只使用單欄日期排序，不需要新增複合索引。

## 十一、回復方式與已知風險

- 回復時可從中央功能清單移除 `production-analysis`入口並還原生產分析規則；既有產能、考勤及員工資料不需轉換或刪除。
- 本功能不保存分析結果，因此公式調整後重新計算即可，不會留下半份新舊結果。
- 主要已知風險已由「每次資料版本改變都重讀完整歷史」降低為「先補讀受影響員工日期」；但標記缺漏、規則未發布或變更範圍超過安全上限時仍會完整重讀。後續須依系統監控的完整載入次數與實際讀取量決定封存或雲端彙總方案。

## 十二、2026 年 8 月 12 日主選單入口修正

- 修正中央功能清單已登記生產分析，但 `index.html（主畫面檔案）`缺少固定側邊欄入口，導致登入後看不到功能的問題。
- 生產分析入口放在「產能」分類內，與「產能登記」同層級，不放入產能登記的內部分頁。
- 更新 `features.js（中央功能清單程式）`的載入版本，避免瀏覽器沿用修改前的功能清單快取。
- 架構測試新增實際側邊欄入口、位置及載入版本檢查，避免只檢查中央清單而漏掉畫面入口。
- 本次不修改分析公式、正式資料、權限內容或 Firebase（雲端資料庫）安全規則。

## 十三、2026 年 8 月 12 日員工分析畫面調整

- `js/production-analysis/employee-analysis.js`改為每位員工每天一列，展開後查看當日完整工序明細，並移除個人分析的部門篩選與部門欄位。
- `js/production-analysis/analysis-calculations.js`新增員工每日分組、缺資料狀態及與個人歷史比較結果；既有當日效率、個人歷史、工序歷史及全線常規效率公式不變。
- `styles/features/production-analysis.css`新增員工每日主列、展開按鈕與工序明細表的桌機排版，沿用現有系統色彩、雙語表頭與操作元件。
- `js/features.js`及`index.html（主畫面檔案）`更新相關程式與樣式載入版本，避免瀏覽器沿用舊畫面快取。
- `tests/production-analysis.test.mjs`新增員工每日分組、展開明細及缺資料原因測試；架構、生產與介面一致性測試同步更新載入版本。
- 本次不修改雲端資料、權限、安全規則、IE 分析公式或部門效率公式；Excel（表格檔）匯出與列印仍保留完整工序明細。

## 十四、2026 年 8 月 12 日增量快取調整

- 生產分析快取升級為第二版，第一次仍建立完整歷史基準；之後資料版本改變時只重讀有異動的員工日期。
- 產能登記與考勤異動完成後建立日期變更標記；若正式環境規則尚未發布、標記遺失或讀取失敗，分析會退回原本的完整分頁讀取，不會顯示半份資料。
- 登出後保留依 UID（使用者識別碼）隔離的產能、考勤與分析快取，避免每天重新下載跨年度資料。
- 本次不修改員工分析、IE（工業工程）分析、部門效率、回推秒數、常規值、高中低位置或可信度公式。
- 新增增量快取靜態驗收，並與產能、架構、系統監控及 Firebase Emulator Suite（Firebase 本機模擬器）安全規則測試一起執行。

## 十五、2026 年 8 月 12 日分析日期操作統一

- 員工分析、IE 分析及部門效率的開始日期與結束日期，統一使用產能登記既有的日期操作方式。
- 每個日期欄位均提供標題旁日曆按鈕，以及欄位右側的前一天、後一天按鈕；鍵盤上下方向鍵也可切換日期。
- 後一天不得超過當天，日期切換後立即套用原有的畫面篩選，不新增雲端查詢或完整資料重讀。
- 共用日期結構與操作集中於 `js/production-analysis/production-analysis.js`，三個分析分頁只使用同一介面，避免各自保存不同規則。
- `styles/features/production-analysis.css`沿用產能登記的日曆按鈕、日期欄及上下切換按鈕尺寸與顏色，僅處理桌機版畫面。
- 本次不修改生產分析公式、雲端資料、權限、安全規則、快取資料內容、匯出或列印欄位。
- 驗收結果：13 項生產分析測試通過，連同架構、生產及介面一致性測試共 73 項通過；另完成日期按鈕實際切換一天、開啟日曆及不得超過今天的程式驗收。

## 十六、2026 年 8 月 12 日員工工序子表表頭調整

- 員工分析外層主表繼續使用原表頭凍結，方便捲動大量員工資料時辨識欄位。
- 展開員工後顯示的當日工序明細屬於子表，表頭固定留在明細原本位置，不跟隨外層主表凍結或漂浮。
- 修正只套用於員工分析的工序子表，不改變全系統共用表格、IE 分析或部門效率。
- 本次不修改生產分析公式、資料、快取內容、權限或安全規則。
- 驗收結果：14 項生產分析測試通過，連同架構、生產及介面一致性測試共 74 項全部通過。

## 十七、2026 年 8 月 12 日未出勤判定修正

- 考勤正常工時與加班工時合計為 0，且當天沒有一般產能及補充工時，代表員工當日未出勤；員工分析不顯示該員工日，也不納入員工日數、效率平均、匯出或列印。
- 考勤總工時為 0，但當天存在一般產能或補充工時，才顯示「考勤時數異常」，提醒管理人員補正考勤。
- 考勤總工時大於 0 但沒有產能，仍顯示「未登記產能」；有工時與產能時維持原公式計算。
- 本次只修改生產分析的呈現判定，不修改考勤原始資料、產能紀錄、部門與 IE 計算、權限或安全規則。
- 驗收結果：15 項生產分析測試通過，連同架構、生產及介面一致性測試共 75 項全部通過。

## 十八、2026 年 8 月 12 日說明集中與異常快捷處理

- 員工、IE 及部門三個分析表移除表格內的說明／算法欄與逐列查看算法按鈕，所有公式集中到各分頁上方的 `Hướng dẫn / 使用說明`；Excel（表格檔）的獨立計算說明工作表與列印公式附錄仍保留。
- 員工分析的「考勤時數異常」可依權限跳到同日考勤並篩選、定位該員工；「未登記產能」可依權限跳到同日產能登記並選好該員工。跳轉不修改任何資料。
- 「與平常相比」及異常狀態標籤改成依內容寬度顯示，採一般表格字級與緊湊內距，避免底框占滿整格。
- 本次不修改效率、回推秒數、常規值、高中低位置、可信度或部門公式，也不修改雲端資料、權限與安全規則。
- 實際修改程式：`js/production-analysis/employee-analysis.js`、`js/production-analysis/ie-analysis.js`、`js/production-analysis/department-analysis.js`、`js/production/production-attendance.js`、`styles/features/production-analysis.css`、`js/features.js`及`index.html（主畫面檔案）`；測試與本文件同步更新。
- 驗收結果：17 項生產分析測試通過；全站非雲端安全規則測試共 130 項全部通過。Firebase Firestore Security Rules（Firebase 雲端資料庫安全規則）未修改，因此本次不重跑其模擬器測試。

## 十九、全域語言顯示模式套用（2026 年 8 月 12 日）

- 生產分析總頁、員工分析、IE（工業工程）分析及部門分析的資料來源、空資料、可信度、優先順序、狀態及動態表格內容已接入中央三種語言模式。
- 分析結果、數值、效率、回推秒數、高中低位置、常規值、篩選與排序公式不變；只有固定說明與狀態文字依模式顯示。
- 單語模式可縮減雙語標籤與表頭預留空間，但不得壓縮數值、異常標記、說明彈出區或必要操作範圍。
- 相關生產分析與共用語言測試已通過；仍需登入後以三種語言模式確認大量資料、長姓名、空資料及異常說明畫面。

## 二十、全域語言殘留修正（2026 年 8 月 13 日）

- 員工分析、IE（工業工程）分析及部門效率上方的統計摘要，改用中央雙語節點建立，修正單語模式仍同時顯示越文與中文的情況。
- 分析表格的自訂表頭及重新整理後建立的表頭統一由共用表格控制確認語言結構，保留原排序、篩選與欄寬行為。
- 本次不修改分析公式、資料來源、日期篩選、增量快取、匯出、列印、權限、安全規則或雲端索引。
- 11 個本次介面程式通過語法檢查，全站 142 項非雲端自動測試全部通過；仍需登入後以真實大量資料檢查長姓名、空資料、異常狀態及表頭空間。

## 二十一、IE 建議交接至工序修改（2026 年 8 月 13 日）

- IE（工業工程）分析的「確認修正」不直接寫入款號表，只把款號、工序號與回推建議秒數交給獨立的「工序修改」頁。
- 工序修改頁開啟後會自動選定目標款號與工序，但必須由 IE 人員檢查目前正式秒數、群組款號、要套用的尺寸、生產中訂單及修改原因，再正式確認。
- 這個交接只改變後續修改流程，不改動員工分析、IE 回推秒數、全產線常規值、可信度或部門效率公式。

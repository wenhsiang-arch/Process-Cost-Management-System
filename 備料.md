# 備料功能規格

最後整理：2026-09-06

## 1. 功能定位

- 導覽名稱：`Xuất phiếu chuẩn bị vật liệu / 備料出單`。
- 內部模組識別：`preparation（備料）`。
- 主權限：`preparationMain（備料主功能）`。
- 子權限：`cutting（備料出單）`，同時管理裁帶與裁片，不增加第二個勾選。
- 左側以 `Xuất phiếu chuẩn bị vật liệu / 備料出單`作為分類抬頭，下方直接顯示`Xuất phiếu cắt dây / 裁帶出單`及`Xuất phiếu cắt chi tiết / 裁片出單`兩個入口；內容區不重複顯示裁帶／裁片切換列。

顯示名稱的調整不改寫既有權限鍵。裁帶業務與 PDF（可攜式文件）規格以 `工具轉換系統.MD`為準；裁片規格以 `裁片出單.md`為準；本機工具操作分別以 `CUTTING_LOCAL_SERVER.md`及 `PIECE_CUTTING_LOCAL_SERVER.md`為準。

## 2. 程式與載入

- 中央功能登記：`js/features.js`。
- 主畫面入口：`index.html`。
- 裁帶保留既有功能程式、樣式、模板資料與內部分頁，不因新增裁片而改寫。
- 裁片使用獨立 `piece-cutting.js`、`piece-cutting-store.js`及`piece-cutting.css`。
- 使用者進入對應子頁後才按需載入該頁程式；主檔版本也只在實際進入裁片頁後讀取，登入時不得預讀模板。

## 3. 權限與安全

- 非管理員必須同時具有 `preparationMain`與`cutting`才能進入。
- 舊角色權限文件缺少 `preparationMain`或`cutting`時視為關閉。
- 前端隱藏入口不能取代安全規則。
- 模板匯入、覆蓋、刪除及 PDF 匯出必須依正式流程建立 `operationLogs（獨立操作紀錄）`。
- 本文件不記錄某次安全規則是否已發布；發布狀態依當次版本實際比對。

## 4. 資料與快取

- 裁帶雲端模板使用 `cuttingTemplates（裁帶模板）`及其分段資料。
- 裁片雲端主檔使用 `pieceCuttingTemplates/main（裁片主檔版本）`及`pieceCuttingTemplateChunks（裁片主檔分塊）`。
- 裁帶模板與裁片主檔是經核准的「同一台可信任電腦可由具有備料出單權限帳號共用」快取例外。
- 此例外不得延伸到訂單、產能、庫存、帳號或其他業務資料。
- 已建檔模板清單須分開顯示檔名、說明、數量、狀態與操作；完整檔名由滑鼠提示提供。

## 5. 驗收重點

1. 側邊欄以備料出單為分類抬頭，下方直接顯示裁帶出單與裁片出單；兩個入口依同一組既有權限正確顯示、鎖定及標示目前頁面。
2. 管理員與已授權角色可用；缺少任一權限、未授權、停用及未登入帳號均不能存取。
3. 裁帶既有分頁、模板、訂單比對、統計與匯出保持不變；裁片依自己的主檔與輸出規格運作。
4. 裁帶與裁片的雲端模板、本機服務、索引、快取、暫存及輸出不得混用。
5. 雙語、只顯示越文、只顯示中文三種模式均使用中央語言設定。
6. 本機服務只接受核准來源，取消儲存或轉檔失敗不留下不完整檔案。

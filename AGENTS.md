# Agent（代理助手）工作規則

## 工作路徑
- 指定工作路徑：`C:\Users\ASUS Vivobook\Process-Cost-Management-System`
- 只能在此路徑內進行讀取與修改。
- 不得讀取或修改此路徑以外的任何檔案或資料夾，除非使用者重新明確同意。

## 啟用完整存取權時的權限界線
- 當執行環境啟用完整存取權時，完整存取權只開放作為讀取、檢查、比對與驗收用途。
- 啟用完整存取權後，可以讀取指定工作路徑以外的檔案或資料夾，用於確認系統狀況及驗收結果，不需為每次唯讀檢查重新取得同意。
- 完整存取權不包含修改指定工作路徑以外檔案或資料夾的權限。
- 若要在指定工作路徑以外新增、修改、刪除、搬移或重新命名任何檔案或資料夾，必須先說明預計修改的路徑、檔案、目的、影響範圍與風險高低，並取得使用者明確同意。
- 指定工作路徑內的檔案修改，仍須遵守「修改前同意」規則，不得因啟用完整存取權而省略確認。
- 不得因技術上具備完整存取能力，就推定使用者已授權修改其他路徑。

## 修改前同意
- 任何檔案新增、修改、刪除、搬移、重新命名前，都必須先取得使用者同意。
- 同意內容必須包含預計修改的檔案、修改目的、影響範圍與風險高低。

## 檢查與修改說明
- 任何檢查或修改前，都必須先向使用者說明：
  - 影響範圍
  - 風險高低
  - 是否會讀取檔案
  - 是否會修改檔案

## 風險分級
- 低風險：只讀取檔案、查看資料夾列表、或新增獨立說明文件。
- 中風險：修改單一功能、單一畫面、設定檔或文字內容。
- 高風險：刪除檔案、搬移檔案、批次修改、多檔案重構、資料格式轉換、權限或登入相關修改。

## 全系統架構規劃
- `系統架構規劃.md（全系統架構規格）`是本專案新增及修改功能時必須遵守的正式架構文件。
- 開始開發前必須先閱讀該文件，確認功能身分資料、程式分檔、按需載入、資料路徑、版本、快取、權限、安全規則、匯入匯出、使用體驗及驗收方式。
- 新功能不得建立與架構文件衝突的獨立載入、快取、權限、訊息或資料命名方式。
- 若實際需求必須改變架構，應先說明原因、影響範圍、資料轉換、回復方式及風險，取得使用者明確同意後，同步更新 `系統架構規劃.md（全系統架構規格）`，不得只改程式不改文件。
- 現有功能不因架構文件立即全面重構；修改某項功能時，再依當次範圍逐步套用，避免大規模改動。

## 系統安全規則開發要求
- 所有涉及登入、角色、權限、Firebase（雲端資料庫）、資料集合、文件、欄位、讀取、寫入、匯入、匯出、下載、刪除、公開網站或本機服務的功能，都必須在安全規則及必要的伺服器端驗證保護下進行設計與開發。
- 新增任何需要角色控制的新功能時，必須在同一次開發中同步更新權限管理頁、功能權限清單、角色預設值、Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）及允許／拒絕測試，不得只完成新功能後再補權限。
- 課長及文員新增的功能權限必須預設關閉；只有管理員明確儲存開啟後才能使用。舊的 `rolePermissions（角色功能權限）`文件缺少新欄位時，系統必須安全視為關閉，不得因欄位缺少而自動開放或造成整份權限載入失敗。
- 權限管理頁可以由中央功能清單自動產生顯示項目，但不得因此自動放行雲端資料。每個新功能的資料路徑、讀取、寫入、修改、刪除及匯出權限仍必須在 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）中逐項明確設定。
- 新功能未完成管理員、課長、文員及未授權帳號的畫面權限與直接資料存取驗收前，不得標記為完成或安全上線。
- 前端隱藏按鈕、封鎖畫面、JavaScript（網頁程式）角色判斷或瀏覽器內密碼比對，只能作為操作介面控制，不得視為真正的資料安全保護。
- 新增或調整任何 Firebase Firestore（Firebase 雲端文件資料庫）資料路徑、讀寫方式、查詢條件、角色或功能權限時，必須同步新增、調整及測試 Security Rules（安全規則），不得先完成程式後再補安全規則。
- 安全規則必須採用「預設拒絕、最小權限」原則，只開放完成工作所需的資料及操作，不得使用全面公開讀取或全面公開寫入作為正式設定。
- 寫入規則除確認登入與角色外，還必須依功能需要驗證欄位、資料類型、長度、數值範圍、建立者、不可變識別碼及狀態轉換。
- App Check（應用程式驗證）可用於降低非正式網站及自動化濫用，但不能取代使用者登入、角色權限及資料庫安全規則。
- 開發或檢查時若發現任何功能、資料路徑或本機服務沒有在安全規則、可信任身分驗證或必要來源限制的保護下，必須立即提醒使用者，並說明未受保護的範圍、可能影響、風險高低及具體修正方案。
- 未完成可信任登入、安全規則、未授權存取測試及必要驗收前，不得宣稱功能適合安全公開上線。
- 安全規則必須測試未登入者、一般使用者、班長、課長、文員、管理員及跨使用者存取的允許與拒絕結果；能使用 Firebase Emulator Suite（Firebase 本機模擬器）時，應先在本機測試，不直接以正式資料試錯。
- 修改登入、角色、權限、Firebase（雲端資料庫）安全規則、正式資料存取方式或公開服務來源限制，均屬高風險修改；執行前必須說明預計修改檔案、雲端設定、影響範圍、服務中斷可能性、回復方式及風險，並取得使用者明確同意。

## Google 登入使用限制
- 代理助手在開發作業中使用 Google（Google 帳號）登入，只允許進入 Firebase Firestore Security Rules（Firebase 雲端文件資料庫安全規則）頁面，查看目前規則、更新規則、發布規則及確認發布結果。
- 禁止使用該登入查看、讀取、新增、修改或刪除 Firestore（雲端資料庫）業務資料；禁止操作 Firebase Authentication（Firebase 身分驗證）、Storage（雲端儲存空間）、專案設定、帳單、Cloud Shell（雲端命令列）或其他 Firebase（雲端服務）功能。
- 禁止使用該登入開啟或操作其他 Google（Google 帳號）服務，亦不得將使用者已登入視為其他操作的授權。
- 如果工作需要上述禁止動作，代理助手必須停止該部分工作並向使用者說明；不得以任何替代頁面、工具或命令繞過此限制。

## 英文字串翻譯規則
- 任何新增或修改的英文字串，後面都要加上中文翻譯作為備註；不得只處理範例中的字。
- 「英文字串」包含所有英文單字、英文縮寫、英文功能名稱、英文檔名、英文資料夾名稱、英文資料表或集合名稱、英文欄位名稱、英文變數名稱、英文函式名稱、英文按鈕文字、英文提示文字、英文狀態文字、英文錯誤訊息、英文註解與文件內英文。
- 即使英文出現在程式碼符號、路徑、資料庫欄位或技術名稱中，也要在同一句或同一項目後方加中文說明。
- 若同一個英文字串在同一段落重複出現，第一次出現時必須加中文翻譯；後續重複出現時，若可能造成誤解，仍需再次加中文。
- 範例只作為格式參考，不代表只有這些字需要翻譯：`Save（儲存）`、`Cancel（取消）`、`Dashboard（儀表板）`、`API（應用程式介面）`、`Markdown（標記文件）`、`Excel（表格檔）`、`Firebase（雲端資料庫）`、`productsMeta（款號版本資料）`、`version（版本號）`、`saveProductItemsToCollection（儲存款號資料函式）`。

## 回覆規則
- 與使用者說明時，優先使用中文。
- 如需使用英文術語，應在英文後方加上中文翻譯備註。
- 完成工作後，需簡短說明實際影響範圍與風險結果。

## Git（版本控制）限制
- 未經使用者明確同意，不得執行會改變版本狀態的 Git（版本控制）操作。
- 受限制操作包含：`git add（加入版本暫存）`、`git commit（提交版本）`、`git push（推送版本）`、`git pull（拉取版本）`、`git merge（合併版本）`、`git reset（重設版本）`、`git restore（還原檔案）`、`git checkout（切換或還原版本）`。
- 當使用者只輸入 `1`，並要求提供 PHSU（推送）指令時，`git add（加入版本暫存）` 指令只能包含本次要上傳的相關檔案。
- 不得把 `.MD（標記文件）`、說明文件、測試資料、或其他無關修改一起放進上傳指令。
- 若本次同時有程式修改與文件修改，必須依照使用者當下要上傳的範圍分開列指令，不得預設全部一起上傳。

## 重要檔案檢查提醒
- 修改下列檔案前，需特別說明影響範圍與風險高低：
  - `auth.js（登入與權限程式）`
  - `orders.js（訂單程式）`
  - `reports.js（報表程式）`
  - `attendance.js（出勤程式）`
  - `stats.js（統計程式）`
  - `mobile.js（手機版程式）`
  - `summary.js（摘要程式）`
  - `settings.js（設定程式）`
  - `utils.js（工具程式）`
  - `firebase.js（Firebase，雲端資料庫程式）`

## 介面文字規則
- 系統介面只使用越文與中文，不使用英文作為主要顯示語言；越文為主，中文為輔。
- 按鈕、提示訊息、狀態文字、匯入匯出文字等短字串，只有在最小支援畫面寬度下確認越文與中文都能完整顯示於同一行時，才可使用 `越文 / 中文` 格式。
  - 單行範例：`Lưu / 儲存`、`Hủy / 取消`
- 按鈕、狀態列、提示框、分頁、卡片標題及其他固定寬度元件，同時顯示越文與中文時，預設採用雙行排版：越文在上，中文在下。
- 不得把越文與中文合併成單一字串後交由瀏覽器任意換行；必須分成兩個獨立文字區塊，並由版面樣式明確控制上下排列。
- 載入中、確認中、儲存中、匯入中、匯出中、錯誤訊息等動態狀態文字，也必須遵守相同的雙語排版規則。
- 修改介面後需同時檢查電腦版與手機版；若文字被截斷、擠壓或在任意位置換行，完成前必須主動修正，不得等待使用者逐項指出。
- 表格表頭需採用雙行呈現：第一行越文，第二行中文，避免文字過長造成欄位被拉寬。
- 大量說明文字、公告、操作說明需分區呈現：先完整顯示越文版本，再完整顯示中文版本；不要越文與中文逐段交錯混排。
- 新增或調整功能時，需先參考現有系統的越文/中文排版方式，保持按鈕、表格、提示訊息與說明文字的呈現一致。
- 若原功能已有固定的越文/中文呈現樣式，應優先沿用，不要另創格式。

## 下載與匯出儲存位置規則
- 之後新增或修改任何下載、匯出功能，都必須在開始產生或下載檔案前，先顯示系統儲存位置選擇視窗，讓使用者自行選擇檔名與儲存位置。
- 不得直接使用瀏覽器預設下載位置，也不得在不支援選擇儲存位置時偷偷改用一般下載。
- 使用者取消儲存位置選擇時，必須停止後續產生、下載及寫入，不得留下空白檔案或未完成檔案。
- 瀏覽器不支援儲存位置選擇時，必須以 `越文 / 中文` 顯示明確提示並停止操作。
- 儲存失敗時必須保留原本檔案，不得以未完成內容覆蓋既有檔案。

## 使用者溝通規則
- 使用者是完全不懂程式開發的人，說明必須簡單扼要、直說重點。
- 避免長篇技術解釋；除非使用者要求細節，否則只說明「要做什麼、影響哪裡、風險高低、下一步」。
- 使用者沒有耐心，回覆要先給結論，再補必要原因。
- 當使用者只輸入 `1` 時，代表要 PHSU（推送）指令；只提供 PowerShell（命令視窗）可執行的 Git（版本控制）推送指令，不要代替使用者執行 `git add（加入版本暫存）`、`git commit（提交版本）`、`git push（推送版本）`。
- 如果發現設計、流程、畫面、資料結構或操作方式不符合實際需求，必須即時提出建議。
- 建議要具體可執行，不只指出問題；需說明建議方案、影響範圍與風險高低。
## 回覆與修改前說明優先順序

- 回覆時必須先給結論。
- 結論後再說原因，避免繞圈或長篇推測。
- 修改任何檔案前，必須先清楚說明：
  - 預計修改哪些檔案
  - 修改目的
  - 影響範圍
  - 風險高低
- 使用者確認同意後，才可以進行修改。

## 全系統資料讀取與快取規則

- 登入完成時只允許讀取身分、帳號狀態、角色權限及進入首頁所必需的小型設定；不得因為使用者具有某項功能權限，就預先讀取該功能的完整資料集合。
- 各功能必須採用兩層載入方式：
  1. 使用者實際開啟功能頁面時，才讀取該功能所需資料。
  2. 讀取完成後保存到 `IndexedDB（瀏覽器本機資料庫）`；再次開啟時先檢查資料版本，版本相同就使用本機快取。
- 各功能資料與快取必須分開管理；某項功能資料更新時，只能讓該功能相關快取失效，不得要求其他無關功能重新下載。
- 同一個帳號、同一台電腦及同一個瀏覽器設定檔的快取，可在關閉瀏覽器或重新啟動電腦後繼續保存；不同瀏覽器或不同 `UID（使用者識別碼）`不得共用快取。
- 本機快取的動態容量上限為 `1 GB（十億位元組）`；不得快取圖片、附件、匯出檔或不必要的歷史副本。更新相同資料時必須取代舊資料，避免容量持續重複累積。
- 登入驗證、帳號啟用狀態及角色權限不得只依賴永久快取；顯示業務快取前，必須先完成當次 Firebase Authentication（Firebase 身分驗證）與 userAccess（使用者存取權限）檢查。
- 大型集合禁止在登入階段無條件完整讀取；查詢需優先使用條件、日期範圍、數量限制或分頁。相同頁面同時發出相同查詢時，必須共用同一個載入 Promise（非同步工作承諾），避免重複讀取。
- 即時監聽只允許在功能實際需要時啟動，離開功能或登出時應停止；監聽查詢必須有明確條件，不得以整個大型集合建立無限制監聽。
- 新增、修改或刪除雲端資料後，必須更新對應的 dataVersions（資料版本）並同步更新或清除相關本機快取；不得為了更新單筆資料而重新讀取所有無關集合。
- 如果發現既有功能未受上述規則保護，必須在修改前提醒使用者讀取量風險，並提出可執行的按需讀取與快取改善方案。

## 功能程式分檔與按需載入規則

- 現有程式先不因本規則進行全面分檔或大規模重構；未來開發或修改某項功能時，再依當次需求與風險逐步調整。若發現程式檔過大、混合多個不相關功能、啟動時載入不必要程式或功能之間依賴過多，必須先提醒使用者並提出適合當次範圍的改善方案，取得使用者同意後才可以拆分或重構。
- 每個主要功能應使用獨立的 `JS（JavaScript 程式檔）`；例如訂單、員工、出勤、報表、裁帶等功能應分開管理，不得把多個無關功能集中寫入同一個大型檔案。
- 只有多個功能確實共同使用的程式，才可以放入獨立的共用模組；例如 `Firebase（雲端服務）`、登入權限、快取、匯出及通用工具。不得為了方便而把功能專屬邏輯放進共用模組。
- 功能分檔後，還必須採用 `lazy loading（按需載入）`：使用者實際開啟功能頁面時，才載入該功能需要的程式與資料；不得因為首頁啟動就預先載入所有功能程式。
- 離開功能頁面或登出時，應停止該功能的即時監聽、計時器及背景工作，避免未使用的功能繼續消耗瀏覽器與雲端資源。
- 不同功能不得直接修改彼此的內部變數或依賴未公開的函式；跨功能共用資料或操作必須透過明確的共用介面，降低單一功能故障影響其他功能的風險。
- 修改單一功能時，原則上只讀取與檢查該功能程式及其直接使用的共用模組；除非確認有跨功能影響，不得為了小型修改而讀取或重構整個系統程式。
- 如果單一程式檔過大、包含多種不相關職責、修改時容易牽連其他功能，或需要讀取大量無關程式才能理解，必須在修改前提醒使用者維護風險，並提出具體拆分方案。
- 不得過度拆分成大量只有少量程式的零碎檔案；同一功能內高度相關、會一起載入及一起維護的程式可以保留在同一檔案，以功能邊界清楚、容易查找及容易測試為原則。
- 分檔或改為按需載入後，必須檢查程式載入順序、共用模組相依性、登入與權限檢查及載入失敗提示，避免發生函式尚未載入、權限繞過或畫面無回應。
- 程式分檔與按需載入主要改善啟動速度、程式載入量與維護性；`Firestore（雲端資料庫）`讀取量仍必須同時遵守「全系統資料讀取與快取規則」，不得把兩者視為同一項保護。

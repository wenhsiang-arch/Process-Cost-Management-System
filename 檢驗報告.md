# HUNTER 檢驗報告功能規格

## 1. 入口與權限

- 「品檢報告範本」是「訂單資料」模組的第三個分頁，與訂單資料、已封存訂單並列；不與裁帶、裁片混用。範本頁的選檔、使用說明、拖曳提示與空狀態使用通用文字，不顯示客戶名稱；目前客戶限制仍由匯出條件判斷。
- 「匯出檢驗報告」小圖示只出現在上方「訂單與工序資料」表格的 HUNTER 訂單操作欄，排列在封存圖示右側，不放在下方「訂單管理」。
- 沿用 `progress（訂單資料）`唯一功能權限；管理員及已獲管理員開啟此權限的角色可匯入、替換、查看範本及匯出。沒有訂單權限、停用、未核准及淘汰角色須由安全規則直接拒絕。
- 目前只使用一份 HUNTER 範本，暫不提供新增其他客戶範本或選擇範本的畫面。
- 範本頁沿用訂單功能的共用操作列：左側選擇／拖入檔案，右側「使用說明」採既有裁帶頁的操作按鈕樣式，點擊才展開簡短流程，排列在儲存按鈕左側；下方以緊湊區塊顯示目前範本及待儲存檔案。上傳框不得超出操作列或截斷右側邊框，三種中央語言模式均保留正式越文及中文內容。

## 2. 範本與資料來源

- 範本限一個工作表的 `.xlsx（Excel 表格檔）`，最大 5 MiB（百萬位元組近似單位）；固定文字 HUNTER、WEBBING WORLD 及其餘格式由範本保留。
- 範本中的 `C6（款號）`、`C7（英文產品描述）`、`F7（英文顏色）`、`C8（訂單號碼）`、`F8（數量）`五格可保留範例文字或數字，不必事先清空，但不可包含公式；原始範本內容不因匯出而改變。輸出時只替換五格內容，不重設原字型、合併儲存格與固定欄位。
- 匯出時按需載入 JSZip（試算表壓縮封裝工具），直接複製範本的工作表 XML（試算表結構）並替換五格，不重新建立整張工作表；`styles.xml（範本樣式檔）`原封不動，字型、字級、欄寬、合併欄位及列印邊界保留。五格必須是範本已建立的儲存格；若範本具有工作表附屬關聯檔（例如圖片、外部連結），多款號時停止並提示，不匯出可能受損的檔案。
- 展開的使用說明以簡短步驟引導：選擇／拖入 `.xlsx` 範本、儲存範本、在上方訂單列表點報告圖示、選擇檔名與儲存位置，並說明每款號一分頁、固定文字與格式保留且五格範例值會被訂單資料替換。
- 上傳同名檔案可覆蓋，但先彈窗確認；不同檔名替換也先確認。取消、驗證或儲存失敗均維持原範本。若範本同時出現兩份，提示「目前存在兩個範本，請檢查」並停止匯出，不自行選擇。
- 原始英文描述與顏色使用已匯入 `orderItems（訂單項目）`的 `description`、`color`；款號由固定 `productId（款號識別碼）`對應目前 `products（款號主檔）`取得。款號代碼在現行系統不允許更改。缺少描述、顏色、數量或主檔對應時停止，不把越文自行翻成英文。
- 每個不同款號建立一個工作表；同款號多列只有描述及顏色一致時才加總數量，不一致就提示檢查訂單。一張訂單輸出一份活頁簿，工作表名稱依款號處理試算表禁用字元及長度限制。

## 3. 儲存、匯出與安全

- `inspectionReportTemplates/main（檢驗報告範本主檔）`保存檔名、大小、內容驗證碼與分段數；`inspectionReportTemplateChunks/main_00000...（範本內容分段）`保存原始檔。替換範本與不可修改的 `operationLogs（獨立操作紀錄）`同批寫入；不新增雲端儲存服務、本機轉檔服務或 `IndexedDB（瀏覽器持久快取）`。
- 範本讀取、更新、分段讀寫及匯出紀錄受 `firestore.rules（資料庫安全規則）`的 `progress（訂單資料）`權限保護；不以畫面圖示作為安全邊界。
- 匯出前重新讀取單張訂單，確認為使用中的 HUNTER 且匯入已完成，再讀該張訂單明細；正式資料不由範本反寫。
- 使用者先選擇檔名與儲存位置，才讀取範本內容與產生檔案；取消或瀏覽器不支援時停止，不退回預設下載。匯出成功留操作紀錄；檔案已儲存而紀錄失敗時明確提示。
- 範本及報告文字仍依既有桌機介面、共用元件及中央「雙語／越文／中文」模式；報告活頁簿內的英文是客戶原始業務資料，不是介面語言。

## 4. 雲端用量預算

- 估算前提：`N`為目標訂單項目數、`K`為未在目前款號記憶體內的不同款號數、`C`為範本分段數（1–12）、`D`為使用者／裝置數、`E`為每台每日匯出次數、`R`為失敗後額外重試次數。正式剩餘額度未知；本機虛構資料及模擬器測試不消耗正式雲端用量。下列為直接文件操作，安全規則的身分／角色相依讀取與網路重試另計。
- 首次開啟範本分頁：最多 2 份摘要，通常 1 次文件讀取；沒有範本的空查詢仍至少計 1 次。重開分頁同樣查詢，沒有持久快取；不讀分段。範本已載入後也不因開頁而重建本機快取。
- 首次匯入：範本摘要檢查與提交交易核對約 3 次讀取（確認時、提交前、交易內）、`C+2`次寫入（`C`個分段、1 主檔、1 操作紀錄）、0 次刪除；替換時若新範本分段較少，最多再刪 `C舊−C新`份多餘分段。檔案瀏覽器讀取與本機解析不消耗資料庫額度。最小 1 段約 3 讀／3 寫；5 MiB 上限約 3 讀／13 寫，另加可能刪除。並行替換導致交易重試時，每次重試會再讀至少 1 次主檔，失敗則不寫入。
- 單次匯出：1 次訂單主檔讀取、至少 `max(1,N)`次訂單項目查詢讀取、最多 `K`次缺少的款號讀取、1 次範本摘要讀取及 `C`次分段讀取，合計約 `2+max(1,N)+K+C`次直接讀取；成功後 1 次操作紀錄寫入，0 次刪除。83 筆明細、1 分段時，若款號已在本機約 86 讀，全部 83 款需補讀約 169 讀。沒有範本、選擇儲存位置取消或驗證失敗時，依已執行階段計讀取，尚未產生檔案及寫紀錄。
- 每台／每人每日約 `E×[2+max(1,N)+K+C]`次匯出直接讀、`E`次匯出紀錄寫；`D`台乘 `D`。例如 10 台各匯出 1 張 83 筆、1 段且已載入款號，約 860 讀／10 寫；最壞若全部款號需補讀，約 1,690 讀／10 寫。範本匯入量再另加，重試 `R`次則對已執行階段逐次追加，無限制人工重試沒有有限上限。
- 傳輸：範本一次上傳最多 5 MiB 原始內容，分段以 Base64（文字編碼）傳送約增加三分之一；每次匯出重新下載 `C`分段及目標訂單、項目、缺少款號文件。報告檔只存使用者本機，不回寫雲端。雲端儲存為一份範本及分段、操作紀錄與索引，替換同位置內容不持續累積多份範本；操作紀錄會累積。
- 匯出封裝工具在選擇儲存位置後從既有網站工具來源載入，屬網站程式傳輸，不增加 Firebase（雲端資料庫）文件讀寫或本機服務；沒有網路且瀏覽器未快取該工具時會明確失敗。每款號額外複製一份工作表內容，報告檔大小及瀏覽器記憶體隨分頁數增加。
- 網站程式版本變更不使訂單或款號業務快取失效，此功能不建立範本持久快取，因此範本快取重建尖峰為 0；若 `D`台同時首次開範本頁，摘要尖峰約 `D`次讀，若同時各匯出一張則依上式乘 `D`，另加既有訂單清單／款號快取冷載入各自的預算。資料量、裝置數或匯出頻率改變時重新估算。
- 最低情況（開頁未匯出）約 1 讀、0 寫／刪；一般情況依實際 `N,K,C,E,D`代入；最壞情況以 5 MiB、12 段、最大實際訂單筆數、所有缺少款號與明確重試次數計。若預算達當日配額 20%／50%或可能中斷，須依全案規則事前另外確認，不在正式資料上試錯。

## 5. 驗收與回復

- 驗收範本字型、合併欄位、五格位置、固定文字與多款號實際活頁簿；目前尚須以使用者最終範本進行真實檔案驗收，示意截圖不能代替檔案。
- 已用兩款號的格式化模擬範本、五格帶範例值的範本及另一套表格程式產生的空白範本，進行位元組寫出及獨立讀回測試：原範本不變，輸出五格替換，固定文字、Times New Roman／Arial（範本英文字型）、字級、粗體、合併欄位、欄寬及列印設定均保留；另以 83 個款號驗證分頁數。這不等於已用使用者最終範本或正式網站逐步驗收。
- 驗收同名覆蓋、不同名替換、失敗保留、兩份範本阻擋、不同顏色阻擋、英文原始欄位缺漏、取消儲存位置及無支援瀏覽器。
- 驗收管理員、所有有效非管理員角色的開／關權限、未登入、未核准、停用、淘汰角色直接讀寫允許與拒絕，以及三種中央語言模式與桌機寬度。
- 本機程式與安全規則修改不代表雲端已發布；上線需使用者自行發布相符規則。若須回復，先停止新報告操作，再以同版網站程式及規則回復；已下載報告與既有訂單不因程式回復而刪除，範本主檔需保留以供重試。

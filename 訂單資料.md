# 訂單資料功能規格

## 1. 文件定位

本文件是訂單資料、訂單匯入、訂單項目、數量調整、出貨分類及封存的現行正式規格。共同架構與介面規則分別以 `系統架構規劃.md`及 `UI設計規範與參照/介面設計規範.md`為準。

目前訂單頁提供訂單與工序資料查詢，並依正式產能累計顯示生產進度；不提供預估完成日或風險判斷。

## 2. 功能與權限

- 正式功能權限為 `progress（訂單資料）`。
- `orderImport（舊訂單匯入權限）`只保留舊權限文件相容，現行畫面與資料存取都以 `progress`為準。
- 非管理員缺少權限欄位時視為關閉；畫面隱藏不能取代安全規則。
- 永久刪除只在已封存分頁提供給管理員；管理員自行判斷業務關聯，不自動驗證無關聯。一次確認顯示訂單、客戶與原始筆數，不要求輸入訂單號碼。刪除只處理訂單、明細及匯入鎖；產能、款號及既有操作紀錄保留，已被其他資料引用時可能留下無法解析的關聯。

## 3. 正式資料與固定身分

### 3.1 訂單主資料

`orders/{orderId}`保存訂單自己的資料，包括：

- 訂單號碼、客戶及總數量。
- 交期、完成／出貨日期及備註。
- 匯入、使用、出貨分類及訂單完成狀態；舊訂單缺少出貨分類欄位時視為尚未出貨，缺少完成欄位時視為未完成。
- 建立、更新與操作紀錄識別。

訂單主資料不保存另一份款號或工序主檔。

### 3.2 訂單項目

- `orderItems/{orderItemId}`的每一份文件對應匯入檔的一列。
- 同一訂單可有多列相同款號，不自動合併；每列以固定 `orderItemId（訂單項目識別碼）`保持獨立。
- 訂單項目保存 `productId（款號固定識別碼）`、數量、列號及 PO、說明、顏色、項目交期、完成／出貨日期、備註等訂單情境。
- 款號與工序名稱、分類、秒數及每小時產能由 Product Resolver（款號解析器）依目前 `products（款號主檔）`取得，未鎖定訂單不保存可過期的第二份正式內容。

## 4. Excel（試算表）訂單匯入

### 4.1 檔案與表頭

- 只接受一個 `.xlsx`或 `.xls`檔案，且檔案必須正好包含一個工作表。
- 使用者選擇或拖入檔案後立即在本機檢查並顯示預覽，不必先完成訂單號碼、客戶及出貨日期；按下確認匯入時才驗證這三項必要資料，未通過不得寫入正式資料。
- `.xlsx`與 `.xls`都先以同一套 `ArrayBuffer（陣列緩衝區）`方式讀取，再由同一個試算表解析入口辨識內容；不得依副檔名建立兩套欄位解析規則。這項相容方式只適用訂單資料檔，不改變品檢、裁帶與裁片範本固定使用 `.xlsx`的限制。
- 訂單號碼只從明確的 `ORDER NO（訂單號碼）`或 `ORDER NUMBER（訂單號碼）`標題取得；號碼可與標題同格或位於右側最近的非空白格。整份檔案只有一個唯一號碼且輸入框仍空白時才自動帶入，找不到或出現多個不同號碼時維持空白，使用者始終可以修改。
- 系統在前 35 個實際列尋找可唯一辨識的款號與數量表頭；表頭上方的標題或說明列直接忽略。
- 款號與數量是必要欄位；說明、顏色、PO、項目交期、完成日期、出貨日期及備註是可選欄位，依表頭名稱辨識，不依固定欄位位置。
- 匯入預覽固定顯示款號、數量、工序數與狀態；只有檔案具有對應表頭時才顯示產品說明或顏色欄。`Width（寬度）`與 `Length（長度）`不顯示也不儲存；沒有獨立顏色表頭時不從產品說明猜測或拆分顏色。
- 預覽狀態只保留本次匯入需要的資料與工序數，不把款號的完整工序陣列複製到預覽，也不建立 `window（瀏覽器全域）`暫存物件。選檔後大型拖放區縮成檔名與「重新選擇」單列；重新選檔、格式拒絕或關閉視窗時清除舊預覽。
- 找不到表頭、同一必要表頭重複、兩個必要欄位落在同一格或無法唯一判斷時停止匯入，不從資料內容猜測欄位。

### 4.2 資料驗證

- 款號必須能唯一對應目前款號主檔；找不到款號時整次匯入停止並列出實際工作表及列號。
- 數量必須是安全範圍內的正整數，不接受空白、0、負數、小數或文字。
- 空白列與重複表頭列可忽略；同一款號出現多列時仍各自建立訂單項目。
- 若檔案具有總數量列，明細加總必須相同；總數量列後不得再出現訂單資料。
- 使用者確認預覽前不得寫入正式資料。

### 4.3 寫入與中斷

- 訂單號碼以 `orderImportLocks（訂單號碼佔用紀錄）`避免重複建立同一正式訂單；使用中與封存的同號訂單都不可重匯，永久刪除後可以重匯。佔用紀錄不綁定某位操作者的後續匯入資格。
- 匯入先建立不可使用的進行中訂單，再分批寫入固定訂單項目；全部完成後才原子標記訂單可用並完成操作紀錄。
- 每批最多 8 筆訂單明細，整份檔案自動連續處理；不是整張訂單的筆數或生產件數上限。批量依目前安全規則的全角色本機實測決定。
- 新匯入先驗證全部明細，再建立訂單與佔用紀錄；各批最多寫入 8 筆，失敗後不以原帳號、原檔或既有檢查點接續。
- 匯入失敗時先核對主資料是否其實已完成；若已完成，以成功為準。若仍未完成，分批清除明細、主資料及佔用紀錄，留下不可變的清理紀錄；下一次重匯也會先完成尚未完成的清理。連線中斷時無法保證當下完成清理，畫面須明示「待清理」，恢復連線後再清理。
- 舊版進行中的訂單同樣由有訂單權限的帳號清理後重建。若僅剩沒有主資料的孤立佔用紀錄，只有顯示零筆且實際查無明細，才留下修復紀錄後重匯；仍有明細或狀態不明時停止，避免誤釋放單號。
- 權限拒絕、登入失效、連線中斷及額度限制分別顯示原因；失敗畫面明確區分「已清理」與「待清理」，不顯示誤導的 0／原檔筆數。已完成訂單再次匯入時提示查看清單，不重複新增。
- 匯入中不發布訂單清單快取版本；最後訂單可用狀態、匯入鎖結案、成功操作紀錄及既有 `dataVersions/orders（訂單快取版本）`一起提交，避免其他裝置快取半成品。
- 完成的 `operationLogs（獨立操作紀錄）`保存操作者、檔名、訂單及實際項目數。

## 5. 訂單清單與工序明細

- 使用者進入訂單資料頁後才載入訂單，不在登入時預讀。
- 上方「訂單與工序資料」是唯一的使用中訂單工作表格，只顯示尚未確認出貨的訂單；未設定實際出貨日的訂單固定排在最上方，其餘只依實際出貨日由近到遠排列，不再以 PO 交期替代排序。不再使用 60 天日期篩選，逾期未出貨訂單仍會留在主表。
- 實際出貨日可在主表獨立保存，不會隨日曆日自動改寫，也不會自動確認出貨。點擊出貨圖示後可核對目前日期、另選日期或確認出貨；確認後訂單移至獨立「已出貨訂單」分頁，依實際出貨日由新到舊排列。日期空白時確認視窗只預設帶入當日供使用者確認，不會在未確認時寫入；取消確認後回到主表並保留原日期。
- 主表不顯示工序數、實際完成日或可展開的款號／工序明細，也不提供款號搜尋；選擇單張訂單只篩選已載入的訂單清單，不讀取該訂單工序。這只移除主表入口，不刪除既有訂單項目、工序或數量調整資料。
- 主表標題右側顯示全部未出貨訂單的總數量，直接加總目前已載入訂單的 `totalQty（訂單總數量）`；選擇單張訂單不改變此總數。
- 未出貨總數量使用系統資訊色底框，數量右側顯示每張訂單的生產進度條。預計秒數為各訂單項目數量乘上目前款號主檔中 `SX（生產）`工序秒數後加總；已完成秒數以相同目前秒數乘上正式有效產能累計後加總。`BL（備料）`、`QC（品檢）`、`DG（包裝）`及補充工時不列入，沒有生產工序時顯示 0%。PO 交期統一使用一般深色粗體；實際出貨日未選時使用淡綠色底框，依台灣日曆日剩餘 0 至 14 天使用資訊深藍色粗體，已超過日期且尚未出貨使用危險紅色粗體，15 天以上維持一般字色。
- 生產進度使用 `productionProcessTotals（工序累計）`，因此一般產能不論月份是否已凍結都會納入；已作廢產能已從累計扣除。進度只是目前訂單畫面，不修改或重算已鎖定的績效獎金快照。
- 生產進度自動或手動更新時，每張訂單的進度條仍只顯示「計算中」。表格上方的共用狀態框改用淡綠色底框，依實際工作依序顯示檢查訂單進度版本、確認變動款號、載入訂單工序、讀取生產累計、處理舊訂單資料、計算進度、儲存本機快取及更新完成；具有批次的階段顯示已完成／總批次，並按目前批次平均時間每秒修正預估剩餘秒數。工作期間不顯示最後更新時間，完成提示短暫顯示後才恢復新的最後更新時間；未執行的工作階段不製造假的讀取進度。
- 主表「完成狀態」預設為未勾選。確認後把當下實際進度保存為 `productionProgressSnapshotPercent（完成進度快照）`，進度條以 100% 寬度填滿並顯示例如 `79% / 100%`，保留實際進度供取消完成時恢復；完成狀態以綠色雙語標籤呈現。已完成訂單不參與自動或手動進度更新。取消完成需再次確認，恢復一般統計但不重置或增加當期更新次數。另一台電腦以訂單正式快照顯示相同結果，不使用該裝置的舊百分比。
- 出貨分類不改變 `lifecycleStatus（訂單使用狀態）`，訂單仍為 `active（使用中）`，因此產能登記、績效獎金及凍結月份仍依原本正式資料與規則運作。
- 主頁不再顯示重複的下方「訂單管理」表格。已封存訂單的搜尋、明細、還原與管理員永久刪除仍在獨立「已封存訂單」分頁；上方工作表保留使用中訂單的封存入口。
- 已載入訂單清單若包含匯入中或匯入失敗的使用中訂單，上方工作表以共用警告樣式顯示單號與狀態；未完成訂單不進入可操作工序清單。此提示只使用既有訂單資料，不另外查詢雲端，也不取代匯入時的失敗與待清理訊息。
- 主表選取與一般瀏覽不載入可展開的款號／工序明細；需要訂單項目的其他正式功能仍依自己的操作按需讀取，不預先讀取全部訂單的全部工序。
- 主表完整使用共用表格控制：左上角欄位勾選器可切換欄位，表頭分隔線可左右拖曳並可雙擊符合內容，欄位偏好依 UID（使用者識別碼）保存在瀏覽器本機；表頭雙語結構由共用程式辨識，三種語言模式皆正常顯示。功能列的「列印表格」只列印目前畫面已載入的訂單表格，取消勾選的欄位不列印，「操作」欄預設永不列印；表格上方的標題、最後更新時間與未出貨總數量均不列印。列印承接目前語言模式，雙語表頭固定上下兩行，並使用獨立的列印欄寬、內距與行高；空白備註顯示為破折號。
- 款號主檔修改後，未鎖定訂單明細使用最新名稱、分類、秒數與每小時產能；訂單號碼、項目數量、PO、顏色、日期及備註不得被款號修改覆蓋。
- 固定款號或工序無法解析時顯示明確錯誤，不退回 `orderProcesses（舊訂單工序）`或其他快照來源。

### 5.1 共用文字與桌機排列

- 訂單主表、使用說明、狀態及視窗的文字與三種語言模式引用 `UI設計規範與參照/介面設計規範.md`。
- `js/orders.js`提供語意類別、欄位識別及使用說明視窗，`styles/features/orders.css`管理日期、訂單號碼、數量、備註、說明視窗及窄桌機換排。
- 長描述與備註提供完整業務原文提示，不因中越文或斜線被誤拆；動態載入、錯誤與空資料提示使用正式雙語節點。
- 介面驗收需包含非空訂單、使用說明、日期及長備註；離線畫面不代替正式寫入、數量計算、匯入或匯出驗收。

## 6. 修改、數量與狀態

### 6.1 訂單欄位

- 只允許修改正式服務列出的交期、完成／出貨日期與備註欄位。
- 修改使用 Transaction（雲端交易）同時寫入訂單與操作紀錄；不得直接覆蓋未允許欄位。

### 6.2 訂單項目數量

- 新數量必須是正整數並填寫原因。
- 修改前重新讀取訂單項目目前修訂值、目前款號工序及各工序已有效生產累計。
- 新數量不得低於任一工序已完成數量；同時被其他使用者修改時，舊修訂不得覆蓋新修訂。
- 訂單項目數量、訂單總數量及操作紀錄在同一筆 Transaction（雲端交易）一起成功或失敗。

### 6.3 封存與還原

- 一般刪除採 `active（使用中）`與 `archived（已封存）`狀態切換，不實體刪除訂單、訂單項目、產能或歷史。
- 封存與還原都保留操作紀錄；狀態已由他人改變時不得用舊畫面覆蓋。

### 6.4 出貨確認與取消

- `shipmentStatus（出貨分類狀態）`只接受 `pending（尚未出貨）`或 `shipped（已出貨）`；尚未出貨可先保存實際出貨日，已出貨時則必須有日期。取消確認只改回尚未出貨並保留日期。
- 確認、改日與取消使用同一筆 Transaction（雲端交易）寫入訂單、不可變操作紀錄及既有訂單版本；不新增權限鍵、不封存訂單，也不修改訂單項目、產能或獎金資料。
- 使用者由功能列點擊「已出貨訂單」進入獨立分頁；該分頁只提供實際出貨日修改、取消確認及適用的檢驗報告匯出，不提供封存。需封存時必須先取消出貨，回到訂單主表操作。

### 6.5 訂單完成與取消

- `productionProgressCompleted（訂單完成狀態）`及 `productionProgressSnapshotPercent（完成進度快照）`只由正式訂單服務的 Transaction（雲端交易）修改，並同步建立不可變操作紀錄及更新既有訂單版本。
- 完成與取消沿用 `progress（訂單資料）`權限，不新增角色權限。完成不修改產能、獎金、凍結月份、出貨或封存資料；列印時完成訂單顯示雙語完成文字，未完成顯示破折號，操作控制不列印。

## 7. 資料路徑

| 路徑 | 用途 |
|---|---|
| `orders` | 訂單主資料、狀態及總數量 |
| `orderItems` | 每一列固定訂單項目與訂單情境 |
| `orderImportLocks` | 訂單號碼唯一匯入鎖定與分批進度 |
| `dataVersions/orders` | 正式訂單清單快取唯一版本來源 |
| `products` | 目前款號與工序正式來源 |
| `productionProcessTotals` | 訂單項目各工序有效生產累計，供數量下限驗證 |
| `orderProductionProgressVersions` | 每張訂單生產累計是否變動的小型版本標記 |
| `operationLogs` | 匯入、修改、數量調整、出貨確認／取消、訂單完成／取消、封存及還原紀錄 |

`orderProcesses`及其他訂單工序快照不是現行 Runtime（執行程式）來源。

## 8. 主要程式責任

| 檔案 | 責任 |
|---|---|
| `js/orders.js` | 訂單匯入解析、畫面、搜尋、明細、修改、封存與歷史入口 |
| `js/order-production-progress.js` | 訂單生產進度計算、版本核對及依 UID 隔離的本機快取 |
| `js/shipped-orders.js` | 已出貨訂單清單、日期修改、取消確認及適用的檢驗報告匯出入口；不提供封存 |
| `js/order-archive.js` | 已封存訂單清單、還原及管理員永久刪除入口 |
| `js/order-service.js` | 訂單與訂單項目的正式交易、工序解析及操作紀錄 |
| `js/order-item-store.js` | 固定訂單項目模型、識別碼、數量及工序累計驗證 |
| `js/product-resolver.js` | 依固定款號／工序識別碼解析目前款號主檔 |
| `js/history.js` | 各主功能共用的操作紀錄按需查詢、工作階段暫存與游標分頁 |
| `js/order-history.js` | 整個訂單主功能各分頁的歷史操作紀錄、雙語摘要與顯示 |

## 9. 讀取、快取與費用界線

- 訂單清單使用共用業務快取並共用同一個進行中載入工作，避免同一頁重複查詢。
- 訂單工序以單張訂單按需載入；已載入結果在目前工作階段重用，款號主檔變更時清除相關解析結果。
- 生產進度使用依 UID（使用者識別碼）隔離的 `IndexedDB（瀏覽器本機資料庫）`快取，另以同 UID 隔離的兩個小型本機標記分別記錄自動與手動嘗試，不保存訂單或產能內容。更新週期固定為台灣時間每日 06:00 至隔日 05:59；每個帳號、瀏覽器設定檔在每個週期各有最多一次自動嘗試及一次經使用者確認的手動嘗試。兩者互不占用；失敗仍計入該類型當期次數且不自動重試。當期自動額度尚未使用時，畫面顯示計算中並等待完成後呈現新結果；當期自動額度已使用時，登入或開頁直接讀取該次本機結果，不等待手動更新共用鎖，也不重新計算。週期內重新登入、重新整理、切換頁面、搜尋、修改日期或備註不會另行開始相同類型嘗試。每次有效嘗試先核對 `system/productsMeta（款號版本資料）`及尚未出貨訂單的生產進度版本；版本未變時不重讀訂單項目、款號或工序累計。款號版本改變時依遞增序號只取得上次之後改動的款號，再以固定 `processId（工序識別碼）`辨識秒數及分類差異；既有 `SX（生產）`秒數修改或改成其他分類不重讀累計，新加入 `SX`只按缺少的工序讀取。訂單進度版本改變時，以該訂單上次成功版本時間查詢 `productionProcessTotals（工序累計）`中後續更新的文件，取得目前絕對累計值，不再重讀該訂單全部 `SX`工序；索引尚未完成、舊快取沒有可靠時間或版本時間異常時，安全退回現行每 30 筆文件識別碼批次完整讀取。每階段最多同時三批，不設定與資料量無關的整體逾時。沒有版本且沒有可信快取的舊訂單，先依訂單確認是否存在任何工序累計；完全沒有累計時直接保存 0%。每次嘗試把版本、訂單項目、款號、累計及舊資料查詢的回傳文件數、查詢批次與最低估計 Reads（文件讀取次數）保存在同一份本機快取並輸出本機紀錄，不新增雲端文件。沒有尚未出貨訂單時進度雲端讀取為 0，也不消耗手動次數。更新失敗時保留上一次成功結果；從未成功時顯示尚無進度資料。
- 已確認完成的訂單直接使用訂單主資料中的完成快照，不查詢該訂單的進度版本、訂單項目、款號或工序累計，也不消耗額外更新次數；取消完成不新增當期更新額度。若全部未出貨訂單均已完成，該次進度雲端讀取為 0。
- 訂單主表不提供款號搜尋與展開明細，因此這兩項操作的訂單項目直接讀取為 0。
- 訂單數量修改需要讀取該項目、訂單、目前款號及該款全部有效工序的累計，讀取量會隨工序數增加；執行前已由使用者明確選定單一項目，不屬於無限制集合掃描。
- 若訂單數量成長到完整集合不適合一次載入，必須改成雲端條件與分頁，不得只擴大本機快取。

### 9.1 介面調整的雲端用量預算

純介面調整的共通雲端用量預算引用 `系統架構規劃.md` 第 11.2 節；正式開頁、匯入及修改仍依第 9 節查詢與用量界線計費。

未出貨總數量沿用同一次訂單清單載入結果在瀏覽器內加總，新增讀取、寫入及刪除均為 0；不建立新查詢、快取或資料版本。
訂單主表沿用共用 `ui-table（共用表格）`，功能樣式只調整桌機欄寬比例。生產進度使用淡藍色填滿區，百分比直接置於色條中央且不另加底框；完成狀態以滿寬色條顯示，實際百分比快照仍保留在文字中。PO 交期統一使用深色粗體；實際出貨日的空白、兩週內及逾期狀態依第 5 節使用淡綠底框、資訊深藍色及危險紅色。這些畫面調整不改變進度計算、資料存取或快取，日期排序只在已載入清單內執行。
移除主表款號搜尋與展開明細不改變訂單清單快取、版本或查詢；選取訂單、查看說明與異常提示都只使用同一份已載入清單。相較修改前，這些入口的訂單項目直接讀取降為 0；新增直接寫入、刪除及雲端儲存皆為 0。網站靜態程式更新傳輸另計，正式剩餘額度未知。
欄位勾選及列印只操作目前瀏覽器內已呈現的表格，不重新查詢正式資料、不建立操作紀錄，也不新增快取版本。每次勾選或列印的最低、一般與最壞新增用量皆為 0 Reads（文件讀取次數）、0 Writes（寫入次數）及 0 Deletes（刪除次數）；雲端傳輸與儲存增量為 0，乘以任何使用者、裝置或操作次數後仍為 0。網站靜態檔案重新載入的傳輸另計，正式剩餘額度未知。
選檔後立即檢查、訂單號碼自動帶入及預覽選用欄位都只解析使用者本機選取的表格檔，不新增雲端查詢、寫入、刪除、傳輸或儲存；最低、一般與最壞新增正式用量均為 0。按下確認匯入後的既有正式用量仍依第 9.2 節計算，沒有因預覽調整而增加。
統一 `.xlsx`／`.xls`本機讀取、移除全域預覽暫存及縮減預覽工序資料，同樣不新增 Reads（文件讀取次數）、Writes（寫入次數）、Deletes（刪除次數）、雲端傳輸或儲存；只減少瀏覽器記憶體中的重複資料。網站靜態檔案重新載入的傳輸另計，正式剩餘額度未知。
生產進度共用狀態框只接收既有更新流程在瀏覽器內回報的階段、批次與耗時，不建立額外查詢或資料文件。每次自動或手動更新的最低、一般及最壞新增用量皆為 0 Reads（文件讀取次數）、0 Writes（寫入次數）、0 Deletes（刪除次數），雲端傳輸與儲存增量為 0；每秒倒數只使用本機計時器。原本進度更新的正式用量、每日限制與快取失效方式不變，網站靜態檔案重新載入傳輸另計。

### 9.2 訂單匯入與失敗清理雲端用量預算

- 前提：`N（原檔明細數）`、`B = ceil(N / 8)（批次數）`。不建立新集合或另一套快取版本；剩餘正式額度未知。本機虛構資料與模擬器測試的正式讀取、寫入、刪除、傳輸及儲存增量皆為 0。
- 無同號訂單時，每次全新匯入的直接 Reads（文件讀取次數）約為 `2B + 5`，Writes（文件寫入次數）為 `N + B + 6`，Deletes（刪除次數）為 0；包含兩次限量單號查詢、初始化、每批與明細同交易的已寫筆數、最後結案、操作紀錄與訂單版本。已寫筆數僅供清理安全核對，不作跨帳號續跑。最低 1 筆約 7 讀／8 寫；29 筆一般情況約 13 讀／39 寫；1,000 筆約 255 讀／1,131 寫。前置款號載入與版本核對另依 `款號管理.md`。
- `M（已寫入但未完成的明細數）`與 `C = ceil(M / 100)（清理批次數）`：失敗清理約 `M + 2C + 5` 次直接讀、`2C + 3` 次寫、`M + 2` 次刪除，外加下一次重匯的完整用量。29 筆中已寫 8 筆時，清理約 15 讀／5 寫／10 刪；若隨後成功重匯，還須再加 13 讀／39 寫。清理批次最多 100 筆，未完成匯入不發布訂單快取版本。
- 安全規則的身分、角色及關聯文件核對另計；保守以每次交易最多 20 次相依核對估算，並乘以批次數與可能重試次數。最低情況沒有清理；一般情況依實際失敗位置加一次清理；最壞情況需指定中斷、重試及再次驗收次數，反覆重試沒有有限總上限。正式剩餘額度未知；本機虛構資料與模擬器測試的正式用量為 0。
- 每位使用者／裝置每日 `K（匯入次數）`：將上述每次讀寫與前置款號用量乘 K，再乘預估使用者／裝置數。例如 10 人每天各匯入一張 29 筆訂單，沒有失敗時直接匯入約 130 讀／390 寫；失敗、清理及重試須逐次另加。
- 傳輸與儲存依實際文件位元組計算：新增明細、訂單、匯入鎖、成功紀錄及版本為儲存增量；清理會刪除部分資料，但不可變清理紀錄持續占空間。原始表格在瀏覽器解析，不上傳原始檔。
- 完成匯入會使既有訂單快取失效；每次冷載入訂單文件數 `O` × 受影響帳號／瀏覽器／裝置 `D` × 重載次數 `R`，尖峰至少按 `O×D×R` 另估，版本檢查與關聯文件核對另計。程式修改本身不清空全體業務快取。

### 9.3 封存與刪除雲端用量預算

- 前提：`O（訂單清單總筆數）`、`N（目標訂單明細數）`、`B=ceil(N/100)（永久刪除批次數）`、`D（受影響帳號／瀏覽器／裝置數）`。正式剩餘額度未知；下列為程式直接讀取，安全規則的身分與關聯文件核對另計。
- 封存分頁沿用訂單快取。第一次冷載入約讀 `max(1,O)+2` 筆（前後各一次版本核對）；已有有效快取且版本相同時，15 秒內記憶體版本仍有效約 0 筆，超過後約 1 筆版本核對。按「重新整理」強制載入約 `max(1,O)+2` 筆。展開一張明細約讀 `max(1,N)` 筆；收合後再展開會重新讀取。
- 一般封存與還原各自直接讀 1 筆訂單、寫 3 筆（訂單、操作紀錄、既有訂單版本）、刪 0 筆。封存確認現在使用清單中不可變的 `itemCount（原始明細筆數）`，額外明細讀取由原先的 `max(1,N)` 降為 0；交易仍重新讀取正式訂單狀態，避免舊畫面覆寫他人變更。重複操作且狀態未變時，約讀 1、寫 0、刪 0。
- 上項 1 筆是封存／還原服務本身；主訂單頁已取消款號搜尋與展開明細，不會再因此補讀訂單項目。已封存分頁仍依使用者明確展開的單張訂單按需讀取。
- 永久刪除 N 筆、B=ceil(N/100)，無衝突正常直接讀 N+2B+5（每批前讀父單、查明細、交易核對，最後空查詢至少計 1，完成再讀鎖）；寫入 2B+5，刪除 N+2。開始與完成各寫版本，批次寫父單與不可變紀錄。需按實際查詢／SDK重試核對修正。
- 29 筆永久刪除一般情況直接 36 讀、7 寫、31 刪；400 筆約 413 讀、13 寫、402 刪。確認前若自行展開明細，另加 N 讀；刪除失敗時畫面為核對狀態另讀最多 1 筆訂單。獨立驗收若重新整理清單，再加 `max(1,O)+2` 讀；若另查目標訂單與鎖定，各加單筆讀取。安全規則相依讀取另以每個直接讀取最多 10、每筆交易最多 20 保守估；交易重試按實際次數加算，事前最壞可先按 5 次估，人工無限制重試則沒有有限上界。
- 每位使用者或裝置每日的操作量，以封存次數×1、還原次數×1、永久刪除各次的 `N+2B+5`，再加實際開頁、展開、刷新與驗收次數計算。例如 10 位使用者各冷開一次含 1,000 筆訂單的清單，約 `10×(1,000+2)=10,020` 直接讀取；同一裝置有效快取再次開啟通常僅 0 至 1 讀。封存、還原各一次且各有 10 台裝置在版本變更後完整重載時，尖峰另加最多 `2×O×10` 筆清單讀取及版本核對；是否真的重載取決於各裝置是否開頁及快取狀態。
- 傳輸依實際讀取文件、刪除紀錄與重試次數計算；刪除釋放訂單及明細原文件空間，但不可變操作紀錄與索引仍占儲存。封存與還原不釋放文件空間。
- 本機虛構資料測試的正式讀／寫／刪／傳輸／儲存增量為 0；正式操作仍依上述預算計算，剩餘額度需在執行前確認。

### 9.4 出貨分類雲端用量預算

- 已出貨分頁沿用同一份訂單清單快取，不建立新集合或第二套快取；在同一工作階段切換分頁新增直接讀取為 0。冷載入、強制重新整理及其他裝置版本失效仍依第 9.3 節的訂單清單預算。
- 每次獨立保存日期、確認、改日或取消出貨，服務直接讀 1 筆訂單，寫入訂單、不可變操作紀錄及既有 `dataVersions/orders（訂單快取版本）`共 3 筆，刪除 0 筆；安全規則相依核對與交易重試另計。重複提交完全相同狀態與日期時直接讀 1 筆，寫入 0 筆。
- 每位使用者或裝置每日執行 `S`次出貨狀態操作，正常直接用量約 `S`讀／`3S`寫；一般 10 人每天各處理 20 張約 200 讀／600 寫。每次版本更新後，其他裝置實際再次開啟訂單功能時，尖峰另按訂單數 `O × 裝置數 D × 重載次數 R`估算。正式剩餘額度未知；本機模擬器測試的正式用量為 0。

### 9.5 生產進度雲端用量預算

- 前提：`O（尚未出貨訂單數）`、`I（這些訂單的項目總數）`、`P（不同款號數）`、`S（冷載入需讀取的 SX 生產工序總數）`、`V（已有產能變動版本的訂單數）`、`D（本期實際更新過的工序累計文件數）`、`R（本期有產能變動的訂單數）`、`C（上次之後有改動的款號文件數）`、`N（款號變更後新納入 SX 且缺少本機累計的工序數）`、`Z（沒有版本且沒有可信快取的訂單數）`、`L（Z 中實際存在的舊工序累計文件數）`、`Q=ceil(O/10)（版本查詢批次數）`、`ZQ=ceil(Z/10)（舊資料確認批次數）`。Firestore（雲端文件資料庫）空查詢仍可能以至少 1 Read（文件讀取次數）計費；安全規則的身分與角色核對、網路重試另計，正式剩餘額度未知。
- 首次冷載入或本機快取遺失：最低約 `1 + Q + max(O,I) + P + ZQ`筆（全部訂單都沒有產能，舊資料確認為空）；一般約 `1 + max(Q,V) + max(O,I) + P + S + max(ZQ,L)`筆；版本文件分布最分散時的查詢最壞值為 `1 + V + Q + max(O,I) + P + S + L + ZQ`筆。對完全沒有報工的訂單，不再按其全部 `SX`工序讀取不存在的累計；寫入與刪除皆為 0，只在本機保存快取。
- 冷載入文件仍按實際回傳內容計算 Reads（文件讀取次數）；訂單項目約 `ceil(O/10)`批、款號約 `ceil(P/30)`批、工序累計約 `ceil(S/30)`批、無版本舊資料確認約 `ZQ`批，每階段最多三批並行。暖快取的產能增量使用 `productionProcessTotals`的 `orderId + updatedAt`複合索引，每張變動訂單一個範圍查詢，一般直接讀取約 `D`筆，最低計費保守估為 `max(D,R)`；例如 591 道工序的訂單只有 1 道變動，累計文件由約 591 Reads 降為約 1 Read。同一道工序期間變更多次仍只回傳其目前一筆絕對累計。
- 下一個 06:00 更新週期且資料未變：最低約 `1 + Q` Reads（文件讀取次數），一般約 `1 + max(Q,V)`；不重讀 I、P、S。同一週期內再次登入、重新整理或切換頁面為 0。若有產能變動，一般另加 `max(D,R)`，不再依訂單全部工序數增加；使用者另按一次手動更新時，再依當時版本與實際變動計算。
- 若增量複合索引尚未建立或仍在建立，程式只對受影響訂單退回現行 `ceil(S/30)`批文件識別碼讀取，結果保持正確；索引完成後自動使用增量查詢。版本查詢遭拒、網路失敗或大型快取保存失敗時保留上次成功快取，小型本機嘗試標記阻止同一期反覆完整重試。舊快取缺少可靠的版本時間時，每張訂單只在首次轉換退回完整讀取一次。
- 完成或取消一張訂單，服務本身各直接讀取 1 筆訂單，寫入訂單、不可變操作紀錄及 `dataVersions/orders（訂單快取版本）`共 3 筆，刪除 0 筆；安全規則相依讀取與交易重試另計。完成後該訂單每次進度更新的版本、項目、款號與累計讀取均降為 0；其他裝置再次開啟訂單頁時，訂單版本失效的清單重載尖峰仍按 `O × D × R`另估。正式剩餘額度未知，本機模擬器驗收正式用量為 0。
- 款號版本變動時，正常讀取 `C`筆上次之後改動的款號，而不是全部 P 筆。以固定工序身分比對後，既有 `SX`秒數修改、`SX`改成其他分類及無關款號修改增加 0 筆累計讀取；其他分類改成 `SX`或新增 `SX`時約增加 N 筆累計讀取。追蹤起點不相符、舊快取沒有遞增序號或增量資料不可驗證時，安全退回讀取涉及的 P 筆款號。
- 本機讀取統計記錄每類成功查詢回傳文件數、查詢批次及把空查詢至少計為 1 的 `estimatedReads（最低估計讀取次數）`；安全規則相依文件、SDK（開發工具）網路重試及控制台最終計費不一定能由瀏覽器完整觀察，仍以 Firebase 控制台為準。統計只寫入原本 UID 隔離的本機快取，雲端新增寫入、刪除、傳輸及儲存皆為 0。
- 每位使用者／裝置每日開頁 `K`次時，只要 `K>0`，自動更新的暖快取且未變動一般用量固定約 `1 + max(Q,V)` Reads，最壞約 `1 + V + Q`，不再乘以開頁或切頁次數；當天後續自動進度讀取為 0。若使用者另按一次手動更新，會再增加同一組版本核對用量；資料未變時全日一般約 `2 × [1 + max(Q,V)]`，最壞約 `2 × [1 + V + Q]`。若其中一次遇到資料變動，另依實際變動增加 I、P 或 S 的直接相關讀取；每日自動與手動合計的最壞上限約為兩次冷載入公式。Writes（寫入次數）、Deletes（刪除次數）、雲端儲存增量皆為 0，本機只更新既有快取與兩個小型嘗試標記。若版本或快取格式更新造成 `D`個裝置重建且每台都使用兩次機會，最壞尖峰約 `2D × [1 + V + Q + max(O,I) + P + S]` Reads。本機模擬器測試的正式用量為 0，正式剩餘額度未知。

### 9.6 歷史操作紀錄分頁雲端用量預算

- 原本主頁上的「數量調整紀錄」按鈕與彈窗移除，數量調整功能及既有不可變操作紀錄保留；舊 `orderAdjustments（訂單調整紀錄）`讀取入口不再使用，避免與正式操作紀錄重複顯示。歷史統一由訂單功能最右側的「歷史操作紀錄」分頁按需讀取。
- 歷史範圍依整個訂單主功能的 `progress（訂單資料）`權限區分，不再用固定動作清單切割；訂單資料、已出貨、已封存、品檢報告範本與報告匯出等分頁所建立的正式紀錄都會顯示。不同操作保留適合自己的摘要，不要求與其他功能的歷史頁使用相同欄位。首次開頁最多 50 Reads（文件讀取次數）；同一次登入切換離開後再開為 0 Reads；每次「載入更多」或「重新整理」最多再增加 50 Reads。
- 本功能不新增 Writes（寫入次數）、Deletes（刪除次數）、雲端儲存或快取版本；傳輸量依實際回傳的最多 50 份小型操作紀錄計算。最低空查詢保守計 1 Read，一般 1 至 50 Reads，反覆人工重新整理沒有有限總上限。
- 沿用既有 `operationLogs（操作紀錄）`資料路徑、`progress（訂單進度）`權限與複合索引，不改變資料安全邊界；正式剩餘額度未知，本機虛構資料測試的正式用量為 0。


## 10. 驗收重點

1. 表頭上方存在說明列時仍能找到唯一款號與數量欄；歧義、缺欄、非法數量與總數不符時不寫入。
1.1 選檔後不需等待訂單基本資料即可顯示檢查結果；唯一 `ORDER NO／ORDER NUMBER`會帶入空白輸入框但仍可修改，多個不同號碼不得自行選用。產品說明及顏色欄只依實際表頭呈現，寬度與長度不得顯示或寫入。
2. 同訂單多列相同款號保持獨立 `orderItemId`，不合併也不選錯項目。
3. 匯入中斷後清理殘留再從頭匯入；使用中與封存的同號訂單均提示已存在，永久刪除後才可重匯。
4. 訂單工序只來自目前款號主檔；款號修改不改變訂單自己擁有的欄位。
5. 數量不得低於任何工序已完成量；並行修改以修訂檢查阻止更新遺失。
6. 封存與還原保留訂單項目、產能及操作紀錄；只有管理員可以開始／接續永久刪除，非封存狀態不能開始，刪除中不能還原。
7. 未授權、停用及無效帳號無法開啟頁面或直接讀寫資料。
8. 三種語言模式、桌機表格、按需明細、錯誤與空資料狀態正常。
9. Runtime 對 `orderProcesses`及舊訂單工序快照保持零讀寫。
10. 下方重複訂單管理表不再出現；主表顯示全部尚未出貨訂單，未選實際出貨日者置頂，其餘依實際出貨日由近到遠排列，不顯示工序數、實際完成日或可展開款號明細，也沒有款號搜尋；選擇訂單不得讀取工序。已出貨分頁依實際出貨日新到舊排列。已選日期不得隨當日時間自動改寫；確認與取消只移動顯示分類且取消後保留日期，訂單仍可供產能、績效及凍結月份使用。
11. 主表仍使用共用表格；數量、進度、PO 交期、實際出貨日與操作欄保持清楚間距，完成訂單的進度條填滿 100% 並保留實際百分比文字。PO 交期統一深色粗體，實際出貨日依空白、兩週內及逾期狀態顯示淡綠底框、資訊深藍色或危險紅色；三種語言模式皆不得截斷或重疊。「使用說明」按鈕位於「匯入訂單」左側，點擊後以共用雙語視窗簡要說明封存、已出貨、報表匯出、實際出貨日顏色及每日進度更新限制；主頁不直接展開完整說明。
12. 主表左上角可勾選顯示欄位並可拖曳調整欄寬；雙語、越文及中文模式的表頭內容不得黏合或遺失。「列印表格」位於「匯入訂單」右側，只列印目前訂單表格及已勾選欄位，「操作」欄固定不列印，也不列印表格上方的標題、更新時間與未出貨總數量；列印表頭跟隨目前語言模式，雙語上下排列，空白備註顯示為破折號，且不新增雲端讀寫或操作紀錄。
13. 主頁不再顯示「數量調整紀錄」按鈕或彈窗；「歷史操作紀錄」位於訂單功能分頁最右側，依整個訂單主功能收錄各分頁操作，只有開頁時才讀取最近 50 筆。欄位順序為操作時間、操作者、訂單／對象、操作內容、變更摘要及結果；訂單／對象不得顯示 `actualShipDate（實際出貨日）`、`remark（備註）`等內部欄位名稱。可載入更多與重新整理，三種語言模式需維持清楚的雙行或單語排版，切換離開再返回不得重複讀取。

### 10.1 匯入與失敗清理的現行驗收

- 訂單號碼已存在且主資料已完成或已封存時，提示已存在，不寫入新訂單；永久刪除完成後才可再次匯入。不能把舊匯入鎖當成「只能由原帳號接續」的永久歸屬。
- 有訂單權限的不同帳號都可處理未完成的匯入：先分批核對並清理已寫明細、主資料與鎖定，留下不可變操作紀錄，再重新匯入；中途斷線則顯示待清理，下一次繼續清理。完成狀態若已提交但回應遺失，以重新讀取的正式主資料為準。
- 沒有主資料的舊孤立鎖僅在已記錄零筆且查無明細時可修復；仍有明細、指向其他訂單或狀態不明時停止，避免誤放行同一單號。
- 單次寫入批次最多 8 筆明細；需驗收 8 筆成功與超額拒絕，以及 29 筆、1,000 筆等整份檔案可分批完成，不把批次上限誤當整張訂單上限。
- 安全規則對 orders、orderItems 的讀取使用既有 canReadOrders（正式訂單讀取權限判斷）；未完成清理只開放有訂單權限的角色且須有正式紀錄，已完成訂單的永久刪除仍限管理員與封存流程。本機驗收須涵蓋全部有效角色、無權限與停用帳號、跨帳號清理及拒絕情境。
- 本文件不以歷次單元測試數量、舊網站載入標記或過往未推送／未發布紀錄判斷目前版本；程式版本以現行載入來源為準，雲端規則發布與正式資料狀態須在需要時另外核對。

### 10.2 封存、還原與管理員永久刪除

- 已封存訂單使用獨立的已封存分頁，與訂單主頁共用訂單快取；封存和還原保留訂單、項目與操作紀錄。主頁的作廢標示使用圓圈斜線圖示，不以垃圾桶暗示立即永久刪除。
- 永久刪除只在已封存分頁提供給管理員；確認顯示訂單、客戶及原始明細筆數，不要求再輸入訂單號碼。確認本身使用清單已載入的 itemCount（原始明細筆數），不額外讀取整張明細。
- 刪除流程為已封存、刪除中、分批清理、完成；每批最多 100 筆，開始、分批與完成留有操作紀錄。中斷後只能接續刪除，不允許還原刪除中的訂單；完成時才釋放單號鎖定。
- 只清除目標訂單、訂單項目及對應匯入鎖；不掃描或刪除產能、款號及既有操作紀錄，也不自動證明業務關聯已消失。永久刪除沒有備份還原功能，回復程式前須先處理正在刪除的資料。
- 封存、還原和刪除開始／完成依既有 dataVersions/orders（訂單快取版本）更新相關清單；中間批次不讓其他裝置讀取半成品。讀寫刪與跨裝置重新載入預算以第 9.3 節為準。

### 10.3 匯入預覽與畫面狀態

- 使用者重新選檔或新檔不合格時，立即清掉上一份預覽並停用確認；非同步讀檔只接受最後一次選檔結果。
- `.xls`與 `.xlsx`各至少驗收一份，確認兩者都經過同一個陣列緩衝區入口、表頭與欄位判斷一致；選檔後縮成單列，重新選擇仍可操作，錯誤檔不得留下上一份可送出的預覽。
- 確認匯入以畫面當下的訂單號碼、客戶與交期為準，避免預覽後修改交期仍寫入舊資料；原始表格解析、款號驗證及分批上限維持第 4 節規格。
- 主訂單頁不提供永久刪除；已封存分頁才提供管理員刪除入口。匯入尚未開始即被拒絕時不顯示誤導的零筆進度，失敗時區分已清理與待清理。
- 驗收包含新檔拒絕後不可送出舊預覽、先後讀檔競爭、預覽後更改交期、封存確認不補讀明細，以及雙語／單語與桌機畫面。若僅修改文件，不以文件更新聲稱已重新執行正式環境驗收。
